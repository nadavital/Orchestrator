# Orchestrator Papercuts Plan

Date: 2026-05-20

This is the active checklist for the current sidebar, notification, modal, archive, tooltip, and typing-latency cleanup pass. Keep this file current as implementation moves, add any newly reported papercuts here, and commit completed slices locally. Remote push is intentionally out of scope while the user is on corporate VPN.

Status key: `Todo`, `Investigating`, `Verified`, `In progress`, `Complete`, `Blocked`.

## Working Rules

- Verify every user report against the current implementation or running UI before calling it fixed.
- Keep changes in small local commits with focused verification.
- Update this file when findings change or a new papercut is added.
- Compare Codex behavior directly from the local Codex app bundle when a report asks for Codex parity.
- Treat Codex as the richest reference implementation, not as a provider-specific destination. When a behavior is useful across providers, build the Orchestrator primitive provider-agnostically first, then map Codex, Claude, Cursor, Copilot, and future providers through adapters.
- Preserve `bootstrap.js` as untracked local state unless the user explicitly says otherwise.

## Provider-Agnostic Parity Rule

Codex parity means matching the maturity of the Codex app where it provides the strongest local reference for reliability, UI shape, and runtime semantics. It does not mean hardcoding Codex-only architecture unless the feature is genuinely Codex-only.

Use this split when implementing the backlog:

- **Provider-agnostic primitives**: session identity, route/deeplink/open-window behavior, worktree/fork lifecycle, scroll and virtualization, composer drafts/attachments, command registry, search/find, notifications, file references, review UX, settings primitives, runtime process robustness, and automation scheduling.
- **Provider adapters**: Codex app-server, Claude CLI/JSONL, Cursor/Copilot MCP/config surfaces, and future provider runtimes should implement the same product contracts through provider-specific APIs.
- **Provider-specific surfaces**: Codex apps/connectors, Codex-native plugin install methods, app-server heartbeat details, or provider-specific permission modes can stay behind adapters, but the UI should expose the shared concept whenever another provider can support it.

## Surface Names

- **Chat Sidebar**: the left navigation rail with projects, pinned chats, recents, and Settings entry.
- **Workbench Panel**: the right-side contextual panel with Changes, Files, Browser, Plan, Agents, Extensions, and side-chat tabs. Legacy code may still call this `ContextSidebar` or `right-sidebar`.
- **Terminal Panel**: the bottom shell panel and its terminal tabs.

## Codex Workbench Panel Comparison

Codex reference came from `/Applications/Codex.app/Contents/Resources/app.asar`, especially `webview/assets/app-shell-CcsLZiAu.js` and `webview/assets/app-shell-BJK30dyj.css`.

Parity rule: do not add broad bespoke Orchestrator workbench behavior until the shared shell, interaction, performance, and robustness gaps below are either closed or intentionally accepted. If a new Orchestrator-specific idea overlaps a Codex primitive, first migrate or adapt the Codex-shaped primitive.

| Area | Codex behavior | Orchestrator gap | Action |
| --- | --- | --- | --- |
| Product naming | Focus area is `right-panel`; user-facing controls say "panel". | The product conversation used "sidebar" for both sides, and code/UI mixed `ContextSidebar`, `right-sidebar`, and panel labels. | Standardize user-facing language to Workbench Panel while leaving legacy class names only where changing them would be churn. |
| Shell architecture | Right, left, and bottom panels share app-shell state, sizing, focus areas, animation, and tab primitives. | Orchestrator has separate hand-rolled CSS/React paths for the Workbench Panel, Terminal Panel, Chat Sidebar, and settings. | Keep migrating toward shared shell primitives as polish continues. |
| Panel surface | Codex right panel is a plain raised pane: thin left border, subtle shadow, no extra card framing. | Orchestrator was close, but right-panel naming and chrome still felt custom. | Preserve the simple pane surface and make header/action styling quieter. |
| Tab strip | Codex shows labeled tabs, supports drag reorder/context menus, uses overflow scrolling, edge fades, and sticky action slots. | Orchestrator collapsed inactive Workbench tabs to icons, making the panel less readable. | Keep labels visible on inactive Workbench tabs and rely on horizontal overflow/compact max widths. |
| Tab weight | Codex uses small text, restrained active backgrounds, and hover close affordances. | Orchestrator active tabs were visually heavier and more segmented. | Lower Workbench tab font/background weight and keep icon actions transparent until hover/focus/active. |
| Resize/width | Codex stores a ratio against main content width, clamps below usable sizes, and switches focus/full-width state at smaller breakpoints. | Orchestrator uses pixel width and an overlay fallback; the focused smoke had an invalid overlay threshold. | Keep current pixel model for now, but verify docked, expanded, and true narrow-overlay behavior. |
| Remaining gap | Codex tab close affordance appears as an overlay and is available beyond the active tab. | Orchestrator `TabButton` only renders close on the active tab. | Backlog unless it blocks UX; changing shared tab semantics needs a broader pass. |

## Codex Parity Matrix

This matrix is the higher-level parity ledger. The goal is not to clone Codex blindly, but to reach the same level of usefulness, efficiency, speed, and robustness before layering on Orchestrator-specific product ideas.

Detailed side-panel/sidebar parity audit: `docs/codex-side-panel-ui-parity-audit.md`.

| Dimension | Codex baseline | Orchestrator current state | Required parity work | Status |
| --- | --- | --- | --- | --- |
| Shared shell model | `AppShell.Root`, `LeftPanel`, `RightPanel`, `BottomPanel`, header slots, focus areas, layout ticks, and panel animation are shared primitives. | Shell behavior is split across `SessionPane`, `ContextSidebar`, `TerminalPanel`, `Sidebar`, and `SettingsModal`. | Extract a shared Orchestrator shell layer for panel sizing, focus areas, resize handles, tab headers, and action slots. | `Todo` |
| Workbench tabs | Tabs are controller-backed, labeled, draggable, overflow-scrollable, fade at edges, and have sticky before/after action slots. | Workbench and Terminal now share a reusable `PanelTabStrip` with readable labels, hover/focus close affordances, middle-click close, active-tab scroll, edge fades, sticky trailing actions, drag reorder, focus-routed close-active-tab shortcuts, preview/pin file-tab behavior, and reset paths for Browser and file/source tabs. | Remaining parity work: full app-shell tab-controller ownership and cross-panel moves beyond Terminal. | `In progress` |
| Panel sizing | Right panel width is ratio-based against main content and reacts cleanly to breakpoints/full width. | Workbench width now persists a ratio alongside the compatibility pixel width, uses ratio-based sizing against the current row, clamps through docked/overlay/full modes, and supports double-click reset. | Remaining shell-level follow-up: move sizing ownership out of `ContextSidebar` into a shared app-shell layout model. | `Complete` |
| Workbench usefulness | Codex right panel can host browser/artifacts/contextual panels with polished tab lifecycle and keyboard shortcuts. | Orchestrator has Browser, Files, Review, Plan, Agents, Extensions, Environment, and side chat. The New tab launcher now exposes the daily coding surfaces including Environment and Extensions, and a split `--workbench-launcher` smoke validates launcher discovery/activation without running the heavier Git/agent workflow suite. | Continue auditing each Workbench tab for Codex-level lifecycle, empty states, toolbar density, keyboard access, and close/reopen behavior. | `In progress` |
| Bottom panel model | Codex treats the bottom shell as a panel with shared app-shell controls, even when the active content is a terminal. | Orchestrator's bottom shell uses shared app-shell panel/tab primitives and now exposes `Toggle bottom panel`, `Resize bottom panel`, `Bottom panel tabs`, and `Hide bottom panel` labels while keeping terminal-specific actions scoped to terminal tabs. | Promote the bottom shell from terminal-only hosting toward a shared bottom panel that can receive non-terminal tabs when the app-shell controller supports it. | `In progress` |
| Performance | Codex shell keeps resizing, tab switching, scroll, and composer interaction responsive during live work. | Streaming drag/typing gates exist, focused Workbench performance and 10,001-message transcript stress gates are in place, and a focused cross-panel keyboard gate now proves Workbench/Terminal close routing without broadening the terminal or inspector suites. | Remaining parity work: add broader mixed-surface stress coverage without folding everything into one broad smoke. | `In progress` |
| Robustness | Codex shell centralizes focus, window size, close-tab shortcuts, and active focus area state. | Orchestrator now tracks main, Workbench, and Terminal focus areas at the app shell and routes `Close Active Panel Tab` through the focused panel. `--cross-panel-keyboard` proves Workbench-focused close preserves Terminal and Terminal-focused close preserves Workbench. Broader shell behaviors still need a shared controller. | Promote remaining shell behaviors into shared app-shell primitives and keep expanding focused cross-panel smoke coverage where new shared behaviors land. | `In progress` |
| Styling system | Codex uses tokenized toolbar heights, row padding, tab radii, shadows, surfaces, tooltip behavior, and settings primitives. | Shared panel tabs now use common app-shell toolbar/tab/action tokens, lower font weight, lighter active states, overlay close affordances, and tighter trailing fades. Shared tooltips now use a Codex-like delayed pointer hover plus stronger outside/escape/viewport dismissal. Token use is still partial across settings and other chrome. | Continue consolidating app chrome tokens and remove remaining surface-specific styling drift, especially settings/content rows and panel toolbar density inside individual tabs. | `In progress` |
| Verification | Codex behavior is mature enough to trust through repeated interaction; parity claims should be screenshot and smoke backed. | Renderer and node TypeScript now pass cleanly again, `npm run build` is available as a baseline gate, and focused smokes cover many surfaces. Not all cross-panel interactions or perceived speed paths are covered yet. | Keep adding parity smoke suites and screenshot checkpoints for Workbench, Terminal, Settings, Chat Sidebar, and cross-panel flows. | `In progress` |

## Codex Bundle Deep Dive Notes

### 2026-05-20: Shell, Panel, And Layout Mechanics

Reference chunks:

- `webview/assets/app-shell-CcsLZiAu.js`
- `webview/assets/app-shell-panel-animation-BXrIvkvo.js`
- `webview/assets/create-resize-observer-CTl6Pw5t.js`

Findings:

- Codex treats the app shell as a slot system, not separate panels. `AppShell.Root` extracts `LeftPanel`, `RightPanel`, `BottomPanel`, `Header`, `HeaderAction`, `RightPanelOutlet`, `RightPanelTabs`, and bottom-panel equivalents from children, then lays them out through one shell context.
- The shell context tracks `headerLeftWidth`, `headerRightWidth`, `leftPanelWidth`, `leftPanelAnimatedWidth`, `mainContentWidth`, `shellWidth`, `rightPanelAnimatedWidth`, and a `rightPanelLayoutTick`. Orchestrator currently recreates pieces of this in `SessionPane`, `ContextSidebar`, `TerminalView`, and CSS.
- Right panel sizing is ratio-based. Codex derives `rightPanelWidthRatio` from a `defaultWidth`, `storageKey`, and current `mainContentWidth`; full-width mode uses a different width mode instead of treating max width as a larger pixel clamp.
- Right panel resize uses a shared resize handle with edge semantics, pointer scaling, double-click reset to default size, and a close behavior when the dragged width falls below the minimum. Orchestrator resize handles are currently per-surface and do not share reset/close semantics.
- Codex right panel is an animated `aside` with `data-app-shell-focus-area="right-panel"`, a thin left border, `shadow-xl`, and an inner absolutely positioned pane. The outer shell owns animation/width; panel contents stay overflow-hidden and stable.
- Codex updates layout state during panel animation through `progress`/`animatedSize`, so dependent layout can respond while the panel animates. Orchestrator mostly reacts after width/state changes and relies on local `ResizeObserver` patches.
- Codex has explicit breakpoint behavior around shell width. At narrower widths it moves focus back to main, disables full-width/right-panel states when necessary, and collapses left panel where appropriate. Orchestrator has local overlay logic in `ContextSidebar` but not one central breakpoint policy.

Migration implications:

- First extract an Orchestrator `AppShell` layer that owns focus areas, measured shell widths, panel resize handles, toolbar heights, and animated sizes.
- Move Workbench and Terminal into this shell before adding new Workbench-only features.
- Treat ratio-based Workbench width as a shell concern, with existing pixel width migrated through a compatibility step.
- Add double-click reset and below-min close semantics to shared resize handles once both panels use them.

### 2026-05-20: Tabs, Focus, And Shortcut Lifecycle

Reference chunks:

- `webview/assets/app-shell-CcsLZiAu.js`
- `webview/assets/thread-side-panel-browser-tab-state-C5u0Yb1s.js`
- `webview/assets/command-keybindings-CahU8007.js`
- `webview/assets/tabs-Dhgr0Bym.js`

Findings:

- Codex right and bottom panels use the same tab-list renderer contract: controller-provided `tabs$`, `displayedTab$`, `activeTabReactKey$`, `activateTab`, `closeTab`, `reorderTab`, `clearClosingTab`, and `closeActiveTab`.
- The Codex app-shell tab row supports before-list content, normal after-list content, and sticky after-list content. Sticky actions are measured with `ResizeObserver` so the scroll area reserves enough end padding.
- Tab overflow is intentional: the strip is horizontally scrollable, hides scrollbars, has left/right edge fades driven by `IntersectionObserver`, and preserves tab labels rather than collapsing to icons.
- Individual Codex tabs measure label overflow and add a local text fade. Close is an overlay affordance that appears on hover/focus and can be used on non-active tabs; middle-click also closes closable tabs.
- Codex reorder uses drag sensors with a small activation distance, sortable context data, separator visibility around the active tab, and layout animation for moved tabs. Orchestrator has manual context-menu move but no drag reorder.
- Codex exposes active shell state to global shortcuts by dispatching `app-shell-shortcut-state-changed` with `focusArea`, `bottomPanelCanCloseActiveTab`, `rightPanelCanCloseActiveTab`, `rightPanelBrowserConversationId`, and image-preview state. The global `close-active-app-shell-tab` command routes to bottom or right based on focus area.
- Orchestrator currently has keyboard shortcuts, but Workbench/Terminal close behavior is not centralized through an active app-shell focus-area model.

Migration implications:

- Build a reusable `PanelTabController` abstraction for Workbench and Terminal before polishing either tab strip further.
- Upgrade shared `TabButton` so close is hover/focus overlay and works for non-active tabs, while preserving accessible labels and middle-click close.
- Add shell-level focus-area state and route close-active-tab/keyboard commands through it.
- Add overflow fades and sticky action-slot measurement to the shared tab strip, then remove the bespoke Workbench and Terminal tab-row CSS.
- Use Codex-style controller semantics as the API boundary: panel content should register tabs and actions; shell should render tab chrome.

### 2026-05-20: Broader Parity Investigation Wave

This pass fanned out read-only investigations across the non-shell Codex parity areas. The MCP/apps/plugins/skills subagent stalled, so that area was completed locally from the current capability docs and implementation.

#### Chat And Thread Runtime

Reference chunks:

- `webview/assets/use-active-conversation-id-CBsI3TAh.js`
- `webview/assets/use-start-new-conversation-DK-qycZW.js`
- `webview/assets/pending-worktree-conversation-BeU8KFPH.js`
- `webview/assets/local-conversation-thread-BX7YNcUw.js`
- `webview/assets/local-conversation-page-Bt6RhPKI.js`
- `webview/assets/remote-conversation-page-CRbylpi9.js`

Findings:

- Codex conversation identity is route-backed for local, remote, and hotkey-thread windows. Orchestrator still treats `activeSessionId` as renderer store state, so deep links, new windows, and reopen semantics are weaker.
- Codex has first-class pending worktree conversations with launch mode, fork source, owner metadata, pin placement, title, goal, and browser-transfer metadata. Orchestrator creates a normal session first and mutates worktree state on first send.
- Codex resumes cold/missing conversations on page open, handles archiving/missing states explicitly, and tracks hot/cold switch readiness. Orchestrator mainly resumes provider threads as part of a run path.
- Codex side chat is an ephemeral forked conversation. Orchestrator side questions are UI-local detached calls, so they do not preserve native fork semantics.
- Codex marks read from visible active conversation state. Orchestrator clears unread on active-session selection, which can mark work read before the transcript is actually visible.

Migration implications:

- Add route-backed session identity before expanding new-window/deeplink behavior.
- Add a pending conversation/worktree launch model instead of relying only on first-send mutation.
- Add page-open lifecycle states such as `resuming`, `missing`, `archiving`, `closed`, and `ready`.
- Use Codex fork-latest/fork-turn semantics for side chats and worktree forks where provider support exists.
- Delay unread clearing until transcript readiness is confirmed.

#### Live Generation And Transcript Rendering

Reference chunks:

- `webview/assets/thread-scroll-layout-Cxloffmz.js`
- `webview/assets/thread-layout-Chou_aJz.js`
- `webview/assets/thread-detail-level-B_mdNLmM.js`
- `webview/assets/conversation-markdown-By6oKuLC.js`
- `webview/assets/right-panel-composer-overlay-scroll-reserve-BZSZnzFs.js`
- `webview/assets/virtualized-turns-JewydgrT.js`

Findings:

- Codex scroll math is distance-from-bottom based and exposes a scroll adapter. Orchestrator still uses direct `scrollTop` and height-delta compensation.
- Codex virtualizes by turn keys with measured offsets, binary search, overscan, first-visible preservation, and `scrollToKey`. Orchestrator virtualizes message/tool rows with custom window math and direct DOM `scrollIntoView`.
- Codex has explicit footer/composer reserve CSS, response spacer state, and follow modes such as `static`, `user_follow`, `prework_watch`, and `prework_follow`. Orchestrator has a good first pass for manual-scroll protection, but no shared scroll controller or composer reserve primitive.
- Existing Orchestrator smokes cover lazy load, search jump, transcript stress, and manual scroll during streaming, but not typing latency during live streaming or very large transcript headroom.
- Codex collapses verbose agent/subagent activity by default: the transcript primarily shows the final agent summary plus elapsed work time, with a chevron to expand earlier agent thinking/tool-call messages. Orchestrator still renders more agent internals directly, so a future transcript/performance slice should add agent-activity collapse to reduce visual noise and long-thread render cost.

Migration implications:

- Introduce a reusable `ThreadScrollController` with distance-from-bottom semantics.
- Route search and lazy-load jumps through stable transcript item keys instead of raw DOM scroll calls.
- Add a composer/footer reserve variable even while the composer remains outside the scroll container.
- Add default-collapsed agent activity groups with elapsed-time summaries and explicit expand/collapse.
- Add typing-latency, composer-reserve, search-jump-anchor, and larger 10k-message stress smokes.

#### Composer And Input Ergonomics

Reference chunks:

- `webview/assets/composer-DXaiOlFj.js`
- `webview/assets/composer-atoms-BeIctnnK.js`
- `webview/assets/composer-0WIQtlLp.css`
- `webview/assets/thread-context-inputs-CFTJKUBX.js`
- `webview/assets/user-message-attachments-C4kFKr_t.js`

Findings:

- Codex composer is ProseMirror-backed, with persisted atoms for enter behavior, prompt history, auto context, and richer input preferences.
- Codex handles pasted files/images, drag/drop, pending uploads, cancel/error surfaces, and broad context chips for files, selected text, native app context, prior conversation, PR checks, and workspace roots.
- Orchestrator has per-session text drafts, paste caret fixes, local attachment chips, slash commands, and permission/question split, but attachment chips are still local `InputBar` state rather than per-session draft state.
- Mixed text+file paste can leave the caret behavior inconsistent with text-only paste, and large file paste reads full buffers in the renderer without pending/cancel feedback.

Migration implications:

- Persist or key composer attachments by session id.
- Add drag/drop file attachment with pending/error/cancel chip states.
- Normalize caret behavior for mixed text+file paste.
- Prompt history and configurable enter behavior are now covered locally; keep deeper provider-backed context behavior separate from any heavier editor-substrate decision.
- Add focused smokes for draft/attachment isolation, paste behavior, slash palette keys, and large attachment latency.

#### Search And Navigation

Reference chunks:

- `webview/assets/search-C95l31xn.js`
- `webview/assets/keyboard-shortcuts-search-input-DxhdKVBY.js`
- `webview/assets/workspace-file-command-menu-bridge-Du8_GPQH.js`
- `webview/assets/use-workspace-file-search-CrqRc_Zo.js`
- `webview/assets/file-tree-search-input-X-DM55OR.js`
- `webview/assets/command-keybindings-CahU8007.js`
- `webview/assets/use-command-hotkey-B6nOAHzG.js`

Findings:

- Codex has a centralized command/keybinding model. Orchestrator still splits command metadata, handlers, menu accelerators, and global `keydown` logic.
- Codex command menu has modes such as file search and routes `Cmd/Ctrl+P` into workspace file search. Orchestrator command palette is clean, but lacks file/search-chat modes and still treats file search as panel-local.
- Codex workspace search is app/server backed, ignored-dir aware, fuzzy ranked, and root-labeled. Orchestrator file search is renderer-recursive, depth/cap limited, and substring based.
- Codex find is cross-surface with active match state, highlight, counts, capped results, and scroll-to-match across conversation and diff. Orchestrator transcript search works but has no shared find model, and diff search only filters changed file paths.
- Codex shortcuts settings are editable with capture, conflicts, remove/reset, and persisted keymap state. Orchestrator shortcuts are static searchable reference rows.

Migration implications:

- Create a unified command registry with metadata, default shortcuts, handlers, enabled state, and palette/menu visibility.
- Add command-palette file mode and `Cmd/Ctrl+P`.
- Move workspace file search into main process or a long-lived search session with fuzzy ranking and ignored dirs.
- Add shared `FindBar` state for transcript and diff, then add diff hunk search and highlighting.
- Add editable shortcut overrides after the registry is real.

#### MCPs, Apps, Plugins, Skills, And Capabilities

Reference sources:

- `docs/provider-resource-dedupe-spike.md`
- `docs/capability-sync-spike.md`
- `docs/codex-appserver-support-matrix.md`
- `src/main/providerResources.ts`
- `src/main/capabilitySync.ts`
- Codex chunks still to inspect more deeply: `mcp-DS0lNDOd.js`, `mcp-settings-BRGoGTc_.js`, `apps-C0n7YO22.js`, `apps-queries-nPdSwUTo.js`, `plugins-page-BZD8O17r.js`, `plugin-install-store-CO2NIqvP.js`, `skills-settings-BbRndg3i.js`, `check-plugin-availability-BLUA-GwE.js`

Findings:

- Orchestrator already has a strong normalized capability model: `ProviderResource`, provider badges, dedupe by resource kind/fingerprint, file-backed create/edit/delete, and sync projection across Claude, Codex, Cursor, and Copilot where safe.
- Capability sync can write Codex `.agents/skills`, portable `.codex-plugin/plugin.json`, Codex plugin marketplace entries, and Codex MCP TOML. Risky provider-native installs are intentionally represented as gated operations.
- Codex app-server support is still mostly read-only for apps, skills, plugins, MCP status, account/model/config, and feature flags. Missing first-class parity includes app connector auth/invocation, `plugin/read/install/uninstall`, `skills/config/write`, MCP reload/OAuth/resource/tool management, native mention insertion, and app/server filesystem/search integration.
- The existing recommendation to keep Capabilities as its own surface still stands. Do not duplicate it into Settings; Settings should host provider/account/config preferences while Capabilities hosts inventory, install, sync, and projection state.

Migration implications:

- Finish the Codex bundle inspection for native app/plugin/skill UI details before changing the Capabilities page heavily.
- Add gated provider-native install/config flows on top of the existing preview/apply plan model.
- Promote read-only app-server resource data into real browser/picker flows with app/skill/plugin mention insertion.
- Add MCP reload/OAuth/resource/tool management only behind explicit confirmation and diagnostics.

#### Automations And Follow-Ups

Reference chunks:

- `webview/assets/automation-schedule-BpeIuMts.js`
- `webview/assets/automation-dialog-BvGLQK24.js`
- `webview/assets/automations-page-s8q9NlzD.js`
- `webview/assets/heartbeat-automation-eligibility--gq6YWh5.js`
- `webview/assets/heartbeat-automation-permissions-wzgH9Qd2.js`
- `webview/assets/heartbeat-automation-thread-bridge-DXwhZizF.js`

Findings:

- Codex supports `cron` and `heartbeat` automations, `ACTIVE`/`PAUSED`/`DELETED` states, RRULE schedule parsing/summaries, execution environments, models, reasoning effort, run-now, optimistic update/revert, and run history.
- Codex heartbeat eligibility is explicit: local host, existing conversation, resumed/not resuming, no active turn, and no pending user input/approval/MCP elicitation.
- Orchestrator now has persisted provider-neutral automation records, thread Add/Edit plus Run now/Pause/Resume/Delete automation actions for heartbeat follow-ups, next-run metadata for interval/common RRULE schedules, pause/resume/delete lifecycle, active-scheduled lifecycle warnings, sidebar paused/active/next-run/running metadata, due-list selection, permission snapshots, run-history records, due-run/manual-run execution boundaries, deferred provider-exit run completion, app-owned scheduler daemon/timer wiring, Automations settings page with Current/Paused/Run history sections, schedule editing UI for manual/interval/RRULE modes, shared session-state eligibility checks, single-flight guards, ineligible skip recording, failure recording, and session permission snapshot capture/execution for permission mode plus tool allow/deny lists. It still does not have provider-backed targets; current active-run queue/steer behavior should not be stretched into scheduled reminders.

Migration implications:

- Continue from the persisted `AutomationManager` runtime and thread action proof, not a bespoke scheduling dialog.
- Continue from the due-run/manual-run execution boundaries, scheduler daemon/timer, Automations page, schedule editing UI, thread action lifecycle, sidebar metadata, permission snapshot enforcement, and in-progress run-state UI around the existing `Automation`, `AutomationRun`, `AutomationTarget`, schedule, permission snapshot, next/last run, and status records.
- Add fake-clock tests for restart hydration, provider executor calls, and real session-state eligibility guards.
- Add heartbeat eligibility mapped from current session/provider state before exposing unattended thread wakeups.

#### Permissions, Approvals, And Safety

Reference chunks:

- `webview/assets/permission-request-model-DztoNKAv.js`
- `webview/assets/permissions-mode-defaults-TVsdTyUZ.js`
- `webview/assets/permissions-mode-helpers-CExUWaUo.js`
- `webview/assets/permissions-mode-visibility-Cz4wOMkW.js`
- `webview/assets/computer-use-app-approvals-query-D_eSSbpv.js`
- `webview/assets/use-permissions-mode-Ch7rOI7L.js`

Findings:

- Orchestrator already separates `permission.requested` from `user_input.requested` across types, lifecycle, transcript cards, pet notifications, system notifications, and app-server runtime handling.
- Backend mapping already separates Codex `approvalPolicy`, `approvalsReviewer`, and sandbox policy, but renderer UI still exposes a static provider-mode abstraction.
- Codex derives visible/default permission modes from host/cwd config requirements, latest-turn params, global/draft scope, full-access/custom visibility, guardian availability, and preferred non-full-access defaults.
- Orchestrator preserves raw `item/permissions/requestApproval` payloads too generically. Codex expands network/filesystem access into explicit request parts.
- Transcript permission card supports `Allow Once`, `Allow Session`, and `Deny`; pet overlay currently lacks the `Allow Session` parity action.

Migration implications:

- Split Codex UI state into explicit `approvalPolicy`, `sandboxMode`, and `approvalsReviewer`, keeping legacy `permissionMode` as compatibility.
- Feed app-server `config/read` and `configRequirements/read` into composer/settings mode visibility.
- Expand permission requests into structured network/filesystem details for transcript and pet cards.
- Add `Allow Session` to the pet approval path.
- Add focus-refresh checks for pending approvals/questions on app focus return.

#### Notifications And Provider Runtime Robustness

Reference chunks:

- `webview/assets/app-server-manager-hooks-BpnIGmYe.js`
- `webview/assets/app-server-manager-signals-BEaGjuc8.js`
- `webview/assets/app-server-notification-debug-signals-B4ZABWlc.js`
- `webview/assets/app-server-connection-state-COgfP2Bg.js`

Findings:

- Orchestrator has the core Codex app-server runtime, EPIPE global guards, and synchronous write-race handling, but not Codex's host-scoped app-server manager model.
- The live app-server session lacks child `error`, stdin `error`, write-callback, pending-request rejection, and typed unexpected-exit handling.
- An unexpected app-server exit can currently flow through `onExit()` and leave the session looking idle instead of failed if no `run.failed` notification arrived.
- Generic reconnect lifecycle exists, but Codex app-server disconnects are not normalized into connection state or restart/resume events.
- Codex keeps an app-server notification debug ring with host/thread/method/severity/noisy classification. Orchestrator now has provider-neutral runtime connection snapshots plus a bounded runtime debug ring.

Migration implications:

- Harden the provider runtime transport contract first, then make `CodexAppServerSession` the first concrete implementation. The same error/exit/write semantics should apply to Claude/Cursor/Copilot process lanes where possible.
- Add provider-host connection state with version, error code, and transition source. Codex app-server can populate the richest version first; other providers should still report process/runtime connection health.
- Emit failed lifecycle state for unexpected app-server exits.
- Continue expanding runtime diagnostics with provider-specific subscription state where the provider exposes it. Connection snapshots and the notification/debug ring are now exposed through provider diagnostics and a hidden provider-details card.
- Main-to-renderer and pet-overlay sends now use a shared safe-send guard for production session, terminal, menu, and pet messages; automated smoke-only sends can stay direct.

#### Review, Diff, Files, And Workspace

Reference chunks:

- `webview/assets/diff-summary-BdtgnJ_7.js`
- `webview/assets/diff-view-mode-Kp3YUUoJ.js`
- `webview/assets/file-diff-B3JvW2yY.js`
- `webview/assets/review-file-tree-side-pane-DTvkJOnY.js`
- `webview/assets/open-workspace-file-DOOUD1lA.js`
- `webview/assets/workspace-directory-tree-DltP8zc2.js`
- `webview/assets/file-preview-page-CJlBKAuy.js`
- `webview/assets/use-workspace-file-search-CrqRc_Zo.js`

Findings:

- Orchestrator Review often prefers rich current-file previews for changed markdown/json/csv/notebook/document/image files before showing diffs. Codex keeps diff rendering as the first-class path with split/unified modes, hunk expansion, line metadata, word diffs, and virtualization.
- Orchestrator changed files are flat filtered rows. Codex uses a review file tree with active path selection, search, comment counts, and scroll-to-file.
- Orchestrator Files panel crawls from the renderer with a 360-entry cap and depth-4 cutoff. Codex uses lazy directory queries and fuzzy search sessions across roots.
- Orchestrator assistant file cards are regex-extracted and can show false positives, especially with missing absolute paths or basename fallback into the workspace. This matches the user's reported bad cards for prose values such as comments and numeric examples.
- Orchestrator strips line suffixes like `foo.ts:42`; Codex open-file routing preserves path, cwd, line, column, end line, target, preview mode, and outcome telemetry.

Migration implications:

- Make Review diff-first and move rich previews behind an explicit Preview toggle.
- Replace flat changed-file rows with a directory tree and selected-file/search state.
- Harden file reference extraction and workspace resolution before adding more card UI.
- Preserve line/column metadata and expand open-file routing to editor-specific line opens.
- Add fixtures for prose false positives, same-basename outside-workspace paths, deep files, >360 files, and changed rich-preview file types.

#### Settings And Personalization

Reference chunks:

- `webview/assets/settings-page-BoavEVDX.js`
- `webview/assets/settings-content-layout-BeqejwUq.js`
- `webview/assets/settings-row-BUhYC5Lf.js`
- `webview/assets/appearance-settings-HaBPvNZV.js`
- `webview/assets/personalization-settings-B0sHaE0K.js`
- `webview/assets/keyboard-shortcuts-settings-IS-vX_o_.js`

Findings:

- Codex settings are routed full-page surfaces with grouped App/Host sections. Orchestrator settings are now cleaner, but still have a smaller flat section set.
- Codex row primitives are more reusable: standard row, compact row, nested row, action row, icon, label, description, and control. Orchestrator has shared pieces, but many settings controls still use one-off row/card layouts.
- Theme parity is closer than expected, but uneven. `opaqueWindows` and `semanticColors.skill` are stored/exposed without complete CSS application, and legacy appearance setters can drop v2 theme fields until reload. Preset themes also appear incomplete in the live UI: clicking some presets does not visibly update every surface users would expect, and the current preset display is too abstract. The next settings/theme pass should decide whether presets deserve their own section or should become a compact dropdown that fills the editable custom theme values, ideally with a small visual preview/swatch so users can understand the result before applying it.
- Codex shortcuts are editable with capture, conflict detection, multiple bindings, reset/remove, and persisted keymap state. Orchestrator shortcuts are static reference rows.
- Codex treats pets as part of personalization, with current pet, built-in/custom groups, create/refresh/open/tuck controls. Orchestrator has pet asset/install controls but a simpler top-level Pets section.
- Codex data controls include archived chat inventory and unarchive. Orchestrator data controls mainly expose profile/user-data paths.

Migration implications:

- Continue migrating settings to Codex-shaped primitives and grouped sections while preserving the liked left nav.
- Fix theme application consistency before adding more theme controls.
- Build editable keybindings on top of the unified command registry.
- Fold pet controls into a broader personalization model or intentionally document why Orchestrator keeps them separate.
- Expand data controls with archived session inventory and restore/unarchive before destructive cleanup flows.

## Additional Codex Parity Dives

These are the next areas to inspect beyond shell/workbench chrome. Each area should produce code-backed notes before broad Orchestrator-specific feature work continues.

| Area | Codex chunks to inspect | Orchestrator surfaces to compare | Why it matters |
| --- | --- | --- | --- |
| Chat/thread runtime | `local-conversation-page-Bt6RhPKI.js`, `local-conversation-thread-BX7YNcUw.js`, `remote-conversation-page-CRbylpi9.js`, `pending-worktree-conversation-BeU8KFPH.js`, `use-active-conversation-id-CBsI3TAh.js`, `use-start-new-conversation-DK-qycZW.js` | `SessionPane`, `ChatView`, `SessionItem`, `sessions.ts`, `sessionManager` | Chat switching, thread lifecycle, active conversation tracking, pending worktrees, and startup path should feel as reliable as Codex before adding more orchestration concepts. |
| Live generation and transcript rendering | `thread-scroll-layout-Cxloffmz.js`, `thread-layout-Chou_aJz.js`, `thread-detail-level-B_mdNLmM.js`, `conversation-markdown-By6oKuLC.js`, `right-panel-composer-overlay-scroll-reserve-BZSZnzFs.js` | `ChatView`, transcript virtualization/lazy loading, composer isolation, transcript stress smokes | Streaming should not block typing or manual scrolling, long threads should remain fast, and hidden/lazy content should feel intentional rather than surprising. |
| Composer and input ergonomics | `composer-DXaiOlFj.js`, `composer-atoms-BeIctnnK.js`, `composer-0WIQtlLp.css`, `thread-context-inputs-CFTJKUBX.js`, `user-message-attachments-C4kFKr_t.js` | `InputBar`, attachment handling, per-chat drafts, slash commands, permission/model controls | The composer is the highest-frequency interaction. It should match Codex-level latency, focus behavior, paste behavior, attachments, context inputs, and keyboard ergonomics. |
| Search and navigation | `search-C95l31xn.js`, `keyboard-shortcuts-search-input-DxhdKVBY.js`, `workspace-file-command-menu-bridge-Du8_GPQH.js`, `use-workspace-file-search-CrqRc_Zo.js`, `file-tree-search-input-X-DM55OR.js` | Command palette, transcript search, file search, diff search, sidebar search if added | Search should be unified, keyboard-first, and predictable across transcript, files, commands, and settings. |
| MCPs, apps, plugins, and skills | `mcp-DS0lNDOd.js`, `mcp-settings-BRGoGTc_.js`, `apps-C0n7YO22.js`, `apps-queries-nPdSwUTo.js`, `plugins-page-BZD8O17r.js`, `plugin-install-store-CO2NIqvP.js`, `skills-settings-BbRndg3i.js`, `check-plugin-availability-BLUA-GwE.js` | `CapabilitiesPage`, settings capabilities, provider runtime discovery, app/plugin install paths | Orchestrator should not invent a parallel capabilities model if Codex already has mature patterns for MCPs, apps, plugins, skills, availability, install state, and settings. |
| Automations and follow-ups | `automation-bN0RD0G3.js`, `automation-dialog-BvGLQK24.js`, `automation-schedule-BpeIuMts.js`, `automations-page-s8q9NlzD.js`, `heartbeat-automation-thread-bridge-DXwhZizF.js`, `heartbeat-automation-eligibility--gq6YWh5.js`, `heartbeat-automation-permissions-wzgH9Qd2.js` | Any future Orchestrator automation/follow-up UI, thread wakeups, scheduled runs | Automations need careful permission, eligibility, schedule, and thread-bridge semantics. We should mirror Codex patterns before building new scheduling UX. |
| Permissions, approvals, and safety | `permission-request-model-DztoNKAv.js`, `permissions-mode-defaults-TVsdTyUZ.js`, `permissions-mode-helpers-CExUWaUo.js`, `permissions-mode-visibility-Cz4wOMkW.js`, `computer-use-app-approvals-query-D_eSSbpv.js`, `use-permissions-mode-Ch7rOI7L.js` | Permission states in sidebar, pet, notifications, transcript controls, provider approval modes | Permission waits are product-critical and easy to make confusing. The UI and runtime should preserve Codex-level clarity between approval mode, permission prompts, and app focus. |
| Notifications and provider runtime robustness | `app-server-manager-hooks-BpnIGmYe.js`, `app-server-manager-signals-BEaGjuc8.js`, `app-server-notification-debug-signals-B4ZABWlc.js`, `app-server-connection-state-COgfP2Bg.js` | provider runtime manager, main/renderer event bridge, pet overlay, system notifications, reconnect handling | Orchestrator has had crashes and EPIPE issues; Codex app-server state is the richest reference, but the resulting transport, reconnect, and diagnostics model should apply to all provider runtimes where possible. |
| Review/diff/files/workspace | `diff-summary-BdtgnJ_7.js`, `diff-view-mode-Kp3YUUoJ.js`, `file-diff-B3JvW2yY.js`, `review-file-tree-side-pane-DTvkJOnY.js`, `open-workspace-file-DOOUD1lA.js`, `workspace-directory-tree-DltP8zc2.js`, `file-preview-page-CJlBKAuy.js` | `DiffPanel`, `FilesPanel`, file-reference extraction, open-file behavior | The user already saw file parsing issues. Diff/file panels need Codex-level parsing, grouping, previews, open behavior, and review ergonomics. |
| Settings and personalization | `settings-page-BoavEVDX.js`, `settings-content-layout-BeqejwUq.js`, `settings-row-BUhYC5Lf.js`, `appearance-settings-HaBPvNZV.js`, `personalization-settings-B0sHaE0K.js`, `keyboard-shortcuts-settings-IS-vX_o_.js` | `SettingsModal`, theme settings, shortcuts, providers, pets, data controls | Recent settings polish was a start. Full parity needs theme depth, shortcuts behavior, sections, row controls, and personalization breadth. |

## Checklist

| ID | Report | Initial Evidence | Plan | Status |
| --- | --- | --- | --- | --- |
| PP-001 | The sidebar loading indicator should live where the time/actions live, not farther inset. | `SessionItem` rendered spinner/status dot before the right metadata/actions slot. | Make the right-side slot mutually exclusive: action button on hover/focus, otherwise running/waiting/error/unread state, otherwise relative creation time. Verify with sidebar smoke/DOM checks. | `Complete` |
| PP-002 | A blank newly created chat should always remain at the top; an older chat rose above it. | Sidebar sort used `latestMessageAt ?? createdAt`, so any recently updated/running chat could jump above a blank active chat. | Add an active blank-chat priority before normal sorting. Verify with unit tests and sidebar smoke if available. | `Complete` |
| PP-003 | Active chats constantly reorder; active chats should stay in a stable order above inactive chats. | `compareSessionsByMode` sorted every unpinned list by latest activity, so streaming or status updates could move active rows repeatedly. | Group live sessions (`running`, `reconnecting`, waiting states) above inactive rows, but keep that group in a stable created-time/id order instead of latest-message order. | `Complete` |
| PP-004 | Permission requests need sidebar state, pet state, and notification behavior when the app is not focused. | Pet notification mapping already treats `waiting_for_permission` and `waiting_for_user` as `waiting`; sidebar `showStatusIndicator` excluded waiting states. App notification code focused on finished sessions. | Add sidebar waiting state first, then inspect app-focus notification behavior and extend if missing. Verify pet tests and notification path. | `Complete` |
| PP-049 | Pinned chats should scroll with projects while remaining visually separate. | Pinned chats behaved like a fixed area above the project list, so scrolling the Chat Sidebar could leave pinned chats detached from project content. | Pinned chats now keep their own section at the top of the Chat Sidebar while sharing the same scroll container as the Projects/Recent chats header and rows. Sidebar smoke verifies the shared scroll container relationship in addition to existing pinned order/unpin behavior. | `Complete` |
| PP-005 | Delete confirmation blurs the whole app and has no visible card background. | `ConfirmDialog` uses `MotionOverlay`; `.motion-overlay-surface` had no background/border/shadow, while backdrop blur was `16px`. | Add shared card styling to the overlay surface and reduce the default blur. Verify delete dialog visually and via CSS/DOM smoke where possible. | `Complete` |
| PP-006 | Codex may archive chats before deleting; investigate and mirror if appropriate. | Codex local bundle includes an `Archive chat` action and app-server archive lifecycle via `thread/archive`, `thread/unarchive`, `thread/unsubscribe`, and `thread/archived`. Orchestrator delete called remove directly. | Add an archive path that hides chats from active lists while preserving their stored record, and keep hard remove for empty/internal cleanup. | `Complete` |
| PP-007 | Header hover chips/tooltips are malformed; one wraps one character per line. | `.orchestrator-tooltip` used `overflow-wrap: anywhere` with normal wrapping and no intrinsic width, which could produce unreadably narrow chips. | Fix tooltip sizing/wrapping constraints and verify tooltip screenshots/DOM measurements. | `Complete` |
| PP-008 | Composer typing lags while an agent is generating. | Streaming updates mutate session/message state; `SessionPane` passes the full session into `InputBar`, and `InputBar` subscribed to the full session store. | Profile/inspect render path, then isolate composer props/state from streaming transcript updates where practical. Verify typing smoke or render-count evidence. | `Complete` |
| PP-009 | Confirm whether app notification behavior covers permission waits when unfocused. | Renderer notification bridge only created a system notification for inactive completed sessions. | Add a deduped permission/user-input waiting notification when the document is not focused. | `Complete` |
| PP-010 | Composer text persists when switching chats; drafts should be per chat and restored when returning. | `InputBar` owned `text` as local component state, so changing the `session` prop did not reset or restore per-session content. Empty draft-only chats were also auto-cleaned during switching. | Store composer draft text in `uiState` by session id, load it whenever `InputBar` changes sessions, and do not auto-remove empty chats with drafts. | `Complete` |
| PP-011 | Sidebar chat hover card is hidden behind the main window and nearly invisible. | Hover card was rendered inside the sidebar subtree; the sidebar uses overflow/blur styling that can clip or create a containing layer for fixed children. | Portal the hover card to `document.body` and raise its z-index above app chrome. | `Complete` |
| PP-012 | Adding a project should put the user on a new chat screen for that project. | Empty-state project add creates and activates a session, but sidebar `handleAddProject` only added the project. | Make sidebar Add Project create/activate a new local chat in the newly added project. | `Complete` |
| PP-013 | Double-clicking a chat in the sidebar no longer renames it. | `SurfaceRow` supports double-click but `SessionItem` did not wire it to rename. | Add double-click rename on chat rows and cover it in sidebar smoke. | `Complete` |
| PP-014 | Rename chat popup lacks Cancel, has redundant divider lines, and the title field can be hard to focus. | `RenameChatDialog` had only a primary button plus bordered header/footer sections. Focus was split between overlay focus management and the dialog's own focus request. | Simplify the dialog, add Cancel, remove redundant dividers, and make the input focus/select reliably. | `Complete` |
| PP-015 | Toggling terminal on a running thread with sidebar open caused the page to go blank. | Terminal panel uses xterm fit/spawn work inside animation/resize paths; unchecked fit errors can break the UI. | Guard terminal fit/resize/open paths and run focused terminal smoke. | `Complete` |
| PP-016 | Closing the last window leaves the app alive but dock click does not reopen a window; app also lacks multi-window support. | `activate` checked `BrowserWindow.getAllWindows()`, which includes the pet overlay, and `mainWindow` was a singleton. | Track real app windows, reopen/focus correctly on macOS activate, and add a New Window menu item. | `Complete` |
| PP-017 | Sidebar hover cards appear too immediately. | Sidebar card opened on `mouseenter`/focus immediately. | Add a short intent delay for pointer hover while preserving instant keyboard focus behavior. | `Complete` |
| PP-018 | Sidebar hover card should only show chat name, project name, and branch. | `SessionItem` hover card included preview, folder, environment, provider, status, and updated age. | Trim the hover card to the requested identity fields and keep it readable above the main window. | `Complete` |
| PP-019 | Browser/right panel cannot expand much beyond initial width unless maximized. | `ContextSidebar` clamped docked panel width against a 640px primary-content minimum and 720px panel maximum, leaving little flexible space on normal laptop widths. | Loosen docked panel limits while preserving a minimum usable chat area and keep full-width maximize as the explicit largest state. | `Complete` |
| PP-020 | New-chat screen lost useful prompt text like "what should we build in {project}". | `ChatView` returned an empty canvas for message-less chats and relied only on composer placeholder text. | Restore quiet project-aware prompt copy and examples in the empty chat canvas. | `Complete` |
| PP-021 | Terminal header styling feels ugly and much less clean than Codex. | Live Orchestrator shows a tall bottom-panel header with a boxed selected tab, visible plus button, and separate boxed clear/hide controls. Codex bundle uses a shared app-shell bottom-panel tab list with `h-toolbar-pane`, subtle tab backgrounds, delayed tooltips, and sticky action slots. | Rebuild the terminal header on the same shell language as the right panel: compact 28px tabs, icon-first action cluster, lighter selected state, no oversized tab bubble, and shared resize/header tokens. Verify with terminal smoke plus screenshot comparison. | `Complete` |
| PP-022 | Sidebar headers look cluttered and less clean than Codex. | Live Orchestrator shows always-visible project header action icons beside small section labels; Codex bundle uses app-shell/section primitives with quieter group headings, row-y spacing, hover actions, and tokenized sidebar surface/toolbar heights. | Make sidebar headers calm section rows: lighter labels, consistent row spacing, hover/focus-revealed organize/add actions, no header crowding, and shared sidebar header/button sizing. Verify with sidebar smoke and desktop screenshot. | `Complete` |
| PP-023 | Settings pages are messy, cluttered, and unlike Codex. | Orchestrator `SettingsModal` mixes one-off header styles, wide two-column setting groups, card grids, sliders, and dense appearance controls in one long scroll. The current left settings nav is liked and should remain. Codex bundle has `SettingsPage`, `SettingsContentLayout`, `SettingsGroup`, `SettingsSurface`, `SettingsRow`, and `SectionedPage` primitives. | Keep the left settings nav, but refactor the pages around Codex-shaped content primitives: a calmer settings shell, bounded content width, sectioned in-page structure for long pages, surface rows with label/description/control, and fewer nested cards. Verify with settings smoke and screenshots for General, Appearance, Providers, Shortcuts, Personalization, and Data controls. | `Complete` |
| PP-024 | App chrome primitives are still too one-off across terminal, right sidebar, sidebar, and settings. | Codex app-shell chunks centralize left/right/bottom panel sizing, headers, tabs, focus areas, and toolbar tokens. Orchestrator currently styles these independently in `SessionPane`, `ContextSidebar`, `Sidebar`, `SettingsModal`, and `index.css`. | Create/adopt shared Orchestrator shell primitives and CSS tokens for toolbar height, tab buttons, panel headers, section headers, action clusters, and surfaces before doing broad polish. Use them to make PP-021 through PP-023 consistent instead of patching each surface separately. | `Complete` |
| PP-025 | The right sidebar also needs the clean Codex-like header treatment. | Naming was ambiguous: PP-022 covered the Chat Sidebar, while the right-side `ContextSidebar`/`right-sidebar` tabbar still has heavier active tab weight, always-prominent action buttons, old "right sidebar" naming, and inactive tabs collapsed to icons unlike Codex. | Standardize product language to Workbench Panel, then polish its tab/header chrome with the same calmer one-line tab/action language used for Terminal Panel. Verify with right-panel smoke and screenshot review. | `Complete` |
| PP-026 | Reach Codex-level Workbench parity before bespoke Orchestrator-specific changes. | The Workbench comparison shows deeper architecture gaps beyond visual polish: shared shell primitives, tab controller behavior, ratio sizing, focus handling, performance checks, cross-panel robustness, and tab-body usability. The New tab launcher now covers Environment and Extensions and has a cheap focused launcher smoke. | Work through the Codex Parity Matrix above in small commits, prioritizing real daily-use gaps and split focused smokes over broad reruns for small UI changes. `pnpm run smoke:ui:changed` now runs the changed-files validation plan directly so tiny slices do not drift back to broad UI smoke by habit. Plan/Goal tab context can now be inserted into the composer under `planAddToChat=true`, extending the same context-handoff pattern as Environment and Agents. | `In progress` |
| PP-027 | Workbench tabs still do not fully behave like Codex tabs. | Codex close affordances are built into the tab and available beyond just the active tab; Codex also has stronger edge fades and sticky action slots. | Shared `PanelTabStrip` now gives Workbench and Terminal hover/focus close affordances, middle-click close, active-tab scroll, overflow edge fades, sticky trailing action slots, drag reorder, and focus-routed close-active-tab shortcuts. Preview/pin file tabs are smoke-covered, and Browser plus file/source tabs now have controller-backed reset paths. Remaining work: full app-shell tab-controller ownership and non-Terminal cross-panel moves. | `In progress` |
| PP-028 | Workbench sizing should be as smooth and robust as Codex. | Codex stores panel width as a ratio against main content while Orchestrator persisted only a pixel width and used overlay fallback. | Added ratio-aware Workbench sizing with pixel compatibility, docked/overlay/full-width clamps, and double-click reset on the shared resize handle. Right-panel smoke verifies docked state, full-width expansion, and narrow overlay behavior. | `Complete` |
| PP-029 | Parity needs speed and robustness gates, not only screenshots. | Current verification is mostly TypeScript plus surface smoke. That catches regressions but does not prove resize, typing, scrolling, and tab switching remain fast under live agent load. | First focused Workbench performance gate is in place as `npm run smoke:ui:auto -- --workbench-perf`, covering tab switching, panel resize, frame gaps, horizontal overflow, and Workbench render count without running the full inspector suite. The transcript stress gate now exercises a 10,001-message thread while requiring bounded mounted rows, lazy older-message loading, early-message search jump, and ready time under budget. The cross-panel keyboard gate now proves focused Workbench and Terminal close routing stay isolated. Remaining work: broader mixed-surface stress coverage. | `In progress` |
| PP-030 | Workbench and Terminal need a shared app-shell tab controller. | Codex uses the same controller-shaped tab renderer for right and bottom panels; Orchestrator rendered Workbench tabs in `ContextSidebar` and Terminal tabs in `SessionPane` separately. | First renderer slices complete: Workbench and Terminal share `PanelTabStrip` chrome/behavior, drag reorder, focus-routed close-active-tab support, preview/pin file-tab state, and reset actions for Browser plus file/source tabs. Remaining work: promote this from shared renderer to full controller with tab registration and cross-panel movement beyond Terminal. | `In progress` |
| PP-031 | Shell focus and global close-tab shortcuts need to be centralized. | Codex dispatches shell shortcut state from active focus area and routes `close-active-app-shell-tab` through right/bottom panel controllers. Orchestrator now tracks main/Workbench/Terminal focus areas and routes `Cmd/Ctrl+W` through the focused panel when a Workbench or Terminal tab can close. | Keep this command path covered by right-panel, terminal, and `--cross-panel-keyboard` smokes while future shell primitives absorb more panel behaviors. | `Complete` |
| PP-032 | Broaden parity research beyond Workbench shell. | Completed the first investigation wave across chat/thread runtime, transcript rendering, composer, search/navigation, capabilities, automations, permissions/safety, notifications/app-server robustness, review/files/workspace, and settings/personalization. | Use the Broader Parity Investigation Wave notes above as the implementation backlog before bespoke Orchestrator changes. | `Complete` |
| PP-033 | Chat switching and live transcript behavior need Codex-level verification. | Codex uses route-backed conversation identity, pending worktree launch state, turn-key virtualization, and distance-from-bottom scroll control. Orchestrator still uses message-row virtualization and direct scroll math. | First hardening slice fixed the packaged session-switch guard: when a summary tail is replaced with the first 40-message transcript page, `ChatView` now expands its render limit to the loaded page, and session-switch telemetry records the first committed visible transcript window before later lazy-load expansion. Route-backed local session identity is now split into PP-037 and smoke-gated with session switching. The session shell now measures the composer/footer reserve as `--composer-reserve-height`, and a split `--transcript-reserve` smoke gates reserve propagation plus bottom-follow behavior without relying on the broad transcript-layout suite. Grouped tool activity rows now use stable first-tool virtual row keys and expose contained message ids so transcript search jumps can target tool results inside grouped rows. Active chat read-state now clears from the transcript-visible path instead of selection/routing. Active pending/failed worktree chats now show a transcript-level lifecycle notice with failed retry coverage. Unknown thread routes now enter an explicit `Opening chat` resolving surface while local archived/missing state is inspected, so stale transcript content is not left visible during page-open checks. Long-thread stress now covers a 10,001-message transcript, bounded virtual rows, lazy older-message loading, and early-message search jump. Remaining work: a fuller shared `ThreadScrollController`, mixed switch/scroll/search stress gates, and provider-native page-open lifecycle states. | `In progress` |
| PP-034 | Capabilities should align with mature apps/MCP/plugins/skills patterns without becoming Codex-only. | Orchestrator already has a normalized `ProviderResource` and capability-sync model, while Codex app-server exposes the richest read/write reference surfaces for apps, skills, plugins, MCP, config, and external agent imports. | Keep Capabilities as the primary provider-agnostic inventory, finish Codex-native bundle inspection, then add adapter-backed native install/config flows, app/skill/plugin pickers, mention insertion, and MCP reload/OAuth/resource controls where each provider supports them. | `Todo` |
| PP-035 | Automations and follow-ups need provider-agnostic scheduling before provider-specific UX. | Codex supports persisted cron/heartbeat automations, RRULE schedules, run-now, pause/delete, run history, execution environments, and heartbeat eligibility/permission snapshots. Orchestrator now has persisted automation records, Add/Edit plus Run now/Pause/Resume/Delete automation thread actions, runtime next-run metadata for interval/common RRULE schedules, pause/resume/delete lifecycle, active-scheduled lifecycle warnings, sidebar paused/active/next-run/running metadata, permission snapshots, due-list selection, run-history records, due-run/manual-run execution boundaries, deferred provider-exit run completion, app-owned scheduler daemon/timer wiring, Automations settings page with Current/Paused/Run history sections, schedule editing UI for manual/interval/RRULE modes, session-state eligibility checks, single-flight guards, ineligible skip recording, failure recording, and session permission snapshot capture/execution for permission mode plus tool allow/deny lists. | Local automation lifecycle is now covered for session-targeted records; provider-backed remote/thread targets remain next. | `In progress` |
| PP-036 | Review/files/workspace parsing should match Codex robustness. | Orchestrator currently has regex file-reference extraction, basename fallback, preview-first review rendering, flat changed-file rows, and capped renderer-side workspace crawling. Codex has diff-first review, file trees, line-aware open routing, lazy workspace trees, and fuzzy search sessions. | File-reference hardening started: absolute references outside the current workspace no longer resolve by basename into unrelated local files, while relative/suffix workspace matches still work. Review now defaults textual and rich binary changed files to the diff/binary state and exposes an explicit Preview/Diff toggle for changed rich files. Workspace file browsing/search now runs through a main-process, ignored-dir-aware, fuzzy-ranked search API instead of capped renderer recursion; tests cover deep files, ignored directories, ranking, browsable listings, and truncation. Changed files now render as grouped directory rows with aggregated stats, Git status parsing preserves paths with spaces without porcelain quotes, the Review list supports Codex-style keyboard movement through active file paths, the Review toolbar exposes focused previous/next changed-file navigation without disrupting the primary Review options/Jump/Refresh order, the Review source summary now exposes visible/smoke-readable workspace-root identity, and selected current-file diff lines can be copied or added to the main composer directly from Review. Remaining work: deeper provider source/root metadata and provider diff navigation ergonomics. | `In progress` |
| PP-037 | Route-backed session identity is needed for Codex-like reliability. | Codex derives active conversation from local/remote/hotkey routes and supports copy links/open-in-new-window semantics. Orchestrator previously consumed app-protocol pending navigation but could not reopen a chat from the renderer route alone. | Added `/threads/{id}` and `#/threads/{id}` route helpers, renderer boot parsing, active-session URL sync, popstate/hashchange session routing, and automated session-switch smoke gates for direct route startup plus route updates during chat switches. Missing and archived route ids now show an explicit recovery surface; archived links can restore/reopen the thread, and missing links can return to the current chat. Sidebar/header session action menus now expose both provider/app deeplink copy and in-app `Copy thread link`, with focused sidebar smoke proving the copied browser/app route matches the selected thread and clipboard contents. Remaining work: provider-native remote/hotkey route semantics. | `In progress` |
| PP-038 | Pending worktree/fork lifecycle should be first-class. | Codex pending worktree conversations carry start-vs-fork mode, source turn, owner metadata, pin placement, title, goal, browser-transfer metadata, and waiting/starting/failed/succeeded lifecycle state. Orchestrator now creates an immediate pending session record for new-worktree thread-menu forks, materializes the git worktree in the background, updates the same row to ready, marks failures explicitly, exposes failed-row retry, scopes cleanup to app-managed `.orchestrator-worktrees/<session id>` paths, provides Worktrees settings inventory/create/open/delete for app-managed worktrees with linked chat archiving, linked-chat switching, and explicit project/base/branch creation, and shows pending/failed lifecycle state in the active chat surface with retry from the failure notice. | Extend the pending lifecycle to new worktree chats, fork-latest/fork-turn, side-chat worktree flows, browser-transfer metadata, raw Git worktree switching, richer local-environment metadata, and provider-native worktree settings adapters. | `In progress` |
| PP-039 | Composer attachments should be isolated per chat. | Text drafts are now per chat, but composer attachments remained local `InputBar` state and could leak across session switches. Codex treats attachment state as part of composer context. | Added `composerAttachments` to per-session UI state, keyed attachment add/remove/clear through the session store, and expanded focused composer smoke coverage so draft plus attachment isolation are verified together. | `Complete` |
| PP-040 | Composer file input needs Codex-like pending states. | Codex supports drag/drop, pasted files/images, pending upload chips, cancel/error states, prompt history, configurable enter behavior, and richer context chips. Orchestrator read pasted files fully in the renderer and had no drag/drop overlay, pending/cancel feedback, prompt recall, or Enter-key preference. | Drag/drop now shows a drop overlay and attaches through the saved-file path; pasted/dropped files show saving/error chips with explicit cancel/dismiss actions; async saves capture the target session; attachment-only empty chats are protected from cleanup; focused composer smoke covers per-chat attachments, attachment-only session preservation, delayed-save switch isolation, delayed large attachment cancel, drop overlay, and drop attachment. Composer now also shows compact workspace/local/additional-directory context chips with accessible detail metadata, smoke-gated as `composerContextChips=true`. Successful sends are stored in per-session prompt history and `ArrowUp`/`ArrowDown` recalls prior prompts while restoring the unsent draft, smoke-gated as `composerPromptHistoryRecall=true`. General Settings now persists `composerEnterBehavior`, with `Enter sends` and `Enter inserts line` modes; the composer switches the send shortcut to `Command/Ctrl+Enter` in newline mode and the focused composer/settings smokes gate `composerEnterBehaviorSetting=true` and `settingsGeneralComposerEnterBehavior=true`. Remaining work: deeper provider-backed attachment/context behavior and richer provider-native context sources. | `In progress` |
| PP-041 | Command, shortcut, and search handling need one registry. | Codex centralizes command metadata/keybindings and uses modeful command-menu search for files/chats/find. Orchestrator split `APP_COMMANDS`, menu accelerators, and global keydown handlers. | Global renderer keydown handling now resolves through the shared app command registry, including command palette, chat creation, find, find-next/find-previous, file search, rename/pin, chat navigation, panel toggles, settings, shortcuts, and chat slots. `Cmd/Ctrl+P` opens the Files Workbench tab and focuses workspace file search; `Cmd/Ctrl+G` and `Cmd/Ctrl+Shift+G` step the active shared find surface or Browser page find. Files search now persists recent queries from Enter or file-result opens and exposes a compact shared-menu history picker in the Files search field, smoke-gated as `filesSearchHistory=true`. Remaining work: deeper provider-indexed search across chat/files/settings where provider/runtime indexes exist. | `In progress` |
| PP-042 | Provider runtime transport must fail loudly and recoverably. | The old EPIPE crash was globally mitigated, but `CodexAppServerSession` still lacked child/stdin error listeners, write callbacks, pending-request rejection, and unexpected-exit failure events; generic PTY lanes already had spawn/exit cleanup tests but fewer low-level transport signals to hook. | Hardened the Codex app-server provider adapter as the first concrete implementation: stdin errors, child errors, write-callback failures, unexpected exits, and pending request cleanup now produce `run.failed` instead of silent idle fallback. Generic PTY lanes remain covered by provider runtime spawn/exit tests; PP-043 will add provider-host diagnostics across adapters. | `Complete` |
| PP-043 | Provider runtime diagnostics should mirror Codex's strongest connection/debug signals. | Codex tracks host-scoped connection states, versions, typed errors, subscriptions, and notification debug rings. Orchestrator had generic event buffers without provider-runtime-specific diagnostics. | Added provider-neutral runtime connection snapshots, generic PTY start/stop/spawn/exit state, Codex app-server initialize/thread/version/failure state, a bounded runtime debug ring, Codex app-server request/notification/permission/unsupported-tool/transport-failure recordings with host/thread/method/severity/noisy metadata, IPC access, provider diagnostics integration, a hidden provider-details Runtime card, and shared safe-send guards for production renderer/pet messages. Remaining follow-up can add provider-specific subscription state when available. | `Complete` |
| PP-044 | Permission modes should be dynamic, explicit, and provider-adapter backed. | Backend mapping separated `approvalPolicy`, `approvalsReviewer`, and sandbox policy, but renderer UI exposed static provider modes. Codex derives mode visibility/defaults from app-server config and cwd requirements; other providers expose different but related permission surfaces. | First slice complete: `ResolvedExecutionPolicy` now carries an explicit provider-backed execution contract for native mode, approval policy, reviewer, sandbox, tool policy, and config source; Codex app-server launch config consumes the same policy mapping as provider runtime info; settings and composer surfaces display compact contract chips; provider tests and settings-provider smoke assert the contract stays visible. Second slice in progress: added a provider-neutral permission runtime context; Codex now reads live app-server `config/read` and `configRequirements/read` into mode defaults/visibility when available, while non-Codex providers retain the same static contract. Composer permission context now exposes a refresh action and announced refresh status for live, fallback, and static contexts. Remaining follow-up: promote successful live Codex context into saved defaults only when the user confirms, and add provider-backed switching proof when real provider paths are available. | `In progress` |
| PP-045 | Permission request cards need richer provider-normalized structure and pet parity. | Codex expands network/filesystem permission requests into explicit parts. Orchestrator preserves raw permissions generically, and pet overlay lacked `Allow Session`; other providers also emit tool/permission denials with provider-specific payloads. | Added a shared permission request detail model for command/file/network/MCP/plan/tool approvals, rendered transcript permission cards as structured detail rows, and gave the pet overlay explicit `Allow Once`, `Allow Session`, and `Deny` actions backed by the existing allow-once/session resume paths. Provider fixtures lock the detail model across Copilot, Codex, Cursor, and Claude MCP/web approvals; main-window focus reconciles stale recoverable waiting/running safety states against the live provider runtime; pet-overlay smoke now clicks `Allow Session` and verifies the session records `allowed_session`. | `Complete` |
| PP-046 | Settings need full command/theme/data parity. | Codex settings are routed full-page surfaces with grouped sections, reusable rows, editable shortcuts, personalization, and archived chat data controls. Orchestrator settings are calmer now but still smaller and partly one-off. Theme presets are also currently confusing: some preset clicks do not appear to fully apply across the app, and the preset UI lacks visual previews or a clear relationship to the custom color values. | Data controls now have provider-neutral archived chat inventory APIs plus Settings UI to refresh, restore, or permanently delete archived chats with confirmation. Shortcuts now support persisted editable primary shortcut overrides with record/reset controls, command-palette display, and global keydown matching. Theme presets now apply concrete light/dark chrome models, preserve the active chrome model when other appearance controls change, and include visual preset cards plus a compact theme preview smoke test. The pet settings entry is now user-facing Personalization, matching Codex taxonomy while keeping the existing internal route stable. Added a reusable shared `SettingsRow` primitive for appearance color/range/toggle controls. Settings section navigation now pushes owned settings routes so browser/app Back can move between Settings sections, smoke-gated as `settingsSectionHistoryNavigation=true`. | `Complete` |
| PP-047 | Capabilities native actions should be gated but real through provider adapters. | Existing sync plans deliberately block provider-native installs and app-server writes without confirmation. Codex exposes richer plugin install/read, skills config, app connector, MCP reload/OAuth, and external agent import flows; Claude/Cursor/Copilot have different native management surfaces. | Add confirmation-backed native actions using the existing preview/apply plan model, starting with Codex `plugin/read/install/uninstall`, `skills/config/write`, and MCP reload/OAuth diagnostics, then map equivalent Claude/Cursor/Copilot actions through adapters. | `Todo` |
| PP-048 | Workspace file search and open-file routing should preserve intent. | Codex preserves cwd, line, column, target, preview mode, and open outcome through a generic open-target model; Orchestrator stripped line suffixes and opened only paths. | File references now preserve line/column metadata separately from paths, cards display the target suffix, and `fs.openPath` carries line/column through a provider-agnostic open-target registry. Targets declare URL-scheme and optional CLI line-target capabilities, so VS Code, VS Code Insiders, Cursor, and Zed behavior is selected by target capability instead of a user-machine-specific branch. Open routing now returns structured outcome telemetry with target, method, line/column, fallback source, opened-with detail, and errors. Settings asks the main process for target availability and disables unavailable editor targets instead of hardcoding the user's local machine. Workspace search integration now preserves cwd/root intent through a shared main-process search result shape used by the Files panel. File/source tabs now retain the last structured open outcome, announce the resolved target/method/line/fallback in the tab status, expose smoke-visible diagnostics for the focused Files gate, show workspace-root identity in the tab chrome, resolve preview file tabs against the encoded host/root so same-path files in different roots/worktrees are distinguishable, emit absolute path/line references when a source tab belongs to a non-primary root, and run selected-line blame against that encoded root. Transcript file-reference cards now retain structured open outcomes too, with status text plus target/method/opened-with diagnostics, can open workspace-contained references from either the primary workdir or configured additional roots directly into a preview Workbench source tab at the referenced line, and can attach a resolved referenced file back into the composer for follow-up prompts. Remaining work: richer preview-mode semantics at non-tab call sites and optional custom target discovery. | `In progress` |
| PP-050 | The app window is nearly impossible to move/drag while a response is generating. | User testing reports native window dragging becomes effectively stuck during active response generation. The first focused smoke reproduced the renderer pressure: 90 visible streaming updates caused 91 `App` commits and 91 `Titlebar` commits. Electron debugger input could not exercise native titlebar dragging in this environment, so the automated gate measures active streaming visibility, app/titlebar commit counts, and frame gaps. | Added a focused streaming-drag smoke that proves the visible streaming path is active, then isolated app chrome from message-only session updates by moving `App` and `Titlebar` to narrower Zustand selectors and memoizing the titlebar. The same smoke now holds `App` and `Titlebar` at 0 commits during the streaming burst while the final streamed text is visible. Manual native drag confirmation in the installed app is still useful because automated debugger drag does not move the window even when idle. | `Complete` |
| PP-051 | Composer typing still has bad latency while a response is streaming. | User testing after PP-050 reports typing while streaming is still bad. The focused smoke reproduced a structural issue: visible streaming caused 111 `SessionPane` commits and 134 `ChatView` commits while typing. | Added a streaming-typing smoke that measures typed text, timer drift, input dispatch, frame gaps, and `InputBar`/`SessionPane`/`ChatView` commits. `SessionPane` now subscribes only to shell/session metadata and terminal UI, while `ChatView`, `RunningAgentsStrip`, and `ContextSidebar` own transcript-heavy session subscriptions. The smoke now passes with `SessionPane` commits reduced to 4 while the stream remains visible and typed composer text is preserved. Remaining future optimization, if needed: batch/collapse transcript rendering itself. | `Complete` |
| PP-052 | Message editing and resend/regenerate flows are missing. | Codex-style chat products support editing a prior user message and continuing from that point, while Orchestrator previously had chat rename/delete/archive but no message-level edit affordance. | First slice adds a provider-neutral `Edit message as draft` action for completed user messages, replacing the active composer draft and attachments, returning focus to the composer, and preserving transcript history. The composer now shows an `Editing a copy` draft-source notice with original-transcript preservation copy, source message id metadata, attachment count metadata, a clear action, and a `Restore previous draft` action when the edit copy replaces existing unsent composer content. Message-level local fork support now provides a first branch point from a selected transcript message. The latest assistant response now also exposes a compact `Regenerate` action backed by the existing provider-neutral retry-last-user-message path and smoke-gated status/a11y checks. Remaining work: richer provider-native edit/resend/regenerate semantics. | `In progress` |
| PP-053 | Conversation forking and branch history need productized UI. | PP-038 tracks pending worktree/fork lifecycle, but conversation forking also needs message-level and latest-turn branching semantics visible to users. | First slices add `Fork from here` on completed user messages and the latest assistant response, plus a provider-neutral fork option that truncates the new fork through the selected message, clears provider session id, appends a lineage marker, persists fork-source metadata, surfaces the source in the sidebar row/hover detail, exposes local/same-worktree/new-worktree message-fork choices when available, keeps forks from locally pinned sources in the local pinned set while clearing provider-owned pin state, and makes whole-chat menu forks explicit latest-turn forks that exclude later control/status rows. Remaining work: provider-native fork/resume mapping where available. | `In progress` |
| PP-054 | Git action buttons and review UI are still thin. | Review/files parsing improved, but the first Orchestrator Stage/Unstage controls were not Codex-like. Bundle check shows Codex Review uses review options, refresh, wrap, expand/collapse, diff mode, and Copy git apply command; staging/commit behavior belongs in a separate Git/commit surface. | Review stays Codex-shaped with no staged/unstaged row clutter or Stage/Unstage toolbar buttons, while the Workbench now has a dedicated Git tab for branch/changes summary, per-file Stage/Unstage/Discard, Create branch, Checkout existing branch, Copy PR command/View PR, Stage all, Unstage all, commit message/Commit staged, confirmed Discard all, and Open Review. Provider inline review comments now parse GitHub suggestion fences, preserve multi-line review-thread start lines, expose Copy/Apply suggestion actions, and apply current unresolved new-side suggestions to the local file with focused `--diff-source` smoke proof. Review copy feedback is independent from Git mutation ownership, and Review's former floating local-git mutation bridge is now retired to an `Open Git` handoff that keeps branch/commit/discard/stage ownership in the dedicated Git tab. Remaining work: live PR creation, provider-hosted Git metadata, and exact live Codex UI timing. | `In progress` |
| PP-055 | Worktree management needs first-class provider-agnostic controls. | PP-038 captures the pending worktree launch shape. Orchestrator now has a provider-neutral Worktrees settings inventory, explicit local create controls for project/base/branch, linked-chat open controls, and targeted cleanup for app-managed local worktrees, including linked chat archiving. | Add raw Git worktree switch flows, richer local metadata/local-environment choices, and provider adapters for Codex, Claude, Cursor, Copilot, and future providers where supported. | `In progress` |
| PP-056 | The generic inspector smoke is noisy and no longer trustworthy. | After the focused Browser tab reset slice, `npm run smoke:ui:auto -- --inspector` reported unrelated failures: empty-state quietness, old inspector-tab text matching, right-panel chrome assumptions, and Review preview/search checks despite the generic inspector fixture having no changed files. Focused `--diff`, `--files`, `--browser`, `--right-panel`, and `--terminal` smokes already own those deeper surface contracts. | Re-scoped `--inspector` to verify Workbench shell/open-state and cross-surface wiring only, while keeping deep Review/Files/Browser/Terminal assertions in their focused smokes. The right-panel compactness check now accounts for the intentional trailing-fade tab overflow, and the Browser screenshot/attachment checks remain owned by focused `--browser`. | `Complete` |
| PP-057 | Styling parity pass should address lingering hover tooltips and crowded Workbench chrome. | PP-056 screenshot review showed the smoke-created header tooltip lingering over the top chrome, and the right-panel screenshots still show dense Workbench tab/header chrome compared with Codex. These are styling-system issues, not one-off inspector failures. | Compared Codex `tooltip-DzyyaBl-.js`, `tabs-BgnCzZaP.js`, and `app-shell-BJK30dyj.css`. Orchestrator shared tooltips now use a calmer delayed pointer-hover lifecycle with outside pointer, Escape, context-menu, scroll, resize, and blur dismissal. Workbench and Terminal tabs now share app-shell sizing/weight tokens, lighter active states, overlay close controls, and less aggressive trailing fade. | `Complete` |
| PP-058 | Workbench and sidebar interiors still miss Codex-level styling parity. | A 2026-05-22 fine-grained audit of Codex bundle chunks found specific remaining gaps in typography, borders, radii, row weights, file tree metrics, search fields, browser tab chrome, settings rows, flyout sizing, hidden-webview lifecycle, and virtualized file/diff rendering. Current Review/Files/Browser screenshots pass smoke but still look heavier and more locally styled than Codex. | Use `docs/codex-side-panel-ui-parity-audit.md` as the styling source of truth. First slices added shared Workbench search/list primitives across Review and Files, lightened badges and row selection, made fixture-backed Workbench smokes reset their git workspace before each run, normalized Browser toolbar/find/local-server chrome, moved Settings rows/search toward shared Codex-like primitives, added route-backed Settings section history plus topbar section search/jump, aligned the routed Settings topbar to the shared app-shell header band/focus-area contract, added in-page Settings search anchors for high-frequency controls like General Composer/File handoff, calmed Chat Sidebar row typography/active states/action motion, added shared bounded menu surfaces with compact menu-row smoke coverage in Sidebar/Review/Files/Capabilities/simple Browser actions, moved confirm/text-input/rename/generic modal dialogs onto a shared compact dialog primitive, added Browser history clear flow in the shared actions menu, Browser hidden-page identity, Browser zoom presets using shared menu rows with smoke proof, added file-tab workspace/root identity, added keyboard navigation to the Workbench New Tab launcher, and made open singleton launcher actions switch to their existing tabs instead of disabling. Remaining slices: deeper Browser lifecycle/screenshot parity, deeper settings page/window structure, broader first-class file-tab call-site semantics, and screenshot gates for each surface. | `In progress` |

## Verification Log

- 2026-05-20: `git status --short --branch` shows `main...origin/main [ahead 1]` and untracked `bootstrap.js`.
- 2026-05-20: Initial code inspection confirms sidebar ordering is activity timestamp based, delete dialog has no surface card style, sidebar waiting status is not in `showStatusIndicator`, pet notification semantics already know waiting states, and tooltip CSS can wrap anywhere.
- 2026-05-20: Added `compareSidebarSessions` with tests for active blank chats and stable live-chat ordering. `npm run test:providers` passed all 170 tests.
- 2026-05-20: Local Codex bundle inspection found UI copy `Archive chat`; action calls `archive-conversation`, and app-server manager sends `thread/archive`, suppresses archived conversations, unpins archived threads, removes them from cache, handles `thread/archived`, and uses `thread/unsubscribe` for empty discarded threads.
- 2026-05-20: Composer latency likely came from `InputBar` subscribing to the entire Zustand store plus receiving a full `session` object. First mitigation isolates store selectors and memoizes the composer against message-only session churn.
- 2026-05-20: Verification passed for first slice: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, and escalated `npm run smoke:ui:auto -- --sidebar`.
- 2026-05-20: Archive slice adds `sessions.archive`, `archivedAt`, active-list filtering, `session:archived`, and Archive chat UI copy. Verification passed: web/node TypeScript, `git diff --check`, escalated sidebar smoke, and `npm run test:providers` with 170 passing tests.
- 2026-05-20: Added PP-010 through PP-012 from user testing: per-chat composer drafts, sidebar hover-card visibility, and Add Project opening a new chat.
- 2026-05-20: Draft/hover/add-project slice complete. Composer drafts are stored per session in `uiState`; draft-only empty chats are no longer auto-removed on chat switch/new-chat cleanup; sidebar hover cards portal to `document.body`; sidebar Add Project creates and activates a new chat in the added project. Verification passed: web/node TypeScript, `git diff --check`, escalated sidebar smoke, and escalated composer smoke with `composerDraftsPerChat: true`.
- 2026-05-20: Added PP-013 through PP-017 from user testing: double-click rename, rename dialog polish/focus, terminal blanking, window reopen/multi-window behavior, and delayed sidebar hover cards.
- 2026-05-20: Rename/window/terminal slice complete. Chat rows now double-click rename; rename dialog has Cancel, no redundant divider rows, and reliable input focus; sidebar hover cards use delayed pointer intent; terminal fit/resize is guarded against hidden/zero-size containers; app tracks real windows separately from pet overlay, supports File > New Window, and reopens/focuses an app window on macOS activate. Verification passed: web/node TypeScript, `git diff --check`, escalated sidebar smoke, and escalated terminal smoke.
- 2026-05-20: Added and completed PP-018 through PP-020 from user testing. Sidebar hover cards now show only chat title, project, and branch when available; right-panel docked sizing allows a wider browser before full-width expansion while preserving a minimum main-chat area; empty new chats show quiet project-aware prompt copy. Verification passed: web/node TypeScript, `git diff --check`, escalated sidebar smoke, escalated browser smoke, and escalated main UI smoke.
- 2026-05-20: Added PP-021 through PP-024 from Codex parity review. Direct Computer Use inspection of Orchestrator confirmed the terminal header, sidebar headers, and settings layout issues; Computer Use cannot inspect `com.openai.codex`, so Codex comparison came from the installed app bundle. Relevant Codex chunks: `app-shell-CcsLZiAu.js`, `app-shell-BJK30dyj.css`, `thread-page-header-DOy8QQg2.js`, `settings-page-BoavEVDX.js`, `settings-content-layout-BeqejwUq.js`, `settings-group-DwDYNKp_.js`, `settings-surface-BLiaJ7K3.js`, `settings-row-BUhYC5Lf.js`, `sectioned-page-C-MGQMtH.js`, and `tabs-Dhgr0Bym.js`.
- 2026-05-20: User clarified the current left settings nav is good. PP-023 now preserves that nav and scopes settings polish to page hierarchy, content primitives, and row/surface layout.
- 2026-05-20: UI chrome cleanup slice complete. Terminal header now uses a compact one-line tab/action strip; sidebar project header actions are hover/focus-revealed; settings keeps the liked left nav but uses a quieter shell/topbar, calmer setting groups, softer panels, and less heavy choice cards. Verification passed: web/node TypeScript, `git diff --check`, `npm run smoke:ui:auto -- --sidebar`, `npm run smoke:ui:auto -- --terminal`, and `npm run smoke:ui:auto -- --settings`; screenshot review caught and fixed terminal tab label wrapping before final terminal smoke.
- 2026-05-20: User clarified "sidebar" meant both sides. Added canonical surface names: Chat Sidebar for the left rail, Workbench Panel for the right contextual panel, and Terminal Panel for the bottom shell. Added PP-025 for Workbench Panel chrome polish.
- 2026-05-20: Workbench Panel comparison and polish slice complete. Codex bundle inspection showed labeled right-panel tabs, sticky tab actions, overflow fades, thin bordered pane surfaces, and ratio-based sizing. Orchestrator now uses Workbench Panel labels, calmer right-panel chrome, visible inactive tab labels, and corrected right-panel smoke coverage for true narrow overlay widths. Verification passed: escalated `npm run smoke:ui:auto -- --right-panel` with screenshot review at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-right-panel-1779312014437.png`.
- 2026-05-20: Expanded the Workbench comparison into a broader Codex Parity Matrix covering usefulness, efficiency, speed, robustness, shared shell structure, sizing, tab lifecycle, styling tokens, and verification gates. Added PP-026 through PP-029 so future work prioritizes parity before bespoke Orchestrator-specific behavior.
- 2026-05-20: Completed two deeper Codex bundle dives: shell/panel/layout mechanics and tab/focus/shortcut lifecycle. Added migration notes for a shared Orchestrator app shell, ratio-based Workbench sizing, shared Workbench/Terminal tab controller, hover-overlay close affordances, overflow fades, sticky actions, focus-area shortcut routing, and shared resize semantics. Added PP-030 and PP-031.
- 2026-05-20: Added Additional Codex Parity Dives beyond Workbench shell: chat/thread runtime, live transcript rendering, composer/input ergonomics, search/navigation, MCP/apps/plugins/skills, automations/follow-ups, permissions/safety, notifications/app-server robustness, review/diff/files/workspace, and settings/personalization. Added PP-032 through PP-036.
- 2026-05-20: Completed the first broader parity investigation wave. Read-only subagents inspected chat/thread runtime, live transcript rendering, composer/input, search/navigation, automations, permissions/safety, notifications/provider-runtime robustness, review/files/workspace, and settings/personalization. The MCP/apps/plugins/skills subagent stalled, so that area was completed locally from `providerResources`, `capabilitySync`, `provider-resource-dedupe-spike`, `capability-sync-spike`, and `codex-appserver-support-matrix`. Added detailed notes plus PP-037 through PP-048.
- 2026-05-20: Corrected the parity framing to be provider-agnostic. Codex remains the richest local reference implementation, but shared concepts should become Orchestrator primitives with provider adapters for Codex, Claude, Cursor, Copilot, and future providers where possible.
- 2026-05-20: Completed PP-042 transport hardening slice. `CodexAppServerSession` now listens for stdin errors and child process errors, uses stdin write callbacks, emits `run.failed` for unexpected exits before a terminal run event, rejects pending client requests on close, clears pending server requests, and avoids silent idle fallback after app-server transport failure. Verification passed: node TypeScript, web TypeScript, compiled targeted `codexAppServerRuntime` test with 4 passing tests, and `npm run test:providers` with 173 passing tests.
- 2026-05-20: Started PP-036 with the file-reference trust fix. `resolveWorkspaceFileReference` no longer resolves absolute references from another workspace by basename into the current workspace, preventing prose or stale absolute paths from becoming misleading file cards when only the basename happens to exist locally. Verification passed: node TypeScript, web TypeScript, and compiled targeted `toolActions` plus `workspaceResolver` tests with 10 passing tests.
- 2026-05-20: Continued PP-036 with the Review diff-first slice. `DiffPanel` now renders textual diffs before rich current-file previews and keeps binary diff markers out of text diff rendering, matching the Codex principle that changed files should open on the diff path first. Verification passed: node TypeScript, web TypeScript, compiled targeted `reviewPreview`, `toolActions`, and `workspaceResolver` tests with 12 passing tests, plus `git diff --check`.
- 2026-05-20: Completed PP-039 composer attachment isolation. Composer attachments now live in per-session UI state alongside composer drafts, including external add-composer-attachment events and remove/clear flows. Verification passed: node TypeScript, web TypeScript, focused escalated `npm run smoke:ui:auto -- --composer` with `composerDraftsPerChat`, `composerAttachmentsPerChat`, and `composerAttachmentsClearedOnSwitch` all true, plus `git diff --check`.
- 2026-05-20: Started PP-048 with line/column-preserving file references. `extractFileReferences` now keeps `:line` and `:line:column` as metadata instead of stripping them, file cards display the target, and `fs.openPath` passes target metadata into VS Code, VS Code Insiders, and Cursor URL opens before falling back to the selected app open path. Verification passed: node TypeScript, web TypeScript, compiled targeted `toolActions` plus `workspaceResolver` tests with 11 passing tests, plus `git diff --check`.
- 2026-05-20: Continued PP-036 with an explicit Review Preview/Diff toggle. Changed rich files default to the textual diff path, and users can switch to the rich current-file preview with a toolbar/menu control. Verification passed: node TypeScript, web TypeScript, focused escalated `npm run smoke:ui:auto -- --diff` with `reviewDiffFirst` and `reviewJsonPreview` both true, plus `git diff --check`.
- 2026-05-20: Addressed read-only review follow-ups for PP-036 and PP-048. Rich binary-native changed files such as images now default to the binary diff state instead of current-file preview, with Preview as an explicit toggle. Focused diff smoke now covers JSON, CSV, DOCX, notebook, image binary-diff default, image preview toggle, and generic binary state. `editorFileUrl` is extracted and tested for URL-scheme line opens, spaces, missing schemes, and invalid target values. Verification passed: node TypeScript, web TypeScript, compiled targeted `editorOpen`, `toolActions`, and `workspaceResolver` tests with 14 passing tests, focused escalated `npm run smoke:ui:auto -- --diff`, and `git diff --check`.
- 2026-05-20: Started PP-040 composer file input parity. Composer now supports drag/drop with an overlay, pending/error attachment chips for pasted or dropped file saves, dismiss/cancel behavior, and target-session-safe async attachment completion. Empty-chat cleanup now treats composer attachments as draft state so attachment-only chats are preserved. Verification passed: node TypeScript, web TypeScript, focused escalated `npm run smoke:ui:auto -- --composer` with `composerAttachmentOnlySessionPreserved`, `composerDropOverlay`, and `composerDragDropAttachment` true, plus `git diff --check`.
- 2026-05-28: Continued PP-040 with visible composer context chips. Active composer surfaces now show the current workspace, local/branch execution mode, and additional-directory count as compact chips with accessible detail metadata. The focused composer smoke seeds two additional context directories and gates `composerContextChips=true`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --composer`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780007565893.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780007565893.png`.
- 2026-05-20: Started PP-041 command registry consolidation. Added keyboard-event resolution to the shared app command registry and routed the renderer global keydown handler through it instead of a separate hard-coded switch. Verification passed: node TypeScript, web TypeScript, compiled targeted `appCommands` tests with 3 passing tests, and `git diff --check`.
- 2026-05-20: Continued PP-041 with registry-backed file search. Added `open-file-search` on `Cmd/Ctrl+P`, exposed it in command palette/shortcuts, opened the Files Workbench tab from the command handler, and focused `workspace-file-search` through a panel event. Verification passed: node TypeScript, web TypeScript, compiled targeted `appCommands` tests with 3 passing tests, focused escalated `npm run smoke:ui:auto -- --files`, and `git diff --check`.
- 2026-05-21: Continued PP-045 permission request detail/pet parity. Added provider fixture coverage across Copilot, Codex, Cursor, and Claude MCP/web permission payloads; main-window focus now reconciles stale recoverable waiting/running safety states against the live provider runtime. Verification passed: `npm run build`, `npm run test:providers` with 190 passing tests, escalated `npm run smoke:ui:auto -- --pet-overlay`, and `git diff --check`.
- 2026-05-21: Completed PP-045 by upgrading pet-overlay smoke from passive action visibility to an actual `Allow Session` click assertion. The focused smoke now verifies `permissionAllowSessionDecision: true` after clicking through the overlay action path. Verification passed: `npm run build`, escalated `npm run smoke:ui:auto -- --pet-overlay`, and `git diff --check`.
- 2026-05-21: Started PP-046 settings data parity. Added archived chat inventory, restore, and permanent-delete APIs plus Data settings UI for refresh/restore/delete with confirmation. Settings smoke now seeds an archived chat and verifies the real row plus Restore/Delete controls. Verification passed: `npm run build`, escalated `npm run smoke:ui:auto -- --settings`, and `git diff --check`.
- 2026-05-21: Continued PP-046 with editable shortcut overrides. Added persisted shortcut overrides, Settings record/reset controls, command-palette display of effective shortcuts, global keydown matching against overrides, command unit tests, and a settings smoke assertion for recording `Open File Search` as `Cmd+Shift+O`. Verification passed: `npm run build`, `npm run test:providers` with 192 passing tests, escalated `npm run smoke:ui:auto -- --settings`, and `git diff --check`.
- 2026-05-21: Continued PP-046 with theme preset cleanup. Preset cards now show compact visual previews and apply concrete chrome theme models for light/dark/system; other appearance controls preserve the active chrome model instead of falling back through the legacy preset path. Settings smoke now applies the Ocean preset and verifies the root chrome surface updates. Verification passed: `npm run build` and escalated `npm run smoke:ui:auto -- --settings`.
- 2026-05-21: Continued PP-046 with the settings taxonomy cleanup. The left settings nav and topbar now call the pet settings surface `Personalization`, matching Codex while preserving the current internal `pets` route and smoke entrypoint.
- 2026-05-21: Completed PP-046 by adding a shared `SettingsRow` primitive and moving appearance color, contrast, sidebar tint, and preference toggles onto it with nested/surface variants. Verification passed: `npm run build`, escalated `npm run smoke:ui:auto -- --settings`, and `git diff --check`.
- 2026-05-21: Completed PP-049. Pinned chats now live inside the shared Chat Sidebar scroll container with the Projects/Recent chats header and rows while retaining their separate Pinned section. Sidebar smoke now asserts the shared scroll container relationship. Verification passed: `npm run build`, escalated `npm run smoke:ui:auto -- --sidebar`, and `git diff --check`.
- 2026-05-21: Continued PP-036/PP-048 with Codex-backed workspace file search parity. Local Codex bundle inspection confirmed the target shape: lazy directory browsing, ignored-dir-aware fuzzy file search sessions, compact `Filter files...` chrome, and flattened tree-like search results. Orchestrator now exposes `fs.searchWorkspace` from the main process, moves the Files panel off renderer recursion, keeps the compact Files UI, and covers deep files beyond the old depth/cap. Verification passed: `npm run build`, `npm run test:providers` with 197 passing tests, escalated `npm run smoke:ui:auto -- --files`, and `git diff --check`.
- 2026-05-21: Continued PP-036 with Codex-style changed-file tree rows in Review. The changed-file list now emits directory ancestors once, aggregates directory file counts and +/- stats, keeps selection/diff behavior on file rows, and uses NUL-delimited Git porcelain status so paths with spaces are not displayed with quotes. Diff smoke now includes a nested changed file and asserts `diffTreeGrouping`. Verification passed: `npm run build`, `npm run test:providers` with 201 passing tests, escalated `npm run smoke:ui:auto -- --diff`, screenshot review at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-diff-1779391419428.png`, and `git diff --check`.
- 2026-05-21: Continued PP-036 with Review keyboard navigation parity. Codex uses active file paths and selection-change callbacks in the review file tree; Orchestrator now keeps the grouped list keyboard-reachable, supports ArrowUp/ArrowDown/Home/End over filtered file rows, scrolls the active file row into view, and exposes stable `data-review-path` hooks for UI verification. Verification passed: `npm run build`, `npm run test:providers` with 202 passing tests, escalated `npm run smoke:ui:auto -- --diff` with `diffKeyboardNavigation: true`, screenshot review at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-diff-1779391789846.png`, and `git diff --check`.
- 2026-05-21: Continued PP-048 with Zed line-target support. Official Zed CLI docs confirm `zed myfile.txt:42:10` opens line/column targets, so Orchestrator now uses the `zed` CLI with `file:line:column` when a file card has a target and the CLI is installed, then falls back to the existing app-open path. Verification passed: `npm run build`, `npm run test:providers` with 203 passing tests, and `git diff --check`. Local live handoff was not available because this machine did not expose a `zed` CLI in the expected locations.
- 2026-05-21: Corrected PP-048 to use a provider-agnostic open-target registry instead of a Zed-specific branch. Targets now declare URL-scheme/app/optional CLI capabilities, and the main open handler dispatches by capability. Verification passed: `npm run build`, `npm run test:providers` with 205 passing tests, and `git diff --check`.
- 2026-05-21: Continued PP-048 with structured open outcome telemetry and target availability discovery. `fs.openPath` now returns target, method, line/column, fallback source, opened-with detail, and errors; settings asks the main process for installed/registered targets and disables unavailable editor choices. CLI command discovery now searches PATH before absolute fallbacks so GUI PATH differences do not skip Homebrew targets. Verification passed: `npm run build`, `npm run test:providers` with 206 passing tests, escalated `npm run smoke:ui:auto -- --settings`, screenshot review at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-settings-1779395787335.png`, and `git diff --check`.
- 2026-05-21: Added PP-050 from user testing: native window dragging becomes nearly impossible while a response is generating. This is tracked as a live-generation/app-shell responsiveness issue and should be verified with a focused streaming drag/perf smoke before claiming a fix.
- 2026-05-21: Completed the first PP-050 fix with reproduce-first evidence. Initial `npm run smoke:ui:auto -- --streaming-drag` failed with visible streaming updates causing 91 `App` commits and 91 `Titlebar` commits. After narrowing `App`/`Titlebar` Zustand subscriptions and memoizing `Titlebar`, the stricter smoke passed with the streaming session active, `stream update 090` visible, 0 `App` commits, 0 `Titlebar` commits, and max frame gap under budget. Verification passed: `npm run build`, `npm run test:providers` with 206 passing tests, escalated `npm run smoke:ui:auto -- --header`, escalated `npm run smoke:ui:auto -- --streaming-drag`, and `git diff --check`. Automated native drag input is not supported by Electron debugger in this environment, so installed-app manual drag confirmation remains recommended.
- 2026-05-21: Added explicit parity gaps from user testing/product review. Automations were already tracked by PP-035 and pending worktrees/forks by PP-038, but the backlog now separately tracks unresolved streaming typing latency (PP-051), message editing/resend (PP-052), conversation forking/lineage (PP-053), git action buttons/review actions (PP-054), and first-class worktree management (PP-055). PP-008 remains historical-complete for the first composer isolation mitigation, but PP-051 is the current unresolved typing-latency bug.
- 2026-05-21: Completed PP-051's first reproduce-first fix. New `npm run smoke:ui:auto -- --streaming-typing` initially failed because streaming produced 111 `SessionPane` commits while the composer was being typed into. After splitting `SessionPane` onto metadata/terminal-only selectors and moving transcript-heavy subscriptions into `ChatView`/panel children, the same smoke passed with visible `typing stream update 110`, typed composer text preserved, max typing timer drift 12.7ms, max input dispatch 0.2ms, max frame gap 16.84ms, `InputBar` commits 0, and `SessionPane` commits 4. Screenshot review confirmed the stream and composer text were visible together. Regression verification passed: `npm run build`, `npm run test:providers` with 206 passing tests, escalated `npm run smoke:ui:auto -- --streaming-typing`, escalated `npm run smoke:ui:auto -- --streaming-drag`, and `git diff --check`.
- 2026-05-21: Corrected PP-054 after a deeper Codex bundle check. The actual Codex Review toolbar does not present Stage/Unstage controls; it centers on review options, refresh, wrap, expand/collapse, diff mode, and Copy git apply command. Orchestrator should keep the new stage/unstage backend APIs for a future dedicated Git surface, but the Review UI should not expose those controls inline.
- 2026-05-21: Implemented the PP-054 correction. Review no longer shows inline Stage/Unstage controls or staged/unstaged row badges; its action menu now includes Copy git apply command. The focused diff smoke initially caught a real embedded-list layout issue where row tooltips made changed-file rows tile sideways and extra per-row stats made the list too tall. Removing row-level tooltip wrappers and making embedded Review file rows consistently compact restored search, keyboard selection, and diff-list checks. Verification passed: `npm run build`, escalated `npm run smoke:ui:auto -- --diff` with all diff checks true, screenshot review at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-diff-1779399797961.png`, and `git diff --check`.
- 2026-05-21: Shared panel-tab foundation slice complete. Added `PanelTabStrip` and moved both Workbench and Terminal tab chrome onto it. Tabs now share readable labels, hover/focus close affordances for inactive tabs, middle-click close, active-tab scroll-into-view, overflow edge fades, and sticky trailing action slots. Verification passed: `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --right-panel`, escalated `npm run smoke:ui:auto -- --terminal`, and screenshot review of both smoke captures. `npx tsc -p tsconfig.web.json --noEmit` still fails on pre-existing unrelated errors in `DiffPanel`, `FilesPanel`, `SettingsModal`, `designSystem` `SurfaceRowProps`, and `Titlebar`.
- 2026-05-21: Completed PP-028 ratio-aware Workbench sizing. `RightPanelState` now stores `widthRatio` alongside the compatibility pixel width, `ContextSidebar` sizes the Workbench from the current row width, resize drags save both width and ratio, and the shared resize handle supports double-click reset for Workbench and Terminal defaults. The first right-panel smoke caught a stale assertion that still required 360px during the intentional narrow-overlay clamp; the smoke now accepts the 280px overlay minimum and passes. Verification passed: `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --right-panel`, escalated `npm run smoke:ui:auto -- --terminal`, and screenshot review at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-right-panel-1779401275558.png`.
- 2026-05-21: Completed PP-031 shell-focus close-active-tab routing. Added `Close Active Panel Tab` to the shared app command registry on `Cmd/Ctrl+W`, tracked app-shell focus across main, Workbench, and Terminal areas, and routed the command to close the focused Workbench tab or Terminal tab while leaving main-chat `Cmd/Ctrl+W` unclaimed. Verification passed: `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --right-panel` with `rightPanelCloseActiveShortcut: true`, escalated `npm run smoke:ui:auto -- --terminal` with `terminalCloseActiveShortcut: true`, and `npm run test:providers` with 207 passing tests.
- 2026-05-21: Continued PP-027/PP-030 with shared tab drag reorder. `PanelTabStrip` now exposes one drag/drop reorder path for Workbench and Terminal tabs, with subtle drag/over visual states, and both panels wire it to their existing move-tab store actions. The first smoke run caught that React state was too slow for same-turn synthetic drag events; switching the active drag id to a ref fixed the real event path. Verification passed: `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --right-panel` with `rightPanelTabDragReorderWorks: true`, escalated `npm run smoke:ui:auto -- --terminal` with `terminalTabDragReorder: true`, and screenshot review at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-right-panel-1779402471035.png` plus `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-terminal-1779402471605.png`.
- 2026-05-21: Restored the clean renderer verification gate before continuing broader parity work. Fixed shared component prop types (`SurfaceRow` review hooks, `Badge` title), shortcut settings command narrowing, and the titlebar session-action menu type boundary without widening Titlebar's transcript-heavy subscriptions. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `npm run build`, `git diff --check`, escalated `npm run smoke:ui:auto -- --header`, and escalated `npm run smoke:ui:auto -- --settings`.
- 2026-05-21: Continued PP-027/PP-030 with Browser Workbench tab reset. Added a controller-backed `resetRightPanelTabState` path for the Browser tab, exposed `Reset tab` in the Workbench tab context menu, and reset URL, zoom, viewport/device mode, visibility, internal browser tabs, history, inspector mode, and browser permission policy state to the Browser defaults without closing the Workbench tab. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `npm run build`, `git diff --check`, and escalated `npm run smoke:ui:auto -- --browser` with `browserTabReset: true` plus screenshot review at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-browser-1779403196775.png`.
- 2026-05-21: Completed PP-056 by re-scoping the generic inspector smoke to stop duplicating focused Review/Files/Browser/Header assertions while keeping Workbench shell wiring, right-panel state, compact tab chrome, tab actions, resize/overlay, and cross-surface tab presence covered. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `npm run build`, `git diff --check`, escalated `npm run smoke:ui:auto -- --inspector`, and escalated `npm run smoke:ui:auto -- --right-panel`; screenshots reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-inspector-1779403970474.png` and `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-right-panel-1779404095469.png`.
- 2026-05-21: Completed PP-057's shared tooltip/tab-density styling pass after checking Codex `tooltip-DzyyaBl-.js`, `tabs-BgnCzZaP.js`, and `app-shell-BJK30dyj.css`. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --header`, escalated `npm run smoke:ui:auto -- --right-panel`, escalated `npm run smoke:ui:auto -- --terminal`, and escalated `npm run smoke:ui:auto -- --browser`; screenshots reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-header-1779430209693.png`, `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-right-panel-1779430579826.png`, `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-terminal-1779430403622.png`, and `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-browser-1779430425548.png`.
- 2026-05-21: Started PP-029's componentized performance gates with a focused `--workbench-perf` smoke. The first attempt failed usefully because debugger-driven resize measured Chromium debugger overhead instead of Workbench responsiveness; the final gate uses in-renderer pointer events and passes with 12 tab switches, max tab-switch 18.4ms, resize delta 87.6px in 192.5ms, max frame gap 9.3ms, no horizontal overflow, and 28 Workbench commits. Verification passed: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --workbench-perf`, escalated `npm run smoke:ui:auto -- --right-panel`, and screenshot review at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-workbench-perf-1779432895268.png`.
- 2026-05-22: Added PP-058 and expanded `docs/codex-side-panel-ui-parity-audit.md` with a fine-grained Codex styling/UI matrix. Bundle evidence came from the current local Codex `app.asar`, including app-shell, tab, file-tree, review, browser, sidebar, terminal, settings, appearance, and keyboard-shortcut chunks. Orchestrator evidence came from focused Review/Files/Browser smokes and screenshots at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-diff-1779434620728.png`, `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-files-1779434641021.png`, and `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-browser-1779434675335.png`.
- 2026-05-22: Started PP-058 with shared Workbench content styling. Review and Files now share `WorkbenchSearchField`, calmer 28px-ish list rows, subtler count badges, lower row font weights, and lighter active selection styling. The focused smoke runner now clears fixture-backed git workspaces before each run after Review smoke exposed a stale-workspace false negative with zero changed files. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --diff`, and escalated `npm run smoke:ui:auto -- --files`; screenshots reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-diff-1779468988567.png` and `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-files-1779469013509.png`.
- 2026-05-22: Continued PP-058 with Browser interior chrome. Browser URL/find fields now use the shared Workbench input metrics, the nested browser tab strip is lighter, action/menu/local-server typography weights are lower, load-error chrome is less card-like, and the focused Browser smoke now asserts calm local-target rows plus the shared find/search chrome. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --browser`, and escalated `npm run smoke:ui:auto -- --right-panel`; screenshots reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-browser-1779469492134.png` and `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-right-panel-1779469526824.png`.
- 2026-05-22: Continued PP-058 with Settings primitive styling. Shared settings rows are less card-like, use lower label weights and lighter separators, settings topbar/surfaces are calmer, and the Shortcuts search now uses `WorkbenchSearchField` instead of the old inspector search field. Settings smoke now asserts calm settings rows plus shared shortcut-search chrome. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --settings`, and escalated `npm run smoke:ui:auto -- --settings-providers`; screenshots reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-settings-1779469846625.png` and `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-settings-providers-1779469876607.png`.
- 2026-05-22: Continued PP-058 with Chat Sidebar row styling. Chat rows now use a CSS-owned 13px/400 title style, softer active/hover backgrounds, no active-row shadow, and opacity-only right-slot transitions so timestamps/actions do not visibly slide. Row action focus no longer opens the chat hover card, and the sidebar smoke now exercises the shared delayed tooltip with pointer events and settled timing. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, and escalated `npm run smoke:ui:auto -- --sidebar`; screenshot reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-sidebar-1779471239037.png`.
- 2026-05-22: Continued PP-058 with shared menu/flyout styling. `MenuSurface` now applies Codex-like bounded popover dimensions, 8px radius, scroll max-height, and compact 28px/13px/400 menu rows without row hover translation. Sidebar, Review, and Files smokes now assert the shared menu chrome so future menu drift is caught alongside the existing functional checks. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --sidebar`, escalated `npm run smoke:ui:auto -- --diff`, escalated `npm run smoke:ui:auto -- --files`, and escalated `npm run smoke:ui:auto -- --right-panel`; screenshots reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-sidebar-1779473154562.png`, `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-diff-1779473172063.png`, `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-files-1779473196436.png`, and `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-right-panel-1779473213295.png`.
- 2026-05-22: Extended PP-058 shared menu/flyout migration into Capabilities. The Create and row action menus now rely on shared `MenuSurface`/`MenuItem` chrome instead of local 680-weight button rows and dialog-shadow flyouts; row action button styles are scoped to the direct toolbar button so they do not leak into menu items. Capabilities smoke now asserts the shared menu chrome for both menus. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, and escalated `npm run smoke:ui:auto -- --capabilities`; screenshot reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-capabilities-1779473617391.png`.
- 2026-05-22: Started PP-033 hardening from the install-time packaged session-switch failure. Root cause: after switching chats, the renderer replaced the 8-message summary tail with the 40-message transcript page, but `ChatView` kept the old render limit of 8 until scroll-triggered lazy loading. The render limit now grows to the loaded first page, and session-switch telemetry records the first committed visible transcript window before auto-lazy expansion. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --session-switch`, escalated `npm run smoke:ui:auto -- --packaged --session-switch`, and escalated `npm run verify:release`; screenshot reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-session-switch-1779474386634.png`.
- 2026-05-22: Continued PP-058 Browser menu polish. `MenuItem` now supports optional `aria-label` and `data-testid`, and Browser page/data action rows plus page context menu rows now use the shared menu item primitive instead of bespoke Browser row chrome. Browser smoke now asserts those migrated rows keep shared 28px/13px/400 metrics and no hover translation while preserving history, clear-data, and add-page-context behavior. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, and escalated `npm run smoke:ui:auto -- --browser`; screenshot reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-browser-1779474995580.png`.
- 2026-05-22: Continued PP-058 shared dialog polish. `ConfirmDialog`, `TextInputDialog`, `RenameChatDialog`, and the generic `Modal` now share compact dialog surface/content/input/action classes instead of bespoke local chrome, with lower title weights, 34px inputs, calmer borders, and no divider bands. Sidebar smoke now asserts rename dialogs use the shared dialog surface. Verification passed: `npx tsc -p tsconfig.web.json --noEmit`, `npx tsc -p tsconfig.node.json --noEmit`, `git diff --check`, escalated `npm run smoke:ui:auto -- --sidebar`, escalated `npm run smoke:ui:auto -- --settings`, and escalated `npm run smoke:ui:auto -- --capabilities`; screenshot reviewed at `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-sidebar-1779475471169.png`.
- 2026-05-28: Started PP-052 with a narrow message edit/reuse slice. Completed user messages now expose an `Edit message as draft` action that replaces the active composer draft and attachments, focuses the composer, and announces the result without deleting or rewriting transcript history. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-layout` with `chatUserMessageEditToDraft=true` plus `chatUserMessageEditAttachments=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780001499310.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780001499310.png`. Remaining PP-052 work is provider-backed edit/resend/fork semantics rather than another local draft-reuse affordance.
- 2026-05-28: Continued PP-052 with latest-assistant regenerate. The latest assistant response now exposes `Regenerate` when a retryable user prompt exists, calls the existing provider-neutral retry-last-user-message path, and announces sent/error state through the same transcript action pattern as Continue. The same pass tightened narrow permission-card action wrapping and made the transcript-layout smoke scan the full virtualized scroll range before checking narrow permission actions. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-layout` with `chatRegenerateLastResponse=true`, `chatRegenerateLastResponseA11y=true`, and `narrowPermissionActionsWrap=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780008552761.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780008552761.png`. Remaining PP-052 work is richer provider-native edit/resend/regenerate semantics and original-history preservation UI.
- 2026-05-28: Started PP-053 with message-level local fork support. `sessions.fork` now accepts a `throughMessageId` option for local branch points, completed user messages and the latest assistant response expose `Fork from here`, and the focused `--transcript-fork` smoke verifies the fork button plus backend truncation through the selected message, provider-session reset, and later-turn exclusion. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-fork`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-layout`; fork evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780002454857.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780002454857.png`; layout regression evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780002293701.json`. Remaining PP-053 work is visible lineage/sidebar polish, latest-turn forks, same/new-worktree message forks, and provider-native fork/resume mapping.
- 2026-05-28: Continued PP-053 with fork lineage visibility and local-fork sidebar cleanup. Local forks now persist `forkedFromSessionId`, `forkedFromSessionName`, `forkedFromMessageId`, `forkedAt`, and `forkMode`; sidebar session rows expose stable lineage attributes, and the hover detail card shows the fork source. Fork creation also clears provider-owned sidebar metadata so a new local fork cannot inherit provider pin, host, worktree-source, or projectless classification from the source thread. The focused `--transcript-fork` smoke now gates `chatMessageForkPersistsLineage=true`, `chatMessageForkSidebarLineage=true`, `chatMessageForkHoverLineage=true`, and `chatMessageForkClearsProviderSidebarMetadata=true` alongside the existing fork/truncation checks. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-fork`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780003052070.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780003052070.png`. Remaining PP-053 work is fork-latest polish, pin/place behavior, same/new-worktree message forks, and provider-native fork/resume mapping.
- 2026-05-28: Continued PP-053 with message-level worktree fork choices. The transcript `Fork from here` action now opens a compact menu when worktree choices are available, exposing local, same-worktree, and new-worktree forks from the selected message. The renderer path now passes the selected mode into `sessions.fork(..., { throughMessageId })`, and the focused smoke verifies the same-worktree UI path plus backend same/new-worktree truncation and lineage. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-fork`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780003457390.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780003457390.png`. Remaining PP-053 work is fork-latest polish, pin/place behavior, and provider-native fork/resume mapping.
- 2026-05-28: Continued PP-053 with local pin placement for forks. Forks from locally pinned source chats now remain locally pinned and receive a pin order immediately after the source, while provider-owned pin metadata is still cleared so provider-readonly pinned state does not leak onto local forks. The focused smoke seeds a source with both local pin and provider pin metadata and now gates `chatMessageForkPreservesLocalPinPlacement=true`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-fork`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780003778217.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780003778217.png`. Remaining PP-053 work is fork-latest polish and provider-native fork/resume mapping.
- 2026-05-28: Continued PP-053 with latest-turn session-menu fork polish. Session action menu fork entries now explicitly say `from latest turn`, fetch the full source session before forking, and pass the latest non-system, non-streaming text message as `throughMessageId` when available. This makes titlebar/sidebar forks match the latest-turn contract instead of cloning later control/status rows. Focused `--transcript-fork` now gates `chatSessionForkLatestTurnUi=true`, `chatSessionForkLatestTurnTruncatesControls=true`, and `chatSessionForkLatestTurnMenuLabel=true`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-fork`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780004191107.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780004191107.png`. Remaining PP-053 work is provider-native fork/resume mapping.
- 2026-05-28: Continued PP-027/PP-030 with file/source tab reset. Workbench file tabs now expose `Reset tab` in the shared tab context menu, clearing source mode/search/selection/reveal/blame view state while preserving tab identity, preview/pin state, host/path, and local annotations. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --files` with `workbenchFileTabReset=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780004708217.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780004708217.png`. Remaining PP-027/PP-030 work is full app-shell tab-controller ownership and non-Terminal cross-panel moves.
- 2026-05-28: Continued PP-036/PP-048 source-tab workflow polish with selected-line composer context. Source file tabs now expose `Add selected line to chat` in the selected-line actions/menu, inserting the exact `path:line` reference plus source text into the main composer and announcing the action. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --files`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780022815383.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780022815383.png`. The focused smoke gates `fileSourceLineAddToChat=true` while preserving source-line utilities, action status, file attachment, blame, annotations, search, virtualization, artifact, and preview gates.
- 2026-05-28: Continued PP-037 with missing/archived thread route recovery. Unknown `/threads/{id}` and `#/threads/{id}` routes now inspect the local session record before failing: archived thread links render a recovery surface with `Restore chat`, restore/reopen the session, and keep the route on that thread; genuinely missing links render `Chat not found` with a return-to-current-chat action. Active-session URL sync pauses while this notice is visible so the original route is not overwritten before the user can act. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --session-switch` with `archivedRouteRecoveryVisible=true`, `archivedRouteRestoreWorks=true`, `missingRouteRecoveryVisible=true`, and `missingRouteReturnWorks=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1780009198894.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1780009198895.png`. Remaining PP-037 work is provider-native remote/hotkey route semantics.
- 2026-05-28: Continued PP-058 Browser actions/history polish. The Browser actions menu History section now includes `Clear history`, which empties the Browser workbench history list, closes the menu, and announces `History cleared` through the Browser status row. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --browser` with `browserHistoryClear=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780009556512.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780009556512.png`.
- 2026-05-28: Continued PP-058 Browser zoom polish. The Browser actions menu now includes a shared `Zoom presets` section for 50%, 75%, 100%, 125%, 150%, and 200%, while preserving the compact zoom out/reset/zoom in row. The focused Browser smoke gates `browserZoomPreset=true` by selecting 125%, verifying the Browser workbench zoom model and active check row, restoring 100%, and preserving the action-menu shared-section/no-overflow checks. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --browser`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780011035006.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780011035006.png`. Remaining PP-058 Browser work is lifecycle/screenshot comparison, not basic history or zoom presets.
- 2026-05-28: Continued PP-046 Settings route ownership. Settings sidebar section clicks and host changes now push the target settings route before updating section state, so routed Settings behaves like a real full-page surface and browser/app Back can return from Appearance to General instead of leaving Settings. The renderer type barrel now exports `settingsRouteUrlForLocation` so Sidebar uses the shared path/hash route helper. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --settings`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780011427313.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780011427313.png`. The first focused smoke caught a blank-render regression from a missing renderer type-barrel export, which was fixed before using the smoke as evidence.
- 2026-05-28: Continued PP-036 with Review toolbar file navigation. Detailed Review now has compact `Previous changed file` and `Next changed file` toolbar actions that use the same active changed-file path model as the grouped tree keyboard navigation while preserving the Codex-like Review options, Jump, and Refresh primary order. The first focused smoke caught that placing the buttons too early broke `reviewToolbarPrimaryOrder`; moving them after Refresh restored the toolbar contract. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --diff-core`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780011874151.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780011874151.png`.
- 2026-05-28: Continued PP-033 with a transcript composer-reserve contract. `SessionPane` now measures the live composer/footer area, exposes `--composer-reserve-height` and data attributes on the primary chat column, wraps the composer in a stable reserve node, and notifies `ChatView` when the reserve changes. The transcript marks itself reserve-aware, applies the reserve as scroll padding, and re-follows bottom while the user is at the latest message. The broad `--transcript-layout` smoke timed out before producing JSON, so the new assertion was split into focused `--transcript-reserve`, which passed with `composerReserveContract=true` and `composerReserveFollowBottom=true`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-reserve`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-reserve-1780012376261.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-reserve-1780012376261.png`.
- 2026-05-28: Continued PP-033 with stable grouped tool-row search jumps. Adjacent tool use/result messages now virtualize under a stable first-tool row id, each virtual row exposes the contained transcript message ids, and search jumps fall back to that row metadata when a matching tool message is inside a grouped tool summary instead of a standalone `data-message-id` row. A new focused `--transcript-tool-jump` smoke seeds a long transcript where the matching tool result starts outside the initial render window and gates `transcriptToolGroupStableKey=true` plus `transcriptToolSearchJump=true`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-tool-jump`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-tool-jump-1780012922398.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-tool-jump-1780012922398.png`.
- 2026-05-28: Continued PP-033 with transcript-visible read-state clearing. Selecting a chat, opening a routed thread, restoring an archived thread, or consuming pending navigation no longer clears unread state as a side effect of setting `activeSessionId`. `ChatView` now clears unread only after the active transcript has rendered at least one visible message or confirmed an empty chat, and exposes `data-active-transcript-read-synced` for smoke proof. The focused session-switch smoke seeds the target chat unread before switching, verifies `unreadPreservedBeforeSwitch=true`, then waits for transcript readiness and verifies `activeReadClearedAfterTranscript=true`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --session-switch`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1780013337276.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1780013337276.png`.
- 2026-05-28: Continued PP-033/PP-038 with active worktree lifecycle state in the main chat pane. Pending and failed worktree sessions now show a `PanelNotice` below the header with source/workdir context, ARIA status/alert semantics, and a failed-state retry action that calls the existing worktree retry path and hydrates the returned session. The new focused `--worktree-lifecycle` smoke seeds pending and failed fixture sessions in a git-backed isolated workspace, verifies the pending notice, verifies the failed notice, clicks retry, and waits for the session to reach a ready `.orchestrator-worktrees/<session id>` workdir. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --worktree-lifecycle`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-worktree-lifecycle-1780013752364.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-worktree-lifecycle-1780013752364.png`.
- 2026-05-28: Continued PP-033/PP-037 with explicit thread-route opening state. Unknown `/threads/{id}` and `#/threads/{id}` routes now immediately replace the active transcript with an `Opening chat` route notice while the local session/archive lookup is pending, then settle to the existing archived restore surface or missing-chat recovery. This prevents stale chat content from staying visible during page-open inspection and gives route transitions a smoke-visible lifecycle state. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --session-switch`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1780019041203.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1780019041203.png`. The focused smoke now gates `sessionRouteResolvingVisible=true` by delaying the route lookup before allowing it to settle to missing.
- 2026-05-28: Continued PP-037 with explicit in-app thread-link copy. Session action menus now include `Copy thread link` beside the app-protocol deeplink action. The copied value uses the same `/threads/{id}` or `#/threads/{id}` route helper as renderer navigation, writes through the app clipboard bridge with browser clipboard fallback, and is smoke-visible for direct clipboard proof. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, focused elevated `node scripts/run-automated-ui-smoke.mjs --sidebar`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --session-switch`. Sidebar evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1780019865173.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1780019865173.png`; session-route evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1780019938197.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1780019938197.png`. The focused sidebar smoke now gates `actionCopyThreadLinkWorks=true` alongside `actionCopyDeeplinkWorks=true` and `actionOpenInNewWindowWorks=true`. During verification, an initial blank-render smoke failure exposed a missing renderer type-barrel export for `sessionRouteUrlForLocation`; exporting it fixed the runtime renderer startup path.
- 2026-05-28: Continued PP-058 with Settings topbar section search. Settings now exposes a compact shared search field in the topbar; typing a visible section/control keyword and pressing Enter pushes the same owned `/settings/{section}` route used by sidebar navigation, respects host-specific visible sections, and shows the matched section target inline. Focused Settings smoke now gates `settingsSearchNavigation=true` by jumping from General to Browser and back through the search field. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --settings`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780014386620.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780014386620.png`.
- 2026-05-28: Continued PP-054 with a dedicated Git Workbench tab. The New tab launcher now exposes Git, the tab shows branch/change/staged/unstaged counts, local changed files, Stage all, Unstage all, and Open Review, and the focused Workbench New Tab smoke gates `workbenchNewTabGitAction=true` by opening Git and exercising stage/unstage transitions in the fixture repo. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780015277484.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780015277484.png`. Remaining PP-054 work is commit/branch/PR/revert/discard ownership and eventually moving local-git bridge behavior fully out of Review.
- 2026-05-28: Continued PP-054 with Git Workbench commit support. The Git tab now has a commit-message field and `Commit staged` action backed by `git:commitStaged`, with empty-message/no-staged guards and refreshed change state after commit. The focused Workbench New Tab smoke now gates `workbenchNewTabGitCommit=true` by opening Git from the right Workbench launcher, staging/unstaging fixture changes, staging again, entering a commit message, and proving the fixture repo is clean after commit. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780015734717.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780015734717.png`. Remaining PP-054 work is branch/PR/revert/discard ownership and eventually moving local-git bridge behavior fully out of Review.
- 2026-05-28: Continued PP-054 with Git Workbench discard ownership. The Git tab now exposes a danger-styled `Discard all` action behind a confirmation dialog, backed by `git:discardPaths` so the Workbench can use the same safe path checks as Review's temporary bridge. The focused Workbench New Tab smoke gates `workbenchNewTabGitDiscard=true` by opening Git from the right Workbench launcher, opening the discard confirmation against a dirty fixture, verifying the destructive copy and cancel path, then continuing through stage/unstage/commit. The Git unit test suite separately covers actual tracked/staged/untracked discard mutation and unsafe-path refusal. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780016316039.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780016316039.png`. Remaining PP-054 work is branch/PR ownership and eventually moving local-git bridge behavior fully out of Review.
- 2026-05-28: Continued PP-054 with local Git Workbench branch creation. The Git tab now has a branch-name field and `Create branch` action backed by `git:createBranch`, checks Git branch-name validity, checks out the new branch in the current workdir, refreshes branch state, and keeps dirty working-tree changes intact. The focused Workbench New Tab smoke gates `workbenchNewTabGitBranch=true` by creating a unique fixture branch from the right Workbench Git tab before continuing through discard confirmation, stage/unstage, and commit. The Git unit suite covers empty-name validation, branch checkout, branch-list current marking, and dirty-file preservation. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780016736246.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780016736246.png`. Remaining PP-054 work is PR creation/open ownership and eventually moving local-git bridge behavior fully out of Review.
- 2026-05-28: Continued PP-054 with Git Workbench PR command ownership. The Git tab now has a Pull Request card that opens existing PR metadata when available and otherwise generates a local `gh pr create --fill --base <base> --head <branch>` command for the current topic branch. The command copy uses the app clipboard bridge and visible status feedback, so the Git tab owns the local handoff from branch/commit to PR without pretending hosted PR creation is available. The focused Workbench New Tab smoke gates `workbenchNewTabGitPrCommand=true` by creating a topic branch, copying the generated PR command, and verifying the clipboard includes the smoke branch. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780017110971.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780017110971.png`. Remaining PP-054 work is live PR creation, provider-hosted Git metadata, and eventually moving local-git bridge behavior fully out of Review.
- 2026-05-28: Continued PP-054 with Git Workbench branch checkout. The Git tab Branch card now includes a branch picker and `Checkout` action backed by `git:checkoutBranch`, so the right Workbench panel can switch back to an existing branch after creating a topic branch without leaving the app. The action validates branch names, refreshes branch/change state, preserves dirty working-tree changes where Git permits the checkout, and surfaces a live status plus `data-git-last-checked-out-branch` proof. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780020466988.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780020466988.png`. The focused smoke now gates `workbenchNewTabGitCheckout=true` alongside branch creation, PR-command copy, discard cancel, stage/unstage, and commit.
- 2026-05-28: Continued PP-054 with per-file Git Workbench stage/unstage controls. Each file row in the Git tab now exposes compact Stage and Unstage icon actions that call the same safe `git.stagePaths` / `git.unstagePaths` IPC paths as the bulk actions, update the row metadata, and publish the existing live action status. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780020692507.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780020692507.png`. The focused smoke now gates `workbenchNewTabGitFileActions=true` by staging and unstaging one file before continuing through branch creation, PR-command copy, checkout, discard cancel, Stage all / Unstage all, and commit.
- 2026-05-28: Continued PP-054 with per-file Git Workbench discard. Each file row in the Git tab now exposes a compact Discard icon action that opens the same confirmation dialog against a single selected path, keeps the safe `git.discardPaths` mutation path, and clears targeted discard state on cancel/close. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780021068460.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780021068460.png`. The focused smoke now gates `workbenchNewTabGitFileDiscard=true` by opening the row-level discard confirmation for one file, verifying the path-specific destructive copy, canceling it, and then continuing through branch creation, PR-command copy, checkout, bulk discard cancel, Stage all / Unstage all, and commit.
- 2026-05-28: Continued PP-054 by decoupling Review copy status from the local-git bridge. Review now renders a standalone bottom status pill when copy/action feedback exists but the current source has no local mutable Git actions, which means provider Last turn `Copy git apply command` visibly confirms success without relying on the local Stage/Unstage/Revert pill. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, focused elevated `node scripts/run-automated-ui-smoke.mjs --diff-source`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --diff-core`; source evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1780021424055.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1780021424055.png`; core evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780021472816.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780021472816.png`. The focused smokes gate `reviewLastTurnGitApplyCopyStatus=true`, `reviewGitApplyCopyStatus=true`, and `reviewFloatingGitActionStatus=true`.
- 2026-05-28: Continued PP-054 with an explicit Review-to-Git handoff. The local Review floating action pill now includes `Open Git`, which opens the dedicated Workbench Git tab for branch/commit/discard workflow ownership while preserving the existing local Review Stage/Unstage/Revert bridge. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --diff-core`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780021759605.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780021759605.png`. The focused smoke gates `reviewFloatingGitOpenTab=true` by opening Git from Review, verifying an idle Git panel, returning to Review, and waiting for hydrated diff rows before continuing the Review core assertions.
- 2026-05-28: Continued PP-052 with edit-as-draft source clarity. `Edit message as draft` now passes source metadata to the composer, and the composer renders an `Editing a copy` notice that explicitly says the original message remains in the transcript, exposes the source message id and attachment count for smoke proof, and provides a clear action for abandoning the copied draft. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-layout`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780022086471.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780022086471.png`. The focused smoke gates `chatUserMessageEditDraftSourceStatus=true` while keeping edit-to-draft, attachments, retry, Continue, Regenerate, transcript layout, and narrow-layout checks green.
- 2026-05-28: Tightened the PP-052 edit-as-draft path so copying an older user message no longer silently discards an existing unsent composer draft. The `Editing a copy` notice now records when a previous draft was saved and exposes `Restore previous draft`, which restores the prior text/attachments and clears the edit-source notice. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-layout`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780022483489.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780022483489.png`. The focused smoke gates `chatUserMessageEditDraftRestore=true` alongside `chatUserMessageEditDraftSourceStatus=true`.
- 2026-05-28: Continued PP-054 with provider review suggestion actions. GitHub review-thread metadata now preserves multi-line `startLine` ranges, provider comment cards detect fenced `suggestion` blocks, and current unresolved new-side suggestions can be copied or applied to the local file from Review. The focused Review smoke also tightened the content-search assertion to target deletion-only diff text instead of Markdown preview text. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --diff-source`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1780018233948.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1780018233948.png`. Remaining PP-054 work is live PR creation, provider-hosted Git metadata, and eventually moving local-git bridge behavior fully out of Review.
- 2026-05-29: Continued PP-041 with Files search recall. The Files search field now has a compact recent-search history button using the shared menu surface, records searches on Enter and when opening a file result from an active search, persists the deduped recent list locally, and lets users restore a previous file/content query directly from the search field. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --files` with `filesSearchHistory=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780023613550.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780023613550.png`.
- 2026-05-29: Continued PP-029/PP-033 with a larger long-thread stress gate. The focused `--transcript-stress` smoke now seeds a 10,001-message transcript instead of the earlier 2,501-message fixture, while preserving the same bounded-row, lazy-load, early-message search-jump, and ready-time assertions. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-stress`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-stress-1780023967775.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-stress-1780023967775.png`.
- 2026-05-29: Continued PP-029/PP-031 with a focused cross-panel keyboard gate instead of broadening the expensive terminal/inspector smokes. `node scripts/run-automated-ui-smoke.mjs --cross-panel-keyboard` now opens Workbench Files/Browser and two Terminal tabs, then proves Workbench-focused `Cmd/Ctrl+W` closes the Workbench tab without touching Terminal and Terminal-focused `Cmd/Ctrl+W` closes the terminal tab without touching Workbench. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --cross-panel-keyboard`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-cross-panel-keyboard-1780024396145.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-cross-panel-keyboard-1780024396145.png`.
- 2026-05-29: Continued PP-054 by retiring Review's temporary local-git mutation bridge now that the dedicated Git Workbench tab owns stage/unstage/discard/commit/branch/checkout/PR-command flows. Review's floating bottom pill now only provides an `Open Git` handoff plus copy/apply status feedback; Stage all, Unstage all, Revert all, and the Review revert confirmation dialog are absent from Review. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --diff-core`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780024835038.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780024835038.png`. The focused smoke now gates `reviewFloatingGitBridgeRetired=true`, `reviewFloatingGitOpenTab=true`, and `reviewFloatingGitActionStatus=true`.
- 2026-05-29: Continued PP-058 with Settings/header band alignment. The routed Settings shell now declares `data-app-shell-focus-area="main"` and the Settings topbar consumes the shared `--app-shell-toolbar-height` band instead of a local 38px header height. The focused Settings smoke now gates this through `settingsTopbarShared=true` by comparing rendered height to the shared token and requiring `data-app-shell-header-band="shared"`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --settings`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780025236863.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780025236863.png`.
- 2026-05-29: Continued PP-058 with Settings in-page search targets. Settings topbar search now includes concrete subtargets for General File handoff and General Composer controls, and matched anchors scroll into view, receive focus, and briefly expose `data-settings-search-active="true"`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --settings`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780025552163.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780025552163.png`. The focused smoke now gates `settingsInPageSearchTarget=true` by searching `composer` and verifying the General Composer group is the active focused result.
- 2026-05-29: Continued PP-058 with Browser Settings in-page search targets. Settings search now includes Browser Data, Browser Permissions, and Browser Domains subtargets, and the Browser page exposes matching focusable anchors. A query such as `downloads` jumps directly to Browser permissions instead of only landing on the Browser page. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --settings`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029250662.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029250662.png`. The focused smoke now gates `settingsBrowserInPageSearchTarget=true` alongside `settingsInPageSearchTarget=true`.
- 2026-05-29: Continued PP-058 with Provider Settings in-page search targets. Settings search now includes Provider picker and Provider defaults subtargets, and the Providers page exposes matching focusable anchors for the provider selector and model/reasoning/permission defaults group. A query such as `models` jumps directly to Provider defaults instead of only landing on the Providers page. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --settings`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029502642.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029502642.png`. The focused smoke now gates `settingsProviderInPageSearchTarget=true` alongside the General and Browser in-page search gates.
- 2026-05-29: Continued PP-058 with Shortcuts Settings in-page search targets. Settings search now includes a Shortcut bindings subtarget, and the Shortcuts page exposes a matching focusable anchor around the search/edit bindings table. A query such as `keybinding` jumps directly to the Shortcuts controls instead of only landing on the Shortcuts page. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --settings`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029723853.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029723853.png`. The focused smoke now gates `settingsShortcutsInPageSearchTarget=true` alongside the General, Browser, and Provider in-page search gates.
- 2026-05-29: Continued PP-055/PP-058 with Worktrees Settings in-page search targets. Settings search now includes a Worktree create subtarget, and the Worktrees page exposes a matching focusable anchor around the project/base ref/branch creation controls. A query such as `base ref` jumps directly to the Worktree create controls instead of only landing on the Worktrees page. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --settings`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780031328081.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780031328081.png`. The focused smoke now gates `settingsWorktreesInPageSearchTarget=true` alongside the General, Browser, Provider, and Shortcuts in-page search gates.
- 2026-05-29: Continued PP-058 with Browser hidden-page identity. The hidden Browser lifecycle state now shows the loaded page title and shortened URL, preserves those values as smoke-visible attributes, and clarifies that the loaded page will resume when shown. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --browser`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780025917536.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780025917536.png`. The focused smoke now gates `browserHiddenIdentity=true` alongside hidden-webview persistence, lifecycle resync, and hidden-webview containment.
- 2026-05-29: Continued PP-058 with Browser screenshot status feedback. Browser screenshot capture now clears stale status before capture, announces `Screenshot saved` through an accessible live status after capture, announces `Screenshot attached` after `Add screenshot`, and reports a danger status when an attach is attempted without a saved artifact. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --browser`. The first focused rerun failed because the new wrapper gate was not wired into the Browser-specific smoke result; after wiring it, rerun passed with `browserScreenshotStatusA11y=true`. Failed evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780030041745.json`; passed evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780030113342.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780030113342.png`.
- 2026-05-29: Continued PP-058 with Browser screenshot artifact actions. The Browser console screenshot row now exposes compact Add to chat, Copy path, and Reveal file actions for the saved PNG artifact. Copy uses the app clipboard bridge, reveal uses `fs.showInFolder`, and the artifact path is smoke-readable. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `pnpm run smoke:ui:suggest`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --browser`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780046018097.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780046018097.png`. The focused smoke now gates `browserScreenshotPathActions=true` and `browserScreenshotAttachment=true`.
- 2026-05-29: Continued PP-036 with richer Review directory metadata. Grouped directory rows in the Review changed-file tree now show aggregated file count plus additions/deletions and expose `data-review-group-path`, `data-review-file-count`, `data-review-additions`, and `data-review-deletions` for focused smoke/debug proof. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --diff-core`. The first focused rerun had `diffDirectoryMetadata=true` but failed the pre-existing `reviewFloatingGitOpenTab` timing gate; rerun passed cleanly with `diffDirectoryMetadata=true`. Failed evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780030663575.json`; passed evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780030681929.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780030681929.png`.
- 2026-05-29: Continued PP-048 with source annotation tab pinning. Creating a source annotation in a Workbench file/source tab now pins the file tab first when it was still a preview tab, so annotation state is not lost by the next preview-file open. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --files`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780030998435.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780030998435.png`. The focused smoke now gates `fileSourceAnnotationPinsTab=true` alongside source annotations, source tab state, file-tab reset/pin, and broader Files/source-tab checks.
- 2026-05-29: Continued PP-057/inspector polish with shared Agents event-detail action chrome. Runtime event `Copy payload` and `Add to chat` now use the shared compact toolbar icon-button primitive instead of local text buttons, preserving accessible labels/tooltips and the existing status region. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The first focused rerun caught a prop-name mismatch that stripped smoke hooks from the toolbar buttons; after fixing it, rerun passed with `agentRuntimeEventActionChrome=true` alongside `agentRuntimeEventCopy=true`, `agentRuntimeEventAddToChat=true`, `agentRuntimeEventAddToChatContext=true`, `agentRuntimeEventFilter=true`, `agentRuntimeFailureGroups=true`, `agentRuntimeIssueTriage=true`, `agentSelectedTimeline=true`, and `agentTransportLog=true`. Failed evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780031626385.json`; passed evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780031671592.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780031671592.png`.
- 2026-05-29: Continued PP-048/PP-058 with file-tab workspace identity. File/source tabs now show a compact workspace-root label in the tab chrome and expose `data-file-tab-workdir` plus `data-file-tab-absolute-path`, so same relative paths opened from different roots/worktrees remain distinguishable in the right Workbench panel. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --files`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780026275802.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780026275802.png`. The focused smoke keeps `workbenchFileTab=true` while requiring the workspace label, workdir, and absolute path diagnostics.
- 2026-05-29: Continued PP-048 with transcript file-reference open outcomes. File-reference cards in assistant messages now retain the last `fs.openPath` result, expose target/method/line/column/fallback/opened-with diagnostics, and announce a compact success status after opening a referenced file. The transcript-layout fixture now seeds the referenced file so this path is proven instead of only checking missing-file suppression. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-layout`; first run failed because the referenced fixture file was not seeded, then rerun passed with `fileReferenceOpenOutcome=true`. Failed evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780026556459.json`; passed evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780026624546.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780026624546.png`.
- 2026-05-29: Continued PP-048 with transcript-to-Workbench file-reference handoff. Workspace-contained transcript file-reference cards now expose a `Workbench` action that opens the resolved path through `openRightPanelFileTab` as a preview source tab, preserving the referenced line as selected/revealed source state. Missing-file cards keep all actions disabled. To avoid bloating the already broad transcript-layout smoke, this slice splits file-reference proof into focused `node scripts/run-automated-ui-smoke.mjs --transcript-file-reference`, which gates `fileReferenceOpenOutcome=true`, `fileReferenceOpenWorkbench=true`, and `fileReferenceMissingActionsDisabled=true`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-file-reference`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780027530147.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780027530147.png`.
- 2026-05-29: Continued PP-048 with additional-root transcript-to-Workbench handoff. `openRightPanelFileTab` now accepts an explicit root, file tab ids encode that root/host, file tabs resolve content and workspace identity from the encoded root, and transcript file-reference cards derive Workbench targets from the primary workdir plus configured `additionalDirs`. The focused split smoke now seeds an additional-root fixture and gates `fileReferenceAdditionalRootWorkbench=true` in addition to the existing open-outcome, Workbench, and missing-action checks. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-file-reference`; first rerun failed only because the external-open result was read before the async result attribute populated, then the wait was tightened and rerun passed. Failed evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780027806247.json`; passed evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780027838839.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780027838839.png`.
- 2026-05-29: Continued PP-048/PP-040 with transcript file-reference Attach-to-chat. Assistant transcript file-reference cards now expose an `Attach` action for resolved files, dispatching the same composer local-file attachment event used by Files/source tabs and announcing card-level attachment status. The focused split smoke now gates `fileReferenceAttachToChat=true` by attaching the additional-root fixture and verifying both the card status and composer attachment status/path. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-file-reference`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028239061.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028239061.png`.
- 2026-05-29: Continued PP-048/PP-040 with root-aware source-tab line references. Source file tabs still keep primary-workspace line references compact as `path:line`, but tabs opened from an encoded non-primary root now copy/add absolute `absolute/path:line` references so composer follow-ups do not lose the root. The focused transcript file-reference smoke opens the additional-root fixture into Workbench, clicks the source-line Add-to-chat action, and gates `fileReferenceAdditionalRootLineAddToChat=true`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-file-reference`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028491456.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028491456.png`.
- 2026-05-29: Continued PP-048 with root-aware source-tab Copy path. Source file tabs now use the same root-aware path for file-level `Copy path`: primary-workspace tabs still copy compact relative paths, while tabs opened from encoded non-primary roots copy absolute paths. The focused transcript file-reference smoke opens the additional-root fixture into Workbench, clicks the source-tab Copy path toolbar action, reads the app clipboard, and gates `fileReferenceAdditionalRootCopyPath=true`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-file-reference`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028669298.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028669298.png`.
- 2026-05-29: Continued PP-048 with root-aware source-tab blame. Source file tabs now call `git.blameLine` with the encoded file root instead of the session's primary workdir, and the workspace badge title uses that same root. The focused transcript file-reference smoke seeds the additional root as a small git repo, opens its fixture into Workbench, and gates `fileReferenceAdditionalRootBlame=true` by verifying committed blame metadata from `Orchestrator Smoke`. Verification passed: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-file-reference`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028914261.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028914261.png`.
- 2026-05-29: Continued PP-044 with composer permission-context refresh. The permission popover now shows a refresh affordance and announced status for live, fallback, and static runtime config contexts, so users can retry config discovery without leaving the composer. Verification stayed intentionally narrow: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --composer`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780032566596.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780032566596.png`. The focused smoke now gates `composerPermissionContextRefresh=true` alongside the existing permission context source signal, blocked-send recovery, provider/model switching, draft, attachment, and toolbar checks.
- 2026-05-29: Continued PP-026/PP-058 with Workbench New Tab keyboard navigation. The Workbench launcher action list now has roving focus: one enabled action is tabbable, arrow keys move through enabled actions, Home/End jump to the first/last enabled action, and disabled actions are skipped. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780032925540.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780032925540.png`. The focused smoke now gates `workbenchNewTabKeyboardNavigation=true` while preserving Git and Agents launcher checks.
- 2026-05-29: Continued PP-026/PP-058 with Workbench launcher singleton switching. Already-open singleton launcher actions such as Git now stay enabled, show an `Open` state chip, and switch to the existing tab instead of becoming inert disabled cards. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780033185880.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780033185880.png`. The focused smoke now gates `workbenchNewTabSingletonSwitch=true` while preserving keyboard navigation, Git, and Agents launcher checks.
- 2026-05-29: Continued PP-033 with transcript tool-summary collapse and smoke harness stability. Grouped tool activity now exposes row-count/error/default-collapsed diagnostics, shows a compact action-count meta label, keeps the body unmounted before expansion, and uses a collapsed virtual-row estimate so long transcripts do not reserve expanded tool space before measurement. The transcript-layout smoke now gates `toolSummaryCollapsedByDefault=true`; the same pass replaced a fixed file-open wait and brittle session activation wait with state-based waits after two useful focused-smoke failures. Verification stayed intentionally narrow: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --transcript-layout`; final evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780039986621.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780039986621.png`.
- 2026-05-29: Continued PP-030/PP-058 with Terminal scoped URL handoff. Terminal link routing now registers per-terminal-id smoke openers, right-panel terminals route URL opens through the shared Browser handoff, and the focused terminal smoke selects the bottom terminal by id before opening a test URL. Verification stayed intentionally narrow: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --terminal`; final evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780041168899.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780041168899.png`. The smoke now gates `terminalLinkScopedRouting=true` while preserving `terminalLinkRouting=true`; broader live Codex terminal timing and mixed-surface stress remain separate.
- 2026-05-29: Continued PP-044/PP-058 with Provider Settings permission-config refresh. The Providers page now exposes an explicit `Refresh` action and live status beside the runtime permission-config row, matching the composer permission popover's ability to re-read live/fallback config without leaving the surface. Verification stayed intentionally narrow: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --settings-providers`; final evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780041717089.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780041717089.png`. The smoke now gates `settingsProviderPermissionRefresh=true`; remote-host settings sync and provider-native account/runtime adapters remain separate.
- 2026-05-29: Continued PP-044/PP-058 and tightened PP-029 validation targeting with Provider Settings runtime activity copy. The provider Runtime details card now has a compact `Copy` action backed by the app clipboard bridge plus visible/announced status, so provider/runtime diagnostics can be handed to chat without manual row selection. Added `pnpm run smoke:ui:suggest`, which maps changed files to the narrowest focused smoke target; for this slice it recommended only `--settings-providers`. Verification stayed intentionally narrow: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --settings-providers`; final evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780042434745.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780042434745.png`. The focused smoke now gates `settingsProviderRuntimeCopy=true` and `settingsProviderRuntimeCopyStatusA11y=true`.
- 2026-05-29: Continued PP-026/PP-058 and tightened PP-029 validation targeting with Workbench launcher coverage. The New tab launcher now exposes Environment and Extensions alongside Files, Side chat, Browser, Git, Review, Agents, and Terminal, so existing coding-control surfaces are discoverable from the main Workbench tab lifecycle entry point. Added split `--workbench-launcher` smoke coverage for launcher discovery, Environment/Extensions activation, open-state chips, and no-overflow without rerunning the heavier Git/agent `--workbench-new-tab` workflow. Verification stayed intentionally narrow: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-launcher`; final evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780042931745.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780042931745.png`. The focused smoke now gates `workbenchLauncherDiscovery=true`, `workbenchLauncherEnvironmentAction=true`, `workbenchLauncherExtensionsAction=true`, `workbenchLauncherOpenState=true`, and `workbenchLauncherNoHorizontalOverflow=true`.
- 2026-05-29: Continued PP-030/PP-058 with bottom-panel control language. The header toggle, resize handle, tablist, and hide action now use bottom-panel wording (`Toggle bottom panel`, `Resize bottom panel`, `Bottom panel tabs`, `Hide bottom panel`) while terminal-specific actions keep terminal wording. This aligns the existing bottom app-shell primitive with the Codex-style bottom-panel direction without pretending non-terminal bottom tabs are implemented yet. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, focused elevated `node scripts/run-automated-ui-smoke.mjs --terminal`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --header`; terminal evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780043369596.json`, header evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1780043394760.json`. The terminal smoke now gates `terminalBottomPanelLabels=true`.
- 2026-05-29: Continued PP-030/PP-058 with the first non-terminal bottom-panel tab. Plan can now move from the right Workbench into the bottom panel, render embedded Plan content there, suppress the duplicate right-panel Plan while bottom-owned, and move back to the right panel from the shared bottom tab menu. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, focused elevated `node scripts/run-automated-ui-smoke.mjs --terminal`, focused elevated `node scripts/run-automated-ui-smoke.mjs --header`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-launcher`; terminal evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780047823386.json`, header evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1780047657321.json`, Workbench launcher evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780047655099.json`. The terminal smoke now gates `bottomPanelPlanTransfer=true`; remaining bottom-panel parity is mixed-surface policy/stress and live Codex timing/animation proof.
- 2026-05-29: Continued PP-030/PP-058 with bottom-hosted Plan handoff stress. The focused Terminal smoke now clicks Plan `Add to chat` after moving Plan into the bottom panel and verifies the embedded Plan status plus main composer context payload before moving Plan back right. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --terminal`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780048294721.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780048294721.png`. The terminal smoke now gates `bottomPanelPlanAddToChat=true` alongside `bottomPanelPlanTransfer=true`.
- 2026-05-29: Continued PP-030/PP-058 with bottom-panel Plan-only close policy. Plan tabs are now closable even when Plan is the final bottom tab, single-terminal tabs remain protected from the tab-close affordance, and final bottom-tab close returns focus to the titlebar bottom-panel toggle. `src/types/panelTabs.ts` now accepts non-terminal bottom tab ids for shared new-tab routing and maps to the focused cross-panel keyboard smoke in `smoke:ui:changed`. Verification stayed focused: `pnpm exec tsc --noEmit`, `pnpm exec tsc -p tsconfig.node.json --outDir out-test --module commonjs`, `node --test out-test/src/main/__tests__/panelTabs.test.js`, focused elevated `node scripts/run-automated-ui-smoke.mjs --terminal`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --cross-panel-keyboard`; terminal evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780049649508.json`, cross-panel keyboard evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-cross-panel-keyboard-1780049675999.json`.
- 2026-05-29: Tightened PP-029 validation targeting for shared panel-tabs logic. `smoke:ui:changed` now runs the compiled node `panelTabs` unit test whenever `src/types/panelTabs.ts` or `src/main/__tests__/panelTabs.test.ts` changes, while preserving the focused `--cross-panel-keyboard` UI smoke mapping for rendered panel-shell behavior. Verification stayed non-Electron for this planner slice: `node -c scripts/suggest-ui-smoke-targets.mjs`, `node scripts/suggest-ui-smoke-targets.mjs --json src/types/panelTabs.ts src/main/__tests__/panelTabs.test.ts`, `pnpm exec tsc -p tsconfig.node.json --outDir out-test --module commonjs`, `node --test out-test/src/main/__tests__/panelTabs.test.js`, `pnpm run smoke:ui:suggest`, and `git diff --check` passed.
- 2026-05-29: Continued PP-030/PP-058 with direct bottom-panel Plan discovery. The bottom-panel toolbar now exposes `Open Plan in bottom panel` next to `New terminal`, activating an existing bottom Plan tab or transferring Plan through the shared panel-tab path with visible status. Verification stayed focused: `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `pnpm exec tsc --noEmit`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --terminal`; terminal evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780050293140.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780050293140.png`. The Terminal smoke now gates `bottomPanelPlanToolbarAction=true`.
- 2026-05-29: Continued PP-030/PP-058 with explicit bottom-panel allowed-tab policy. Shared panel-tab code now exposes Terminal and Plan as the current bottom-panel transfer kinds, unsupported right Workbench tabs show a visible `Bottom panel supports Terminal and Plan tabs.` context-menu boundary, and `smoke:ui:suggest` restores `--right-panel` for ContextSidebar tab-menu/transfer diffs so launcher-only split coverage does not under-test shell menu policy changes. Verification stayed targeted: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `pnpm exec tsc -p tsconfig.node.json --outDir out-test --module commonjs`, `node --test out-test/src/main/__tests__/panelTabs.test.js`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --right-panel`; right-panel evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1780050734087.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1780050734087.png`. The Right Workbench smoke now gates visible allowed-tab boundary coverage under `rightPanelTransferUnsupportedBoundary=true`.
- 2026-05-29: Continued PP-026/PP-058 with Plan discovery in the Workbench launcher. When goal/plan/review-mode state exists and Plan is not bottom-hosted, the New Tab launcher now includes Plan as a singleton action, switches to the right Workbench Plan tab, and reports `Open` state like Environment/Extensions. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-launcher`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780051042787.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780051042787.png`. The focused smoke now gates `workbenchLauncherPlanAction=true` and a 10-action launcher inventory.
- 2026-05-29: Continued PP-026/PP-058 with Workbench Plan close/reopen lifecycle. The right Workbench tabstrip now treats Plan as an explicitly opened tab instead of always deriving it from active goal/review state, so closing Plan dismisses the tab while the Workbench launcher keeps Plan available as a `New` singleton action for intentional reopen. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-launcher`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780051493371.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780051493372.png`. The focused smoke now gates `workbenchLauncherPlanCloseReopen=true`.
- 2026-05-29: Continued PP-027/PP-058 with right Workbench final-close keyboard focus restoration. The app-level close-active-tab command now detects final right Workbench tab closes and restores focus to the header side-panel toggle, matching the existing close-button final-tab path. `smoke:ui:suggest` now routes App/smoke diffs for right-panel close-active/focus work to `--right-panel` instead of falling back to a broader header-only suggestion. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --right-panel`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1780051960125.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1780051960125.png`. The focused smoke now gates `rightPanelLastTabShortcutFocusRestored=true`.
- 2026-05-29: Continued PP-030/PP-058 with right-panel Terminal shortcut close cleanup. The app-level close-active-tab command now detects right-panel Terminal tab ids, kills the underlying shell, and removes the tab through `closeTerminalTab`, matching the pointer close path. The focused Terminal smoke creates a right-panel terminal through the right-panel new-tab shortcut, checks its shared transfer metadata, closes it through the shortcut path, and verifies the service snapshot is no longer running that terminal. The same pass updated Plan bottom-panel transfer proof to open Plan through the Workbench launcher before moving it, matching the explicit Plan lifecycle. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --terminal`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780052420305.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780052420305.png`. The focused smoke now gates `terminalRightPanelCloseShortcut=true`.
- 2026-05-29: Continued PP-044/PP-058 with composer rapid context handoffs and tighter smoke targeting. Composer `orchestrator:add-composer-text` events now append against the live per-session store draft, so rapid context insertions from adjacent panels keep all context blocks instead of racing against a stale render closure. Edit-as-draft replacement snapshots the live stored draft/attachments before replacement. `smoke:ui:suggest` now maps `InputBar.tsx` and composer-specific smoke harness diffs to the focused `--composer` target, keeping small composer slices away from broad smoke. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --composer`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780052908503.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780052908503.png`. The focused smoke now gates `composerRapidContextAppend=true`.
- 2026-05-29: Continued PP-027/PP-030/PP-058 with shared panel-tab keyboard context menus and tighter smoke targeting. Right Workbench and bottom-panel tabs now open their context menus from `Shift+F10` or the Menu key, using an anchored keyboard point so move/reset/close/transfer actions are not pointer-only. `smoke:ui:suggest` now maps shared tab-strip/context-menu diffs to `--right-panel` and `--terminal`, and suppresses unrelated Workbench-launcher smoke for tab-menu-only ContextSidebar edits. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, focused elevated `node scripts/run-automated-ui-smoke.mjs --right-panel`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --terminal`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1780054270165.json` and `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780054300530.json`. The focused smokes now gate `rightPanelTabKeyboardContextMenu=true` and `terminalTabKeyboardContextMenu=true`.
- 2026-05-29: Continued PP-048/PP-058 with Files row keyboard context menus and tighter smoke targeting. Files tree rows now open their row action menu from `Shift+F10` or the Menu key, anchored to the row through the shared Workbench tree context-menu path, so Add to chat/Open in Workbench/Copy path are no longer pointer-only. `smoke:ui:suggest` now maps WorkbenchTree/Files row-menu diffs to `--files`, uses zero-context dirty diffs to avoid context-line false positives, and suppresses unrelated right-panel smoke for this Files-only tree slice. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --files`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780054759606.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780054759606.png`. The focused smoke now gates `filesRowKeyboardContextMenu=true`.
- 2026-05-29: Continued PP-036/PP-058 with Review row keyboard context menus. Review changed-file rows now expose Open in Workbench, Copy path, and Reveal file from a row context menu opened by `Shift+F10` or the Menu key through the shared Workbench tree path. Copy path uses Review's existing clipboard/status channel, and `smoke:ui:suggest` maps this row-menu work to focused `--diff-core` proof. Verification stayed focused: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `git diff --check`, `pnpm run smoke:ui:suggest`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --diff-core`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780055920173.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780055920173.png`. The focused smoke now gates `reviewRowKeyboardContextMenu=true`.
