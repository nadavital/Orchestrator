# Side Panel Parity Stabilization Ledger

Last updated: 2026-05-28

This ledger exists to turn the large Codex side-panel parity worktree into reviewable, durable app progress. Until the current dirty tree is split into commits, pause new parity feature work except for fixes required to make a slice verifiable.

## Current State

- Branch: `codex-side-panel-parity-stabilization`.
- Base: `origin/main` at `ba31ea6 Add sidebar parity handoff notes`.
- Current dirty tree: broad side-panel parity work across shell, terminal, browser, files, sidebar, settings, review, automations, smoke harness, and docs.
- Nothing was staged at the start of this stabilization pass.
- Product priority has shifted from broad Codex parity closure to day-to-day coding usability. Keep side-panel polish active, but treat deep DOCX/XLSX/PPTX/PDF renderer fidelity as Phase 2 unless it directly blocks coding workflows.
- The current Review selected-line action cleanup has passed:
  - `pnpm exec tsc --noEmit`
  - `npm run smoke:ui:auto -- --diff-core`
  - `npm run smoke:ui:auto -- --diff-source`
- Latest Review source evidence:
  - JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779685442980.json`
  - Screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779685442980.png`

## Commit Slices

Commit these as separately verified slices where practical.

### Phase 1: Daily Coding Usability

1. Shared shell, header, panel, route/window, and command primitives.
2. Main chat thread readability, streaming/tool-call states, errors, approvals, retry/continue behavior, and long-thread ergonomics.
3. Composer controls for attachments, context, provider/model/permission selection, keyboard behavior, submit/stop states, and side-chat consistency.
4. Right Workbench side panel polish across Review, Browser, Files/source tabs, Side Chat, and useful inspector/context panels.
5. Terminal bottom/right panel behavior for coding workflows.
6. Sidebar organization and provider thread metadata.
7. Settings route ownership, page split, app protocol, provider/account/runtime/browser/shell preferences, and adapter-backed unavailable states.
8. Browser manager and webview actions that support real app-building flows.
9. Review/diff workspace and local Git controls.
10. Automations model, scheduler, and UI actions where they support coding sessions.
11. Smoke harness and product-usability docs.

### Phase 2: Artifact And Provider Depth

1. Full DOCX layout/style/pagination/comment/revision fidelity.
2. Full XLSX/PPTX canvas, object, editing, chart, formula, and persisted-edit fidelity.
3. Full PDF canvas rendering and provider-backed annotations.
4. Provider-backed artifact comments, blame, and metadata.
5. Non-Codex provider adapters that require real provider contracts rather than local fixtures.

## Mixed Files That Need Care

Do not blindly file-stage these unless the target commit intentionally owns all touched hunks:

- `src/main/index.ts`
- `scripts/run-automated-ui-smoke.mjs`
- `src/types/index.ts`
- `src/renderer/src/store/sessions.ts`
- `src/renderer/src/index.css`
- `src/renderer/src/components/shared/designSystem.tsx`
- `src/renderer/src/App.tsx`
- `src/main/git.ts`
- `src/renderer/src/components/shared/SessionActionsMenu.tsx`
- `docs/codex-side-panel-ui-parity-audit.md`

## Verification Policy

Before committing a slice:

1. Run the narrow unit tests for that slice.
2. Run the focused smoke for that slice.
3. Run `pnpm exec tsc --noEmit`.
4. Run `git diff --check`.
5. Update docs only with evidence that was produced by the current worktree.

Before considering the stabilization complete:

1. Run `npm run build`.
2. Run `npm run test:providers`.
3. Run `npm run smoke:visual:side-panels -- --out tmp/side-panel-visual-inventory-current --full`.
4. Run any packaged checks required by shell/settings changes.

## Remaining Goal Gaps

These remain real goal gaps and should not be claimed complete from local fixture smokes:

- Main chat thread, composer, inspector/context panel, and Settings still need an explicit day-to-day coding usability audit and implementation pass. Codex parity evidence is useful here, but Orchestrator-owned workflow quality is the success bar. General Settings now smoke-gates preferred-editor save status and persistence under `settingsGeneralActionStatus=true` and `settingsGeneralPreferredEditorPersistence=true`; Local Personalization Settings now have a first pass with persisted custom instructions/coding preferences, visible/announced save status under `settingsPersonalizationActionStatus=true`, and focused smoke coverage; provider Settings now expose existing runtime capability gaps as compact boundaries, shared command-output card chrome, a shared provider-picker surface, a shared model-list manager, config-editor shared surface/status semantics under `settingsProviderConfigEditorShared=true`, and unavailable provider install-command copy/status coverage under `settingsProviderInstallCommandCopy=true` plus `settingsProviderInstallCommandStatusA11y=true`; Appearance Settings now keep color swatches, import controls, and chrome editor controls on shared surface/input chrome, and theme sharing copy/import status is smoke-gated under `themeSharingStatusA11y=true` plus `themeCopyClipboard=true`; Data Controls Settings now smoke-gates data-path copy through the app clipboard bridge plus action status semantics under `settingsDataControlsActionA11y=true` and `settingsDataControlsCopyPathClipboard=true`; Browser Settings now smoke-gates live save/clear status and labelled domain-rule controls; Browser local dev-server refresh/hide/restore and route removal now expose live action feedback under `browserLocalTargetActionStatus=true` plus `browserLocalRouteActionStatus=true`; Automations Settings now smoke-gates pause action status, row grouping, and action labels under `settingsAutomationsActionA11y=true`; Worktrees Settings now smoke-gates create/delete status announcements plus row/conversation/action names under `settingsWorktreesActionA11y=true`; Shortcuts Settings now smoke-gates shortcut save/conflict/clear status under `settingsShortcutActionStatusA11y=true`; Files row/source copy actions use the app clipboard bridge under `filesRowCopyPathClipboard=true` and `fileSourceActionStatus=true`, source-tab composer attachment dispatch gates under `fileSourceAddToChat=true`, and content-search open-to-line gates under `filesContentSearchOpenLine=true`; empty-chat starter actions, failed-run retry recovery, failed-run retry live status, tool-failure retry recovery, tool-failure retry live status, first-pass assistant continuation, continuation live status role under `chatContinueLastTurnA11y=true`, assistant-message copy bridge/status under `chatMessageCopy=true`, transcript thinking indicator status under `thinkingIndicatorDuringUpdate=true`, partial-response stopped recovery plus live partial-continue proof, queued follow-up cancellation, steering follow-up cancellation, compact active-run queue summaries, active-run composer stop control/status under `composerStopRunControl=true` plus `composerStopRunStatus=true`, first-pass long-thread history/load/find ergonomics, active-thread provider/model/agent/effort composer settings with model persistence and live Codex resumed-thread proof under `composerActiveThreadModelSwitchPersisted=true` plus `npm run live:codex-composer-model-switch`, provider-choice visibility under `composerActiveThreadProviderChoices=true`, provider switching under `composerActiveThreadProviderSwitch=true`, persisted provider/runtime/model settings under `composerActiveThreadProviderSwitchPersisted=true`, persisted provider policy/effort settings under `composerActiveThreadProviderSwitchPolicyPersisted=true`, send-after-settings flush under `composerSendFlushesPendingSettings=true`, and explicit choice semantics under `composerAgentChoiceA11y=true`, `/model` slash command routing under `composerSlashModelOpensSettings=true`, slash-opened model/permission menu focus under `composerSlashModelFocusesMenu=true` and `composerSlashPermissionsFocusesMenu=true`, blocked-send permission recovery focus under `composerSendStatusActionFocusesPermissions=true`, composer permission config source signalling under `composerPermissionContextSignal=true`, live-disabled permission blocking under `composerContextDisabledPermissionBlocksSend=true`, composer popover trigger ARIA state, composer popover roving keyboard navigation, composer advanced permission-rule saved feedback under `composerPermissionRuleStatus=true`, blocked-send composer status, blocked-send live-region semantics, composer attachment add/remove/save/cancel/failure status under `composerAttachmentStatus=true`, composer side-chat attachment loss prevention under `composerSideChatAttachmentGuard=true`, transcript approval/user-input recovery live regions, transcript user-input option state under `userInputOptionSelection=true`, transcript permission decision labels under `permissionActionsRecoverable=true` plus `permissionCard=true`, non-command permission cards under `permissionVariantCards=true`, side-chat retry/multiline composer behavior, first-pass side-chat context metadata, side-chat visible/announced answer status under `sideChatActionStatusA11y=true` plus retry status under `sideChatRetryStatusA11y=true`, side-chat personalization context under `sideChatPersonalizationContext=true`, and side-chat follow-up context continuity under `sideChatFollowupContext=true` now have coverage. Remaining main-chat/composer work should target deeper context/permission workflow polish, exact live Codex long-thread timing only when live evidence is available, live non-command permission fixtures when deterministic, and non-Codex provider-backed lifecycle proof; remaining Settings work should target provider-native account/runtime adapters, real remote adapters, and higher-value daily coding workflows.
- Current composer/transcript evidence also gates queued and steering action feedback directly: `composerQueuedCancelStatus=true`, `composerSteeringCancelStatus=true`, `queuedFollowUpStartFailureVisible=true`, `queuedFollowUpStartFailureState=true`, `composerSlashEscapePreservesDraft=true`, `composerSendFailureRestoresDraft=true`, `composerSendFalseRestoresDraft=true`, `composerSendProviderFalseCleansTranscript=true`, `composerContextDisabledPermissionBlocksSend=true`, `composerSendFlushesPendingSettings=true`, `errorRecoveryRetryPrepareFailure=true`, `userInputResumePrepareFailure=true`, `permissionResumePrepareFailure=true`, `permissionProfileDecisionLabels=true`, `chatContinueFailedStartCleansSyntheticPrompt=true`, `composerSlashPermissionsFocusesMenu=true`, and `composerPermissionRovingKeyboard=true` passed in the focused composer/transcript/streaming smokes. Remaining work is not another local action-status pass; it is deeper context/permission workflow polish and real provider-backed lifecycle proof.
- Current live composer/provider evidence now includes `npm run live:codex-composer-appserver`, which starts the real Codex app-server in a disposable git workspace with Orchestrator's Codex model/effort/policy mapping, verifies the returned managed permission profile, observes a completed `commandExecution` item, checks the git-visible proof edit, and records the final assistant token in `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-appserver-live-proof/result.json`. It also includes `npm run live:codex-composer-approval`, which intentionally triggers and answers a real `item/commandExecution/requestApproval` with numeric JSON-RPC id `0`, observes `serverRequest/resolved`, and verifies the approved parent-directory proof write in `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-approval-live-proof/result.json`. `npm run live:codex-composer-resume` proves a non-ephemeral Codex thread can be resumed from a fresh app-server process with the same thread id and prior-turn context; artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-resume-live-proof/result.json`. `npm run live:codex-composer-runtime-lifecycle` proves Orchestrator's `CodexAppServerRuntimeManager` emits the expected runtime events while starting with `thread/start`, resuming with `thread/resume`, preserving the same provider session id, and retaining prior-turn context; artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-runtime-lifecycle-live-proof/result.json`. `npm run live:codex-composer-session-lifecycle` proves the compiled main-process `sessionManager` path creates a Codex app-server session, sends through `sendMessage`, continues through `continueLastTurn`, retries through `retryLastUserMessage`, and keeps the same provider session id while recording live start/resume methods; artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-session-lifecycle-live-proof/result.json`. `npm run live:codex-composer-renderer-lifecycle` proves the renderer transcript buttons invoke live Codex send/continue/retry behavior through the app, with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-lifecycle-1779986634530.json`. `npm run live:codex-composer-partial-continue` proves visible partial-response continuation through the renderer, with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-partial-continue-1779987197970.json`. `npm run live:codex-composer-model-switch` proves a resumed Codex provider thread uses the updated Orchestrator session model, with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-model-switch-1779987672302.json`. Focused local/parser/UI evidence now covers Codex app-server file-change and permission-profile approval card variants under `permissionVariantCards=true`, with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-permission-1779988391389.json`. Remaining provider-backed lifecycle work is deterministic live file/profile permission request forcing and non-Codex provider adapters.
- Current transcript Review-card evidence gates the local Undo, open-Review, and file-row accessible names directly: `reviewTranscriptCardUndo=true` requires the Undo button to expose its `Undo N changed files` scope through both native title and `aria-label` without clicking the destructive action, and `reviewTranscriptCard=true` requires the `Review` action plus each visible file row to expose contextual accessible labels for Review scope, status, path, and change counts. Remaining Review Undo/open-source work is provider-backed checkpoint/rollback/source support, not another local transcript-card label pass.
- Inspector/context first pass: Agents is now reachable from the compact top-aligned Workbench New tab even before a live subagent exists, and that New tab now uses a command-list launcher under `workbenchNewTabListLauncher=true` instead of a large card grid. The Agents empty state shows session/runtime context under `workbenchNewTabAgentsAction=true`, runtime issue rows summarize failed/waiting events under `agentRuntimeIssueTriage=true`, issue failures are grouped by cause under `agentRuntimeFailureGroups=true`, recent runtime events can be search-narrowed under `agentRuntimeEventFilter=true`, severity/source facets narrow noisy runtime streams under `agentRuntimeEventFacetFilters=true`, matching runtime events can be selected for a bounded detail payload with contextual row labels and pressed-state semantics under `agentRuntimeEventDetail=true`, selected event payloads can be copied through the app clipboard bridge under `agentRuntimeEventCopy=true`, selected event details can be inserted into the main composer with thread/runtime/status/workspace context under `agentRuntimeEventAddToChatContext=true`, selected agent tabs now show a compact lifecycle timeline under `agentSelectedTimeline=true`, raw provider transport output is summarized/redacted under `agentTransportLog=true`, the Environment header settings control now opens Providers settings under `environmentSettingsOpensProviders=true` instead of being an enabled dead icon, and Environment's provider Web search source now exposes an explicit unavailable boundary under `environmentSourceBoundary=true` instead of appearing actionable. Remaining inspector work should focus on richer provider-backed observability and real coding workflow gaps.
- Extensions panel primitive migration is closed for the current embedded right-panel surface: `/extensions` now exposes shared summary, disclosure, file-row, command-section, command-row, and item-row contracts under `extensionsPanelSharedPrimitives=true`. Remaining Extensions work should be evidence-led provider data/functionality gaps rather than another local chrome pass.
- Codex-style global/thread find parity has first-pass coverage: the shared `content-search-input` with `Search chat` and `Search diffs` scopes is implemented for transcript/Review/file-source search, Browser-focused `Cmd+F` still routes to Browser find, and the comparison script now treats `rightPanelFindShortcutRouting=true` as the Codex-style shared-find contract. Exact live Codex focus timing and pixel spacing remain open.
- Header/right-panel ownership now has a first pass: the session header side-panel control is labelled as `Toggle side panel`, declares `right-workbench` ownership, and opens the Workbench New tab launcher when no tabs exist under `headerPanelEmptyFallback=true`. Exact live Codex header/right-panel boundary timing and pixel spacing remain open.
- Full escalated side-panel comparison on 2026-05-28 refreshed all 27 local captures with `mismatchCount=0`, `needsSmokeCount=0`, and `needsProofCount=0` after repairing the Files overflow fixture and the multi-window focus/menu-routing fixture. The multi-window focused rerun passes `firstWindowBrowserFocusArea=true`, `firstWindowBrowserMenuEnabled=true`, `focusSwitchRestoresFirstWindowMenu=true`, and `menuCommandRoutedToFocusedWindow=true` after opening Browser from the active New tab launcher. The remaining blocked rows require nonblank live Codex screenshots or provider/runtime contracts.
- Live Codex side-by-side visual comparison across the 23-surface inventory.
- Route/window lifecycle and installed-app replacement proof.
- Provider-backed Review checkpoint Undo, cloud/hosted sources, PR metadata, comments, and blame. Local Review git actions and Review copy status are now smoke-gated with `reviewFloatingGitActionStatus=true` and `reviewGitApplyCopyStatus=true`.
- Provider pin mutation and richer hosted/cloud/worktree sidebar reconciliation.
- Live provider-emitted Browser/browser-use proof and non-Codex browser/local-server adapters where available. Local Browser URL copy status is now smoke-gated with `browserCopyUrlStatus=true`.
- Real remote-host Settings adapters and Codex-style provider-native Host Personalization for memory/personality/custom instructions. Local Orchestrator personalization is implemented separately and should not be mistaken for provider-native sync.
- Dedicated accessibility audit for keyboard-only traversal, broader screen-reader labels, and reduced motion. First-pass composer popover focus restoration, trigger expanded/collapsed ARIA state, roving keyboard navigation, composer blocked-send live status, composer active-thread provider-choice row under `composerActiveThreadProviderChoices=true`, composer settings choice groups/pressed state under `composerAgentChoiceA11y=true`, inspector runtime-event row labels/pressed state under `agentRuntimeEventDetail=true`, shared chat/diff find result status under `rightPanelFindStatusA11y=true`, shared find scope labels under `rightPanelFindScopeLabelA11y=true`, shared find close focus restoration under `rightPanelFindCloseFocusRestored=true`, transcript message-copy live status, transcript retry/continue live status, transcript Review-card Undo/open-action/file-row accessible names under `reviewTranscriptCardUndo=true` and `reviewTranscriptCard=true`, transcript user-input option state and sent/error live regions, transcript permission decision/error action semantics plus contextual decision labels, Browser load-error alert/action-group semantics, Browser URL copy live status, Browser local-server action status, Terminal clipboard live status, Terminal clear-action status under `terminalClearActionStatus=true`, Terminal tab action status under `terminalTabActionStatus=true`, Terminal failure/end status recovery semantics, Browser Settings status/domain-control semantics, Personalization Settings save status under `settingsPersonalizationActionStatus=true`, Worktrees Settings action/status semantics, and Review local git/copy-action status semantics are now smoke-gated, but this does not close the whole-app accessibility pass.
- Deep Office/PDF renderer work remains tracked, but it is Phase 2 unless a coding workflow depends on it.

See `docs/codex-side-panel-live-comparison.md` for the current surface-by-surface Codex evidence and smoke-harness assessment.
