# Side Panel Parity Stabilization Ledger

Last updated: 2026-05-30

This ledger exists to turn the large Codex side-panel parity worktree into reviewable, durable app progress. Until the current dirty tree is split into commits, pause new parity feature work except for fixes required to make a slice verifiable.

## Current State

- Branch: `codex-side-panel-parity-stabilization`.
- Base: `origin/main` at `ba31ea6 Add sidebar parity handoff notes`.
- Current dirty tree: broad side-panel parity work across shell, terminal, browser, files, sidebar, settings, review, automations, smoke harness, and docs.
- Nothing was staged at the start of this stabilization pass.
- Product priority has shifted from broad Codex parity closure to day-to-day coding usability. Keep side-panel polish active, but treat deep DOCX/XLSX/PPTX/PDF renderer fidelity as Phase 2 unless it directly blocks coding workflows.
- The current Review selected-line action cleanup has passed:
  - `pnpm exec tsc --noEmit`
  - `npm run smoke:ui:auto -- --diff-core`
  - `npm run smoke:ui:auto -- --diff-source`
- Latest Review source evidence:
  - JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779685442980.json`
  - Screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779685442980.png`

## Commit Slices

Commit these as separately verified slices where practical.

### Phase 1: Daily Coding Usability

1. Shared shell, header, panel, route/window, and command primitives.
2. Main chat thread readability, streaming/tool-call states, errors, approvals, retry/continue behavior, and long-thread ergonomics.
3. Composer controls for attachments, context, provider/model/permission selection, keyboard behavior, submit/stop states, and side-chat consistency.
4. Right Workbench side panel polish across Review, Browser, Files/source tabs, Side Chat, and useful inspector/context panels.
5. Terminal bottom/right panel behavior for coding workflows.
6. Sidebar organization and provider thread metadata.
7. Settings route ownership, page split, app protocol, provider/account/runtime/browser/shell preferences, and adapter-backed unavailable states.
8. Browser manager and webview actions that support real app-building flows.
9. Review/diff workspace and local Git controls.
10. Automations model, scheduler, and UI actions where they support coding sessions.
11. Smoke harness and product-usability docs.

### Phase 2: Artifact And Provider Depth

1. Full DOCX layout/style/pagination/comment/revision fidelity.
2. Full XLSX/PPTX canvas, object, editing, chart, formula, and persisted-edit fidelity.
3. Full PDF canvas rendering and provider-backed annotations.
4. Provider-backed artifact comments, blame, and metadata.
5. Non-Codex provider adapters that require real provider contracts rather than local fixtures.

## Mixed Files That Need Care

Do not blindly file-stage these unless the target commit intentionally owns all touched hunks:

- `src/main/index.ts`
- `scripts/run-automated-ui-smoke.mjs`
- `src/types/index.ts`
- `src/renderer/src/store/sessions.ts`
- `src/renderer/src/index.css`
- `src/renderer/src/components/shared/designSystem.tsx`
- `src/renderer/src/App.tsx`
- `src/main/git.ts`
- `src/renderer/src/components/shared/SessionActionsMenu.tsx`
- `docs/codex-side-panel-ui-parity-audit.md`

## Verification Policy

Before committing a slice:

1. While iterating, run `pnpm run smoke:ui:changed:static` for the generated whitespace/type/syntax plan.
2. Run the narrow unit tests for that slice.
3. Run the focused smoke for that slice, or `pnpm run smoke:ui:changed:smoke` when the changed-files plan already maps exactly to the desired Electron target.
4. Run `pnpm exec tsc --noEmit` and `git diff --check` if they were not already included in the generated static plan.
5. Update docs only with evidence that was produced by the current worktree.

Routine Phase 1 iteration should not run the no-flag/full UI smoke for tiny source diffs. Use the generated focused target first, and reserve full inventory/no-flag smoke for uncovered broad-smoke changes, coordinated shell contracts, package dependency/config/runtime changes, or pre-merge confidence checkpoints. Package `scripts`-only edits are now classified separately so proof-harness command additions do not create false broad-smoke pressure.

For milestone/dogfood checks, use `npm run smoke:ui:daily-coding` instead of the full side-panel inventory. The core set covers the header/session/composer/transcript/workbench/Review/Files/Browser/Terminal/Settings path that matters for daily coding. The optional `--full` set adds permission/user-input, Agent Activity, Environment, provider Settings, and side chat. This runner is intentionally slower than changed-file smokes and intentionally narrower than visual parity inventory.

2026-05-29 daily-coding smoke-runner checkpoint:

- Added `scripts/run-daily-coding-smoke.mjs` and `npm run smoke:ui:daily-coding`.
- `--list` prints the core/full target sets, `--only` validates a comma-separated subset, `--packaged`/`--installed` reuse the same target set against packaged app modes, `--keep-going` records every failure instead of stopping at the first failed target, and `--verbose` restores nested per-smoke output when investigating a specific target.
- Verification passed for syntax/list/package JSON/whitespace and an elevated representative `--only header` run. Latest concise-output daily manifest: `/Users/nadav/Desktop/Orchestrator/tmp/daily-coding-smoke/daily-coding-smoke-1780100928719.json`.
- Full elevated milestone run passed with `pnpm run smoke:ui:daily-coding --full --keep-going`; manifest `/Users/nadav/Desktop/Orchestrator/tmp/daily-coding-smoke/daily-coding-smoke-1780100822131.json`. The runner now defaults to concise per-target pass/fail lines while retaining nested output on failure or `--verbose`, so milestone checks remain readable.
- Packaged subset follow-up: `npm run pack:mac` passed, then elevated `pnpm run smoke:ui:daily-coding --packaged --only header,session-switch,composer,workbench-new-tab,browser,terminal --keep-going` passed against `dist/mac-arm64/Orchestrator.app`; manifest `/Users/nadav/Desktop/Orchestrator/tmp/daily-coding-smoke/daily-coding-smoke-1780102021969.json`. The pass followed a real Workbench/Git fix: generated PR commands now open in a fresh terminal tab so they do not concatenate with existing terminal drafts.
- Failure artifact traceability follow-up: the daily-coding runner now records nested smoke JSON/screenshot paths from stdout or stderr, so failed targets remain directly inspectable in the manifest even when the child smoke prints diagnostics to stderr. The changed-file planner now runs the runner unit test for daily-coding runner edits. Verification: `npm run smoke:ui:changed:static`, `pnpm run smoke:ui:daily-coding --list`, and elevated `pnpm run smoke:ui:daily-coding --only header`; header manifest `/Users/nadav/Desktop/Orchestrator/tmp/daily-coding-smoke/daily-coding-smoke-1780102327846.json`.
- 2026-05-30 refresh: elevated `pnpm run smoke:ui:daily-coding --full --keep-going` passed all 17 targets after the latest Review/Settings/provider-boundary checkpoints; manifest `/Users/nadav/Desktop/Orchestrator/tmp/daily-coding-smoke/daily-coding-smoke-1780107805539.json`. No local daily-coding workflow failure was exposed, so remaining work should continue to focus on live/provider proof, real dogfood breaks, and explicit adapter boundaries.
- 2026-05-30 runner ergonomics follow-up: the focused composer/transcript/inspector/settings dogfood pass succeeded with `pnpm run smoke:ui:daily-coding --only composer,transcript-layout,agent-inspector,settings --keep-going`; manifest `/Users/nadav/Desktop/Orchestrator/tmp/daily-coding-smoke/daily-coding-smoke-1780110673203.json`. The runner now strips a leading package-manager `--` separator before option parsing, so older copied commands no longer fail with `Unknown option: --`.
- 2026-05-30 side-panel cluster follow-up: the focused Review/Files/Browser pass succeeded with `pnpm run smoke:ui:daily-coding --only diff-core,files,browser --keep-going`; manifest `/Users/nadav/Desktop/Orchestrator/tmp/daily-coding-smoke/daily-coding-smoke-1780110950730.json`. This pass did not expose a product UI failure, but it did show `--files` as a 38.2s target. The daily-coding runner now records sorted `slowTargets` using a default 30s threshold, plus an optional `--slow-threshold-ms`, so expensive surfaces are visible when choosing between focused and milestone validation.
- 2026-05-30 remaining-surface follow-up: the focused session-switch, Workbench/Git, Environment, provider Settings, side-chat, permission-card, and user-input-card pass succeeded with `pnpm run smoke:ui:daily-coding --only session-switch,workbench-new-tab,environment,settings-providers,side-chat,transcript-permission,transcript-user-input --keep-going`; manifest `/Users/nadav/Desktop/Orchestrator/tmp/daily-coding-smoke/daily-coding-smoke-1780111279369.json`. No targets failed and no target crossed the 30s slow threshold, so current local Phase 1 evidence is broad enough to avoid another full daily-coding suite until a meaningful app-shell/workflow change lands.

2026-05-29 final local proof-gate refresh:

- `npm run build` passed.
- `npm run test:providers` passed with 306/306 tests.
- Elevated `npm run smoke:visual:side-panels -- --out tmp/side-panel-visual-inventory-current --full` passed 27/27 captures.
- The gate refresh fixed stale harness assumptions around Review primary toolbar ordering, Review loading card width, Review source selected-line handoff result export, and Plan opening from the Add Workbench tab launcher when Agents is the only mounted right-panel tab.

Before considering the full stabilization complete:

1. Keep the local proof gates above green after future changes.
2. Run packaged checks only when a slice changes shell/settings/package behavior.
3. Do not claim live Codex screenshot parity or non-Codex provider parity from local fixture smokes.

Current completion checkpoint: see `docs/phase-1-daily-coding-completion-audit.md`. Routine Phase 1 work should come from that audit's must-fix list; avoid adding more local copy/status/action affordances unless a daily coding workflow is actually broken.

## Remaining Goal Gaps

These remain real goal gaps and should not be claimed complete from local fixture smokes:

- Live Codex UI pixel proof is currently a capture boundary, not an active local-polish target. Elevated `npm run compare:codex-side-panels -- --capture-live-codex --no-fail` refreshed the comparison at `2026-05-30T02:26:03.862Z` with `mismatch=0`, `needsSmoke=0`, `needsProof=0`, and 19 remaining parity gaps. CoreGraphics found a visible Codex window (`id=65887`, bounds `0,30,1384,1320`), but window and region `screencapture` failed and the full-screen fallback produced an all-black PNG. Do not spend more Phase 1 cycles on live Codex spacing/timing rows until a nonblank capture route or manual side-by-side evidence is available.
- Codex pinned-thread mutation is now a proven app-server boundary, not an active local-polish target. Refreshed `npm run live:codex-pinned-threads` evidence from `2026-05-30T02:57:56.589Z` proves `thread/list` works through the current app-server, while `list-pinned-threads`, `set-thread-pinned`, and `set-pinned-threads-order` return unknown-variant errors. Orchestrator should keep provider-projected pin actions read-only until a supported Codex route exists; current artifact: `tmp/codex-pinned-threads-live-proof/result.json`.
- Codex Goal mutation is now a proven app-server boundary, not an active local-polish target. Orchestrator parses `thread/goal/updated` / `thread/goal/cleared` and preserves Plan goal metrics from live and persisted transcript data, but `npm run live:codex-composer-goal` showed `/goal ...` and `/goal clear` are handled as ordinary turn text by the current Codex app-server. The Codex slash command is therefore not advertised, and the Plan clear affordance is disabled with `planGoalClearBoundary=true` until a real app-server goal mutation route is proven.
- Unavailable remote-host Settings pages now expose a direct Provider Settings handoff in addition to Local recovery. Focused Settings smoke gates `settingsHostUnavailableProviderSettingsHandoff=true`, so remote-host adapter boundaries lead users to provider/account/runtime configuration instead of becoming a dead end. Real remote-host Settings adapters and provider-native host personalization remain open.
- Main chat thread, composer, inspector/context panel, and Settings still need an explicit day-to-day coding usability audit and implementation pass. Codex parity evidence is useful here, but Orchestrator-owned workflow quality is the success bar. General Settings now smoke-gates preferred-editor save status and persistence under `settingsGeneralActionStatus=true` and `settingsGeneralPreferredEditorPersistence=true`; Settings content now moves focus into the named content region on open/host/section changes under `settingsContentFocusOnOpen=true`, unavailable host pages can recover directly to editable Local settings under `settingsHostUnavailableLocalRecovery=true`, and Back to chat returns focus to the main composer under `settingsCloseFocusRestored=true`; Local Personalization Settings now have a first pass with persisted custom instructions/coding preferences, visible/announced save status under `settingsPersonalizationActionStatus=true`, and focused smoke coverage; provider Settings now expose existing runtime capability gaps as compact boundaries, shared command-output card chrome, a shared provider-picker surface, a shared model-list manager, config-editor shared surface/status semantics under `settingsProviderConfigEditorShared=true`, labelled provider thinking/mode segmented controls under `settingsProviderSegmentedControlLabels=true`, and unavailable provider install-command copy/status coverage under `settingsProviderInstallCommandCopy=true` plus `settingsProviderInstallCommandStatusA11y=true`; Appearance Settings now keep color swatches, import controls, and chrome editor controls on shared surface/input chrome, and theme sharing copy/import status is smoke-gated under `themeSharingStatusA11y=true` plus `themeCopyClipboard=true`; Data Controls Settings now smoke-gates data-path copy through the app clipboard bridge plus action status semantics under `settingsDataControlsActionA11y=true` and `settingsDataControlsCopyPathClipboard=true`; Browser Settings now smoke-gates live save/clear status and labelled domain-rule controls; Browser local dev-server refresh/hide/restore and route removal now expose live action feedback under `browserLocalTargetActionStatus=true` plus `browserLocalRouteActionStatus=true`; Browser inspector target actions now publish visible/announced results under `browserTargetActionStatus=true`; Browser clear-data actions now publish visible/announced status under `browserClearDataStatusA11y=true`; Automations Settings now smoke-gates pause action status, row grouping, and action labels under `settingsAutomationsActionA11y=true`; Worktrees Settings now smoke-gates refresh feedback under `settingsWorktreesRefresh=true`, direct row `Copy path`/`Terminal` handoffs under `settingsWorktreesPathActions=true`, plus create/delete status announcements and row/conversation/action names under `settingsWorktreesActionA11y=true`; Shortcuts Settings now smoke-gates shortcut save/conflict/clear status under `settingsShortcutActionStatusA11y=true`; Files row/source copy actions use the app clipboard bridge under `filesRowCopyPathClipboard=true` and `fileSourceActionStatus=true`, Files row context menus are keyboard-openable under `filesRowKeyboardContextMenu=true`, shared Files tree traversal is smoke-gated under `filesTreeKeyboardNavigation=true`, source-tab composer attachment dispatch gates under `fileSourceAddToChat=true`, source-line keyboard navigation and toolbar line-to-chat handoff gate under `fileSourceLineKeyboardNavigation=true` plus `fileSourceLineAddToChat=true`, source-comment feedback gates under `fileSourceAnnotationStatus=true`, content-search open-to-line gates under `filesContentSearchOpenLine=true`, Files/source-tab menu-trigger state gates under `filesActionMenuTriggerState=true` plus `workbenchFileTabActionMenuState=true`, and artifact preview open-options trigger state gates under `filesArtifactOpenOptionsTriggerState=true`; empty-chat starter actions, failed-run retry recovery, failed-run retry live status, tool-failure retry recovery, tool-failure retry live status, first-pass assistant continuation, continuation live status role under `chatContinueLastTurnA11y=true`, assistant-message copy bridge/status under `chatMessageCopy=true`, transcript thinking indicator status under `thinkingIndicatorDuringUpdate=true`, partial-response stopped recovery plus live partial-continue proof, queued follow-up cancellation, steering follow-up cancellation, compact active-run queue summaries, active-run composer stop control/status under `composerStopRunControl=true` plus `composerStopRunStatus=true`, first-pass long-thread history/load/find ergonomics, active-thread provider/model/agent/effort composer settings with model persistence and live Codex resumed-thread proof under `composerActiveThreadModelSwitchPersisted=true` plus `npm run live:codex-composer-model-switch`, active custom model visibility under `composerActiveThreadCustomModelVisible=true`, provider-choice visibility under `composerActiveThreadProviderChoices=true`, provider switching under `composerActiveThreadProviderSwitch=true`, persisted provider/runtime/model settings under `composerActiveThreadProviderSwitchPersisted=true`, persisted provider policy/effort settings under `composerActiveThreadProviderSwitchPolicyPersisted=true`, send-after-settings flush under `composerSendFlushesPendingSettings=true`, and explicit choice semantics under `composerAgentChoiceA11y=true`, `/model` slash command routing under `composerSlashModelOpensSettings=true`, slash-opened model/permission menu focus under `composerSlashModelFocusesMenu=true` and `composerSlashPermissionsFocusesMenu=true`, blocked-send permission recovery focus under `composerSendStatusActionFocusesPermissions=true`, composer permission config source signalling under `composerPermissionContextSignal=true`, live-disabled permission blocking under `composerContextDisabledPermissionBlocksSend=true`, composer popover trigger ARIA state, composer popover roving keyboard navigation, composer advanced permission-rule saved feedback under `composerPermissionRuleStatus=true`, blocked-send composer status, blocked-send live-region semantics, composer attachment add/remove/save/cancel/failure status under `composerAttachmentStatus=true`, async pasted/dropped attachment switch isolation under `composerAsyncAttachmentSwitchIsolation=true`, manual file-picker attachment switch isolation under `composerManualAttachmentSwitchIsolation=true`, composer side-chat attachment loss prevention under `composerSideChatAttachmentGuard=true`, transcript approval/user-input recovery live regions, transcript user-input option state under `userInputOptionSelection=true`, transcript permission decision labels under `permissionActionsRecoverable=true` plus `permissionCard=true`, non-command permission cards under `permissionVariantCards=true`, side-chat retry/multiline composer behavior, side-chat input focus on open/switch under `sideChatInputFocusOnOpen=true`, side-chat send-control naming under `sideChatSendLabel=true`, first-pass side-chat context metadata, side-chat visible/announced answer status under `sideChatActionStatusA11y=true` plus retry status under `sideChatRetryStatusA11y=true`, side-chat personalization context under `sideChatPersonalizationContext=true`, side-chat follow-up context continuity under `sideChatFollowupContext=true`, and side-chat transcript handoff under `sideChatAddToChat=true` now have coverage. Remaining main-chat/composer work should target deeper context/permission workflow polish, exact live Codex long-thread timing only when live evidence is available, live non-command permission fixtures when deterministic, and non-Codex provider-backed lifecycle proof; remaining Settings work should target provider-native account/runtime adapters, real remote adapters, and higher-value daily coding workflows.
- Current composer/transcript evidence also gates queued and steering action feedback directly: `composerQueuedCancelStatus=true`, `composerSteeringCancelStatus=true`, `queuedFollowUpStartFailureVisible=true`, `queuedFollowUpStartFailureState=true`, `composerSlashEscapePreservesDraft=true`, `composerSendFailureRestoresDraft=true`, `composerSendFalseRestoresDraft=true`, `composerSendProviderFalseCleansTranscript=true`, `composerContextDisabledPermissionBlocksSend=true`, `composerSendFlushesPendingSettings=true`, rapid side-panel context handoff preservation under `composerRapidContextAppend=true`, `errorRecoveryRetryPrepareFailure=true`, `userInputResumePrepareFailure=true`, `userInputOptionDescriptionsA11y=true`, `userInputQuestionMetadata=true`, `userInputStructuredPayload=true`, `userInputSecretTranscriptRedacted=true`, `permissionResumePrepareFailure=true`, permission request detail copy under `permissionCopyDetails=true`, `permissionProfileDecisionLabels=true`, `chatContinueFailedStartCleansSyntheticPrompt=true`, `composerSlashPermissionsFocusesMenu=true`, `composerPermissionRovingKeyboard=true`, and app-shell skip navigation under `appSkipLinksKeyboard=true` passed in the focused composer/transcript/streaming smokes. Remaining work is not another local action-status pass; it is deeper context/permission workflow polish and real provider-backed lifecycle proof.
- Provider user-input parser/type diffs now route to the focused `--transcript-user-input` smoke and suppress the unrelated Provider Settings false positive when the `src/main/providers.ts` diff only touches user-input metadata. Shared user-input answer-contract diffs across preload, IPC, session manager, provider runtime, Codex app-server runtime, and renderer env types also route to focused user-input smoke instead of Composer/Provider Settings/Session lifecycle. The changed static plan cleans `out-test`, compiles node tests, and runs both `out-test/src/main/__tests__/providers.test.js` and `out-test/src/main/__tests__/codexAppServerRuntime.test.js`, so parser/runtime contract changes get unit coverage without dragging every UI smoke into small transcript slices.
- Transcript file-reference cards now expose direct resolved-path actions under `fileReferenceCardCopyPath=true` and `fileReferenceCardInsertTerminal=true`: Copy writes through the app clipboard bridge with polite status, and Terminal opens the bottom Terminal with the shell-quoted resolved path inserted as an editable draft. The smoke planner now keeps file-reference source/harness diffs on `--transcript-file-reference` instead of broad Transcript/Files/Terminal runs.
- Main-chat code blocks now expose a direct copy action under `codeBlockCopy=true`, including copied/error state, polite status, app clipboard bridge proof, highlighted-code text extraction, and preserved wide/narrow internal scrolling. Code-block diffs now route to the focused transcript-layout smoke instead of drifting into fork-only validation. Remaining code-block work is provider-native rich code actions and live Codex timing/pixel parity, not another local copy button pass.
- Current transcript long-thread/tool-summary evidence now gates the default-collapsed grouped-tool contract directly: `toolSummaryCollapsedByDefault=true` proves grouped tool activity starts closed, exposes the expected row count/error/default-collapsed diagnostics, shows the compact action count, and keeps the body unmounted before expansion; `toolSummaryExpanded=true`, `toolSummaryBounded=true`, and `toolSummaryScrollable=true` still cover the expanded state. Expanded command tool rows now expose a direct app-clipboard copy action under `toolActivityCommandCopy=true`, direct composer handoff under `toolActivityCommandAddToChat=true`, paired result-output composer context under `toolActivityCommandResultAddToChat=true`, and direct bottom-terminal execution under `toolActivityCommandRunTerminal=true`. Long-thread history controls now expose context-rich accessible labels under `longThreadLoadControlA11y=true` while retaining bounded mount counts and lazy-load/search-jump proof. The matching implementation uses a compact collapsed virtual-row estimate instead of sizing grouped tools as if several expanded rows were visible. Remaining PP-033 work is deeper live long-thread timing, provider-backed lifecycle proof, richer provider-native command metadata across event shapes, and any shared scroll-controller extraction that removes real complexity.
- Main-chat message reuse now has a first local slice: completed user messages expose `Edit message as draft`, replace the active composer draft and attachments, focus the composer, and announce the transcript action. The composer shows the copied-draft source, preserves/restores any previous draft, and now announces both Restore and Clear outcomes; focused transcript-layout smoke gates this under `chatUserMessageEditToDraft=true`, `chatUserMessageEditAttachments=true`, `chatUserMessageEditDraftSourceStatus=true`, `chatUserMessageEditDraftRestore=true`, and `chatUserMessageEditDraftClear=true`. Remaining message-edit work is provider-backed edit/resend/fork semantics and original provider-history mutation, not another local copy-to-composer action.
- Main-chat message branching now has a first provider-neutral local/worktree slice: completed user messages and the latest assistant response expose `Fork from here`, and `sessions.fork(..., { throughMessageId })` creates a fork truncated through the selected message, clears provider session id and provider-owned sidebar metadata, appends a lineage marker, persists fork-source metadata, and surfaces the source in the sidebar row/hover detail. When worktree choices are available, the message action exposes local, same-worktree, and new-worktree modes. Titlebar/sidebar session-menu fork entries explicitly fork from the latest real transcript turn and exclude later control/status rows. Forks from locally pinned sources stay locally pinned immediately after the source while provider-readonly pin state is still stripped. Focused `--transcript-fork` gates this under `chatSessionForkLatestTurnUi=true`, `chatSessionForkLatestTurnTruncatesControls=true`, `chatSessionForkLatestTurnMenuLabel=true`, `chatMessageForkButtonVisible=true`, `chatMessageForkMenuTriggerState=true`, `chatMessageForkMenuChoices=true`, `chatMessageForkSameWorktreeUi=true`, `chatMessageForkFromHere=true`, `chatMessageForkTruncatesLaterTurns=true`, `chatMessageForkClearsProviderSession=true`, `chatMessageForkClearsProviderSidebarMetadata=true`, `chatMessageForkPreservesLocalPinPlacement=true`, `chatMessageForkPersistsLineage=true`, `chatMessageForkSameWorktreeFromHere=true`, `chatMessageForkNewWorktreeFromHere=true`, `chatMessageForkSidebarLineage=true`, and `chatMessageForkHoverLineage=true`. Remaining fork work is provider-native fork/resume support.
- Current live composer/provider evidence now includes `npm run live:codex-composer-appserver`, which starts the real Codex app-server in a disposable git workspace with Orchestrator's Codex model/effort/policy mapping, verifies the returned managed permission profile, observes a completed `commandExecution` item, checks the git-visible proof edit, and records the final assistant token in `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-appserver-live-proof/result.json`. It also includes `npm run live:codex-composer-approval`, which intentionally triggers and answers a real `item/commandExecution/requestApproval` with numeric JSON-RPC id `0`, observes `serverRequest/resolved`, and verifies the approved parent-directory proof write in `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-approval-live-proof/result.json`. `npm run live:codex-composer-plan` proves a real app-server turn emits `turn/plan/updated` without command execution; artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-plan-live-proof/result.json`. `npm run live:codex-composer-resume` proves a non-ephemeral Codex thread can be resumed from a fresh app-server process with the same thread id and prior-turn context; artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-resume-live-proof/result.json`. `npm run live:codex-composer-runtime-lifecycle` proves Orchestrator's `CodexAppServerRuntimeManager` emits the expected runtime events while starting with `thread/start`, resuming with `thread/resume`, preserving the same provider session id, and retaining prior-turn context; artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-runtime-lifecycle-live-proof/result.json`. `npm run live:codex-composer-session-lifecycle` proves the compiled main-process `sessionManager` path creates a Codex app-server session, sends through `sendMessage`, continues through `continueLastTurn`, retries through `retryLastUserMessage`, and keeps the same provider session id while recording live start/resume methods; artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-session-lifecycle-live-proof/result.json`. `npm run live:codex-composer-renderer-lifecycle` proves the renderer transcript buttons invoke live Codex send/continue/retry behavior through the app, with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-lifecycle-1779986634530.json`. `npm run live:codex-composer-partial-continue` proves visible partial-response continuation through the renderer, with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-partial-continue-1779987197970.json`. `npm run live:codex-composer-model-switch` proves a resumed Codex provider thread uses the updated Orchestrator session model, with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-model-switch-1779987672302.json`. Focused local/parser/UI evidence now covers Codex app-server file-change and permission-profile approval card variants under `permissionVariantCards=true`, with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-permission-1779988391389.json`; targeted runtime coverage now proves the app-server bridge answers file-change and permission-profile requests with the expected accept/session, turn-scoped permissions, and denial payloads. Remaining provider-backed lifecycle work is deterministic live file/profile permission request forcing and non-Codex provider adapters.
- Current non-Codex provider evidence is explicit but not passing: elevated `npm run live:claude-capabilities` now preflights no-quota Claude probes and records `Unavailable` when auth/runtime readiness is stale. The 2026-05-30 refresh shows `claude --version` reports `2.1.51`, `auth status`, MCP list, plugin list, and agents probes pass, but `claude auto-mode defaults` returns API 401 invalid credentials, so the harness skips structured `plain`, `file_ops`, `plan_mode`, and `streaming` scenarios. Artifact `/Users/nadav/Desktop/Orchestrator/tmp/claude-live-capabilities/_summary/summary.json`. Do not claim Claude daily-coding parity until this live proof can run with valid credentials.
- Provider Settings now mirrors that boundary in-app: safe no-quota probe auth failures promote the provider Auth row to an error, and the focused Provider Settings smoke gates the slower runtime diagnostics path under `settingsProviderRuntimeCopy=true`, `settingsProviderRuntimeAddToChat=true`, and related status checks. Evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780103128372.json`.
- `npm run live:codex-composer-user-input` now exists as an opt-in live Codex user-input harness, but current evidence is a provider-mode boundary rather than a passing proof: Codex Default mode reports `request_user_input is unavailable`, emits no `item/tool/requestUserInput`, and records that in `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-user-input-live-proof/result.json`. Deterministic live user-input forcing should stay tracked as blocked by Codex tool availability until Plan mode or another live provider mode can emit the native request.
- Live Review rollback evidence is current as of 2026-05-30: elevated `npm run live:codex-review-appserver` produced 3 normalized `diff.updated` events with provider session/turn ids, no checkpoint ids, and a successful `numTurns:1` `thread/rollback` that left both the proof file and git diff edited. Artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-review-appserver-live-proof/result.json`. Provider-history checkpoint Undo should therefore remain unavailable; the local reverse-patch Undo is still the daily-coding fallback for exact provider diffs.
- Hosted Review metadata now surfaces secondary inline-comment adapter failures instead of silently dropping them. `gh pr view` metadata can still load, but a failing GitHub GraphQL review-thread request adds `providerWarnings`; the Review metadata menu smoke-gates the warning row under `reviewMetadataMenuState=true`, and the Git Pull Request card now exposes the same warning count/status while preserving `workbenchNewTabGitPrMetadata=true`. Latest Review evidence `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-metadata-1780108434406.json`; latest Git evidence `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780108728138.json`. Remaining hosted Review work is live commented-PR proof, provider-native non-GitHub adapters, provider-hosted source roots, and checkpoint rollback.
- Current transcript Review-card evidence gates the local Undo, Last turn local reverse-patch Undo, open-Review, and file-row accessible names directly: `reviewTranscriptCardUndo=true` requires the local Undo button to expose its `Undo N changed files` scope through both native title and `aria-label` without clicking the destructive action, `reviewLastTurnLocalUndoAvailable=true` requires Last turn provider diffs to expose local reverse-patch undo while preserving the unsupported provider-checkpoint rollback attributes, and `reviewTranscriptCard=true` requires the `Review` action plus each visible file row to expose contextual accessible labels for Review scope, status, path, and change counts. Right-side Review menu-trigger state is now locally smoke-gated under `reviewMetadataMenuState=true`, `reviewOptionsMenuState=true`, `reviewSourceRefMenuState=true`, and `reviewFileJumpMenuState=true`; Review changed-file row context menus are keyboard-openable under `reviewRowKeyboardContextMenu=true`, Review row file attachments are smoke-gated under `reviewRowAddToChat=true`, Review row path-to-terminal drafts are smoke-gated under `reviewRowInsertPathTerminal=true`, Review row Git focus handoff is smoke-gated under `reviewRowOpenGit=true`, Review options git-apply terminal drafts are smoke-gated under `reviewGitApplyTerminalHandoff=true`, Review tree traversal is smoke-gated under `reviewTreeKeyboardNavigation=true`, and Review diff-line traversal is smoke-gated under `reviewDiffLineKeyboardNavigation=true`. Remaining Review Undo/open-source work is provider-history checkpoint rollback and provider-backed source support, not another local transcript-card label pass.
- Inspector/context first pass: Agents is now reachable from the compact top-aligned Workbench New tab even before a live subagent exists, and that New tab now uses a command-list launcher under `workbenchNewTabListLauncher=true` instead of a large card grid. The Agents empty state shows session/runtime context under `workbenchNewTabAgentsAction=true`, the Session section can insert bounded thread/runtime/status/workspace/message/event/agent context into the main composer under `agentSessionContextAddToChat=true`, runtime issue rows summarize failed/waiting events under `agentRuntimeIssueTriage=true`, issue failures are grouped by cause under `agentRuntimeFailureGroups=true`, grouped failures can be inserted into the main composer with thread/runtime/status/workspace and bounded event context under `agentRuntimeFailureGroupAddToChat=true`, recent runtime events can be search-narrowed under `agentRuntimeEventFilter=true`, severity/source facets narrow noisy runtime streams under `agentRuntimeEventFacetFilters=true`, matching runtime events can be selected for a bounded detail payload with contextual row labels and pressed-state semantics under `agentRuntimeEventDetail=true`, selected event payloads can be copied through the app clipboard bridge under `agentRuntimeEventCopy=true`, selected event details can be inserted into the main composer with thread/runtime/status/workspace context under `agentRuntimeEventAddToChatContext=true`, selected agent tabs now show a compact lifecycle timeline under `agentSelectedTimeline=true`, raw provider transport output is summarized/redacted under `agentTransportLog=true`, the Plan tab can insert active goal/review/task context into the main composer under `planAddToChat=true`, the Environment header settings control now opens Providers settings under `environmentSettingsOpensProviders=true` instead of being an enabled dead icon, Environment's provider Web search source now exposes an explicit unavailable boundary under `environmentSourceBoundary=true` instead of appearing actionable, and unavailable Environment rows expose disabled state plus a reason-bearing label under `environmentDisabledRowsA11y=true`. Remaining inspector work should focus on richer provider-backed observability and real coding workflow gaps.
- Selected Agent Activity transcripts can now be copied or inserted into the main composer under `agentSelectedTranscriptCopy=true` and `agentSelectedTranscriptAddToChat=true`, including bounded thread/agent/status/runtime/workspace/transcript context plus visible selected-agent status. Selected-agent timeline rows now open the matching Event detail payload under `agentSelectedTimelineOpenDetail=true`, with selected/pressed state on the timeline row. Agent Activity source/harness diffs now route to `--agent-inspector` and suppress redundant Composer smoke when the Agent Activity fixture proves the clipboard/composer/detail transfer directly.
- Environment now routes missing PR metadata to the right local owner instead of leaving a disabled `Create pull request` row: `environmentCreatePrOpensGit=true` proves the row becomes `open-git-pr`, opens the Git Workbench tab, and keeps the focused validation plan on `--environment`. Remaining PR work is hosted PR creation/submission and provider-native PR metadata/actions.
- Environment `Commit` now routes to the Git owner surface under `environmentCommitOpensGit=true`, while the separate `Changes` row remains the Review entry point. Remaining Git/Review work is hosted PR creation/submission, provider-native PR metadata/actions, checkpoint Undo, and deeper GitHub review workflows.
- Environment-to-Git handoffs now preserve target intent instead of opening a generic Git tab: `environmentBranchOpensGit=true` opens Git focused on Branch controls, `environmentCommitOpensGit=true` focuses Commit controls, and `environmentCreatePrOpensGit=true` focuses Pull Request controls. The Environment `Local` row also exposes direct workspace path copy and bottom-terminal draft handoffs under `environmentWorkspacePathActions=true`. The changed-files smoke selector maps these Environment-owned handoff/store diffs to focused `--environment` only, keeping small Phase 1 workflow slices out of the full Workbench New Tab/Git/Terminal smokes.
- Inspector transport logs now have a direct redacted composer handoff under `agentTransportLogAddToChat=true`, and the changed-file smoke selector maps `EventInspectorPanel.tsx` edits to the Workbench New Tab/Agent Activity smoke so inspector changes do not drift into static-only validation. Remaining inspector work is richer provider-backed observability and live provider transport contracts, not another local read-only summary pass.
- Agent Activity permission/user-input event details now have a direct transcript focus handoff under `agentRuntimeEventOpenInChat=true`: `Open approval in chat` / `Open question in chat` scrolls and focuses the matching permission or user-input card in the main transcript, including result-card focus markers and visible/polite status in both surfaces. The changed-files smoke selector keeps this source/harness diff on `--workbench-new-tab` and suppresses redundant transcript/header smokes. Remaining inspector work is provider-backed observability, deterministic live approval fixtures, and non-Codex runtime contracts.
- Workbench launcher discovery now has a cheap focused proof separate from the heavy Git/agent workflow suite: Environment and Extensions are exposed from the New tab launcher, activating either surface creates/switches to its tab, already-open singleton rows show `Open`, and Plan now appears as a singleton launcher action whenever goal/plan state exists and Plan is not bottom-hosted. Plan also has a real close/reopen lifecycle: closing the right Workbench Plan tab dismisses it instead of re-deriving it from active goal state, and the launcher returns the Plan action to `New` for intentional reopen. Right Workbench tab context menus are now keyboard-openable under `rightPanelTabKeyboardContextMenu=true`, so move/reset/close/transfer actions are no longer pointer-only. Failed worktree retry recovery now restores focus to the composer after the lifecycle notice clears under `worktreeRetryFocusRestored=true`. `--workbench-launcher` gates `workbenchLauncherDiscovery=true`, `workbenchLauncherEnvironmentAction=true`, `workbenchLauncherPlanAction=true`, `workbenchLauncherPlanCloseReopen=true`, `workbenchLauncherExtensionsAction=true`, `workbenchLauncherOpenState=true`, and `workbenchLauncherNoHorizontalOverflow=true`. Remaining Workbench usefulness work is deeper tab-body lifecycle, keyboard traversal beyond the existing launcher/tab-menu/tree pass, full app-shell tab-controller ownership, and product-worthy non-Terminal cross-panel moves.
- Git Workbench now exposes the generated PR command as clipboard output, direct composer context, and an editable bottom-terminal draft under `workbenchNewTabGitPrCommand=true`, `workbenchNewTabGitPrCommandHandoff=true`, and `workbenchNewTabGitPrCommandTerminalHandoff=true`. It also derives the hosted GitHub compare/create route from the local remote under `workbenchNewTabGitPrCreateUrl=true`, but only enables hosted PR opening when the topic branch is known published; unpublished branches show/copy the safe `git push -u <remote> <branch>` command under `workbenchNewTabGitPrPushCommand=true`. Git now also exposes an explicit authenticated `gh pr create` action for published branches with smoke-gated disabled-state safety under `workbenchNewTabGitPrCreateAction=true`; successful live creation refreshes hosted PR metadata into the card. Manual refresh feedback is smoke-gated under `workbenchNewTabGitRefreshStatus=true`, current Git status can be handed to the composer under `workbenchNewTabGitStatusHandoff=true`, changed-file rows can copy exact paths through the app clipboard/status channel under `workbenchNewTabGitFileCopyPath=true`, attach changed files to the composer under `workbenchNewTabGitFileAddToChat=true`, insert exact paths into an editable bottom-terminal draft under `workbenchNewTabGitFileInsertTerminal=true`, and open non-deleted changed files in a Workbench file tab under `workbenchNewTabGitFileOpenWorkbench=true`. Review/Git selected-file handoff is now bidirectional: Review opens Git with the selected file highlighted under `reviewGitHandoffSelectedFile=true`, and Git file rows open Review with that file selected under `gitReviewHandoffSelectedFile=true`. Remaining Git/Review work is live GitHub push/result reconciliation, provider-native PR actions beyond GitHub CLI, and deeper GitHub review workflows.
- Browser actions now expose the existing page-context composer handoff from the toolbar menu under `browserActionsPageContext=true`, not only the webview context menu. The handoff sends URL/title/visible page structure to the main composer, announces status from the Browser panel, and the changed-files smoke selector keeps this Browser-owned composer insertion on focused `--browser` instead of adding redundant generic Composer smoke. Remaining Browser work is live provider-emitted browser-use behavior, broader client tools only when real requests justify them, and exact Codex browser timing proof.
- Files `Add to chat` now announces successful file attachment handoffs from the Files action menu under `filesAddToChatStatus=true`, using the existing visible/polite Files action status region instead of requiring users to infer completion only from the composer attachment pill. Files action menus also expose an editable shell draft handoff under `filesInsertPathTerminal=true`, opening the bottom Terminal and inserting the selected workspace-relative path without executing it. The changed-files smoke selector maps these Files-owned handoffs to focused `--files`, keeping routine Files polish on static checks plus the Files smoke. Latest evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780079135980.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780079135980.png`.
- Provider Settings now gates explicit permission-config refresh under `settingsProviderPermissionRefresh=true`, so users can re-read live/fallback permission config from Settings with visible status instead of relying on passive page/provider changes. Provider Settings runtime activity can now be copied through the app clipboard bridge with visible/announced status under `settingsProviderRuntimeCopy=true` and `settingsProviderRuntimeCopyStatusA11y=true`, and inserted into the active chat draft under `settingsProviderRuntimeAddToChat=true` plus `settingsProviderRuntimeAddToChatStatusA11y=true`, giving daily coding diagnostics a direct handoff path to chat/issues. Provider-only Settings source/harness diffs now map to focused `--settings-providers` instead of also running generic Settings smoke. Codex sidebar metadata refresh now exposes immediate/result live status under `settingsProviderSidebarRefresh=true`, so `Refresh chats` is no longer only a silent provider reconciliation control. Remaining Settings work is provider-native account/runtime adapters, remote-host sync, and higher-value daily coding workflows.
- Settings search now shows a compact host-filtered result list with ArrowDown/ArrowUp selection and keeps the existing Enter/trailing-chip jump path under `settingsSearchResults=true` plus `settingsSearchNavigation=true`. Remaining Settings work is not another local search pass; it is provider-native account/runtime adapters, remote-host sync, and higher-value daily coding workflows.
- Extensions panel primitive migration is closed for the current embedded right-panel surface: `/extensions` now exposes shared summary, disclosure, file-row, command-section, command-row, and item-row contracts under `extensionsPanelSharedPrimitives=true`. Extensions instruction context can now be inserted into the main composer under `extensionsInstructionsAddToChat=true`, including provider-neutral instruction-file and command/skill/MCP summaries with visible/polite status. Remaining Extensions work should be evidence-led provider data/functionality gaps rather than another local chrome pass.
- Codex-style global/thread find parity has first-pass coverage: the shared `content-search-input` with `Search chat` and `Search diffs` scopes is implemented for transcript/Review/file-source search, Browser-focused `Cmd+F` still routes to Browser find, and the comparison script now treats `rightPanelFindShortcutRouting=true` as the Codex-style shared-find contract. Exact live Codex focus timing and pixel spacing remain open.
- Header/right-panel ownership now has a first pass: the session header side-panel control is labelled as `Toggle side panel`, declares `right-workbench` ownership, opens the Workbench New tab launcher when no tabs exist under `headerPanelEmptyFallback=true`, exposes controlled-panel state under `titlebarPanelToggleState=true` for the right Workbench and bottom Terminal toggles, keeps closed shared app-shell panels inert under `headerClosedPanelInert=true`, gates Chat actions menu trigger state under `headerActionMenuState=true`, and restores focus to the header side-panel toggle when the final right Workbench tab is closed through the close-active-tab shortcut under `rightPanelLastTabShortcutFocusRestored=true`. Exact live Codex header/right-panel boundary timing and pixel spacing remain open.
- Current Terminal/bottom-panel evidence now gates scoped URL routing directly: `terminalLinkScopedRouting=true` proves focused smoke opens URLs through the terminal-id-specific opener instead of a shared global, while `terminalLinkRouting=true` still proves the Browser handoff result. Right-panel terminals now use the same `openRightPanelBrowserUrl` path as bottom terminals, and right-panel Terminal shortcut close now kills the shell and removes the terminal tab under `terminalRightPanelCloseShortcut=true` instead of only removing the Workbench tab. Bottom shell controls now use panel wording under `terminalBottomPanelLabels=true` (`Toggle bottom panel`, `Resize bottom panel`, `Bottom panel tabs`, `Hide bottom panel`) while terminal-specific tab actions keep terminal wording, and bottom-panel tab context menus are now keyboard-openable under `terminalTabKeyboardContextMenu=true`. The first non-terminal bottom-panel tab is now smoke-gated under `bottomPanelPlanTransfer=true`: Plan is opened intentionally through the Workbench launcher, can move from the right Workbench into the bottom panel, render there, suppress the duplicate right-panel Plan tab while bottom-owned, and move back to the right panel from the shared bottom tab menu. The same focused Terminal smoke now gates bottom-hosted Plan handoff under `bottomPanelPlanAddToChat=true`, proving the embedded Plan action still sends current goal context to the main composer. Plan-only bottom tab lifecycle now has a close-policy pass: Plan remains closable when it is the last bottom tab, single-terminal close protection remains intact, final bottom-tab close returns focus to the titlebar bottom-panel toggle, and `src/types/panelTabs.ts` changes now map to the focused cross-panel keyboard smoke plus the compiled `panelTabs` unit test in `smoke:ui:changed`. Bottom-panel discovery now has a direct Plan toolbar entry under `bottomPanelPlanToolbarAction=true`. The current allowed-tab policy is explicit: bottom-panel transfer supports Terminal and Plan only, and unsupported right Workbench tabs now show a visible `Bottom panel supports Terminal and Plan tabs.` boundary under `rightPanelTransferUnsupportedBoundary=true`. Remaining bottom-panel work is additional tab kinds only if product-worthy, live Codex timing/animation proof, and broader mixed-surface stress.
- Active terminal output can now be inserted directly into the main composer from the bottom Terminal toolbar under `terminalOutputAddToChat=true`, including cwd/terminal-id context, bounded fenced output, disabled empty/non-terminal states, and visible/polite bottom-panel status. Selected terminal text can also be inserted from the same toolbar under `terminalSelectedOutputAddToChat=true`, preserving cwd/terminal context while avoiding whole-scrollback copy.
- Terminal command-scoped handoff now has a first pass under `terminalCommandOutputAddToChat=true`: the active terminal tracks submitted command boundaries, records the output offset, exposes the active terminal id/last command on the bottom-panel chrome, and can insert a bounded latest-command excerpt with cwd/terminal/command context into the main composer. Remaining Terminal handoff work is provider/native terminal metadata, richer terminal output controls, and live Codex timing proof.
- Right-panel Terminal handoff parity now has a first pass under `terminalRightPanelOutputAddToChat=true`, `terminalRightPanelSelectedOutputAddToChat=true`, and `terminalRightPanelCommandOutputAddToChat=true`: moved/right-created Terminal tabs expose the same whole-output, selected-output, and latest-command composer handoffs in the Workbench tab chrome, including cwd/terminal/command context, bounded excerpts, smoke-readable command/selection state, and visible/polite status. Remaining Terminal handoff work is provider/native terminal metadata, richer terminal output controls, and live Codex timing proof.
- Full escalated side-panel comparison on 2026-05-28 refreshed all 27 local captures with `mismatchCount=0`, `needsSmokeCount=0`, and `needsProofCount=0` after repairing the Files overflow fixture and the multi-window focus/menu-routing fixture. The multi-window focused rerun passes `firstWindowBrowserFocusArea=true`, `firstWindowBrowserMenuEnabled=true`, `focusSwitchRestoresFirstWindowMenu=true`, and `menuCommandRoutedToFocusedWindow=true` after opening Browser from the active New tab launcher. The focused right-panel smoke now also gates shared Workbench tab roving focus under `rightPanelTabRovingFocus=true`, so inactive tabs and inactive close buttons no longer create extra Tab stops, tablist naming under `rightPanelTabListLabel=true`, primary Review/Files/Browser toolbar semantics under `rightPanelToolbarLabels=true`, and final-tab close focus restoration under `rightPanelLastTabCloseFocusRestored=true`. The remaining blocked rows require nonblank live Codex screenshots or provider/runtime contracts.
- Live Codex side-by-side visual comparison across the 23-surface inventory.
- Route/window lifecycle and installed-app replacement proof.
- Provider-backed Review checkpoint Undo, cloud/hosted sources, PR metadata, comments, and blame. Local Review git actions, Review copy status, and Review git-apply terminal drafts are now smoke-gated with `reviewFloatingGitActionStatus=true`, `reviewGitApplyCopyStatus=true`, and `reviewGitApplyTerminalHandoff=true`; Review hosted metadata now has a direct forced refresh path with smoke-readable metadata state so checks/reviewers/comments can update from the Review surface, and hosted metadata auth/network/API failures now surface as errors instead of being flattened into "no hosted metadata found." Remaining hosted Review work is live PR result reconciliation after remote mutations, live commented-PR proof, checkpoint Undo, hosted source roots, and deeper provider workflows.
- Local mutable Review sources now expose selected-file stage/unstage directly in the Review toolbar and row menu under `reviewSelectedGitPathActions=true`, including in-place local/staged/unstaged state refresh and a conflict-aware `Mark selected file resolved` stage label. Inline merge-conflict helpers now also expose a completion step after conflict choices are applied: `reviewMergeConflictMarkResolved=true` proves the conflict area shows `Mark resolved`, stages the file through the existing Review path action, clears the conflicted state, and keeps the local Review state refreshed. Commit/discard/branch operations remain in Git; provider-backed checkpoint Undo, hosted/cloud sources, comments/blame adapters, multi-file conflict batching, and deeper hosted conflict workflows remain open.
- Local Review source-root identity is now smoke-gated with `reviewSourceRootMetadata=true`, so the active source summary identifies the workspace root in the compact toolbar. Remaining Review/files source work is provider-hosted root/worktree metadata and deeper source/provider diff navigation, not another local root-label pass.
- Local Review selected-line handoff is now smoke-gated with `reviewDiffLineComposerHandoff=true`, so current-file diff lines can be copied or inserted into the main composer without first opening a source tab. Local Review row file attachment, path-to-terminal draft, and Open-Git focus handoffs are smoke-gated with `reviewRowAddToChat=true`, `reviewRowInsertPathTerminal=true`, and `reviewRowOpenGit=true`, so changed-file rows can attach the selected file to chat, insert the selected path into the bottom Terminal, or jump to the matching Git row without copy/paste or toolbar detours. Remaining Review/source work is provider-hosted comments/blame/source metadata and old-side/provider navigation semantics.
- Provider pin mutation and richer hosted/cloud/worktree sidebar reconciliation. The Codex pinned-thread live proof now records the exact current provider boundary (`thread/list` supported; `list-pinned-threads`, `set-thread-pinned`, and `set-pinned-threads-order` unsupported with unknown-variant JSON-RPC errors), so provider-projected pin rows stay read-only until a safe live list/set/order surface exists. Sidebar session, project, and organize menu-trigger state is locally smoke-gated under `sidebarActionMenuTriggerState=true`, `projectActionMenuTriggerState=true`, and `organizeMenuTriggerState=true`.
- Live provider-emitted Browser/browser-use proof and non-Codex browser/local-server adapters where available. Local Browser URL copy status is now smoke-gated with `browserCopyUrlStatus=true`, Browser clear-data action status under `browserClearDataStatusA11y=true`, Browser actions menu trigger state is smoke-gated under `browserActionsMenuTriggerState=true`, saved Browser screenshot artifact actions are smoke-gated under `browserScreenshotPathActions=true` plus `browserScreenshotAttachment=true`, parsed browser-use runtime state now exposes visible/announced Browser chrome under `browserUseStatusVisible=true`, and Browser Security per-session origin rows now expose Allow/Block/Clear controls for download/upload policy state under `browserSecurityPolicyOriginControls=true` plus mutually exclusive Allow/Block current-origin updates under `browserSecurityPolicyMutualExclusion=true`. Live Browser runtime evidence is current as of 2026-05-30: elevated `npm run live:codex-browser-appserver` completed a real Codex turn but emitted 0 browser-use events, sent 0 browser/tool server requests, and replied `CODEX_BROWSER_LIVE_NO_BROWSER`; artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-browser-appserver-live-proof/result.json`.
- Browser visual comments/design tweaks now include the current saved screenshot artifact path in the composer handoff when a screenshot has been captured for the current page, and focused Browser smoke gates that workflow under `browserCommentScreenshotContext=true`. Remaining Browser annotation work is live provider-emitted browser-use/design-change proof, provider application of visual tweaks, and non-Codex browser/local-server adapters where available.
- Browser visual comments now also auto-attach that current screenshot artifact to the main composer under `browserCommentScreenshotAttachment=true`, so the marked page image travels with the coordinate/design-tweak context instead of relying on a separate Add screenshot action. Browser now retains submitted visual comments/design tweaks in a local inspector history under `browserCommentHistory=true`, with copy and add-to-chat reuse actions. Remaining Browser annotation work is provider-applied visual changes, live provider-emitted browser-use proof, and non-Codex browser/local-server adapters where available.
- Real remote-host Settings adapters and Codex-style provider-native Host Personalization for memory/personality/custom instructions. Local Orchestrator personalization is implemented separately and should not be mistaken for provider-native sync.
- Dedicated accessibility audit for keyboard-only traversal, broader screen-reader labels, and reduced motion. First-pass app-shell skip navigation under `appSkipLinksKeyboard=true`, header panel toggle state under `titlebarPanelToggleState=true`, closed shared app-shell panel inert state under `headerClosedPanelInert=true`, header Chat actions menu trigger state under `headerActionMenuState=true`, sidebar session/project/organize menu trigger state under `sidebarActionMenuTriggerState=true`, `projectActionMenuTriggerState=true`, and `organizeMenuTriggerState=true`, Review options/source/metadata/file-jump menu trigger state under `reviewOptionsMenuState=true`, `reviewSourceRefMenuState=true`, `reviewMetadataMenuState=true`, and `reviewFileJumpMenuState=true`, Review row context menus under `reviewRowKeyboardContextMenu=true`, shared Review tree traversal under `reviewTreeKeyboardNavigation=true`, Review diff-line traversal under `reviewDiffLineKeyboardNavigation=true`, Files/source-tab menu trigger state under `filesActionMenuTriggerState=true` and `workbenchFileTabActionMenuState=true`, Files row context menus under `filesRowKeyboardContextMenu=true`, shared Files tree traversal under `filesTreeKeyboardNavigation=true`, source-line keyboard navigation under `fileSourceLineKeyboardNavigation=true`, artifact preview open-options trigger state under `filesArtifactOpenOptionsTriggerState=true`, Browser actions menu trigger state under `browserActionsMenuTriggerState=true`, shared Workbench tab roving focus under `rightPanelTabRovingFocus=true`, final-tab Workbench close focus restoration under `rightPanelLastTabCloseFocusRestored=true`, shared Workbench tablist naming under `rightPanelTabListLabel=true`, primary coding toolbar semantics under `rightPanelToolbarLabels=true`, Settings content focus on open/host/section changes under `settingsContentFocusOnOpen=true`, Settings close focus restoration under `settingsCloseFocusRestored=true`, Environment disabled-row state/labels under `environmentDisabledRowsA11y=true`, shared Settings/Capabilities segmented-control labels under `settingsProviderSegmentedControlLabels=true` plus `capabilitySegmentedControlLabels=true`, composer popover focus restoration, trigger expanded/collapsed ARIA state, roving keyboard navigation, composer blocked-send live status, composer active-thread provider-choice row under `composerActiveThreadProviderChoices=true`, composer settings choice groups/pressed state under `composerAgentChoiceA11y=true`, inspector runtime-event row labels/pressed state under `agentRuntimeEventDetail=true`, shared chat/diff find result status under `rightPanelFindStatusA11y=true`, shared find scope labels under `rightPanelFindScopeLabelA11y=true`, shared find close focus restoration under `rightPanelFindCloseFocusRestored=true`, transcript message-copy live status, transcript retry/continue live status, transcript Review-card Undo/open-action/file-row accessible names under `reviewTranscriptCardUndo=true` and `reviewTranscriptCard=true`, transcript user-input option state and sent/error live regions, transcript permission decision/error action semantics plus contextual decision labels, Browser load-error alert/action-group semantics, Browser URL copy live status, Browser local-server action status, Terminal clipboard live status, Terminal clear-action status under `terminalClearActionStatus=true`, Terminal tab action status under `terminalTabActionStatus=true`, Terminal hide focus restoration under `terminalHideFocusRestored=true`, Terminal final close-active-panel focus restoration under `terminalFinalCloseFocusRestored=true`, Terminal failure/end status recovery semantics, Browser Settings status/domain-control semantics, Personalization Settings save status under `settingsPersonalizationActionStatus=true`, Worktrees Settings action/status semantics, and Review local git/copy-action status semantics are now smoke-gated, but this does not close the whole-app accessibility pass.
- Deep Office/PDF renderer work remains tracked, but it is Phase 2 unless a coding workflow depends on it.
- Smoke cadence guardrail: `pnpm run smoke:ui:suggest` now maps the dirty file list to focused smoke targets before Electron validation, and launcher-only `ContextSidebar` changes route to `--workbench-launcher` instead of the heavier Workbench workflow suite. ContextSidebar tab-menu/transfer diffs restore `--right-panel`, so shell menu policy changes do not get under-tested by the launcher split. Workbench-launcher harness diffs map to `--workbench-launcher` without falsely adding Terminal just because the launcher inventory contains a `terminal` action id. Composer message-edit/draft-source diffs now map to both `--composer` and `--transcript-layout`, so cross-surface edit-draft flows are not under-tested by the generic composer fixture. Transcript code-block diffs now stay on `--transcript-layout` and suppress accidental fork-only routing from nearby diff context. Transcript permission-card diffs now stay on `--transcript-permission` instead of the generic transcript-layout target. Transcript user-input card diffs now route to `--transcript-user-input` instead of broad transcript layout for `userInput*`, `chat-user-input`, and `data-user-input` proof. Transcript fork diffs now map to `--transcript-fork`, worktree lifecycle diffs now map to `--worktree-lifecycle`, and Review merge-conflict diffs now map to `--diff-conflict` while suppressing redundant Review core/design-system smoke when the conflict fixture owns the proof. The Browser/Composer harness diff rules avoid unrelated transcript or worktree smoke edits. `pnpm run smoke:ui:changed` executes the generated changed-files plan directly, including `git diff --check`, TypeScript when source/package files changed, changed-script syntax checks, focused static/unit checks such as compiled `panelTabs`, and only the suggested focused UI smokes. It flags only broad-trigger files that are not already covered by a focused smoke rule; broad harness/main-process files covered by focused diff rules are reported separately as covered. Use it for each small Phase 1 slice, then intentionally escalate to full visual inventory only when the change touches a coordinated shell contract.
- Settings search diffs now route to `--settings` and suppress Browser, transcript-permission, and design-system false positives when shared harness/CSS text only references browser/permission/search terms inside the Settings search scenario. This keeps the routine loop at `pnpm run smoke:ui:changed:static` plus the single generated Settings smoke for small Settings slices.
- Worktrees Settings path-handoff diffs now route to `--settings` and suppress redundant Terminal, Composer, and design-system smoke when the Settings fixture itself verifies copy-path clipboard writes, bottom-terminal draft insertion, visible/polite status, and focus cleanup under `settingsWorktreesPathActions=true`.
- Provider Settings command-handoff diffs now route to `--settings-providers` and suppress redundant Terminal, generic Settings, and design-system smoke when the Provider Settings fixture itself verifies explicit provider command terminal drafts plus visible/polite command-output status under `settingsProviderCommandTerminalHandoff=true` and `settingsProviderCommandTerminalStatusA11y=true`.
- Tool activity command-handoff diffs now route to `--transcript-layout` and suppress redundant generic Composer/Terminal smoke when the Transcript fixture itself verifies insertion into the main composer or command execution in the bottom Terminal.
- Git PR-command handoff diffs now route to `--workbench-new-tab` and suppress redundant generic Composer/Terminal smoke when the Git fixture itself verifies insertion into the main composer or editable bottom-terminal draft.
- Git changed-file path handoff diffs now route to `--workbench-new-tab` and suppress redundant Terminal smoke when the Git fixture itself verifies the editable bottom-terminal draft, active bottom-panel terminal id, and Git status feedback.
- Git changed-file add-to-chat diffs now route to `--workbench-new-tab` and suppress false Files smoke when the Git fixture itself verifies the composer attachment pill/status and Git action feedback.
- Git commit-message draft diffs now route to `--workbench-new-tab` only; the Git fixture verifies the staged-file composer context and Git status feedback under `workbenchNewTabGitCommitDraftHandoff=true`, so routine validation does not need generic Composer smoke.
- Files path handoff diffs now route to `--files` and suppress redundant Terminal smoke when the Files fixture itself verifies the editable bottom-terminal draft and active bottom-panel terminal id.
- Review row path handoff diffs now route to `--diff-core` and suppress redundant Terminal smoke when the Review fixture itself verifies the editable bottom-terminal draft, active bottom-panel terminal id, and Review status feedback.
- Review git-apply terminal-draft diffs now route to `--diff-core` and suppress redundant Terminal smoke when the Review fixture verifies the generated `printf %s $'...' | git apply -` draft, active bottom-terminal id, and visible/polite Review status under `reviewGitApplyTerminalHandoff=true`.
- Review row add-to-chat diffs now route to `--diff-core` and suppress redundant Composer/Files smoke when the Review fixture itself verifies the composer attachment pill/status and Review action feedback. Review row Open-Git diffs stay on `--diff-core` because the Review fixture proves the Git focused-row handoff directly.
- Environment workspace path handoff diffs now route to `--environment` and suppress redundant Terminal/Workbench smoke when the Environment fixture verifies copied path state, editable bottom-terminal draft state, active terminal id, and visible/polite Environment status under `environmentWorkspacePathActions=true`.
- Extensions instruction handoff diffs now route to `--extensions` and suppress redundant Composer smoke when the Extensions fixture verifies provider-neutral instruction context insertion into the main composer plus visible/polite Extensions status under `extensionsInstructionsAddToChat=true`.
- Agent Activity session-context and inspector-runtime diffs now route to the dedicated `--agent-inspector` target and suppress redundant Workbench launcher/Composer smoke when the Agent Activity fixture verifies session-summary copy/add-to-chat, runtime-event detail/copy/add-to-chat/open-in-chat, failure-group copy/add-to-chat, transport-log copy/add-to-chat, selected transcript copy/add-to-chat, selected timeline detail navigation, facet filters, and visible/polite inspector status. The broader `--workbench-new-tab` target remains responsible for Git and full New Tab workflows, so routine Agent Activity work no longer reruns unrelated Git gates.
- Source tab path handoff diffs now route to `--files` and suppress redundant Terminal smoke when the Files/source fixture itself verifies the editable bottom-terminal draft, active bottom-panel terminal id, and file-tab status feedback under `fileSourcePathTerminal=true`.
- Terminal-to-composer handoff diffs intentionally stay on `--terminal`: the terminal smoke proves the composer insertion and bottom/right-panel status itself, so the planner suppresses generic Composer/right-panel launcher targets for whole-output, selected-output, and latest-command terminal handoffs.
- Review/Git selected-file handoff diffs now route to `--diff-core` and suppress unrelated Workbench launcher/New Tab/design-system smoke when the shared tab store, Git panel, Review panel, smoke harness, or local CSS only change focused Review/Git file handoff. This keeps routine Review/Git iteration at `pnpm run smoke:ui:changed:static` plus the single generated Review smoke.

See `docs/codex-side-panel-live-comparison.md` for the current surface-by-surface Codex evidence and smoke-harness assessment.
