# Installed App Visual Regression List

This list tracks the product-facing visual and interaction regressions found during installed-app dogfooding. It is separate from the broader Codex parity ledger: these are issues visible in the local Orchestrator app that should be judged by manual dogfood plus focused Electron smokes.

## Current Fixes

| Issue | Status | What changed | Verification |
| --- | --- | --- | --- |
| Panel tab widths oscillate between too wide and truncated labels | Fixed in `7dc61fb`; hardened in current pass | Removed forced active-tab width, made tab close controls participate in layout, protected short common labels like New tab, Plan, Browser, Git, and Terminal from unnecessary ellipsis, then fixed active-tab scroll measurement and compacted file tabs so newly opened file tabs stay visible without 190px slabs. | `npm run smoke:ui:auto -- --terminal`; `npm run smoke:ui:auto -- --files`; screenshots showed terminal/bottom labels intact and the active file tab visible inside the overflow row. |
| Workbench fixed tabs still ellipsized under the action cluster | Improved in current pass | Protected fixed Workbench labels such as Environment, Review, Files, Browser, Git, Plan, Agents, and Extensions from ellipsis and allowed the tab strip to overflow horizontally before cutting known short labels. | `npm run smoke:ui:auto -- --diff-core`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780265823453.png`; `npm run smoke:ui:auto -- --browser`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780265849875.png`; `npm run smoke:ui:auto -- --terminal`. |
| Right and bottom panel split lines still read heavier than the surrounding shell | Improved in current pass | Replaced the full-strength inline AppShell panel borders with the same quiet mixed border treatment used by the rest of the panel chrome. | `npm run smoke:ui:auto -- --diff-core`; `npm run smoke:ui:auto -- --browser`; `npm run smoke:ui:auto -- --terminal`. |
| Bottom panel presents as terminal-only | Improved in `7dc61fb`; hardened in current pass | Added direct bottom-panel actions for Plan, Browser, Files, Review, and Side chat; terminal-only actions now only show while a terminal tab is active. The titlebar and command palette now frame this as the bottom panel instead of Terminal, and bottom-panel quick/menu opens explicitly activate the requested tab. | `npm run smoke:ui:auto -- --terminal`; gates include `bottomPanelPlanTransfer`, `bottomPanelPlanToolbarAction`, `bottomPanelOpenTabMenu`, `terminalMoveBackToBottom`, and `terminalBottomPanelLabels`. Screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780264977710.png`. |
| Side-chat panel lost its translucent surface | Improved in `7dc61fb`; tuned in current pass | Restored frosted/translucent side-chat panel and composer material with blur/saturation, then reduced the side-chat host/composer opacity so it no longer paints as the same solid slab as the main chat. | `npm run smoke:ui:auto -- --side-chat`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-side-chat-1780263776942.png`; `npm run build`. |
| Installed app can lag behind packaged app | Fixed in `c986072` | Installer now cleanly replaces `/Applications/Orchestrator.app`, clears xattrs, ad-hoc signs, and refreshes LaunchServices/Spotlight import. | Installed `app.asar` hash matched packaged `app.asar`; `codesign --verify --deep --strict` passed. |
| Installed app can appear windowless when foreground verification is blocked or `ready-to-show` is delayed | Improved defensively in current pass | Added a guarded startup fallback that shows the BrowserWindow after a short delay if `ready-to-show` has not fired, so a renderer startup stall cannot leave the app invisible. | The post-reinstall foreground proof became blocked after macOS reached the lock screen, which made Computer Use/System Events unable to inspect normal app windows. `npm run build`; `npm run pack:mac`; reinstalled `/Applications/Orchestrator.app`; installed `app.asar` hash `3f727a8612e15a1ceba68803126e93e63cbaf0b7c1624558455b523bd5f2b693`; `codesign --verify --deep --strict /Applications/Orchestrator.app`. Foreground visual proof still needs an unlocked session. |
| Automations appears in both Settings and the main window | Fixed in current design-language pass | Kept Automations as a standalone main workflow and normalized stale Settings `automations` state back to General so Settings navigation/search no longer treats it as a settings page. | `settingsNavigationGroupsForHostKind('local')` excludes `automations`; direct `/settings/automations` route still opens the standalone page. |
| Duplicate vertical scrollbars in chat action menus | Improved in current design-language pass | Removed the extra scroll owner from the session action menu so `MenuSurface` owns scrolling once. | Targeted code inspection plus focused UI smoke/build; manual menu screenshot still useful. |
| Provider settings mixes everyday defaults with diagnostics | Improved in current design-language pass | Moved provider capability checks and boundary summaries into the existing Details disclosure, calmed the model/mode controls, stopped the permission mode control from stretching across the row, and kept permission refresh/status behavior available from Details. | `npm run smoke:ui:auto -- --settings-providers`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780263556021.png`; `npm run build`. |
| Provider details still looked like nested admin cards | Improved in current design-language pass | Removed the heavy bordered Details container/card chrome and converted provider diagnostic status blocks into quiet rows, preserving the diagnostic content behind the disclosure. | `npm run smoke:ui:auto -- --settings-providers`; screenshot inspected at `tmp/design-language-audit-settings-providers-after/settings-providers.png`; `npm run build`. |
| Settings pages had inconsistent, heavy one-off dividers and controls | Improved in current settings pass | Added shared quiet input/field primitives, moved the Worktrees create action out of the cramped field grid, and reduced the strongest settings-only dividers across Personalization, Data controls, Browser domain rules, Automations rows, Worktrees, and Shortcuts. | `npm run smoke:ui:auto -- --settings`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780264600645.png`; `npm run smoke:ui:auto -- --settings-providers`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780264688586.png`; `npm run build`. |
| Settings shared controls still carried too much boxed material | Improved in current pass | Lowered settings panel, button, input, segmented-control, shortcut-row, worktree-state, automation-state, path/code, and advanced editor border/background strength so cross-page settings surfaces read as rows and controls instead of nested cards. | `npm run smoke:ui:auto -- --settings`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780266864424.png`; `npm run smoke:ui:auto -- --settings-providers`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780266868995.png`; `npm run build`; rebuilt and reinstalled `/Applications/Orchestrator.app`; installed `app.asar` matched packaged hash `3eba54905f7ac870b411c0a4d0917e3816c46f2fb1456228e1c5baddc86fce39`; `codesign --verify --deep --strict /Applications/Orchestrator.app`; live installed-app inspection via Computer Use confirmed General and Providers render with the quieter row/control treatment, including expanded provider Details. Direct `--installed` smoke currently aborts during macOS terminal app registration before writing a report, while LaunchServices `open -n /Applications/Orchestrator.app` launches the app normally. |
| Remaining settings card grids still read heavy | Improved in current pass | Reduced Appearance preset/preview card weight and height, compacted Pet overlay choices, reduced Personalization textarea material, and stopped the Worktrees create action from spanning the whole form. | Live installed-app inspection via Computer Use found the remaining heavy spots on Appearance, Pet overlay, Personalization, and Worktrees; `npm run smoke:ui:auto -- --settings`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780268700620.png`; `npm run smoke:ui:auto -- --settings-providers`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780268749280.png`; `npm run build`; rebuilt and reinstalled `/Applications/Orchestrator.app`; installed `app.asar` matched packaged hash `6c09610feff7f67ae73035e73884dea0d924f73e0c6ddce67b73152117101339`; `codesign --verify --deep --strict /Applications/Orchestrator.app`; app-scoped Computer Use inspection confirmed the updated Appearance, Pet overlay, and Personalization surfaces in the installed app. |
| Final settings utility pages still had table/button/code-slab weight | Improved in current pass | Softened shared settings action buttons, Browser selects/domain chips, Data controls path display, and the Shortcuts search/table rows so Browser, Shortcuts, and Data controls sit closer to Codex-style quiet row surfaces instead of boxed admin controls. | Live installed-app inspection via Computer Use found the remaining Browser action-button weight, Shortcuts table density, and Data controls path slab before this pass; `npm run smoke:ui:auto -- --settings`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780270123187.png`; `npm run build`; `npm run pack:mac`; rebuilt and reinstalled `/Applications/Orchestrator.app`; installed `app.asar` hash `5e79afaa872368cd01b931199b8fec09a70ed167174ef8d664ec874bc935525a`; `codesign --verify --deep --strict /Applications/Orchestrator.app`; foregrounded installed-app Computer Use inspection confirmed Browser, Shortcuts, and Data controls now use the quieter row/control treatment. |
| Appearance settings expose expert theme controls by default | Improved in current design-language pass | Moved raw theme editing and portable theme import/export behind an Advanced appearance disclosure so the page opens on mode, presets, preview, accent, typography, and layout controls first. | `npm run smoke:ui:auto -- --settings`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780262018520.png`; `npm run build`. |
| Browser settings expose advanced domain policy lists by default | Improved in current design-language pass | Moved allowed/blocked domain lists for opening, downloads, and uploads behind a Domain rules disclosure so Browser settings opens on data cleanup and permission defaults first. | `npm run smoke:ui:auto -- --settings`; `npm run build`. |
| App chrome animations feel sluggish | Improved in current design-language pass | Shortened shared edge, row, control, overlay, and panel motion tokens and reduced row enter travel so menus, disclosures, panels, and list rows settle faster without disabling motion. | `npm run smoke:ui:auto -- --settings`; `npm run smoke:ui:auto -- --header` after rerunning separately from a parallel smoke build-output race; `npm run build`. |
| Left sidebar shows an accidental two-tone edge against content | Improved in current design-language pass | Sidebar, content, settings, and automations now share one shell material token; topbars use the same header token so the boundary reads continuous instead of striped. | `npm run smoke:ui:auto -- --header`; visual screenshots inspected at `tmp/design-language-audit-shell-chrome/header.png` and `settings.png`. |
| Traffic-light controls feel mispositioned against shell content | Improved in current design-language pass; verified in installed app | Moved the macOS traffic-light anchor to a tighter native inset and widened the collapsed-sidebar header safe-area token so renderer chrome cannot sit under the window buttons. | Code-verified `trafficLightPosition` and header safe-area token; foregrounded installed-app Computer Use inspection on `5e79afaa872368cd01b931199b8fec09a70ed167174ef8d664ec874bc935525a` showed the native traffic lights sitting above the sidebar without overlapping Settings, chat, or Workbench content. The earlier full-desktop capture with Codex foregrounded is not used as proof. |
| Stale or deleted chat routes show `Chat not found` instead of recovering | Fixed in current design-language pass | Missing local/archive routes now recover to a valid local chat and clear stale hash routes; archived routes still show the restore flow. | `npm run smoke:ui:auto -- --session-switch`; gates include `sessionRouteResolvingVisible`, `missingRouteRecoveryVisible`, `missingRouteReturnWorks`, and `missingRouteNewChatWorks`. |
| Add project can appear frozen | Improved in current design-language pass | Sidebar and empty-state add-project flows now show selecting/adding/opening status, disable duplicate starts, and timeout slow project/session IPC paths into a visible error instead of an indefinite busy state. | `npm run smoke:ui:auto -- --empty-state`; `npm run smoke:ui:auto -- --sidebar`; `npm run build`. Manual folder-picker dogfood still needed for native dialog placement. |
| Composer feels busier than Codex composer | Improved in current design-language pass; tuned in current pass | Quieted the base composer material, shortened the persistent model control to the model label, made attachment icon-only, replaced the duplicate provider permission icon with a generic settings control, reduced the composer dock/card opacity and border weight, and made context chips read as quiet metadata instead of filled pills. | `npm run smoke:ui:auto -- --composer`; latest screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780265209401.png`; `npm run build`. |
| Composer thread settings popover still looked like a mini settings card | Improved in current pass | Reduced the popover border/shadow strength, increased glass translucency, softened row separators, and quieted choice chips so model/provider controls feel like a lightweight menu rather than a nested settings panel. | `npm run smoke:ui:auto -- --composer`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780266312268.png`; installed `npm run smoke:ui:auto -- --installed --composer`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780266484645.png`. |
| Composer permission menu could be covered by its trigger tooltip | Improved in current pass | Suppressed composer toolbar tooltips while their dropdown is expanded, preserving closed-state labels while preventing the permission/menu tooltip from floating over the open popover. | Foregrounded installed-app inspection on `5e79afaa872368cd01b931199b8fec09a70ed167174ef8d664ec874bc935525a` exposed the overlap; `npm run build`; `npm run pack:mac`; reinstalled `/Applications/Orchestrator.app`; installed `app.asar` hash `3f727a8612e15a1ceba68803126e93e63cbaf0b7c1624558455b523bd5f2b693`; `codesign --verify --deep --strict /Applications/Orchestrator.app`. Focused `--composer` smoke is currently unreliable in this environment: one run failed focus checks while the installed app was foregrounded, later runs timed out during dev startup before writing a report, and final Computer Use inspection was blocked by the macOS lock screen. |
| Composer menu triggers remounted while suppressing tooltips | Fixed in current pass | Kept the tooltip wrapper stable while disabling open-state tooltip behavior, preventing stale trigger nodes and restoring reliable aria-expanded/focus return checks. Further reduced composer dropdown/chip visual weight, and aligned the provider-switch smoke assertion with the intentionally compact model-only trigger label. | `npm run smoke:ui:auto -- --composer`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780279069126.png`; `npm run build`; `npm run pack:mac`; reinstalled `/Applications/Orchestrator.app`; installed `app.asar` hash `2a52a83d68cc5624479b6726049f98e18946a88241fdac81e99b8ae78a00a6bb`; `codesign --verify --deep --strict /Applications/Orchestrator.app`; `npm run smoke:ui:auto -- --installed --composer`; screenshot produced at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780279202909.png`. |
| Plugins/Capabilities page read like a heavy admin table | Improved in current pass | Flattened the Capabilities header, controls, table shell, row dividers, active segmented tabs, and provider chips so the page uses quieter row separation instead of a boxed table/card stack. | `npm run smoke:ui:auto -- --capabilities`; `npm run build`. The smoke currently returns to the composer after Add to chat, so the screenshot is not a final capabilities-page capture. |
| Workbench and Review/Git chrome still had IDE-strength split lines | Improved in current pass | Softened the shared right-panel edge, Workbench header chrome, diff/panel toolbar dividers, Workbench search field material, and Environment/Git row/card separators so Review, Browser, Environment, and Git read closer to quiet utility surfaces instead of boxed IDE panes. | `npm run smoke:ui:auto -- --diff-core`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780277943082.png`; `npm run smoke:ui:auto -- --browser --foreground`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780278129869.png`; `npm run smoke:ui:auto -- --environment`; `npm run build`; `npm run pack:mac`; reinstalled `/Applications/Orchestrator.app`; installed `app.asar` hash `c7798783c0022f35623da9fca66a8c1ce473a4928266afd04e9251fafea23e44`; `codesign --verify --deep --strict /Applications/Orchestrator.app`. Non-foreground Browser smoke remains focus-sensitive in this environment, but the foreground run passed the full Browser suite. |
| Installed and packaged direct-launch smoke harness appeared blocked | Rechecked and currently passing | Re-ran the direct executable paths used by `--installed` and `--packaged`; both now produce reports and screenshots, so the stale harness blocker is no longer listed as open. | `npm run smoke:ui:auto -- --installed --settings`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780278577831.png`; `npm run smoke:ui:auto -- --packaged --settings`; screenshot produced at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780278612003.png`. The invalid prior full-desktop capture with Codex foregrounded remains excluded from proof. |
| Side panel content is not usable when bottom panel is expanded | Improved in current design-language pass | The Workbench panel now receives bottom-panel open/expanded state and compacts new-tab rows plus side-chat, Plan, Files, Review, Browser, Environment, and Git chrome when the bottom panel is tall, preserving more usable vertical space. | `npm run smoke:ui:auto -- --terminal`; `npm run smoke:ui:auto -- --files`; screenshots inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780263124710.png` and `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780263070230.png`. |
| Bottom-expanded Workbench needed installed-app proof | Improved in current pass | Rechecked the newly installed app with the bottom panel open: Plan remains readable in the right Workbench while Browser/Terminal share the bottom panel, and Review/Git handoff surfaces pass on the same installed build. | `npm run smoke:ui:auto -- --installed --terminal`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780279329371.png`; `npm run smoke:ui:auto -- --installed --diff-core`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780279382457.png`; installed `app.asar` hash `2a52a83d68cc5624479b6726049f98e18946a88241fdac81e99b8ae78a00a6bb`. The `--installed --environment` flow passed but its final screenshot lands in Providers settings after validating the settings handoff, so it is not counted as Environment panel visual proof. |
| Environment with a tall bottom panel lacked installed screenshot proof | Improved in current pass | The terminal smoke now deliberately ends on the Environment panel in the right Workbench with the bottom panel resized tall, and gates the Environment active-tab state, compact row density, right-panel bottom-expanded state, and bottom-panel height before writing the screenshot. | `npm run smoke:ui:auto -- --terminal`; `npm run build`; `npm run pack:mac`; reinstalled `/Applications/Orchestrator.app`; `codesign --verify --deep --strict /Applications/Orchestrator.app`; `npm run smoke:ui:auto -- --installed --terminal`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780279963939.png`; installed `app.asar` hash `88e3c7eeff4c99c11cd8f84a3c0659cf0f2bff1623839215bafb506d18d985ef`. |
| Bottom terminal toolbar shows dead actions in the resting state | Improved in current pass | Kept terminal output, selected-output, and latest-command handoff controls in the DOM for accessibility/smoke coverage, but visually hides them while disabled so the resting bottom toolbar is focused on tab movement and panel switching. | Installed-app Computer Use inspection found the crowded resting bottom toolbar; `npm run smoke:ui:auto -- --terminal`; screenshot inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780269245896.png`; `npm run build`; rebuilt and reinstalled `/Applications/Orchestrator.app`; installed `app.asar` matched packaged hash `7fc93f5558816c5410938af42b656ebc272514aa8e93256b9ea5f9d51e2ef50b`; `codesign --verify --deep --strict /Applications/Orchestrator.app`; app-scoped Computer Use inspection confirmed the bottom panel opens in the installed app with the updated toolbar styling. |
| Git reads like a heavyweight top-level tab instead of contextual actions | Improved in current design-language pass | Git is no longer a default Workbench launcher; Review and Environment now expose branch, commit, pull-request, and commit-option handoffs with contextual labels instead of “Open Git” language while preserving the full tab for explicit workflows. | `npm run smoke:ui:auto -- --environment`; `npm run smoke:ui:auto -- --diff-core`; screenshots inspected at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1780261813612.png` and `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780261811229.png`; `npm run build`. |

## Open Regressions

| Issue | Impact | Proposed fix | Proof needed |
| --- | --- | --- | --- |
| Settings pages still need cross-page dogfood | General, Providers, Appearance, Pet overlay, Personalization, Worktrees, Browser, Shortcuts, and Data controls now have foregrounded installed-app inspection behind the quieter settings treatment. Remaining risk is broader edge-state dogfood, not the default settings layout. | Continue flattening dense controls into Codex-style section rows when new heavy controls appear, and keep advanced controls behind disclosure. | Manual edge-state checks for expanded disclosures, long shortcut/search states, archived-chat inventory, and destructive Browser/Data actions. |
| Side panel bottom-expanded compaction needs broader page dogfood | Plan plus bottom Browser/Terminal now has installed-app screenshot proof, Environment has installed proof with a tall bottom panel, and Review/Git handoff surfaces pass on the same installed build. Remaining risk is the explicit Git-as-primary tab and manual extremes beyond the smoke-resized bottom-panel height. | Continue applying the bottom-expanded state to high-density panels only where content would otherwise become cramped. | Manual bottom-expanded screenshot for Git-as-primary plus one manual stress pass at the maximum bottom-panel height. |
| Git should not be a heavyweight dedicated tab for every workflow | Improved through contextual Review/Environment handoffs and quieter Workbench/Git chrome, but the full Git tab still needs installed-app taste dogfood to confirm it now feels like an advanced destination instead of a primary mode. | Continue moving common actions into Review/File/Workbench context actions; keep full Git only for explicit branch/status workflows. | Manual Git workflow dogfood plus `--environment`/`--diff-core` proof after installed-app foreground capture is available. |
| Add project real-folder path still needs installed-app dogfood | The foregrounded installed app now shows visible “waiting for folder selection” status and opens the native folder picker frontmost; cancel returns to the app. Long-running real-folder indexing still needs proof against an actual local repo. | Run the picker against a real local repo from `/Applications/Orchestrator.app`, verify progress/status does not freeze, and capture logs if it stalls. | Manual add-project run against a local repo plus visible status/error-state check. |
| App still has too many visible borders and sluggish animations in places | Design language feels heavier than Codex. | Continue removing nested card borders, prefer full-bleed panels/rows, and shorten slow transitions using shared motion tokens. | Manual contact sheet across sidebar, main chat, Workbench, Settings, side chat, and bottom panel. |

## Operating Rule

Do not treat the side-panel parity report as the visual regression list. The parity report is useful calibration, but this list should be updated only from installed-app dogfood, focused screenshots, or a user-visible workflow break.
