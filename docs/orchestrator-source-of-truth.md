# Orchestrator Source Of Truth

Last updated: 2026-05-29

This is the canonical execution plan for Orchestrator. Every long-running implementation pass should start here, update this file as work lands, and treat the older docs in `docs/` as supporting research or historical evidence.

The goal is a first-class desktop GUI for local coding agents, with Claude Code as the first complete provider and Codex, Cursor, Copilot, and future CLIs mapped through the same Orchestrator-native abstractions.

For provider-specific implementation work, use `docs/provider-integration-runbook.md` as the adapter/runtime/test map before touching UI.

Recent side-panel parity checkpoint:

- Provider Settings permission-refresh checkpoint: Provider Settings now exposes an explicit `Refresh` action beside the live/fallback permission-config status, mirroring the composer permission popover's refresh affordance for daily provider configuration work. The action publishes a polite live status and smoke-visible runtime-context metadata. Focused `--settings-providers` smoke gates this as `settingsProviderPermissionRefresh=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780041717089.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780041717089.png`.
- Terminal scoped URL handoff checkpoint: Terminal URL handoff now registers terminal-id-scoped smoke openers and right-panel terminals pass URL opens through the same `openRightPanelBrowserUrl` Browser handoff as the bottom terminal. The focused terminal smoke gates `terminalLinkScopedRouting=true` while preserving `terminalLinkRouting=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780041168899.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780041168899.png`. This is a Terminal/Browser routing correctness fix, not another broad UI smoke pass.
- Transcript tool-summary collapse checkpoint: grouped tool activity now uses the same compact default-collapsed contract for runtime layout and smoke proof. The transcript virtualizer estimates collapsed tool groups at compact disclosure height instead of pre-paying for up to eight expanded rows, the tool summary exposes row-count/error/collapsed diagnostics plus a visible action count, and the transcript-layout smoke now gates `toolSummaryCollapsedByDefault=true` while keeping expanded scrollability covered. Focused validation stayed narrow: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and elevated `node scripts/run-automated-ui-smoke.mjs --transcript-layout` passed; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780039986621.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780039986621.png`.
- Inspector failure-group handoff checkpoint: Agents runtime failure groups now expose a compact `Add to chat` action that inserts thread/runtime/status/workspace context plus a bounded grouped-failure summary into the main composer. The focused Workbench New Tab smoke gates this as `agentRuntimeFailureGroupAddToChat=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780039280329.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780039280329.png`. This is a local inspector-to-composer workflow fix; richer provider-backed observability remains separate.
- Composer custom model visibility checkpoint: active thread model controls now surface the current custom model as an active `Custom` choice when the session model is outside the visible catalog, instead of hiding it behind the compact trigger label. The focused Composer smoke gates this as `composerActiveThreadCustomModelVisible=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780038821637.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780038821637.png`. This is a thread-menu usability fix; app-server model catalog promotion remains separate.
- Codex Review target checkpoint: Codex-backed Review panels now expose native inline `review/start` actions for uncommitted, branch/base-branch, commit, and custom-instruction review. Branch source mode labels the action `Start Codex base review`, commit source mode labels it `Start Codex commit review`, and Review options expose a custom-instructions start control. The focused Review source smoke gates visible target metadata as `reviewCodexBaseStart=true`, `reviewCodexCommitStart=true`, and `reviewCodexCustomStart=true`; latest evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1780038449015.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1780038449015.png`.
- Codex `review/start` live-proof checkpoint: `pnpm run live:codex-review-start` now compiles the provider parser, starts the real Codex app-server in a disposable git workspace, and proves native inline review for uncommitted, base-branch, commit, and custom-instruction targets. Latest target artifacts: `/Users/nadav/Desktop/Orchestrator/tmp/codex-review-start-live-proof/result.json`, `/Users/nadav/Desktop/Orchestrator/tmp/codex-review-start-base-branch-live-proof/result.json`, `/Users/nadav/Desktop/Orchestrator/tmp/codex-review-start-commit-live-proof/result.json`, and `/Users/nadav/Desktop/Orchestrator/tmp/codex-review-start-custom-live-proof/result.json`. These proofs remain opt-in for Review slices, not routine small-change smoke.
- Capability mention handoff checkpoint: Capabilities rows for mentionable skills, plugins, and apps now expose `Add to chat`, append a provider-compatible mention (`$skill`, `plugin://...`, or `app://...`) to the active chat draft, close the Capabilities surface, and restore composer focus. Focused `--capabilities` smoke gates this as `capabilityAddToChat=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-capabilities-1780035120364.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-capabilities-1780035120364.png`.
- Focused smoke-selection checkpoint: `scripts/run-automated-ui-smoke.mjs --list-views` and `pnpm run smoke:ui:list` now print the available focused UI smoke targets grouped by surface, with scope notes and examples. Use this before validation planning for small UI/product slices, then run the narrowest target plus code-level checks instead of the default broad smoke. Default no-flag smoke remains reserved for shared shell changes that are not covered by a listed target.
- Codex Review-mode checkpoint: `enteredReviewMode` / `exitedReviewMode` app-server items now normalize into typed `review.mode.changed` events instead of generic status text, persist as `Review mode: active/exited` transcript rows, and make the right Workbench Plan tab available after live events or reload. The Plan panel renders a compact Review mode section with the review summary and an Open Review action. `pnpm run test:providers` gates parser/message normalization, and focused `--plan` smoke gates `planReviewMode=true` plus `planReviewModeOpen=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-plan-1780034332685.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-plan-1780034332685.png`.
- Codex Goal command checkpoint: Codex sessions now expose `/goal` as a provider slash command with `runtime="app-server"` and `handler="send-to-provider"`, and the Plan goal block exposes a compact `Clear` action that routes `/goal clear` through the normal session send path with visible/announced status. `pnpm run test:providers` gates the slash-command routing, and focused `--plan` smoke gates `planGoalClearAction=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-plan-1780033984816.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-plan-1780033984816.png`.
- Plan goal persisted-metrics checkpoint: the Plan panel now reconstructs status, tokens used, token budget, elapsed time, and progress from persisted `Goal:` transcript status rows after reload, matching the useful context from live `thread/goal/updated` events. Focused `--plan` smoke gates this under `planGoalPersistedMetrics=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-plan-1780033652489.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-plan-1780033652489.png`.
- Transcript read-state checkpoint: selecting or routing to a chat no longer clears its unread state immediately. `ChatView` now clears unread only after the active transcript is rendered or confirmed empty, exposes a read-sync marker on the transcript scroller, and the focused `--session-switch` smoke gates `unreadPreservedBeforeSwitch=true` plus `activeReadClearedAfterTranscript=true` alongside route, lazy-load, search, and timing checks.
- Transcript tool-group jump checkpoint: grouped tool activity rows now use a stable first-tool virtual row key, expose their contained message ids through virtual-row metadata, and transcript search jumps can target a tool result inside a collapsed tool group even when that group is outside the current render window. The new focused `--transcript-tool-jump` smoke gates `transcriptToolGroupStableKey=true` and `transcriptToolSearchJump=true`, keeping this PP-033 scroll/search contract out of the slower broad transcript-layout suite.
- Transcript composer-reserve checkpoint: the session shell now measures the live composer/footer height and exposes it as `--composer-reserve-height` plus smoke-visible reserve metadata on the primary chat column. `ChatView` marks the transcript as reserve-aware, applies the reserve to transcript scroll padding, and re-follows the bottom when the composer reserve changes. The new focused `--transcript-reserve` smoke gates `composerReserveContract=true` and `composerReserveFollowBottom=true` without running the full transcript-layout suite.
- Review toolbar navigation checkpoint: detailed Review now exposes compact `Previous changed file` / `Next changed file` toolbar actions after the existing Review options, Jump, and Refresh controls. The buttons reuse the same selected changed-file model as the tree keyboard navigation, close open Review menus when used, and are smoke-gated as `reviewToolbarFileNavigation=true` without disturbing `reviewToolbarPrimaryOrder` or `reviewToolbarHeaderRow`.
- Settings section history checkpoint: Settings sidebar section changes now push their routed `/settings/{section}` URL instead of relying on a later replace-only state sync, so browser/app Back can step from Appearance back to General inside Settings. Focused Settings smoke gates this as `settingsSectionHistoryNavigation=true`, including route attributes, URL path/hash state, and Back navigation recovery.
- Browser zoom presets checkpoint: the Browser actions menu now exposes six shared-menu zoom presets, `50%`, `75%`, `100%`, `125%`, `150%`, and `200%`, alongside the existing zoom row. Focused Browser smoke gates this as `browserZoomPreset=true` by selecting `125%`, verifying the Browser workbench zoom model and active check state, and restoring `100%` before continuing the rest of the Browser lifecycle checks.
- Composer prompt history checkpoint: successful sends are now stored in per-session composer prompt history, with `ArrowUp`/`ArrowDown` recalling prior prompts and restoring the unsent draft without interfering with slash-palette navigation or normal multiline editing. Focused composer smoke gates this as `composerPromptHistoryRecall=true` while preserving draft isolation, attachment isolation, async attachment switching, pending cancel, manual attachment switching, permission, provider/model, and toolbar checks.
- Edit-as-draft restore checkpoint: `Edit message as draft` now keeps copied prompt editing explicit without destroying an existing unsent composer draft. When a copied edit replaces a non-empty draft, the composer-side `Editing a copy` notice records that the previous draft was saved and exposes `Restore previous draft`; the focused transcript-layout smoke gates `chatUserMessageEditDraftSourceStatus=true` and `chatUserMessageEditDraftRestore=true`.
- Files selected-line context checkpoint: source file tabs now expose `Add selected line to chat` in the selected-line menu/inline action cluster. It inserts the exact `path:line` reference plus source text into the main composer, announces `Added selected line to chat`, and is focused-smoke gated as `fileSourceLineAddToChat=true`.
- Browser history cleanup checkpoint: the Browser actions menu now includes a first-class `Clear history` action inside the History section. It clears the Browser workbench history list, closes the menu, and announces `History cleared` through the existing status row. Focused Browser smoke gates this as `browserHistoryClear=true` while preserving Browser history-menu, zoom, lifecycle, webview, local-target, inspector, security, and fork-transfer checks.
- Main-chat regenerate checkpoint: the latest assistant response now exposes a compact `Regenerate` action beside Continue/fork/copy when a retryable user prompt exists. The action uses the existing provider-neutral retry-last-user-message path, announces `Regenerate sent`/failure state, and focused transcript-layout smoke gates `chatRegenerateLastResponse=true` plus `chatRegenerateLastResponseA11y=true`. The same pass tightened narrow permission-card action wrapping and made the narrow transcript smoke re-query virtualized permission cards after scrolling.
- Environment add-to-chat checkpoint: the Environment panel now has an explicit `Add environment to chat` action that inserts workspace, provider, status, branch, change totals, PR metadata, and source availability into the composer through the shared composer insertion path. Focused Environment smoke gates this as `environmentAddToChat=true`.
- Composer context chips checkpoint: the composer now shows compact context chips for current workspace, local/branch execution mode, and additional-directory count with accessible metadata. Focused composer smoke gates this as `composerContextChips=true` while preserving existing draft, attachment, queue, permission, provider/model, and toolbar checks.
- File/source open outcome checkpoint: Files source tabs now keep the last `fs.openPath` result instead of discarding it, announce the resolved editor target/method/line/fallback in the action status, and expose structured diagnostic attributes so the focused Files smoke can gate `fileOpenTargetOutcomeDiagnostic=true`.
- Composer pending attachment checkpoint: delayed pasted/dropped attachment saves now expose pending chips with explicit `Cancel saving ...` labels, failed chips use `Dismiss ...`, and focused composer smoke gates a delayed large attachment cancellation with `composerPendingAttachmentCancel=true` so a canceled save cannot attach after it eventually resolves.
- Shared find-step command checkpoint: `Find Next` and `Find Previous` now live in the shared app command registry with `Cmd/Ctrl+G` and `Cmd/Ctrl+Shift+G`. The app shell routes them to the active shared chat/diff/source find bar or Browser page find, and focused right-panel smoke gates `rightPanelFindStepShortcutRouting=true`.
- Route-backed local session identity checkpoint: local chat routes now use `/threads/{id}` for app/http renderers and `#/threads/{id}` for file/hash renderers. Renderer startup parses those routes before empty-session cleanup, preserves the requested session, syncs active local chat changes back into the route, and handles `popstate`/`hashchange` session navigation in loaded windows. Missing/archived route ids now render an explicit recovery surface instead of silently doing nothing; archived links can restore and reopen the thread, while missing links can return to the current chat. Focused session-switch smoke now gates direct route startup, archived/missing route recovery, and route updates with `startupRouteBacked=true`, `startupRouteTranscriptFound=true`, `archivedRouteRecoveryVisible=true`, `archivedRouteRestoreWorks=true`, `missingRouteRecoveryVisible=true`, `missingRouteReturnWorks=true`, `firstRouteUpdated=true`, and `secondRouteUpdated=true`.
- Active parity steering rule: panel/header interaction is a first-class comparison target. Any sidebar, main titlebar, Files, Browser, Review, Workbench right-panel, or bottom Terminal work must keep the `Header and panel interaction` comparison row green and should inspect `tmp/codex-side-panel-comparison/header-panel-contact-sheet.html`; a focused surface smoke alone is not enough if the combined shell no longer matches Codex's coordinated header/sidebar/Files/Browser/Review/Workbench-right-panel/bottom-panel relationship.
- Live Codex screenshot evidence rule: `/private/tmp/codex-current-screen.png` is not valid evidence merely because it exists. The comparison now discovers visible Codex windows through CoreGraphics, attempts window-id and bounds-region capture before full-screen fallback, inspects PNG nonblank metrics, marks black captures as `stale/incomplete`, and reports optional live screenshot file evidence as `passed=false` when those image checks fail without turning smoke-backed rows into mismatches. The latest refresh found one visible Codex window but window capture failed with `could not create image from window`, region capture failed with `could not create image from rect`, and full-screen fallback produced `nonBlack=0`, `lumaStdDev=0`, and `colors=1`, so current header/panel work should rely on Codex bundle evidence plus Orchestrator smoke until a usable live screenshot is captured.
- Browser dynamic client-tool checkpoint: Orchestrator now advertises `orchestrator.browser_open`, `orchestrator.browser_read`, `orchestrator.browser_click`, `orchestrator.browser_type`, `orchestrator.browser_screenshot`, `orchestrator.browser_fill`, `orchestrator.browser_key`, `orchestrator.browser_select`, `orchestrator.browser_check`, and `orchestrator.browser_scroll` for Codex app-server sessions, returning visible target metadata from read/open, executing click/type/fill/key/select/check/scroll actions in the actual Browser webview through `browser:clientToolCall`, and capturing the current webview as a saved PNG artifact with screenshot metadata. Focused Browser smoke passed with `browserClientToolBridge=true`, `browserClientToolActions=true`, `browserClientToolScreenshot=true`, and `browserClientToolAdvancedActions=true`; final evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779860477150.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779860477150.png`. Live `npm run live:codex-browser-tools` proof passed at `2026-05-27T05:42:19.601Z` with real Codex `item/tool/call` requests for open/read/click/type/screenshot/fill/key/select/check/scroll/final-read and `CODEX_BROWSER_LIVE_OK`; artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-browser-tools-live-proof/result.json`. The dev full side-panel comparison passed at `2026-05-27T05:49:25.342Z`, and the installed `/Applications/Orchestrator.app` full side-panel comparison passed at `2026-05-27T05:58:55.301Z`; both gate `browserClientToolActions=true`, `browserClientToolScreenshot=true`, and `browserClientToolAdvancedActions=true` in the Browser lifecycle row. Native browser-use event streaming, image-content screenshot return semantics, provider design-change application, full agent-driven installed Browser turn proof, and exact live Browser pixel/timing proof remain separate.
- Live Codex composer lifecycle checkpoint: `npm run live:codex-composer-appserver` proves Orchestrator's Codex model/effort/policy mapping can start a real app-server turn and complete an allowed command in a disposable workspace; `npm run live:codex-composer-approval` proves Orchestrator can receive and answer a real `item/commandExecution/requestApproval`; `npm run live:codex-composer-resume` proves a non-ephemeral Codex thread can be resumed from a fresh app-server process with the same thread id and prior-turn context; `npm run live:codex-composer-runtime-lifecycle` proves Orchestrator's `CodexAppServerRuntimeManager` emits the expected runtime events while starting with `thread/start`, resuming with `thread/resume`, preserving the same provider session id, and retaining prior-turn context; `npm run live:codex-composer-session-lifecycle` proves the compiled `sessionManager` path sends, continues, and retries on the same live Codex provider session; `npm run live:codex-composer-renderer-lifecycle` proves the real renderer transcript Continue and Retry buttons drive live Codex resume turns on the same provider session; `npm run live:codex-composer-partial-continue` proves the visible `Partial response stopped` transcript state resumes through the renderer Continue button on the same provider session; and `npm run live:codex-composer-model-switch` proves a persisted Orchestrator model change is used for `thread/resume` and `turn/start` on an existing Codex provider session. Focused local/parser/UI coverage now proves Codex app-server file-change and permission-profile approval cards render as `File Approval` and `Permission Profile` under `permissionVariantCards=true`. Artifacts live under `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-appserver-live-proof/result.json`, `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-approval-live-proof/result.json`, `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-resume-live-proof/result.json`, `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-runtime-lifecycle-live-proof/result.json`, `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-session-lifecycle-live-proof/result.json`, renderer lifecycle evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-lifecycle-1779986634530.json`, partial-continue evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-partial-continue-1779987197970.json`, model-switch evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-model-switch-1779987672302.json`, and permission-variant evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-permission-1779988391389.json`. Remaining lifecycle proof is deterministic live file/profile permission forcing, non-Codex adapters, and live Codex UI pixel/timing proof.
- Review provider-source unsupported-boundary checkpoint: unsupported provider-native Review sources must stay explicit instead of masquerading as implemented parity. The source menu now shows disabled provider rows with exact reasons (`No provider turn diff`, `Cloud review adapter missing`, `No local provider source`, or `No provider worktree source`) and exposes those reasons through smoke-visible attributes. Focused Review source smoke passed with `reviewProviderSourceUnavailableReasons=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779856661582.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779856661582.png`. The full side-panel comparison passed at `2026-05-27T04:48:16.500Z` with the Review provider metadata row gating this check; cloud/hosted Review adapters and provider checkpoint restore remain separate future provider contracts.
- Header/panel interaction checkpoint: live Codex screenshot evidence at `/private/tmp/codex-current-screen.png` shows the Codex right-panel/review shell chrome starting in the same compact top band as the conversation header/titlebar instead of below it. Orchestrator now renders the session titlebar inside the primary chat column while the Workbench right panel remains a sibling in the same session row, so the right panel shell/chrome and titlebar share one top seam. Focused right-panel smoke passed with `rightPanelHeaderSeam=true` plus the existing right-panel shell checks; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779779236406.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779779236406.png`. Focused Header smoke still passes after moving the titlebar; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1779779280949.json`. Terminal behavior and visual smokes also pass after the structural change, with visual evidence at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-visual-1779779310086.png`. Exact live Codex pixel/timing comparison remains separate.
- Compact header profile checkpoint: Orchestrator no longer renders the isolated profile as a wide `Automated Ui Smoke profile` pill in the primary titlebar. The badge is now a compact icon-only toolbar-scale control with the profile name preserved in tooltip/ARIA, matching the live Codex screenshot direction of compact top-right header controls. Focused Header smoke passed with `profileBadgeCompact=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1779802561267.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1779802561267.png`. The side-panel visual inventory now includes `header.png`, and the full comparison passed with 25 captures, `fixture-covered=8`, `aligned=2`, `mismatch=0`, including `profileBadgeCompact=true` in the dedicated header/panel row.
- Header action chrome checkpoint: the parity list now explicitly covers how panels interact with the header band. Orchestrator renders titlebar chat actions, side-panel toggle, terminal toggle, and the isolated profile badge as compact toolbar chrome instead of a heavier independent button cluster, and the executable contract includes `headerActionChromeCompact=true` in the dedicated header/panel interaction row. Focused Header smoke passed with `headerActionChromeCompact=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1779805058201.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1779805058201.png`. Focused right-panel smoke passed with `headerActionChromeCompact=true`, `headerPanelSharedBand=true`, and `rightPanelHeaderSeam=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779805152557.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779805152557.png`. The full side-panel comparison passed with 25 captures, `fixture-covered=8`, `aligned=2`, and `mismatch=0`; manifest created at `2026-05-26T14:24:18.754Z`.
- Review path-first file-header checkpoint: live Codex screenshot evidence shows the selected Review file header reading path-first, without a prominent colored status glyph before the path. Orchestrator now tags selected Review file headers with `data-review-file-header-style="codex-path-first"` and hides the decorative change glyph so `review-base.txt` starts the visible header row. Focused Last turn Review smoke passed with `reviewFileHeaderPathFirst=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-last-turn-1779805927189.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-last-turn-1779805927189.png`. The full side-panel comparison passed with 25 captures, `fixture-covered=8`, `aligned=2`, and `mismatch=0`; manifest created at `2026-05-26T14:37:36.138Z`.
- Header/panel interaction contract checkpoint: the parity ledger now treats panel/header interaction as a dedicated runnable contract across the left sidebar, main session header, Files/Browser/Review/Workbench right panel, and bottom Terminal panel instead of a one-off right-panel note. Orchestrator reduced the left sidebar drag spacer from 64 px to 48 px so the first utility row sits closer to the window/header band, added `data-testid="sidebar-window-drag-spacer"`, and focused sidebar smoke passed with `sidebarTopInsetCodexLike=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779780312214.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779780312214.png`. The contract also gates `sessionHeaderInPrimaryColumn=true` through the `transcript-narrow` main-shell capture, `profileBadgeCompact=true` through the focused `header` capture, `filesHeaderPanelSeam=true` through the focused Files capture, `browserHeaderPanelSeam=true` through the focused Browser capture, and right/bottom panel attachment checks through Workbench, Review, and Terminal captures. The current full visual inventory is now covered by the following multi-window checkpoint.
- Header/panel multi-window checkpoint: the full side-panel visual inventory now includes `multi-window-focus` as a first-class App Shell / Window Lifecycle capture, and the `Header and panel interaction` comparison row gates its route/window/menu proof alongside the visual shell captures. Latest full comparison: `npm run compare:codex-side-panels -- --run-smoke --full --no-fail` passed with 27 captures, `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`; the latest comparison report `/Users/nadav/Desktop/Orchestrator/tmp/codex-side-panel-comparison/comparison-report.json` was created at `2026-05-27T03:28:15.455Z`. Orchestrator now scopes pending new-window navigation to the target renderer window instead of a single global pending slot, avoids retaining pending navigation for already-loaded deep-link targets, and the multi-window capture passes `secondWindowCreated=true`, `secondWindowNavigated=true`, `pendingNavigationConsumedOnce=true`, `pendingNavigationWindowScoped=true`, `loadedDeepLinkDoesNotLeavePendingNavigation=true`, `firstWindowBrowserFocusArea=true`, `firstWindowBrowserMenuEnabled=true`, `secondWindowBrowserMenuDisabled=true`, `backgroundWindowMenuDoesNotClobberFocusedWindow=true`, `activeWindowAfterRefocus=true`, `focusSwitchRestoresFirstWindowMenu=true`, and `menuCommandRoutedToFocusedWindow=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-multi-window-focus-1779852428108.json`; copied screenshot `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-current/multi-window-focus.png`. Raw Electron `BrowserWindow.getFocusedWindow()` still remains separate from the comparison gate because this automation path does not restore the OS-level focused-window primitive.
- App-shell panel open/close telemetry checkpoint: Codex bundle evidence in `tmp/codex-app-assets/app-shell-tab-controller-B2eCi4Le.js` emits `codex_thread_side_panel_opened`, `codex_thread_side_panel_closed`, and `codex_thread_side_panel_tab_viewed` with active-tab and route-kind metadata. Orchestrator's shared `AppShellPanel` now records `panel.opened` / `panel.closed` for right and bottom app-shell panels with `panelSurface`, `activeTab`, and `routeKind`, and the Workbench comparison row gates the right-panel path with `rightPanelPanelOpenCloseTelemetry=true`. Focused right-panel smoke passed with that gate; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779850119191.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779850119191.png`. The refreshed installed comparison passed with 27 captures and no failures; installed right-panel evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779852816510.json`; manifest `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-installed-full/manifest.json`, created at `2026-05-27T03:37:08.348Z`; comparison report `/Users/nadav/Desktop/Orchestrator/tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T03:41:51.077Z`. Broader route/window lifecycle, exact live Codex timing, and pixel spacing remain separate.
- Installed side-panel smoke checkpoint: the side-panel visual inventory and Codex comparison runner accept `--packaged` and `--installed` app modes. `--packaged` launches renamed temp bundles from `dist/mac-arm64/Orchestrator.app`; `--installed` targets `/Applications/Orchestrator.app` with the same isolated smoke profile and user-data isolation. After `npm run pack:mac`, `npm run smoke:ui:auto -- --packaged --sidebar` passed with `hoverCardMaterial=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779848313353.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779848313353.png`. `npm run smoke:visual:side-panels -- --packaged --out tmp/side-panel-visual-inventory-packaged-core` passed with 8 core captures and no failures; manifest `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-packaged-core/manifest.json`, created at `2026-05-27T02:21:23.400Z`. After the Browser advanced dynamic-tool slice, `npm run pack:mac` and `npm run install:mac` rebuilt and installed `/Applications/Orchestrator.app`; elevated `npm run compare:codex-side-panels -- --run-smoke --full --installed --no-fail --smoke-out tmp/side-panel-visual-inventory-installed-full` passed with 27 captures, `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`; manifest `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-installed-full/manifest.json`, created at `2026-05-27T05:58:55.047Z`; comparison report `/Users/nadav/Desktop/Orchestrator/tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T05:58:55.301Z`; installed Browser evidence includes `browserClientToolScreenshot=true` and `browserClientToolAdvancedActions=true`, and installed multi-window evidence still includes `loadedDeepLinkDoesNotLeavePendingNavigation=true`.
- Browser Use policy persistence checkpoint: Codex's Browser Use settings bundle exposes approval/history and domain policy controls, and Orchestrator now persists matching local Browser Use defaults in `browserUsePolicy`. Settings > Browser writes approval, history, download, upload, and domain allow/block defaults, and new Browser workbenches adopt those defaults in the right-side Browser Security pane without overriding per-session changes. Focused Settings smoke passed with `settingsBrowserPolicyPersistence=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779855428419.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779855428419.png`. Focused Browser smoke passed with `browserPersistedPolicyDefaults=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779855428672.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779855428672.png`. The refreshed full side-panel comparison passed with 27 captures, `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`; manifest `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-current/manifest.json`, created at `2026-05-27T04:22:34.215Z`; report `/Users/nadav/Desktop/Orchestrator/tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T04:22:34.470Z`. Provider-backed Browser Use adapters and remote-host policy state remain separate.
- Sidebar footer collapse checkpoint: live Codex screenshot evidence shows a small footer collapse control beside Settings in the left sidebar. Orchestrator now exposes `sidebar-footer-collapse-toggle` beside the Settings footer row, collapses to a 48 px icon rail, and restores to the 240 px sidebar through `sidebar-rail-expand-toggle`. Focused Sidebar smoke passed with `sidebarFooterCollapseAffordance=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779803856358.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779803856358.png`. The full side-panel comparison passed with 25 captures, `fixture-covered=8`, `aligned=2`, and `mismatch=0`; manifest: `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-current/manifest.json`, created at `2026-05-26T14:03:25.425Z`.
- Browser dynamic client-tool checkpoint: live `npm run live:codex-browser-appserver` proof still shows the tested browser-use prompt emits no provider-native browser-use events or browser-specific client tool requests, so full native browser-use parity remains unproven. A separate live `npm run live:codex-dynamic-tools` proof confirms the stdio app-server path accepts `thread/start.dynamicTools`, sends `item/tool/call` for the advertised `orchestrator.browser_bridge_status` tool, accepts Orchestrator's `DynamicToolCallResponse`, and completes with `CODEX_BROWSER_LIVE_OK`; artifact: `/Users/nadav/Desktop/Orchestrator/tmp/codex-dynamic-tools-live-proof/result.json`. The newer live `npm run live:codex-browser-tools` proof advertises the production `orchestrator.browser_open`, `orchestrator.browser_read`, `orchestrator.browser_click`, `orchestrator.browser_type`, `orchestrator.browser_screenshot`, `orchestrator.browser_fill`, `orchestrator.browser_key`, `orchestrator.browser_select`, `orchestrator.browser_check`, and `orchestrator.browser_scroll` tool specs, receives live Codex app-server `item/tool/call` requests for those tools plus final read, and completes with `CODEX_BROWSER_LIVE_OK`; artifact: `/Users/nadav/Desktop/Orchestrator/tmp/codex-browser-tools-live-proof/result.json`. Orchestrator advertises those same renderer-backed tools for production Codex app-server sessions, opens the Browser panel via `browser.manager_state`, routes supported calls through `browser:clientToolCall` / `browser:clientToolResponse`, returns URL/title/visible-structure results from the Browser webview, executes click/type/fill/key/select/check/scroll, and captures a saved Browser screenshot artifact. Focused Browser smoke proves the main IPC to renderer webview loopback with `browserClientToolBridge=true`, `browserClientToolActions=true`, `browserClientToolScreenshot=true`, and `browserClientToolAdvancedActions=true`; latest focused Browser evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779860477150.json`. The unsupported boundary remains user-visible for unimplemented dynamic tools. Next Browser expansion should be native browser-use event streaming, image-content screenshot return semantics, provider design-change application, or installed-app end-to-end Browser agent proof only from real provider requests, explicit product scope, or a full installed-app end-to-end run.
- Files notebook output-summary/raw-output checkpoint: Codex's `notebook-preview-panel-CAO-aRhM.js` reads nested/direct `outputSummaries[index].summaryMarkdown` metadata, renders summaries before raw stream/text/JSON/error outputs, hides summarized raw payloads behind a `Raw output` disclosure, and prefers `text/plain` before JSON when both notebook MIME payloads are present. Orchestrator now preserves notebook output summary metadata, renders summary markdown above supported raw outputs, keeps raw payloads behind Codex-style disclosures when summarized, and follows text/plain-before-JSON precedence. Focused Files smoke passed with `filesNotebookOutputSummaries=true`, `filesNotebookRawOutputDisclosure=true`, and `filesNotebookOutputRendering=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779823093321.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779823093321.png`.
- Files artifact title/type header checkpoint: Codex's DOCX and PDF preview panels strip `.docx`/`.pdf` from the visible artifact title and render a shared artifact header with a separate `DOC`/`PDF` type label; notebook previews render `IPYNB` plus cell count in the artifact type text. Orchestrator now uses the same extensionless title/type split for PDF, DOCX, and notebook previews, and focused Files smoke gates it with `filesArtifactHeaderTitleType=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779809072098.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779809072098.png`. Full side-panel comparison passed with 25 captures, `fixture-covered=8`, `aligned=2`, `mismatch=0`, and manifest created at `2026-05-26T15:29:36.749Z`.
- Files PDF page/zoom/invert/presentation checkpoint: Codex's PDF artifact preview has page navigation, zoom controls, invert-colors controls, and `artifact-pdf-presentation` mode with an Exit control. Orchestrator now parses local PDF page count, renders previous/next page controls plus a `current/total` indicator in the header center slot, renders zoom out/in controls with a percent indicator, exposes an invert-colors toolbar control that applies a real iframe filter, updates the PDF iframe URL fragment for the selected page/zoom, and opens a Codex-named PDF presentation surface with floating previous/current-next/Exit controls plus click and keyboard navigation. Focused Files smoke passed with `filesPdfPreviewControls=true` and `filesPdfPresentationMode=true` against a two-page fixture; latest evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779817150443.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779817150443.png`. Full visual inventory passed with 25 captures, manifest created at `2026-05-26T17:37:32.737Z`, and the comparison report passed at `2026-05-26T17:37:43.482Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`. PDF canvas rendering, annotations/comment layer, download handling, and provider-backed PDF metadata remain open.
- Files DOCX page/zoom checkpoint: Codex's DOCX preview uses a dedicated `docx-preview-panel` / `codex-docx-preview` surface, strips `.docx` from the artifact title, exposes `docx-preview-zoom-trigger`, and shares artifact page/zoom controls. Orchestrator DOCX previews now render previous/next page controls in the artifact header center slot, zoom out/in controls with a percent indicator, a page-count meta item, and a document page shell. Focused Files smoke passed with `filesDocumentPageControls=true` against a two-page DOCX fixture; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779818606033.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779818606033.png`. The refreshed side-panel comparison passed at `2026-05-26T18:12:50.756Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`. Full DOCX renderer fidelity, true layout pagination, comments, download/provider metadata, and provider-backed artifact annotations remain open.
- Files spreadsheet/slides artifact-renderer checkpoint: Codex's artifact tab bundle maps `.xlsx`/`.xlsm` to spreadsheet artifacts and `.pptx` to slides artifacts, with workbook/presentation renderer paths in `artifact-tab-content.electron-DayvYBGS.js`. Orchestrator now classifies XLSX/XLSM and PPTX as explicit artifact preview states instead of generic binary fallback, with extensionless titles, `XLSX` / `PPTX` type labels, compact Copy path/Open/Reveal toolbar actions, no unsupported source toggle, and lightweight local OOXML renderers: XLSX sheets render as table previews and PPTX slides render as outline cards. Focused Files smoke passed with `filesSpreadsheetPreview=true`, `filesSlidesPreview=true`, `filesSpreadsheetSlidesArtifactBoundary=true`, `filesSpreadsheetRenderer=true`, and `filesSlidesRenderer=true`; latest evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779818131482.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779818131482.png`. Full visual inventory passed with 25 captures, manifest created at `2026-05-26T17:53:41.092Z`, and the comparison report passed at `2026-05-26T17:56:13.834Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`. Full workbook grid controls, slide canvas rendering, editing/annotation hooks, and provider-backed artifact metadata remain open.
- Files spreadsheet/slides control checkpoint: Codex's Popcorn workbook and presentation panels receive `renderHeaderZoomControl`; the presentation bundle also exposes `popcorn-presentation-page-navigation`, `popcorn-presentation-zoom-select`, and `popcorn-presentation-thumbnails`. Orchestrator XLSX previews now render one active sheet at a time with previous/next sheet controls and zoom controls, and PPTX previews now render a focused active slide surface with previous/next slide controls, zoom controls, thumbnails, and outline cards. Focused Files smoke passed with `filesSpreadsheetControls=true` and `filesSlidesControls=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779819702367.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779819702367.png`. Full side-panel comparison passed with 25 captures, `fixture-covered=8`, `aligned=2`, `mismatch=0`, and report created at `2026-05-26T18:27:38.157Z`. Full Popcorn workbook grid interaction, slide canvas fidelity, speaker notes, editing/annotation hooks, and provider-backed artifact metadata remain open.
- Files spreadsheet sheet-tab checkpoint: Codex's Popcorn workbook bundle exposes `popcorn-pages-bar`, `popcorn-sheet-tab-*`, `popcorn-add-sheet`, and `popcorn-zoom-select`. Orchestrator XLSX previews now render a read-only bottom sheet-tab bar for parsed workbook sheets, mark the active tab with `aria-selected`, switch sheets through tab clicks, and expose a disabled add-sheet affordance instead of pretending local preview editing is supported. Focused Files smoke passed with `filesSpreadsheetSheetTabs=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779843784918.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779843784918.png`. Full side-panel visual inventory passed with 26 captures, manifest created at `2026-05-27T01:08:34.204Z`, and comparison report passed at `2026-05-27T01:08:40.223Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`.
- Files spreadsheet active-cell checkpoint: Codex's Popcorn workbook bundle exposes `popcorn-formula-bar` and `popcorn-selection` in addition to sheet tabs and zoom. Orchestrator XLSX previews now render workbook-style row/column headers, selectable read-only cells, root/formula-bar active-cell metadata, and a read-only formula/value field that updates when cells are selected or sheets change. Focused Files smoke passed with `filesSpreadsheetActiveCell=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779844791812.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779844791812.png`. Full side-panel visual inventory passed with 26 captures, manifest created at `2026-05-27T01:25:35.975Z`, and comparison report passed at `2026-05-27T01:25:39.063Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`. Editable workbook behavior, formula editing/evaluation, comments, annotations, full Popcorn renderer fidelity, and provider-backed artifact metadata remain open.
- Live Codex screenshot capture checkpoint: `npm run compare:codex-side-panels` accepts `--capture-live-codex`, which now discovers visible Codex windows through CoreGraphics and tries window-id capture, bounds-region capture, and full-screen fallback before validating the selected screenshot with the same PNG nonblank metrics used by the header/panel comparison rows. The latest elevated attempt found one visible Codex window (`id=39726`, bounds `0,30,1384,1320`), but window capture failed with `could not create image from window`, region capture failed with `could not create image from rect`, and full-screen fallback still failed as black (`nonBlank=false`, `nonBlack=0`, `lumaStdDev=0`, `colors=1`), so exact live Codex pixel comparison remains unavailable in this environment. The comparison still passed at `2026-05-27T04:31:15.939Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`; use bundle evidence plus Orchestrator smoke/contact sheets until a nonblank live capture is available.
- Files notebook metadata/code-snippet checkpoint: Codex's `notebook-preview-panel-CAO-aRhM.js` reads direct and nested Codex cell metadata titles/descriptions and renders described code cells with description markdown plus a nested read-only Code disclosure containing a titled `Python` code snippet. Orchestrator now preserves direct and nested Codex notebook cell metadata, renders metadata titles/descriptions, and hides described source behind a Code disclosure with a Python-titled code shell. Focused Files smoke passed with `filesNotebookCellMetadata=true` and `filesNotebookCodeSnippet=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779823811213.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779823811213.png`.
- Files notebook cell spacing checkpoint: Codex's `notebook-preview-panel-CAO-aRhM.js` uses `px-4 py-4 sm:px-6 sm:py-5`, a centered `max-w-3xl` cell column, `gap-4`, `rounded-lg` cells, and `px-4 py-2` cell headers. Orchestrator notebook previews now use a centered 768 px cell column, 16 px cell gap, Codex-like responsive preview padding, 8 px cell radius, and 8 px/16 px summary headers. Focused Files smoke passed with `filesNotebookCellSpacing=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779834968831.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779834968831.png`. Full side-panel visual inventory passed with 26 captures, manifest created at `2026-05-26T22:41:47.977Z`, and the comparison report passed at `2026-05-26T22:41:59.925Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`.
- Files artifact Open/options checkpoint: Codex's `artifact-preview-status-D5kFRFQE.js` renders artifact header actions around a primary Open affordance with adjacent options/reveal behavior, not as separate equal Open/Reveal toolbar buttons. Orchestrator now groups JSON, CSV, DOCX, PDF, XLSX, PPTX, and IPYNB artifact preview actions behind a primary `Open` toolbar button plus `Open options` chevron/menu, while keeping custom PDF/DOCX/spreadsheet/slides page and zoom controls. Focused Files smoke passed with `filesArtifactPreviewControls=true` and `filesArtifactOpenOptions=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779840151421.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779840151421.png`. Full side-panel visual inventory passed with 26 captures, manifest created at `2026-05-27T00:08:31.179Z`, and comparison report passed at `2026-05-27T00:08:34.265Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`.
- Files file-source action-label checkpoint: Codex's `review-file-source-tab-CjS7Xe_W.js` uses `File viewer options`, `Open in editor`, and `Enable/Disable word wrap` in the file-source tab actions. Orchestrator now uses those labels for the file-tab ellipsis, open actions, selected-line open action, toolbar wrap control, and shared options menu. Focused Files smoke passed with `workbenchFileTabCodexActionLabels=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779840863976.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779840863976.png`. Full side-panel visual inventory passed with 26 captures, manifest created at `2026-05-27T00:20:20.371Z`, and comparison report passed at `2026-05-27T00:20:24.298Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`.
- Files file-source action-cluster checkpoint: Codex's `review-file-source-tab-CjS7Xe_W.js` renders the visible file-source action cluster as `File viewer options` plus `Open in editor`, with rich/source, word wrap, copy path, artifact preview, and git blame in the options menu. Orchestrator now keeps those secondary actions in the options menu or selected-line affordances and exposes only the Codex-style options/open pair in the visible file-tab action cluster. Focused Files smoke passed with `workbenchFileTabCodexActionCluster=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779841776063.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779841776063.png`. Full side-panel visual inventory passed with 26 captures, manifest created at `2026-05-27T00:37:54.902Z`, and comparison report passed at `2026-05-27T00:38:02.171Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`.
- Files file-source shared-find checkpoint: Codex's `review-file-source-tab-CjS7Xe_W.js` registers source search through `sourceSearchSource`, and `review-runtime-bridge-CZUIqW4U.js` owns the shared `content-search-input` with `Search chat` / `Search diffs` scopes. Orchestrator now removes the visible inline file-source search field and routes focused file-tab `Cmd+F` into shared thread find in diff scope, with query/step/close events driving source line highlights and active-match metadata. Focused Files smoke passed with `fileSourceSearch=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779842770987.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779842770987.png`. Full side-panel visual inventory passed with 26 captures, manifest created at `2026-05-27T00:52:33.639Z`, and comparison report passed at `2026-05-27T00:52:37.405Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`.
- Files notebook output/source chrome checkpoint: Codex's `notebook-preview-panel-CAO-aRhM.js` renders notebook cell source bodies with `px-4 py-3` and code-cell outputs in a separate `border-t` secondary-tinted band with `px-4 py-3`, `gap-3`, and an inset top highlight. Orchestrator now wraps source content in a padded source body and renders outputs in a separate bordered, lightly tinted output band with 16 px horizontal padding, 12 px vertical padding, 12 px output gap, and an inset top highlight. Focused Files smoke passed with `filesNotebookOutputChrome=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779835804010.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779835804010.png`. The visual-inventory wrapper now time-boxes individual captures and accepts completed output JSON plus copied screenshots with all checks passing, preventing child shutdown hangs from blocking a comparable manifest. Full side-panel visual inventory passed with 26 captures, manifest created at `2026-05-26T22:59:57.583Z`, and the comparison report passed at `2026-05-26T23:00:10.516Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`.
- Files notebook stream/text output item chrome checkpoint: Codex's `notebook-preview-panel-CAO-aRhM.js` renders summarized stream/text outputs as rounded `bg-token-main-surface-primary/40 p-3` item containers and raw payloads as rounded code-block surfaces with `p-3`. Orchestrator now gives stream/text notebook output items Codex-like rounded, lightly tinted item containers and 12 px raw-output padding. Focused Files smoke passed with `filesNotebookOutputItemChrome=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779837123625.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779837123625.png`.
- Files notebook rich-output item chrome checkpoint: Codex's `notebook-preview-panel-CAO-aRhM.js` renders markdown/image outputs as contained item surfaces and error outputs as red-tinted, bordered `p-3` containers. Orchestrator now uses a separate `notebook-output-types-smoke.ipynb` fixture to prove markdown, error, and image notebook output item chrome without destabilizing the existing stream/text notebook fixture. Focused Files smoke passed with `filesNotebookRichOutputItemChrome=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779837960139.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779837960139.png`. The visual-inventory runner now launches each capture directly with a process-group timeout, fixing the full-inventory hang observed in `diff-core`. The refreshed full visual inventory for that slice passed with 26 captures and `filesNotebookRichOutputItemChrome=true` in the Files capture; Files evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779838398949.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779838398949.png`; manifest created at `2026-05-26T23:36:53.060Z`; comparison report created at `2026-05-26T23:40:07.774Z` with `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`. Provider-backed artifact metadata and full artifact renderer fidelity remain open.
- Files notebook execution-count checkpoint: Codex's `notebook-preview-panel-CAO-aRhM.js` preserves code-cell `execution_count`, renders `Cell N of M`, shows `Run N`, and exposes a disabled per-cell run affordance in the read-only notebook preview. Orchestrator now preserves `execution_count`, renders cell position and `Run N` labels, and keeps per-cell run disabled. Focused Files smoke passed with `filesNotebookExecutionCount=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779806945035.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779806945035.png`. Full side-panel comparison passed with 25 captures, `fixture-covered=8`, `aligned=2`, `mismatch=0`, and manifest created at `2026-05-26T14:54:12.281Z`.
- Terminal default-height checkpoint: current Codex root app-shell evidence from `tmp/codex-app-assets/app-shell-Bh-lgoQk.js` stores `app-shell:bottom-panel-height` with a 280 px fallback, clamps through `Tn`, and passes `defaultSize: 280` to the top-edge resize handle, while `thread-page-bottom-panel-state-D1Lz0U4Y.js` keeps Terminal content below shared bottom-panel chrome. Orchestrator now opens and resets Terminal to 230 px content plus 50 px shared chrome for a 280 px total bottom panel, migrates old 260 px and 350 px stored defaults to the Codex-aligned height, and smoke-gates the total through `terminalBottomPanelSizeDecomposition=true`, `terminalResizeReset=true`, `terminalResizeKeyboard=true`, and `terminalSharedAnimationController=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779813550275.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779813550275.png`. The visual Terminal path also gates the same size decomposition with healthy content; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-visual-1779813551158.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-visual-1779813551158.png`.
- Workbench transfer comparison checkpoint: Codex's `app-shell-tab-controller-B2eCi4Le.js` exposes shared `moveTabTo` / `receiveMovedTab` controller semantics while the current bottom-panel renderer is still terminal-specific. Orchestrator now uses generic transfer copy in both directions (`Move tab to right panel` and `Move tab to bottom panel`), keeps Review/Files/Browser right-to-bottom moves as explicit unsupported boundaries, and includes a separate `terminal-behavior` capture in the full side-panel inventory so transfer behavior is directly comparable without overloading the stable Terminal screenshot. Focused Terminal smoke passed with `terminalSharedTransferModel=true`, `terminalMoveToRightPanel=true`, and `terminalMoveBackToBottom=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779822058884.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779822058884.png`. Focused right-panel smoke passed with `rightPanelTransferUnsupportedBoundary=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779822056924.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779822056924.png`. Full visual inventory now passes with 26 captures and the comparison report remains green at `fixture-covered=8`, `aligned=2`, `mismatch=0`, `needsSmoke=0`, and `needsProof=0`.
- Sidebar top-actions and width checkpoint: live Codex design evidence is now available through a macOS screenshot route even though direct Computer Use inspection of `com.openai.codex` remains blocked. The captured Codex left sidebar shows `New chat`, `Search`, `Plugins`, and `Automations` as the top utility rows above Pinned/Projects/Chats, with a roughly 240 px sidebar instead of Orchestrator's older wide 300 px layout. Orchestrator now mirrors that top stack in the left sidebar with Codex-like compose/search/grid/clock icons, wires New chat to session creation, Search to the command palette, Plugins to the Capabilities surface, and Automations to Settings > Automations, and fixes the sidebar width at 240 px. Focused Sidebar smoke passed with `sidebarPrimaryActionsCodexOrder=true`, `sidebarPrimaryActionsBeforePinned=true`, `sidebarSearchPrimaryAction=true`, `sidebarPluginsPrimaryAction=true`, `sidebarAutomationsPrimaryAction=true`, and the tightened `sidebarWidthToken=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779775263541.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779775263541.png`.
- Live Review rollback proof: added `npm run live:codex-review-appserver`, which creates a disposable git workspace, runs a real Codex app-server turn that edits `live-review-proof.txt`, captures raw `turn/diff/updated` JSONL plus normalized `diff.updated` events, probes `thread/rollback`, and archives the throwaway persisted thread. Live proof passed with 3 normalized diff events, no checkpoint ids, and a successful `thread/rollback` call using `{ threadId, numTurns: 1 }`; artifact: `/Users/nadav/Desktop/Orchestrator/tmp/codex-review-appserver-live-proof/result.json`. The rollback response removed the turn from thread history but did not revert the workspace file or git diff, so provider Review Undo remains intentionally disabled until a real working-tree checkpoint restore path exists.
- Live Composer app-server proof: added `npm run live:codex-composer-appserver`, which creates a disposable git workspace, starts the real Codex app-server with Orchestrator's Codex model/effort/policy mapping, verifies the returned `gpt-5.4-mini` / low-effort / `on-request` / `workspace-write` managed permission profile, observes a completed `commandExecution` item for the proof command, checks the git-visible proof edit, and records the final `CODEX_COMPOSER_LIVE_OK` assistant token. Artifact: `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-appserver-live-proof/result.json`. The current allowed workspace-write command produces `permissionRequestCount=0`, so explicit approval-card proof remains separate from this model/policy/command-execution proof.
- Live Composer command-approval proof: added `npm run live:codex-composer-approval`, which reuses the Codex composer proof harness but asks Codex to write just outside the disposable workspace so the managed permission profile requires approval. The proof observes `item/commandExecution/requestApproval` with numeric JSON-RPC id `0`, answers it, observes `serverRequest/resolved`, verifies the completed command and parent-directory proof file, and records `CODEX_COMPOSER_LIVE_OK`. Artifact: `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-approval-live-proof/result.json`.
- Review provider checkpoint metadata boundary: Codex app-server `turn/diff/updated` events now preserve provider session id, turn id, and optional checkpoint id on the normalized `diff.updated` event. The transcript Last turn Review card exposes those fields, distinguishes `missing-checkpoint` from unsupported checkpoint Undo, and keeps provider Undo disabled instead of falling back to local git discard. Focused provider coverage passed with `npm run test:providers` at 292 tests; focused Review source smoke passed with `reviewTranscriptCardLastTurn=true` and `reviewLastTurnGitApplyCommand=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779771068089.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779771068089.png`. Real provider checkpoint restore remains blocked until a provider supplies a checkpoint id plus a rollback API.
- Files PDF artifact-header checkpoint: Codex's `artifact-tab-content.electron-DayvYBGS.js` routes PDF artifacts through `PdfPreviewPanel` with artifact-owned header/right-content handling, and renders a header plus fallback state when PDF loading fails instead of exposing a bare iframe. Orchestrator PDF file previews now use the shared file-preview header chrome with PDF badge, size, Copy path/Open/Reveal controls, and a contained iframe body, while keeping the artifact import-kind/source boundary from the prior slice. Focused `npm run smoke:ui:auto -- --files` passed with `filesPdfPreview=true`, `filesPreviewHeaderShared=true`, `filesArtifactPreviewControls=true`, and `filesArtifactTabModel=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779677788403.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779677788403.png`.
- Browser-use no-mutation checkpoint: Codex's Browser manager treats browser-use viewport/capture/cursor state as capture/paint-host metadata layered over the existing browser route, not as a tab/navigation/composer mutation. Orchestrator's Browser smoke now snapshots the visible Browser panel, active body-hosted webview object, body host, active tab id, URL/address, partition/source host, viewport mode, hidden webview count, and composer text before browser-use activation, then proves the same visible browser state survives browser-use activation and teardown while the capture/cursor metadata turns on and off. Focused Browser smoke passed with `browserUseNoMutation=true` plus all existing Browser checks; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779678965678.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779678965678.png`.
- Files artifact-tab model checkpoint: Codex's `artifact-tab-file-kind-fsu6JKhI.js` maps side-panel artifact import kinds (`csv`, `docx`, `ipynb`, `pdf`, `pptx`, `tex`, `tsv`, `xlsm`/`xlsx`) to document/notebook/pdf/slides/spreadsheet artifact types and only exposes artifact source viewing for `csv`, `ipynb`, `tex`, and `tsv`. Orchestrator now has a shared provider-agnostic artifact-tab classifier, exposes artifact type/import-kind/source-support metadata on Workbench file tabs, and adds a rendered `View source` action for source-capable artifact previews without exposing it for DOCX-style document artifacts. `pnpm test src/main/__tests__/artifactTabs.test.ts` and `pnpm exec tsc --noEmit` passed. Focused Files smoke passed with `filesArtifactTabModel=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779677434399.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779677434399.png`.
- Full side-panel visual inventory refresh checkpoint: refreshed the full screenshot matrix after the Browser body-host slice and stale Review smoke-gate cleanup. `npm run smoke:visual:side-panels -- --out tmp/side-panel-visual-inventory-current --full` passed with 23 captures and no failures; manifest: `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-current/manifest.json`. The refresh includes Review entry, empty, loading, narrow, core, source, and preview screenshots; focused Review empty evidence now passes with `reviewEmptyStateCalm=true` at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-empty-1779674872016.json`, and Review loading evidence passes with `reviewLoadingKeepsSidePane=true` at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-loading-1779674876904.json`. This is current local screenshot proof, not final live Codex side-by-side pixel parity.
- Shell transfer-boundary checkpoint: Codex's `app-shell-tab-controller-B2eCi4Le.js` exposes generic `moveTabTo` / `receiveMovedTab`, while the current thread bottom-panel bundle remains terminal-specific. Orchestrator now keeps the shared transfer model explicit: terminal tabs can move right-to-bottom and bottom-to-right, while Review, Files, and Browser right-to-bottom attempts expose a shared unsupported-tab-kind boundary instead of silently pretending bottom renderers exist. `pnpm test src/main/__tests__/panelTabs.test.ts` and `pnpm exec tsc --noEmit` passed. Focused `npm run smoke:ui:auto -- --right-panel` passed with `rightPanelTransferUnsupportedBoundary=true` now covering Review, Files, and Browser menu metadata; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779676886741.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779676886741.png`.
- Browser cross-host DOM webview transfer checkpoint: Codex's `browser-sidebar-manager-ivre5jEI.js` keeps a manager-level webview map and exposes `transferWebview(sourceConversationId, targetConversationId)` to reassign the existing webview plus browser-use state to the fork target. Orchestrator now carries Browser transfer source/target host metadata through `transferBrowserWorkbench`, lets the target Browser manager inherit the source webview host/partition while exposing the target panel host, and smoke-gates that the active body-hosted webview DOM object is the same object after a local fork. Focused Browser smoke passed with `browserForkDomTransfer=true` and all existing Browser checks; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779676488818.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779676488818.png`. Focused right-panel smoke passed after the transfer metadata change; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779676519116.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779676519116.png`.
- Browser body-attached webview host checkpoint: Codex's Browser manager owns real `webview` elements through body-attached containers with fixed visible geometry and hidden paint-host containment (`browser-sidebar-manager-ivre5jEI.js`). Orchestrator's `BrowserWebviewManager` now portals active and hidden Browser webviews into `document.body`, keeps the panel subtree as the measurement/overlay/controller surface, marks body-host ownership on the manager/webview DOM, and smoke-gates active body-fixed containment plus hidden body-host persistence. Focused Browser smoke passed with `browserWebviewManagerBoundary=true`, `browserHiddenWebviewContainment=true`, `browserCommentPreviewOriginal=true`, `browserCommentDesignTweak=true`, and the existing Browser lifecycle/local-target/inspector/fork-transfer gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779673771673.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779673771673.png`. Focused right-panel smoke also passed after the body-host move with Browser command routing and visual reset intact; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779673813372.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779673813372.png`.
- Browser host-scoped partition checkpoint: Codex's Browser manager scopes webview storage by conversation/app route (`browser-sidebar-manager-ivre5jEI.js` uses per-conversation browser route partitions and body-owned webview lifecycle records). Orchestrator now derives Browser webview partitions from the Browser host id instead of sharing one global `persist:orchestrator-side-browser` partition across sessions/tabs, exposes host-scoped partition metadata on the Browser panel/manager/webviews, and routes Browser clear-data actions to the current host partition with a guarded main-process fallback. Focused helper coverage passed in `pnpm test src/main/__tests__/browserManagerEvents.test.ts`; `pnpm exec tsc --noEmit` passed. Focused Browser smoke passed with `browserWebviewManagerBoundary=true`, `browserClearData=true`, `browserWebviewPersistence=true`, and the existing Browser lifecycle/comment/inspector/local-target/fork-transfer gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779673424882.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779673424882.png`.
- Sidebar Codex thread-list items/nested metadata checkpoint: Codex's current sidebar bundle uses `items`-style inbox/task collections and nested local conversation, remote task, and pending-worktree records (`sidebar-thread-list-signals-FpAb9VJn.js`, `sidebar-project-group-signals-B2IlZT8R.js`). Orchestrator's provider sidebar metadata adapter now accepts `items` in addition to array/`data`/`threads`, normalizes nested `conversation`, `task`, and `pendingWorktree` records, and projects projectless, cloud, remote-host, worktree, preview, and timestamp metadata onto matching Codex sessions. Focused unit coverage passed via `npx tsc -p tsconfig.node.json --outDir out-test --module commonjs` plus `node --test out-test/src/main/__tests__/sessionOrdering.test.js`; `pnpm exec tsc --noEmit` passed. Focused Sidebar smoke passed with the existing provider metadata/grouping gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779672991967.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779672991967.png`.
- Review selected-file workspace checkpoint: Codex's Review model keeps an active file path and pairs the main file renderer with the right-side changed-files rail (`review-navigation-model-T6VawcmA.js`, `review-file-tree-side-pane-CGo_VDrf.js`, `review-file-source-tab-CjS7Xe_W.js`). Orchestrator's detailed Review main pane now renders only the selected file instead of stacking every changed file vertically, while preserving the changed-files rail as the multi-file navigator. Focused smoke gates `data-review-main-render-mode="selected-file"` plus a single visible Review file section backed by the full changed-file count. Focused Review core smoke passed; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1779671713133.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1779671713133.png`. Focused Review source smoke passed after updating source/filter assertions to select files through the rail; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779672261620.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779672261620.png`.
- Review metadata comments checkpoint: Codex's Review toolbar/flyout has a comments lane alongside PR, checks, and reviewers. Orchestrator's GitHub PR metadata adapter now requests `comments`, normalizes a general PR comment summary with authors and URL, renders a comments row in the shared Review metadata flyout, and gates it in the focused Review entry smoke. Focused `npm run smoke:ui:auto -- --diff-entry` passed with `reviewMetadataToolbar=true` and `reviewMetadataFlyoutShared=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-entry-1779767282058.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-entry-1779767282058.png`. The following checkpoint extends this to inline/threaded summary metadata.
- Review metadata review-thread checkpoint: Orchestrator's GitHub metadata adapter now uses the PR GraphQL node id to fetch `reviewThreads`, folds inline/threaded review-comment totals, unresolved thread count, authors, and URL into the existing Review comments row, and keeps the optional GraphQL fetch isolated so PR/check/reviewer metadata still loads if review-thread access fails. Live `gh api graphql` accepted the `reviewThreads` query shape against PR #2, and focused Review entry smoke gates `data-review-metadata-comments-unresolved="1"` plus visible `1 unresolved` detail in the comments row; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-entry-1779767861626.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-entry-1779767861626.png`. Per-line provider comment rendering remains separate from this summary metadata slice.
- Review provider comment line-rendering checkpoint: the GitHub `reviewThreads` parser now also normalizes path, side, line, author, body, URL, resolved, outdated, and created-at fields into `providerCommentsByPath`. Detailed Review merges those provider comments into the existing line comment stack as read-only `provider` cards, preserves local draft/saved comments separately, and keeps side-rail comment-count badges accurate. Focused Review source smoke passed with `reviewLineComments=true` and `reviewSidePaneCommentCount=true` against a seeded GitHub provider comment plus a local saved comment on the same diff line; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779768713722.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779768713722.png`. Live commented-PR proof remains pending because PR #2 currently has no review threads.
- Review provider comment blame checkpoint: live GitHub GraphQL schema introspection confirmed `PullRequestReviewComment` exposes `commit` and `originalCommit`. Orchestrator now requests those fields for review-thread comments, normalizes the preferred original commit into provider comment blame metadata, and renders the commit/author on read-only provider comment cards with smoke-readable attributes. Focused Review source smoke passed with `reviewLineComments=true`, now including provider comment blame checks; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779773532407.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779773532407.png`. Live commented-PR proof is still pending; this is real adapter-shape support plus fixture-backed rendering proof.
- Review merge-conflict action-helper checkpoint: Codex's current `file-diff-DVuLEIDo.js` injects merge-conflict utilities through `renderMergeConflictUtility` with `merge-conflict-action-${hunkIndex}-${lineIndex}-${conflictIndex}` slots. Orchestrator Review now preserves local Git unmerged state as `FileChange.status = "U"` with `conflicted` metadata, renders the Conflict badge/glyph, detects conflict marker blocks in unresolved local files, shows a compact Current / Incoming / Both helper beside the marker line with Codex-shaped slot metadata, rewrites only the chosen marker block, and refreshes Review content without pretending the conflict is staged or marked resolved. `pnpm test src/main/__tests__/gitChanges.test.ts` passed with conflict-marker diff coverage. Focused `npm run smoke:ui:auto -- --diff-conflict` passed with `reviewMergeConflictHelpers=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-conflict-1779675864451.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-conflict-1779675864451.png`. Focused `--diff-core` passed after the shared diff-line renderer changes; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1779675933405.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1779675933405.png`.
- Settings Personalization/Pet overlay taxonomy checkpoint: Codex's current `personalization-settings-YskFHfNU.js` reads `selectedHostId` from `settings-host-context-B5kZhSF6.js` and loads host-scoped memory, personality, and custom-instructions surfaces. Orchestrator now separates that Host `Personalization` route from its app-specific `Pet overlay` settings, keeps Personalization explicitly unavailable until a real provider host adapter exists, and treats Pet overlay as app-scoped so it no longer masquerades as Codex Personalization. Focused Settings smoke passed with `settingsPersonalizationHostBoundary=true`, `settingsSidebarGroupedNav=true`, and the split App/Host labels; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779678561891.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779678561891.png`. Focused Pets smoke passed after the rename with `petsSettingsContentLayout=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-pets-1779678586608.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-pets-1779678586608.png`.
- Settings app-protocol deep-link checkpoint: Codex Settings is route-owned and host-context aware (`settings-page-mdGEmNCB.js`, `settings-back-route-Cfgk-CZa.js`, `settings-host-context-B5kZhSF6.js`). Orchestrator now parses `orchestrator://settings/<section>?host=...`, routes it through main/preload/renderer pending navigation, and preserves a non-local deep-linked host while Settings preferences load. Focused `npm run smoke:ui:auto -- --settings-deeplink` passed with `settingsDeepLinkRoute=true`, proving `/settings/providers?host=codex%3Aremote-mac`, active section `providers`, host id `codex:remote-mac`, and host kind `remote`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-deeplink-1779679851579.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-deeplink-1779679851579.png`.
- Settings packaged renderer-route checkpoint: Codex Settings owns `/settings/:section/*` routes in `settings-page-mdGEmNCB.js`, so Orchestrator packaged windows now load the main renderer through a privileged internal `orchestrator-app://renderer/` protocol instead of `file://.../index.html`. The custom protocol serves bundle assets normally and maps `/settings/...` back to `index.html`, letting Settings use `/settings/providers?host=...` paths in packaged builds while preserving the old `file://` hash parser as a direct-file fallback. `pnpm test src/main/__tests__/settingsRoutes.test.ts`, `pnpm exec tsc --noEmit`, and `npm run pack:mac` passed. Packaged `npm run smoke:ui:auto -- --settings-deeplink --packaged` passed with `settingsDeepLinkRoute=true`; route debug proves `protocol: "orchestrator-app:"`, `pathname: "/settings/providers"`, empty hash, and host id `codex:remote-mac`. Evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-deeplink-1779681835187.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-deeplink-1779681835187.png`.
- Review diff-content search checkpoint: Codex's Review changed-files rail participates in diff-domain search through `review-file-tree-side-pane-CGo_VDrf.js`. Orchestrator's Review rail now indexes loaded diff text and preview text in addition to paths, keeps content-matching files visible, and shows `data-review-file-search-match-count` badges beside matching rows. Focused `npm run smoke:ui:auto -- --diff-source` passed with `reviewSearchContent=true` for a query present only in the diff body; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779680223342.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779680223342.png`.
- Review search active-match checkpoint: Codex's Review search model keeps active match ids and caps result collection at 250 in `review-file-tree-side-pane-CGo_VDrf.js`. Orchestrator's Review rail now adds previous/next match controls, a capped 250-result navigation list over loaded diff/preview content, `250+` count semantics, root/control active-path metadata, and active row highlighting via `data-review-search-active`. Focused `npm run smoke:ui:auto -- --diff-source` passed with `reviewSearchContent=true` after clicking `review-search-next-match`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779680746643.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779680746643.png`.
- Review search diff-body active-line checkpoint: Codex marks real diff text matches with `codex-thread-find-match`, marks the active hit with `codex-thread-find-active`, maps diff results to line locations, and scrolls the active hit into view. Orchestrator now threads the active Review search match ordinal into the rendered `DiffLines` surface, exposes per-line match metadata, highlights the active diff row with `data-review-search-active-line`, and scroll-reveals it when stepping matches. Focused `npm run smoke:ui:auto -- --diff-source` passed with `reviewSearchContent=true` while additionally proving the selected `Nested Folder/nested note.md` diff body has an active visible search line containing `review tree grouping`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779681153265.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779681153265.png`. Remaining Review search parity is cross-conversation/thread-find integration, mark-level inline text wrapping, exact virtualized reveal/scroll behavior, over-cap UI beyond `250+`, and provider-backed review metadata/source gaps.
- Review large-diff threshold checkpoint: Codex's current `parse-diff-BXe0fwiB.js` marks individual large files at Codex-scale thresholds: more than 15,000 changed lines, more than 3 MB changed bytes, or a changed line over 1 MB. Orchestrator's shared `resolveReviewDiffRenderWindow` now uses those changed-line/byte/max-line thresholds instead of treating ordinary 3k-row diffs as large, while still rendering a bounded 600-row preview once a file crosses the Codex-scale guard. Focused Review source smoke passed with `reviewLargeDiffWindow=true` against a 15,202 changed-line fixture; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779670756786.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779670756787.png`.
- Settings destructive-dialog checkpoint: Codex's shared dialog layout remains the reference for compact destructive confirmations (`dialog-layout-D-x8fMKh.js`, `automation-dialog-BYgYAePq.js`, `automation-shared-9U9-pQH8.js`). Orchestrator Settings Worktrees, Automations, and Data controls now use the shared `ConfirmDialog`/`DialogContent`/`DialogHeader`/`DialogFooter` path instead of native `window.confirm`, with `data-testid` hooks for destructive confirmation surfaces. Focused Settings smoke passed with the Worktrees delete flow asserting shared dialog structure before deletion; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779670138096.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779670138096.png`.
- Worktrees Settings search checkpoint: Orchestrator's shared Settings topbar search now includes a Worktree create subtarget, and the Worktrees page exposes a focusable `worktrees-create` anchor around project/base ref/branch controls. Focused Settings smoke passed with `settingsWorktreesInPageSearchTarget=true`, proving a `base ref` query routes to Worktrees, marks the search chip with `data-settings-search-target-anchor="worktrees-create"`, focuses the Create group, and preserves the existing Settings gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780031328081.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780031328081.png`.
- Settings app-router route checkpoint: Codex's current Settings page is route-scoped at `/settings/:section/*` with a host-context path (`settings-page-mdGEmNCB.js`, `settings-back-route-Cfgk-CZa.js`, `settings-host-context-B5kZhSF6.js`). Orchestrator centralizes Settings route parsing/building in shared types, uses real `/settings/<section>?host=...` paths when the renderer is served over HTTP/dev or the packaged `orchestrator-app://renderer/` protocol, and keeps `#/settings/<section>` as a direct `file://` fallback parser. Focused Settings smoke passed with `settingsRouteOwned=true`, `settingsHostContext=true`, `settingsHostSectionFiltering=true`, and `settingsHostAdapterBoundary=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779669631857.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779669631857.png`. Packaged Settings deep-link smoke passed with `orchestrator-app:` path-route debug; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-deeplink-1779681835187.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-deeplink-1779681835187.png`. Provider Settings smoke also passed with `/settings/providers`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1779669653727.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1779669653727.png`.
- Browser local-server route-normalization checkpoint: Codex's Browser sidebar consumes provider-reported local-server route notifications, and development servers commonly advertise `0.0.0.0` bind addresses even though the navigable loopback target is `127.0.0.1`. Orchestrator now normalizes provider/renderer local-server routes from `0.0.0.0` to `127.0.0.1`, preserves loopback IPv4/IPv6 hosts, rejects remote hosts, strips hashes, and smoke-gates that the normalized route appears under the live local target. Focused Browser smoke passed with `browserLocalServerRouteNormalization=true` plus all existing Browser lifecycle, local-target, comment, inspector, menu, fallback, and shell-tab gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779669295817.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779669295817.png`.
- Files content-search checkpoint: Codex's workspace browser uses a first-class fuzzy file-search session (`use-workspace-file-search-C73UkUkc.js`) and shared file-tree search UI instead of a renderer-only crawl. Orchestrator's shared `searchWorkspace` path now keeps path search primary while adding bounded small-text content hits with line/snippet metadata for Files. Focused Files smoke passed with `filesContentSearch=true` plus existing file tree, file-tab, source-search, preview, action-menu, fallback, lazy-directory, and sticky-folder gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779668812086.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779668812086.png`.
- Sidebar pinned-order checkpoint: Codex's pinned-thread bridge exposes ordered pin mutations (`set-thread-pinned` with `beforeThreadId` and `set-pinned-threads-order`). Orchestrator now supports local pinned-row drag reorder in the Chat Sidebar through a shared ordering helper, persisted session pin orders, main/preload IPC, and smoke-visible `data-sidebar-pinned-reorder="local"` state. Focused Sidebar smoke passed with `sidebarPinnedDragReorder=true` plus the existing provider pinned, projectless, worktree, custom-section, action-menu, automation, and row-state gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779668262567.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779668262567.png`.
- Review shared source-viewer checkpoint: Codex's Review source tab path uses the same file-source viewer model as workspace file tabs, including selected source lines, gutter utilities, blame, and virtualized source rendering. Orchestrator's detailed Review `Load full files` / source-preview path now wraps the shared `FilePreview` source renderer instead of its bespoke source-line list, while preserving Review-specific blame controls and smoke-visible state. Focused Review source smoke passed with `reviewFullSourceRows=true`, `reviewFullSourceBlame=true`, `reviewLoadFullFile=true`, `reviewLineOpensFileSourceTab=true`, hidden-context, and large-diff gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779667607626.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779667607626.png`.
- Review side-pane resize checkpoint: Codex's changed-files rail uses the shared left-edge resize primitive path from `review-file-tree-side-pane-CGo_VDrf.js`, not a local button. Orchestrator's Review changed-files pane now uses the shared `PanelResizeHandle` and `useAppShellResizeController` path with separator semantics, left-edge metadata, ARIA value bounds, pointer drag, keyboard resize, and double-click reset. Focused Review source smoke passed with `reviewSidePaneResize=true` plus all source/comment/blame/hidden-context/large-diff gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779666509234.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1779666509234.png`.
- Terminal bottom-panel size-decomposition checkpoint: Codex's shared bottom-panel animation primitive animates the whole panel size while the Terminal surface owns a content region below shared chrome. Orchestrator now exposes and smoke-gates Terminal content height, chrome height, total shell height, and default/reset height across the bottom shell, chrome, and tab panel; the focused Terminal smoke verifies the app-shell animation target equals content height plus chrome height. Evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779666210652.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779666210652.png`.
- Settings content-layout checkpoint: Codex's `settings-content-layout-Bnulb0lM.js` uses a reusable content-level title/subtitle/action header above the scroll body. Orchestrator now has a shared `SettingsContentLayout` primitive across the local Settings pages: General, Appearance, Providers, Automations, Worktrees, Shortcuts, Personalization, Browser, and Data controls. Focused Settings smoke passed with `settingsContentLayout=true` across the migrated pages plus `settingsBrowserPage=true`, `settingsBrowserSurface=true`, `settingsBrowserModule=true`, and all existing Settings gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779854070171.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779854070171.png`. Dedicated Pets/Personalization smoke passed with `petsSettingsContentLayout=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-pets-1779667146960.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-pets-1779667146960.png`.
- Shared shell transfer-boundary checkpoint: Codex's app-shell tab controller exposes generic `moveTabTo` / `receiveMovedTab` semantics across right and bottom controllers. Orchestrator now exposes a shared transfer capability resolver and a session-level `transferSessionPanelTab` action, routes existing terminal bottom/right moves through that shared action, forwards menu-surface metadata hooks, and marks non-terminal right-to-bottom moves as explicitly unsupported instead of hiding the boundary. Focused right-panel smoke passed with `rightPanelTransferUnsupportedBoundary=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779664174471.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779664174471.png`. Focused Terminal smoke still passes with `terminalSharedTransferModel=true`, `terminalMoveToRightPanel=true`, and `terminalMoveBackToBottom=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779664201401.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779664201401.png`.
- Shared panel animation checkpoint: Codex's app-shell panel animation bundle exposes mounted state, animated size, opacity, and progress from one shared primitive. Orchestrator's `MotionPanel` now has the same shared animation model for right and bottom panels, exposes deterministic animation/progress/animated-size/target-size/mounted metadata, respects reduced motion, and drives panel width/height through the shared animation state instead of only CSS transitions. Focused Terminal smoke passed with `terminalSharedAnimationController=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779663282115.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779663282115.png`. Focused right-panel smoke passed with `rightPanelSharedAnimationController=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779663345466.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779663345466.png`.
- Shared resize accessibility checkpoint: the shared app-shell resize separator now supports keyboard resizing and exposes `aria-valuemin`, `aria-valuemax`, and `aria-valuenow` in addition to the Codex-style separator role, overlay geometry, pointer drag, and double-click reset. This is an Orchestrator accessibility improvement on the shared primitive: the current Codex resize chunk centralizes pointer resize behavior and separator semantics but does not expose keyboard handling. Focused Terminal smoke passed with `terminalResizeKeyboard=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779662894867.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779662894867.png`. Focused right-panel smoke passed with `rightPanelResizeKeyboard=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779662896888.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779662896888.png`.
- Shared resize-controller checkpoint: Codex app-shell resize behavior is centralized in the shell primitive, with edge-aware delta math, zoom-normalized pointer coordinates, pointer-cancel cleanup, below-min handling, and double-click reset. Orchestrator now has the same shared controller behind Terminal top-edge and Workbench left-edge resize separators instead of duplicate local drag state. Focused Terminal smoke passed with `terminalResizeReset=true` and `terminalResizeHandleOverlay=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779662391410.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779662391410.png`. Focused right-panel smoke passed with `rightPanelResizeReset=true` and `rightPanelCloseBelowMin=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779662393272.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779662393272.png`.
- Shared resize-handle separator checkpoint: Codex app-shell resize handles render as `role="separator"` elements with `aria-orientation`, not as buttons. Orchestrator's shared `PanelResizeHandle` now uses the same separator semantics while preserving edge metadata, pointer drag, and double-click reset. Focused Terminal smoke verifies the separator handle through `terminalResizeReset=true` and `terminalResizeHandleOverlay=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779662101956.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779662101956.png`. Focused right-panel smoke also passed: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779662098184.json`.
- Shared resize-handle overlay checkpoint: the current Codex bundle renders app-shell resize handles as absolute 16 px overlay separators translated half outside the panel edge, so they do not consume panel layout space. Orchestrator's shared `PanelResizeHandle` now follows that geometry for Terminal top-edge and Workbench left-edge handles. Focused Terminal smoke verifies `terminalResizeHandleOverlay=true` and `terminalResizeReset=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779661796903.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779661796903.png`. Focused right-panel smoke also passed after the shared CSS change: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779661818780.json`.
- Terminal resize/reset checkpoint: shared shell resize handles now expose edge metadata, Terminal owns a top-edge handle, Workbench owns a left-edge handle, and the Terminal smoke drags the top-edge handle before double-clicking back to the default height. Focused Terminal smoke verifies `terminalResizeReset=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779661460460.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779661460460.png`. Terminal visual smoke also passed: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-visual-1779661489167.json`.
- Terminal content-spacing checkpoint: Codex's terminal xterm boundary uses line-height `1.2`, terminal token background/foreground, and `px-4 pb-3` content spacing. Orchestrator now matches those rendered terminal-surface values and focused Terminal smoke verifies `terminalContentSpacing=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779660921070.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779660921070.png`. Terminal visual smoke also passed: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-visual-1779660953202.json`.
- Terminal shared-header chrome checkpoint: Terminal bottom-panel chrome now uses the shared `app-shell-panel-chrome` / `PanelTabStrip` path instead of terminal-local header/tabstrip CSS. Focused Terminal smoke verifies `terminalHeaderSharedChrome=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779660570424.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779660570424.png`. Terminal visual smoke also passed: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-visual-1779660598742.json`.
- Terminal token-matrix checkpoint: the current installed Codex bundle maps `--vscode-terminal-background`, foreground, selection, inactive selection, and all normal/bright ANSI terminal tokens into xterm. Orchestrator now has a shared terminal token map, resolves terminal-surface tokens before root tokens, exposes the resolved xterm-facing matrix on the Terminal surface, and focused Terminal smoke verifies `terminalThemeTokenMatrix=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779660167397.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779660167397.png`.
- Terminal service snapshot checkpoint: Codex keeps terminal service/session state separate from visible terminal panel state, and Orchestrator now exposes a terminal service snapshot through main IPC/preload with per-terminal id, work directory, status, buffer length, and exit state. Focused Terminal smoke verifies `terminalServiceSnapshot=true` across the bottom terminal and bottom-to-right transfer path; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779659776450.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779659776450.png`.
- Shared panel-tab transfer checkpoint: Codex's app-shell controller exposes shared move/receive semantics, so Orchestrator now has a shared `transferPanelTab` primitive and the existing Terminal bottom/right move flow uses it instead of terminal-specific remove/add state. Focused Terminal smoke verifies `terminalSharedTransferModel=true`, `terminalMoveToRightPanel=true`, `terminalRightPanelNewTabShortcut=true`, and `terminalMoveBackToBottom=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779659230452.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779659230452.png`.
- Browser local-target chooser checkpoint: Browser empty-state local servers now render as a compact icon-first chooser instead of a wide table-like list with repeated URL metadata. Focused Browser smoke verifies `browserLocalTargetsCompactChooser=true` alongside existing Browser lifecycle/comment/inspector/menu gates; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779658694849.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779658694849.png`. Focused right-panel visual smoke captured the corrected chooser at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779658718345.png`.
- Sidebar row-slot checkpoint: session rows are now text-first like the current live Codex screenshot, while provider/thread/label-color identity remains row-shell metadata and pin/unpin lives in the consolidated trailing action slot next to the chat actions menu. Focused Sidebar smoke gates `sessionRowsTextFirst=true`, `sidebarPinnedRowsTextFirst=true`, and `sidebarPinActionsConsolidated=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779801617818.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779801617818.png`. Full visual inventory passed with 24 captures and refreshed `chat-sidebar.png` at `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-current/chat-sidebar.png`.
- Multi-window command-routing checkpoint: app menu commands now target an app-owned active Orchestrator window instead of the most recently created window. `npm run smoke:ui:auto -- --multi-window-focus` now gates `activeWindowAfterRefocus=true`, `focusSwitchRestoresFirstWindowMenu=true`, and `menuCommandRoutedToFocusedWindow=true` after opening a second window and switching command state back to the first. Evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-multi-window-focus-1779624345721.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-multi-window-focus-1779624345721.png`. Raw Electron `BrowserWindow.getFocusedWindow()` still reports false after programmatic refocus in this automation path, so live/manual OS focus proof remains separate.
- Review Environment action-state checkpoint: Environment now has direct visual smoke coverage. It shows staged/unstaged change state, a Commit row that routes to Review, and PR metadata as an enabled `View pull request` row; `--environment` passes with `environmentPanelVisual=true`, `environmentActionRows=true`, and `environmentSources=true`. Full side-panel inventory now has 23 captures including `environment.png`.
- Workbench new-tab surface checkpoint: the right-panel plus control now opens a real Codex-style `New tab` Workbench tab/page with action cards for Files, Side chat, Browser, Review, and Terminal. Focused right-panel smoke verifies `workbenchPanelNewTabPage=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779619959129.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779619959129.png`.
- Workbench tab close checkpoint: Codex's root app-shell tab renderer places closable tab controls on the start edge (`start-0`) with `codex.tabs.closeNamed` labels. Orchestrator's shared `PanelTabStrip` now uses the same start-edge close geometry instead of right-edge close padding, and focused right-panel smoke verifies `workbenchPanelTabCloseStartEdge=true`; evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779815710407.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779815710408.png`.
- Packaged right-panel shell checkpoint: `npm run pack:mac` rebuilt `dist/mac-arm64/Orchestrator.app`, and `npm run smoke:ui:auto -- --packaged --right-panel` passed against the packaged app with `rightPanelMenuCommandState=true`, `rightPanelBrowserCommandRouting=true`, `rightPanelFindShortcutRouting=true`, and `rightPanelBrowserVisualReset=true`. Evidence JSON: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779618973456.json`; screenshot: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779618973456.png`.
- Native menu command-state checkpoint: the renderer now publishes shell-derived command availability to the main process per window, the native menu applies enabled/disabled state to shared app commands, and focused right-panel smoke verifies `rightPanelMenuCommandState=true` across Review, Browser, and main/composer focus.
- Terminal right-panel shortcut checkpoint: shared panel new-tab routing now sends `Cmd+T` to the focused panel target, so right-panel Terminal focus creates another right-panel terminal tab while Browser focus still creates Browser tabs and main chat still opens Browser; focused Terminal smoke verifies `terminalRightPanelNewTabShortcut=true`.
- Workbench Browser visual-reset checkpoint: focused right-panel smoke now resets Browser back to the normal one-tab state after proving focused Browser `Cmd+T` command routing, and verifies `rightPanelBrowserVisualReset=true`; the refreshed core visual inventory no longer preserves the temporary nested Browser tab strip in `workbench-right-panel.png`.
- Sidebar provider-worktree source checkpoint: sessions can now carry `providerWorktreeSourceRoot`, `providerWorktreeRoot`, `providerWorktreeHostId`, and `providerWorktreeHostLabel`; connection grouping splits remote-host worktree and pending-worktree rows by provider host; focused Sidebar smoke verifies `providerWorktreeMetadata=true`.
- Sidebar provider-projectless checkpoint: sessions can now carry `providerProjectless` and `providerProjectlessThreadId`; the projectless Chats group treats those provider ids as authoritative even when a local project id exists; focused Sidebar smoke verifies `providerProjectlessMetadata=true`.
- Sidebar provider-pinned checkpoint: provider-projected pinned rows now merge with local pinned rows through `providerPinned`, `providerPinOrder`, and `providerPinnedThreadKey`; focused Sidebar smoke verifies `providerPinnedMetadata=true` and pinned rows stay out of Projects/Connections.
- Sidebar provider-source checkpoint: sessions can now carry `providerThreadSource`, `providerHostId`, and `providerHostLabel`; connection grouping splits Codex cloud and remote-host records; and `npm run smoke:ui:auto -- --sidebar` verifies cloud/host fixtures without sidebar overflow.
- Review loading-state checkpoint: `npm run smoke:ui:auto -- --diff-loading` now verifies detailed Review's real per-file loading fallback, keeps the changed-files side pane visible, and captures deterministic screenshot evidence.
- Review narrow-state checkpoint: `npm run smoke:ui:auto -- --diff-narrow` now verifies the Review Workbench tab in a constrained overlay layout with contained toolbar, changed-files pane, and readable diff content.
- Review empty-state checkpoint: detailed Review now uses a centered icon/title/body no-change state instead of bare text, and `npm run smoke:ui:auto -- --diff-empty` verifies it with `reviewEmptyState=true` and `reviewEmptyStateCalm=true`.
- Workbench panel selector checkpoint: active right-panel DOM/CSS/test selectors now use provider-neutral `workbench-panel-*` naming instead of `right-sidebar-*`; focused right-panel smoke passed with `workbenchPanelChromeCompact=true`, and core side-panel visual inventory refreshed `workbench-right-panel.png`.

## Operating Rules

- This file owns the product goal, feature status, implementation backlog, and completion gates.
- Code is the authority for whether something is implemented. This file is the authority for whether it is complete enough to claim.
- A feature is not complete until it has product behavior, provider/runtime mapping, UI treatment, automated coverage, and live or no-quota verification when applicable.
- Do not expose provider capability matrices to normal users. Keep capability detail in diagnostics, settings, tests, and this plan.
- Do not add a user-visible runtime picker for normal chat. Users choose the provider and task; Orchestrator chooses the right runtime path.
- Provider-specific behavior must translate at the provider edge into Orchestrator concepts. Shared UI should not become Claude-shaped, Codex-shaped, or Copilot-shaped.
- Every new live provider transcript shape that we rely on should be saved as a fixture before being called supported.
- Mutating provider-state commands, destructive commands, account/login/logout flows, and quota-spending diagnostics must be gated by confirmation or routed to explicit settings/user-terminal handoff.

## Status Vocabulary

| Status | Meaning |
| --- | --- |
| `Complete` | Implemented, covered by automated tests, and verified live/no-quota where applicable. |
| `Implemented` | Wired in code, but still needs stronger fixture, live, GUI, or installed-app verification. |
| `Partial` | Some behavior exists, but important paths or UI states are missing. |
| `Planned` | Product behavior is specified, but implementation has not started. |
| `Research` | Needs CLI/SDK/repo verification before product behavior should be designed. |
| `Gated` | Possible, but should only run with explicit user confirmation because it is destructive, mutates provider/account state, or spends quota. |
| `Blocked` | Cannot be completed until an external dependency, auth state, provider capability, or product decision changes. |
| `Won't Do` | Intentionally out of scope; include the reason. |

## Current Product Decisions

| Decision | Current answer |
| --- | --- |
| Primary Claude path | Structured Claude CLI print mode: `claude -p --output-format stream-json --verbose --include-partial-messages`, with Sonnet by default and per-run hook settings for approvals. |
| Native Claude terminal | Removed from the normal chat runtime. Use structured Claude JSON for chat; provider-management actions should use Orchestrator-native settings or the separate user terminal, not the old Claude-native chat parser. |
| Runtime choice | Hidden from normal users. Advanced diagnostics may show runtime health, but chat should not ask users to choose JSON vs CLI. |
| Provider abstractions | Use Orchestrator-native `session`, `assistant.text`, `tool`, `permission`, `user_input`, `plan`, `agent`, `diff`, `command`, `workspace`, `attachment`, and `usage` concepts. |
| Claude slash commands | App-owned slash commands run in Orchestrator. Prompt-like project/global commands and skills should be discovered and expanded where possible. True provider TUI commands route to terminal overlay. |
| Permissions | Tool approvals and user questions are separate UX lanes. Never answer `AskUserQuestion` through the generic permission resume path. |
| Subagents | Active agents appear as compact chips above the composer. Clicking a chip opens or focuses an agent transcript tab in the sidebar. |
| Sidebar | One right sidebar with tabs for Diff, Agents, Terminal/Raw, Skills, and other secondary detail. The main transcript stays calm. |
| Headless/automation | Keep structured/headless paths for normal Claude chat and provider smoke tests. For other providers, use headless automation where it is reliable, but do not pretend it exposes full interactive UX. |
| Installation | A feature is user-ready only after the dev build works and the installed app has been rebuilt/installed when the user needs to try it. |

## Completion Target

Orchestrator is complete for Claude Code when a user can use the installed app for normal Claude coding sessions without needing to open the raw Claude TUI, except for explicitly terminal-only provider management.

The expected experience:

- Assistant text streams smoothly without horizontal overflow.
- File create/edit/delete/read/search/shell/MCP/web actions appear as concise summaries.
- Diff and file cards make repo changes inspectable without dumping patches into chat.
- Tool approvals, plan approvals, and user questions pause the run cleanly and resume correctly.
- Stop, queue, and steer work consistently while a run is active.
- Subagents are visible as active/completed chips and transcript tabs, not raw event spam.
- Slash commands, skills, MCP, plugins, agents, auth, and diagnostics are available in the right surfaces without cluttering chat.
- All provider-specific behavior maps into shared Orchestrator abstractions so other CLIs can be added without rewriting the product.

## Source Documents

These files remain useful, but this file is the root plan:

| Document | Role |
| --- | --- |
| `docs/provider-cli-spec.md` | CLI evidence and provider capability reference. |
| `docs/claude-cli-map.md` | Claude-specific CLI to Orchestrator mapping reference. |
| `docs/claude-code-support-test-matrix.md` | Historical Claude live/fixture matrix. Promote important facts here as work continues. |
| `docs/provider-capability-research.md` | Research notes from provider/open-source/SDK investigations. |
| `docs/capabilities-surface-matrix.md` | Current capability ownership matrix for Claude/Codex file-backed, plugin-backed, and provider-managed resources. |
| `docs/capability-sync-spike.md` | Spike and implementation plan for syncing/importing capabilities across Claude, Codex, and future provider projections. |

## Orchestrator-Native Abstractions

All providers should translate into these shapes at the adapter/runtime boundary.

| Abstraction | Purpose | Provider examples |
| --- | --- | --- |
| `ProviderRuntime` | Starts, streams, resumes, stops, and cleans up a provider run. | Claude structured CLI, Codex exec automation, Codex app-server, future Copilot SDK/ACP. |
| `Session` | Durable chat/task state, provider session id, cwd, model, effort, permission policy, queued input. | Claude `session_id`, Codex resume id, Cursor chat id. |
| `Message` | User/assistant transcript text and compact cards. | Assistant deltas/finals, user prompts, queued steering. |
| `ToolActivity` | Normalized tool start/progress/completion/failure. | Claude `tool_use`, Codex tool call, Copilot tool events. |
| `WorkspaceChange` | File create/edit/delete/read and diff provenance. | Claude Write/Edit/DeleteFile, Codex patch/apply, Cursor write events. |
| `PermissionRequest` | Provider asks whether an action may proceed. | Claude PreToolUse hook, Codex approval, Copilot permission handler. |
| `UserInputRequest` | Provider asks a question or requests structured user input. | Claude `AskUserQuestion`, MCP elicitation, Copilot ask-user. |
| `PlanState` | Planning/todo/approval state. | Claude `EnterPlanMode`, `ExitPlanMode`, `TodoWrite`; Cursor plan mode; Copilot plan. |
| `AgentNode` | Parent/child agent lifecycle, transcript, tools, status, usage. | Claude `Task`/`Agent`, Codex multi-agent, Copilot subagents. |
| `CommandSurface` | Safe provider command inventory and gated provider management actions. | `claude mcp list`, `codex review`, `copilot plugin list`. |
| `ExtensionSurface` | Skills, slash commands, plugins, MCP tools, rules, provider config. | `.claude/skills`, Codex plugins, Cursor rules, Copilot skills. |
| `UsageSummary` | Tokens, cost, duration, rate limit, budget. | Claude JSON usage, Copilot SDK usage, Codex runtime metrics. |
| `Attachment` | Files/images/resources added to a run. | Claude `--file`, Codex `--image`, future provider artifacts. |

## Feature Registry

### Runtime And Session Core

| Feature | Target UX | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Claude structured session | Default Claude chat streams from structured CLI with hook bridge. | `Complete` | `src/main/sessions.ts`, `src/main/providers.ts`, provider tests, live structured smoke, installed-app P1/P3 smokes. | Keep covered when Claude CLI event schemas change. |
| Claude bidirectional input | Send queued/steer/user-question replies into the same provider process where possible. | `Research` | P1-002 through P1-007 pass with current structured resume/interrupt path; Claude supports `--input-format stream-json`, but P1 does not require replacing one-prompt-per-process. | Revisit only if P3 user-question/plan flows require same-process stdin semantics. |
| Claude PTY overlay | Old Claude-native chat parser and prompt bridge. | `Won't Do` | Structured JSON supports the needed Claude chat surfaces with cleaner events. | Removed the Claude-native parser/prompt bridge; use structured JSON for chat and the separate user terminal for manual provider management. |
| Claude selectable native chat runtime | Users choose Native Terminal for normal Claude chats. | `Won't Do` | Structured JSON supports core Claude Code tools/subagents/MCP/slash prompt flows with cleaner events; native warm follow-up was fragile in UI smoke. | No chat-runtime support remains; use Orchestrator settings or the separate user terminal for provider management. |
| Provider runtime abstraction | One interface for start/send/resolve/stop across structured CLI, exec automation, SDK, and app-server transports. | `Partial` | `src/main/providerRuntime.ts` owns current subprocess lifecycle, Claude hook prep, Codex app-server dispatch, stop, and interrupt-for-steer. | Keep broadening only as Cursor/Copilot structured runtimes are added. |
| Resume/continue | Continue provider sessions with preserved provider session ids and user-visible continuity. | `Complete` | Claude resume command construction now covers permission and user-input continuation prompts; installed-app P1/P2/P3 smokes verified multi-turn continuity, allow-once/session continuation, AskUserQuestion choices/free-form answers, and plan continuation. | Keep the top-level row reconciled with detailed P1/P3 evidence after future runtime changes. |
| Stop | Stop consistently interrupts current run and leaves composer usable. | `Complete` | Installed-app smokes cover assistant text, tool execution, and permission pause; tests cover interrupted message settlement and stop availability. | Keep covered when approval or provider runtime changes. |
| Queue next message | Users can type while a run is active; message sends immediately after the current run completes. | `Complete` | Installed-app P1-006 queued follow-up ran to `P1_QUEUE_FOLLOWUP_OK`; composer state and queued card were visible. | Add lower-level state-machine coverage if queue logic broadens beyond one pending follow-up. |
| Steer after current tool | Queued message has a `Steer` action that injects at the next sensible boundary. | `Complete` | Installed-app P1-007 retry interrupted the active text stream and ran the follow-up to `P1_STEER_RETRY_OK`; provider runtime and lifecycle tests cover the intentional interrupt path. | Re-test with an active tool boundary during P2 Bash smokes. |
| Installed app update path | User can run the latest committed build locally. | `Implemented` | Installed-app smoke checklist below; final installed artifact verified with Computer Use on 2026-05-12. | Keep checklist current and run it after major UI/runtime changes. |

### Transcript And Layout UX

| Feature | Target UX | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Streaming assistant text | Text appears as soon as provider emits it, with no artificial throttling unless needed for paint stability. | `Complete` | `npm run smoke:ui:auto -- --scroll` verifies the streaming cursor appears during an active simulated stream, disappears after completion, and the final streamed text is not duplicated. | Keep partial/final dedupe covered when provider streaming event shapes change. |
| Streaming scroll behavior | New tokens keep the transcript pinned only while the user is already following the latest output; manual upward scroll wins immediately. | `Complete` | `npm run smoke:ui:auto -- --scroll` covers scrolling up during a simulated streaming update, preserving position, returning with Jump to latest, and final streaming settlement. | Keep this invariant when changing transcript rendering, virtualization, or message batching. |
| No horizontal app scroll | Main pane, markdown, code blocks, tables, cards, and sidebar content never force page-level horizontal scroll. | `Complete` | `npm run smoke:ui:auto -- --transcript-layout` covers long code, tables, file cards, expanded tool summaries, and document/transcript scroll width. | Keep viewport-resized screenshot automation as a future enhancement if Browser/Playwright stability improves. |
| Code block behavior | Long code scrolls inside the block, not the whole app. | `Complete` | `npm run smoke:ui:auto -- --transcript-layout` verifies long code stays bounded and internally scrollable at desktop and narrow widths. | Keep covered when markdown/code rendering changes. |
| Markdown tables | Table cell text wraps responsively instead of forcing horizontal scroll. | `Complete` | `npm run smoke:ui:auto -- --transcript-layout` verifies table bounds and cell wrapping at desktop and narrow widths. | Keep covered when markdown/table rendering changes. |
| Tool summaries | Main transcript shows concise counts and action labels; detail is expandable and bounded. | `Complete` | `ToolCallCard`, `ChatView`, provider fixtures, and transcript-layout smoke for large expanded tool summaries. | Keep max-height behavior when adding new tool detail renderers. |
| Raw events | Raw provider event noise stays out of the main transcript. | `Complete` | `npm run smoke:ui:auto -- --transcript-layout` injects raw provider data/events and verifies the sentinel stays out of the transcript at desktop and narrow widths. | Keep enforcing in UI tests as new provider event buffers are added. |
| File reference cards | Created/referenced files appear as cards that open existing files and do not falsely say missing. | `Complete` | Installed-app P2-008 smoke verified cwd-relative, absolute, quoted path with spaces, long path with spaces, generated file, missing file, and `~/...` references after parser/card-cap fixes. | Keep covered by file-reference unit tests and installed-app smoke when parser/UI changes. |
| Activity/sidebar simplicity | Secondary information is available but not crowded or duplicated in header/sidebar. | `Complete` | Sidebar smoke covers global pinned chats, hover pin/unpin, double-click rename, running spinner, and unread/error-only dots; header status chip was removed. | Keep secondary controls out of the main transcript/header unless they directly unblock the current run. |
| Earlier transcript pages | Older messages are available without making fast chat switching feel like data loss. | `Implemented` | Transcript paging/search keeps initial render bounded. Product decision: do not expose implementation copy like "hidden for faster chat switching" in the default transcript. | Treat paging as an implementation detail, not a product concept: keep full transcript persistence/search, use quiet "Show earlier" affordances, and consider transcript virtualization as the longer-term path for very long chats. |
| Transcript virtualization | Very long threads feel complete and scrollable without mounting every rendered message at once. | `Complete` | Transcript rows now render through a measured virtual window with overscan; older chunks lazy-hydrate near the top with visible-row anchor preservation, and 421-message plus 2,501-message smokes verify bounded mounted rows. | Tune row-height estimates if long real-world markdown/tool-heavy transcripts expose anchor drift. |

### Files, Diff, And Workspace Effects

| Feature | Target UX | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| File create | Transcript summary, file card, Diff row, and click-to-open behavior. | `Complete` | Installed-app P2-001 smoke created `p2-created-by-claude.txt`, showed tool summary/file card, and Diff showed the untracked file; filesystem content matched `P2_CREATE_OK`. | Keep covered by repo-action fixture plus live smoke after workspace-effect UI changes. |
| File edit | Summary, exact file target, Diff row with additions/deletions. | `Complete` | Installed-app P2-002 smoke edited `p2-edit-target.txt`, showed file card, and Diff showed `+1 -1`; filesystem content matched the requested two-line result. | Keep covered by repo-action fixture plus live smoke after workspace-effect UI changes. |
| File delete | Clear deletion summary and Diff warning. | `Complete` | Installed-app P2-003 smoke deleted `p2-delete-target.txt`; file card showed missing/disabled actions and Diff showed deleted-file mode with removed baseline line. | Keep deletion rows visually checked in Diff edge-case smoke. |
| File read/search/list | Compact summary; no raw JSON; searchable targets in expanded details. | `Complete` | Installed-app P2-004 smoke read/listed/searched the repo, surfaced `P2_SEARCH_NEEDLE`, and showed `Read 2 files · Listed 1 listing` without raw JSON in chat. | Keep compact summary tests current as tool vocabulary changes. |
| Bash/shell | Permission-aware command summary with bounded output. | `Complete` | Installed-app P2-005 through P2-007 smokes covered Bash allow once, allow session, and deny; denied command ended with explicit permission-denied error and did not create the target file. | Add fixture if Claude denial event shape changes. |
| Workspace provenance | Session knows cwd, worktree/base/branch, provider session id, and generated artifact roots. | `Implemented` | App-managed worktrees exist; Worktrees settings can create local app-managed chats with explicit base/branch and open linked worktree chats; header metadata shows project/folder, branch when available, and provider/model metadata without adding chat-title icon chrome. | Add generated artifact roots and deeper fork/worktree provenance when those flows become first-class. |
| Git state | Diff panel reflects changed files and risky deletes/large patches. | `Complete` | Installed-app P2-009 smoke showed modified, added/staged, deleted, untracked, and large modified states with previews for deleted, large, and staged-added files. | Keep as a required installed-app smoke after Diff renderer changes. |

### Permissions, Questions, And Plan Mode

| Feature | Target UX | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Claude hook broker | Tool approval cards can resolve without killing/replaying the Claude process when a hook is pending. | `Implemented` | `src/main/approvalBroker.ts`, tests. | Live mutating-tool approval flow with Sonnet. |
| Allow once | Allows one action without changing session settings. | `Complete` | Permission card behavior, tests, and live installed-app Write smoke. | Keep covered by regression tests when approval broker changes. |
| Allow session | Persists scoped grant and resumes. | `Complete` | Session allowlist handling, tests, and live installed-app two-Write smoke. | Add path/tool scoped grants before broadening beyond tool names. |
| Deny | Denies cleanly without corrupting session state. | `Complete` | Fixture coverage, tests, and live installed-app denied Write smoke. | Keep stop/deny interaction covered in P1/P3 tasks. |
| Permission scopes | Tool/path/url/MCP scopes display compactly and map back to provider flags/settings. | `Complete` | Installed-app cards showed readable path scopes for Write and parser tests cover file path, URL, MCP, Bash, and plan summaries. | Keep parser tests current as provider payloads change. |
| AskUserQuestion | User question card with choices/custom answer, separate from permissions. | `Complete` | Installed-app P3-004/P3-005 smokes verified option selection, free-form answer, user-input resume, and exact Claude echo replies. | Improve answered-card labeling in P6-004. |
| SendUserMessage/brief updates | Provider user-facing questions/updates map to user input or assistant status appropriately. | `Implemented` | `brief-usage.jsonl` maps `SendUserMessage` to assistant status; live Claude 2.1.140 `--brief` probes on 2026-05-13 exposed no `SendUserMessage` tool and returned normal assistant text plus usage. | Keep parser support; treat current live Claude brief output as assistant text unless a future CLI emits the tool. |
| Plan mode enter | Plan state appears in sidebar/card without crowding transcript. | `Complete` | Dev CUA structured smoke verified the Plan rail/sidebar renders TodoWrite tasks, ExitPlanMode plan bodies, markdown summary, and survives event-buffer reload via saved transcript reconstruction. | Keep saved-message reconstruction covered as provider payloads change. |
| Plan approval | `Approve Plan` and `Keep Planning` resume correctly. | `Complete` | Installed-app P3-006/P3-007 smokes verified native `Plan Ready`, approve-then-Write permission, `Kept planning`, and no workspace edit on keep-planning. | Save richer plan fixtures in P7-002. |
| Permission mode picker | Product labels map to provider-native policy. | `Complete` | Installed-app CUA verified Settings default `Mode` shows Auto selected; composer shows Auto, Plan, and Ask first as primary choices; Auto-edit, Preapproved only, raw allow/deny/tools/dirs, and Bypass unsafe live behind Advanced. | Existing sessions can retain their saved mode. |

### Agents And Subagents

| Feature | Target UX | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Active agent chips | Running agents appear above composer, not as a noisy sidebar list. | `Complete` | Installed-app P4-002 smoke showed a running Task chip during a long `sleep 18` subagent run. | Keep covered by installed-app smoke after agent UI changes. |
| Agent sidebar tabs | Clicking an agent chip opens/focuses that agent transcript tab. | `Complete` | Installed-app P4-002/P4-003 smokes verified running and completed agent tabs with cleaned transcript content. | Keep active/completed states in renderer tests where practical. |
| Task tool | `Task` creates/updates/completes an `AgentNode`. | `Complete` | Installed-app P4-001 smoke delegated README reading through Task and showed final subagent transcript in the Agents sidebar. | Keep `task-agent.jsonl` and live Task smoke current as Claude event shapes change. |
| Agent tool | `Agent` maps to same shared agent model. | `Implemented` | Fixtures. | Live selected-agent run if locally available. |
| Sidechain/nested transcript | Child transcript captured without raw event spam. | `Complete` | Installed-app P4-001/P4-004 smokes showed Task sidechain transcript in the sidebar with cleaned child content instead of raw JSON. | Save richer raw sidechain transcript in P7-005 for fixture refresh. |
| Agent failures | Failed/cancelled subagents show compact state and useful error. | `Complete` | Installed-app P4-005 denial smoke found and fixed stuck active chips; retry showed failed red sidebar state and no created denied file. | Keep failure finalization covered for event buffers and saved transcripts. |
| Multi-provider agents | Codex/Copilot/Cursor agent events use same `AgentNode` model. | `Partial` | Generic fixtures. | Add provider-specific live/fixture captures after Claude is solid. |

### Slash Commands, Skills, And Composer Commands

| Feature | Target UX | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| App slash commands | `/pet`, `/diff`, `/settings`, etc. are provider-neutral app actions. | `Complete` | Slash palette tests plus installed-app P5 smoke showed app/project/global/skill grouping and no duplicate `/agents` entry. | Keep grouped palette behavior covered when adding provider commands. |
| App command palette | Global app actions are discoverable outside the composer slash palette. | `Complete` | `Cmd+K`/`Ctrl+K` and `Cmd+Shift+P`/`Ctrl+Shift+P` open a grouped global palette; native menu accelerators cover new chat, search, rename, pin/unpin, navigation, panels, Settings, and Shortcuts. | Keep palette, native menu accelerators, and Shortcuts settings in sync as app-level actions change. |
| Provider slash commands | Prompt-like provider commands appear only where supported and useful. | `Complete` | Installed-app P5 smoke ran project command `/ui-smoke`, disposable global command `/orchestrator-global-smoke`, project skill `/skill:tiny-skill`, and disposable global skill `/skill:orchestrator-global-smoke`. | Add cache/invalidation if repeated scans become visible. |
| Built-in Claude TUI commands | True TUI-only commands open terminal overlay or provider management UI. | `Complete` | Settings P5 smoke showed read-only MCP/plugins/agents surfaces and `Purge project state` remained an explicit manual terminal handoff. | Add terminal-launch buttons only if the handoff UX is requested. |
| Project commands | Discover `.claude/commands` and render in command palette. | `Complete` | Installed-app P5 smoke ran `.claude/commands/ui-smoke.md` and returned `P5_PROJECT_COMMAND_OK`; `project-command.jsonl` fixture covers the parsed shape. | Keep fixture current as Claude command output changes. |
| Global commands | Discover `~/.claude/commands`. | `Complete` | Installed-app P5 smoke created a disposable global command, ran `/orchestrator-global-smoke` to `P5_GLOBAL_COMMAND_OK`, then removed the temporary files. | Do not leave smoke-only global commands in user config. |
| Project skills | Discover `.claude/skills` and expose useful runnable entries. | `Complete` | Installed-app P5 smoke discovered and ran `/skill:tiny-skill`, returning `tiny skill loaded`; `project-skill.jsonl` covers the parsed shape. | Keep project skill directory rendering in Skills panel. |
| Global skills | Discover `~/.claude/skills`. | `Complete` | Installed-app P5 smoke created a disposable global skill, ran `/skill:orchestrator-global-smoke` to `P5_GLOBAL_SKILL_OK`, then removed the temporary files. | Do not leave smoke-only global skills in user config. |
| Skill variables | Expand `${CLAUDE_SESSION_ID}`, `${CLAUDE_SKILL_DIR}`, `$ARGUMENTS` where provider semantics allow. | `Partial` | `$ARGUMENTS` expansion is covered for discovered slash commands. | Add session/skill-dir variable expansion only after confirming Claude semantics for those contexts. |
| Command safety | Mutating/provider-state commands require confirmation or terminal handoff. | `Implemented` | Provider command surfaces block quota/mutating commands and settings renders them as terminal/confirmation handoffs. | Add explicit terminal-launch buttons only after confirming the desired handoff UX. |

## Claude Code Structured-First Coverage Map

The product target is first-class Orchestrator UI on top of Claude structured JSON/JSONL output. Native Claude remains a provider-management escape hatch, not a selectable chat lane.

| Claude Code surface | Structured-first Orchestrator mapping | Status | Remaining work |
| --- | --- | --- | --- |
| Assistant text and partials | Flat transcript text with streaming deltas and no raw JSON. | `Complete` | Keep partial/final dedupe fixtures current. |
| File tools: Read, Write, Edit, Delete, Glob/Grep/LS | Compact tool summaries, file cards, and Diff ownership. | `Complete` | Keep live workspace-effect smoke in git-backed repo after UI changes. |
| Bash | Permission card, bounded command/output details, resumed execution. | `Complete` | Refresh fixtures if Claude denial/hook payload changes. |
| MCP tools during a run | Tool cards and permission scopes using the shared MCP action vocabulary. | `Complete` | Add more real MCP server fixtures as local servers become available. |
| AskUserQuestion | User-input card with choices/free-form answer; never permission UI. | `Complete` | Polish answered-card copy if UX gets crowded. |
| SendUserMessage / `--brief` | User-facing updates map to a compact assistant status card when the tool appears; current live Claude 2.1.140 emits ordinary assistant text. | `Implemented` | `brief-usage.jsonl`; live probes with `--brief` and budget caps returned `ORCH_BRIEF_STATUS_OK` as assistant text and did not list `SendUserMessage` in tools. Keep fixture parser support and re-probe after Claude CLI changes. |
| Plan mode, TodoWrite, ExitPlanMode | Plan sidebar/card, plan approval card, keep-planning path. | `Complete` | Prefer structured plan fixtures over native plan placeholder evidence going forward. |
| Task/Agent subagents | Agent chips, sidebar tabs, cleaned nested transcripts, failure states. | `Complete` | Keep sidechain fixtures fresh; add more selected-agent live runs when useful. |
| `--agent` selected launch agent | Composer agent picker; launch-only provider flag. | `Complete` | Add clearer empty-state messaging when no agents are configured. |
| `--agents <json>` custom agents | Future agent editor/importer with validation. | `Planned` | Design only after built-in configured agents stay stable. |
| Project/global slash commands | Discover prompt-like commands and expand/send through structured runs. | `Complete` | Add cache invalidation only if repeated scans are visible. |
| Claude skills as slash commands | Skills panel plus slash palette entries for project/global skills. | `Complete` | Add variable semantics beyond `$ARGUMENTS` after confirming Claude behavior. |
| Cross-provider Capabilities page | One inventory for skills, plugins, MCPs, apps/connectors, agents, hooks, commands, and instructions across Claude and Codex. | `Implemented` | `src/main/providerResources.ts`, `src/main/capabilityCreator.ts`, `src/main/capabilityManager.ts`, `src/renderer/src/components/CapabilitiesPage.tsx`, `docs/capabilities-surface-matrix.md`, provider tests. | Keep provider-native install/update/auth/reload actions gated until explicit confirmation UX exists. |
| Capability sync/import | Users can backfill missing provider projections or import provider-native resources as portable Orchestrator capabilities. | `Implemented` | `src/main/capabilitySync.ts`, `src/main/__tests__/capabilitySync.test.ts`, `src/renderer/src/components/CapabilitiesPage.tsx`, `docs/capability-sync-spike.md`. | File-backed skill/plugin/MCP sync is live with dry-run UI and registry metadata. Provider-native install execution remains gated until explicit confirmation UX exists. |
| Built-in interactive slash commands | Orchestrator-native surfaces where safe; manual provider-management stays out of normal chat. | `Implemented` | `/btw` probe with `claude -p --output-format json --max-budget-usd 0.02 "/btw ..."` returned `/btw isn't available in this environment.` with zero turns/cost, confirming it is not a structured `-p` surface. Orchestrator now owns `/btw` side questions instead of depending on provider TUI state. |
| MCP list/get | Settings inventory and details without raw JSON spam. | `Complete` | Keep failed-local-server states readable. |
| MCP add/remove/reset/config mutations | Gated provider-management handoff. | `Gated` | Confirmation/terminal handoff only; never silently mutate provider config. |
| Plugin list | Settings inventory without raw JSON spam. | `Complete` | Recheck when local Claude plugins exist. |
| Plugin install/enable/disable/update/uninstall | Gated provider-management handoff. | `Gated` | Confirmation/terminal handoff only. |
| Auth status | Compact settings/diagnostics status. | `Complete` | Settings diagnostics smoke verifies compact status cards, and live Claude capability smoke covered auth status. |
| Login/logout/setup-token/update/install/project purge | Explicit terminal handoff or confirmation. | `Gated` | Keep destructive/provider-state actions out of chat runtime. |
| `--mcp-config`, `--strict-mcp-config`, `--plugin-dir`, `--plugin-url` | Session-scoped advanced launch config. | `Planned` | Add only with validation and a clear user-facing settings surface. |
| Attachments / `--file` | Shared attachment model in composer. | `Implemented` | Composer local-file chips pass attachment context into runs; Claude resource attachments map to `--file file_id:relative_path` in command tests. Live-test provider-hosted file resources when a safe file id is available; expand to image/resource provider support later. |
| Usage/cost/budget/fallback | Settings-level provider diagnostics, not per-session sidebar chrome. | `Complete` | Settings diagnostics smoke verifies a usage/cost/budget card backed by captured local `UsageSummary` plus provider quota/budget support status. |
| Worktree/tmux/from-pr/fork/name/remote-control | Advanced session-launch or provider-management controls. | `Partial` | Prefer app-managed worktrees. Local app-managed worktree inventory/create/open/delete is surfaced in Settings with explicit project/base/branch controls and linked-chat switching; only surface provider-native extras with clear value. |
| Doctor/ultrareview/debug/chrome/IDE | Diagnostics or gated provider actions. | `Research` | Classify each as no-quota diagnostics, quota-spending, or terminal-only before implementation. |

### MCP, Plugins, Agents Config, And Provider Management

| Feature | Target UX | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Claude auth status | Settings shows compact status, not raw CLI output. | `Complete` | Settings diagnostics smoke verifies compact status cards, and live Claude capability smoke covered auth status. | Keep auth text compact and redact provider output. |
| Claude login/logout | Explicit terminal handoff or confirmation; never silent. | `Gated` | CLI help verified. | Design confirmation/terminal flow. |
| Claude MCP list/get | Settings renders servers/tools compactly. | `Complete` | Installed-app P5 smoke rendered compact `git` and `wiki-server` failed-to-connect rows, and `MCP details` expanded structured scope/status/type/command/args without raw JSON spam. | Keep local failure states readable and non-blocking. |
| Claude MCP add/remove/reset | Confirmation or terminal handoff only. | `Gated` | CLI help verified. | Add gated command flow in settings. |
| Claude `.mcp.json` prompt | Provider-management prompt outside normal chat. | `Won't Do` | Removed with the Claude-native parser/prompt bridge. | If needed later, implement as an explicit Settings or terminal-handoff flow. |
| Claude plugin list | Settings renders plugins compactly. | `Complete` | Installed-app P5 smoke rendered plugin list as compact `None` output. | Recheck if local Claude plugins are later configured. |
| Claude plugin mutations | Explicit confirmation or terminal handoff. | `Gated` | CLI help verified. | Add gated flow or mark terminal-only. |
| Claude agents list | Settings shows configured agents compactly. | `Complete` | Installed-app P5 smoke showed `4 active agents`: `Explore · haiku`, `general-purpose · inherit`, `Plan · inherit`, `statusline-setup · sonnet`; P4-006 reused the same native list in the composer. | Keep parser tolerant of CLI heading/count formatting. |
| Claude agent mutation | Confirmation/terminal handoff. | `Gated` | CLI help verified. | Decide product scope. |
| Doctor/update/install/setup-token/project purge | Diagnostics or terminal-only; destructive/system flows gated. | `Gated` | CLI help verified. | Add policy table before implementation. |

### Attachments, Images, Usage, And Advanced Launch

| Feature | Target UX | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Claude file attachments | Composer can attach local files as prompt context and provider file resources via `--file`. | `Implemented` | `Attachment` model, composer file chips, IPC file picker, and command construction test for `--file file_abc:docs/context.md`. | Live-test a real `file_id:relative_path` resource when one is safely available. |
| Codex images | Composer image attachment only when provider/runtime supports it. | `Planned` | Codex help verified. | Add shared attachment capability after Claude file path. |
| Usage/cost | Capture provider usage where emitted and route it to settings/provider diagnostics instead of a per-session inspector tab. | `Complete` | `brief-usage.jsonl`, provider tests, live Claude `--brief` usage/cost fields, and settings smoke now verify local captured runs/tokens/cost/duration plus quota/budget support status. | Keep usage rollups local-profile scoped until cross-device sync exists. |
| Rate limits/errors | Auth/rate/quota errors are classified and actionable. | `Complete` | `failure-categories.jsonl` plus provider tests classify auth, quota, rate-limit, and generic model failures into distinct session statuses. | Keep classifier strings narrow enough to avoid false positives. |
| Claude launch extras | `--agent`, `--agents`, `--name`, `--session-id`, `--fork-session`, `--from-pr`, `--worktree`, `--tmux`, `--fallback-model`, `--max-budget-usd`, `--json-schema`, `--file`. | `Partial` | `--agent` is surfaced in the composer agent picker and command tests cover launch-only `--agent` behavior. | Add only remaining launch extras with clear user value. |
| Provider profiles/backends | Codex local/OSS, Cursor Bedrock/API key, Copilot custom providers. | `Research` | Help/package evidence. | Defer until Claude support is complete. |

### Cross-Provider Parity

| Feature | Target UX | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Codex exec automation | Deterministic structured lane for smoke/automation. | `Partial` | Live `codex exec --json` on 2026-05-13 emitted `item.completed` / `agent_message`; `exec-item-agent-message.jsonl` now covers that shape. | Keep exec as automation; do not fake interactive approvals through exec. |
| Codex app-server approvals | Real approval UX via Codex app-server protocol. | `Implemented` | `src/main/codexAppServerRuntime.ts` starts stdio app-server, opens/resumes threads, starts turns, and answers command/file/permission approval requests; `npm run live:codex-composer-approval` passed against Codex 0.128.0 with a real `item/commandExecution/requestApproval` round trip. | Add live file/profile permission fixtures and auto-review-specific proof before promoting auto-review as a default. |
| Codex MCP elicitation | Map to `user_input.requested` and answer through app-server. | `Implemented` | `app-server-approval-question.jsonl` covers `mcpServer/elicitation/request`; `CodexAppServerRuntimeManager` responds to elicitation requests. | Capture a real MCP elicitation transcript when a local MCP server asks for form input. |
| Cursor print mode | Structured stream lane. | `Partial` | Adapter/tests; keychain caveats. | Add partial-output parsing and keychain-aware diagnostics. |
| Cursor plan/ask/worktree/MCP/rules | Shared plan/workspace/extension surfaces. | `Research` | Help verified. | Implement after Claude/Codex core. |
| Copilot prompt/interactive/SDK | Map rich SDK/CLI events to Orchestrator abstractions. | `Research` | Package/CLI research. | Defer until Claude is complete; keep diagnostics honest. |
| Antigravity SDK | SDK/runtime lane for Antigravity agent sessions, approvals, usage, and diagnostics. | `Research` | 2026-05-19 launch research maps the Gemini Interactions API Antigravity managed agent plus `google-antigravity` Python SDK to Orchestrator contracts; no local import/runtime fixture exists yet. | Verify PyPI/package metadata in a temp environment, capture streaming/policy/session fixtures, and keep CLI details out of `provider-cli-spec.md` until `antigravity --help` or official CLI docs are available. |
| Provider diagnostics | Binary/version/auth/models/probes distinguish missing, auth error, keychain error, smoke pass, and captured local usage. | `Complete` | `smoke:providers` plus settings smoke verifies provider diagnostics, compact auth/version/model status, and usage/cost/budget diagnostics. | Continue updating as provider probes change. |
| Capabilities inventory | First-class global left-nav surface for skills, plugins, apps, MCP servers, agents, hooks, and commands across providers. Project instruction files stay in project/chat context. | `Implemented` | `src/renderer/src/components/CapabilitiesPage.tsx`; `src/main/providerResources.ts`; `providers:listResources`; `npm run smoke:ui:auto -- --capabilities` passed with screenshot evidence. | Add provider-native marketplace install/update/auth actions behind confirmations. |
| Custom capabilities | Create global portable skills, portable plugin packages, and MCP configs from one screen where possible; edit/remove file-backed global skills/plugins/MCP config entries. | `Partial` | `src/main/capabilityCreator.ts`; `src/main/capabilityManager.ts`; `capabilityCreator.test.ts`; `capabilityManager.test.ts`; `npm run test:providers` passed 141/141, including portable plugin marketplace and mirror lifecycle coverage. | Promote provider-native plugin install/enable and MCP reload once confirmation flows are designed. |

### Pets And App Polish

| Feature | Target UX | Status | Evidence | Next action |
| --- | --- | --- | --- | --- |
| `/pet` command | Composer slash command toggles pet visibility. | `Implemented` | Prior change. | Add renderer test if available. |
| Built-in pets | Packaged app includes all bundled pets, including Psyduck asset. | `Implemented` | `resources/pets`, `extraResources`. | Verify installed app asset load after next install. |
| Pet animation fidelity | Pets animate consistently with Codex expectations where possible. | `Partial` | User observed hover/idle differences. | Compare Codex pet behavior if accessible; adjust state machine. |
| Pet permission notifications | Approval notification defaults to safer one-time grant. | `Implemented` | Prior change. | Verify with live permission request. |

## Execution Backlog

Work in this order unless the user explicitly redirects. A long-running agent should pick the first unchecked item whose dependencies are satisfied, implement it, verify it, update this file, and commit at stable checkpoints.

Each task below must end with evidence in this file. Prefer exact command names, fixture names, screenshots, commit hashes, or a short live-observation note. If a task cannot be verified in the current environment, mark it `Blocked` with the concrete blocker.

### Verified Checkpoints

| ID | Checkpoint | Status | Evidence |
| --- | --- | --- | --- |
| V-001 | Canonical source-of-truth document exists and older docs point here. | `Complete` | `docs/orchestrator-source-of-truth.md`; older planning docs cleaned up in previous checkpoint. |
| V-002 | Runtime backbone owns provider process lifecycle and Claude hook prep. | `Complete` | `src/main/providerRuntime.ts`; `npm run test:providers`. |
| V-003 | Claude command and skill discovery exists for project and global scopes. | `Complete` | `src/main/claudeExtensions.ts`; slash command tests; live project command/skill discovery smoke on `/private/tmp/orchestrator-agent-ui-smoke`. |
| V-004 | Compact settings surfaces exist for Claude MCP, plugins, agents, auth, and auto-mode defaults. | `Implemented` | `src/renderer/src/components/SettingsModal.tsx`; provider tests; needs installed-app settings smoke. |
| V-005 | Permission cards support allow once, allow session, and deny without confusing user questions. | `Implemented` | `src/main/approvalBroker.ts`; `src/main/sessions.ts`; live Write allow-once, deny, and allow-session smoke in installed app. |
| V-006 | Plan approval reaches Claude plan flow instead of prompting for `~/.claude/plans/*.md`. | `Implemented` | Live installed-app smoke: Claude plan artifact write auto-allowed, `Plan Ready` card shown, real workspace file write still prompted. |
| V-007 | Completed subagents appear in the Agents sidebar with cleaned transcript text. | `Implemented` | Live installed-app smoke on Task subagent; raw `agentId`/`<usage>` trailer removed by `src/types/activityView.ts`. |
| V-008 | Project skills render directory-backed Claude skills in the Skills panel. | `Implemented` | Live installed-app smoke showed `.claude/skills/tiny-skill/SKILL.md` as `Project skills 1 file`. |
| V-009 | Latest source/test checkpoint committed. | `Complete` | Commit `97d30c39` (`Polish Claude agent UI flows`); working tree clean afterward. |
| V-010 | P2 workspace effects, file references, Bash permissions, and Diff edge cases verified in installed app. | `Complete` | Installed-app P2 smoke on `/private/tmp/orchestrator-agent-ui-smoke`; `npm run test:providers` 126/126 and `npx tsc -p tsconfig.web.json --noEmit` passed. |
| V-011 | P3 permissions, user questions, plan approval, permission scopes, and mode picker verified in installed app. | `Complete` | Installed-app P3 smoke on `/private/tmp/orchestrator-agent-ui-smoke`; `toolActions.test` covers URL/MCP/long-path permission summaries. |
| V-012 | P5 command/skill/settings surfaces verified in installed app. | `Complete` | Installed-app CUA smoke ran project/global commands, project/global skills, MCP details, plugin list, agents list, and purge-state handoff on `/private/tmp/orchestrator-agent-ui-smoke`. |
| V-013 | P7 fixture refresh and failure classification verified. | `Complete` | Added `hook-approval.jsonl`, `plan-approval-live.jsonl`, `project-command.jsonl`, `project-skill.jsonl`, `sidechain-real.jsonl`, `mcp-web-approval.jsonl`, and `failure-categories.jsonl`; `npm run test:providers` passed. |
| V-014 | P4 selected Claude agent launch verified in installed app. | `Complete` | Composer listed configured Claude agents, selecting `Explore` changed the run label to `Claude · Explore · Sonnet 4.6 · High`, and the installed-app run returned `P4_SELECTED_AGENT_OK`. |
| V-015 | Isolated UI verification profile exists. | `Complete` | `src/main/appProfile.ts` selects `ORCHESTRATOR_PROFILE` / `ORCHESTRATOR_USER_DATA_DIR` before stores open; `npm run smoke:app` launches a separate dev Electron profile with a visible titlebar badge, clean user data, and pet overlay disabled by default. Packaged smoke copies `dist/mac-arm64/Orchestrator.app` to a temp renamed bundle. |
| V-016 | Claude native chat runtime retired. | `Complete` | Composer no longer exposes a Structured/Native runtime picker; stale Claude chat sessions normalize back to structured/headless before sending; Claude-native terminal parser/prompt code and runtime-parity script were removed. |
| V-017 | Codex app-server chosen as the rich Codex runtime target. | `Complete` | Local Codex 0.128.0 app-server help and generated v2 schema expose structured approvals, questions, diffs, plans, agents, plugins, skills, MCP, account, model, filesystem, hooks, and terminal APIs. |
| V-018 | Brief/status events, usage metadata, attachments, `/btw` side questions, and automated detached UI smoke landed. | `Complete` | `brief-usage.jsonl`; `npx tsc -p tsconfig.node.json --noEmit`; `npx tsc -p tsconfig.web.json --noEmit`; `npm run test:providers`; `npm run smoke:ui:auto`; live Claude 2.1.140 `--brief` probes showed usage/cost fields but no `SendUserMessage` tool. |
| V-019 | First-class Capabilities page landed. | `Complete` | `src/renderer/src/components/CapabilitiesPage.tsx`; `src/main/capabilityCreator.ts`; `npm run build`; `npm run test:providers` 141/141; `npm run smoke:ui:auto -- --capabilities` screenshot `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-capabilities-1778792716697.png`. |
| V-020 | Sidebar pin ordering and noisy file-reference cards hardened. | `Complete` | `src/types/sessionOrdering.ts`; `sessionOrdering.test.ts`; unresolved relative prose references now disappear instead of showing missing cards; `npm run test:providers` 156/156, `npm run build`, `npm run smoke:ui:auto -- --sidebar`, and `git diff --check` passed. |
| V-021 | Header thread identity and provenance access landed. | `Complete` | Header keeps a title-first compact visible band; project/folder, branch, and provider/model provenance are available through the title tooltip and chat actions menu instead of a visible strip. Chat actions include copy folder path and copy session ID. `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, and focused smokes pass with `headerIdentity`, `headerMetadataTooltipOnly`, and `headerActionMenu`. |
| V-022 | Sidebar project actions completed CP-003. | `Complete` | Project headers now support rename, pin/unpin, open folder, archive chats, and remove project. Sidebar smoke verifies project action menu labels, project rename, pinned-project ordering, existing pinned-chat ordering, hover pin, rename, running spinner, and unread/error-only dots. |
| V-023 | Settings structure split landed. | `Complete` | Settings now separates Providers & models from Provider diagnostics and Data controls. Settings smoke verifies Appearance taxonomy, Provider diagnostics, and Data controls sections. |
| V-024 | Theme parity parser and settings coverage landed. | `Complete` | `src/types/themeSharing.ts` owns the portable `codex-theme-v1:` schema used by Settings and node tests. `test:providers` verifies round-trip, defaults, bad prefix, invalid variant, invalid hex, invalid contrast, and invalid semantic colors; settings smoke verifies theme import/sharing controls. |
| V-025 | File-card confidence smoke landed. | `Complete` | Transcript-layout smoke proves unresolved relative prose cards are suppressed while explicit absolute missing references still render as missing with disabled actions. |
| V-026 | Sidebar live-transition smoke landed. | `Complete` | Sidebar smoke drives a pinned chat through running and completed/unread states and verifies its pin order remains stable. |
| V-027 | Transcript polish gate re-verified. | `Complete` | `transcript-stress`, `session-switch`, and `scroll` smokes passed, covering long-thread virtualization, lazy hydration, search jump, session switching, and manual-scroll behavior during streaming. |
| V-028 | Packaged pet-overlay smoke passed. | `Complete` | `npm run pack:mac`, `npm run smoke:ui:auto -- --packaged --pet-overlay`, and packaged resource listing verified bundled pet atlases and packaged overlay behavior. |
| V-029 | Right-panel tab menu and reorder landed. | `Complete` | Inspector smoke verifies the right-panel tab context menu and active tab order changes. |
| V-030 | Browser workbench controls landed. | `Complete` | Inspector smoke verifies real in-page find matches, zoom state, mobile viewport width, completed cache reload, screenshot, and embedded local page loading. |
| V-031 | Files tab binary/empty-state coverage landed. | `Complete` | Inspector smoke verifies bounded preview loading, binary preview fallback, no-results state clearing the stale preview/actions, and add-to-chat from the Files tab. |

### Codex Parity Matrix

The installed Codex app is the reference for desktop polish, but Orchestrator should translate those patterns into provider-neutral concepts instead of becoming a clone of one provider. This matrix tracks the visible gaps found by inspecting `/Applications/Codex.app` and comparing it against the current Orchestrator code.

| Area | Codex behavior | Orchestrator today | Gap | Status | What we need to do |
| --- | --- | --- | --- | --- | --- |
| Sidebar row primitive | Dense single-line task row with title, right metadata, status/env slots, hover-revealed actions, keyboard/focus states, and hidden-on-hover metadata. | `SessionItem` now uses compact metadata-first rows with stable status/action slots and keyboard focus behavior. | Core row behavior is covered. | `Complete` | Revisit only when new connection/cloud metadata becomes real app data. |
| Sidebar hover details | Rich delayed hover card shows title, project/folder, environment, worktree/cloud/remote context, and status details. | Hover cards show title, preview, project, folder, branch/worktree, provider/model, status, and updated time. | Core hover metadata is covered. | `Complete` | Add connection/cloud fields only when those concepts are first-class. |
| Sidebar environment identity | Compact local/cloud/worktree/remote icons with tooltips and full labels in hover cards. | Rows use compact local/worktree environment icons with labels in hover cards and accessible titles. | Core local/worktree identity is covered. | `Complete` | Add remote/cloud identity only when backed by provider/app state. |
| Sidebar status model | Active, follower, idle, needs-resume, read-only, realtime voice, automation next run, unread, error, and running states. | Running spinner, unread/error dots, permission/question previews, active row styling, pinned live-transition stability, persisted session-targeted heartbeat automation records, automation runtime next-run/run-history metadata, scheduler-backed due execution, Automations settings page, schedule editing UI, active-scheduled lifecycle warning, thread action lifecycle controls, automation permission snapshot capture/execution, and paused/active/next-run/running row metadata are implemented. | Broader automation gaps are provider-specific states without Orchestrator data. | `In progress` | Keep automation row state backed by runtime records and focused sidebar/settings smoke. |
| Pinned and recent ordering | Pinned section is global and row metadata does not reorder unexpectedly. | Global pinned section exists, stable pin ordering is implemented, and sidebar smoke covers live running/completed/unread transitions. | No known open gap. | `Complete` | Keep sidebar smoke around pin/status transitions. |
| Sidebar organization | Organize popover supports project/connection/recent/chronological views, created/updated sort, filtering, projectless Chats-first preference, custom-section drag/drop membership, row reorder, and section reorder, show more/less, project pinning and actions. | Persisted organize/sort modes, projectless Chats, projectless Chats-first preference, provider-projectless metadata, provider-aware connection grouping, provider-pinned merge metadata, provider-worktree source/root/host metadata, pinned projects, custom-section membership/drop/row reorder/section reorder, project rename/open/archive/remove actions, project collapse, new chat, and show more/less are implemented. | Real hosted/cloud adapter inputs, provider worktree metadata population, provider projectless metadata population, provider pin list/set adapters, and provider-backed automation state remain. | `In progress` | Keep sidebar smoke covering organization, projectless chats, Chats-first preference, provider-projectless metadata, provider-pinned metadata, provider-worktree metadata, custom-section drag membership/order/section reorder, connection grouping, project actions, pinned ordering, and status-dot transitions. |
| Header thread identity | Header carries env icon, secondary metadata line, and thread actions such as copy cwd/session/app link, fork/worktree, side chat. | Header is intentionally quiet after status chip removal and now shows compact project/folder/branch/provider identity, plus backed chat actions for copy links/ids, open in new window, stop, side chat, and local/same/new-worktree forks. | Primary identity and backed actions are in place without bringing back noisy status chrome. Remaining work is provider-native worktree adapters and message-level fork lineage, not basic header/action placement. | `Implemented` | Keep deeper actions inside the chat actions menu and only add provider-native worktree extras when those flows are backed by stable product behavior. |
| Settings structure | Many focused sections: General, Appearance, Git, Connections, Worktrees, Agent, Personalization, Shortcuts, Usage, Browser, Computer Use, MCP, Hooks, Plugins, Skills, Data Controls. | Settings now separates General, Appearance, Providers & models, Provider diagnostics, Automations, Worktrees, Shortcuts, Pets, Browser, and Data controls while keeping Capabilities as a first-class left-nav surface. Browser has a host-scoped page backed by real in-app Browser data clearing and explicit session-scoped permission boundaries. The Settings left nav is grouped into Codex-like App and Host sections, and the Host section owns a provider-host selector when remote-host session metadata exists. Shortcuts support edit/capture/reset, conflict detection, custom multi-binding, per-binding clear/remove, disabled default binding state, and native menu refresh. Worktrees is backed by provider-neutral local inventory/create/open/delete behavior for app-managed worktrees. | Git/Connections/Usage/Computer Use should appear only when those concepts have dedicated product behavior; global Browser Use approval/history/domain persistence, route/page ownership, host-specific page filtering/behavior, and platform-specific shortcut capture edge cases remain future work. | `Complete` | Keep settings smoke covering grouped nav, host nav action/context selector, shortcut per-binding clear, Appearance taxonomy, Provider diagnostics, Browser, Automations, Worktrees, and Data controls; add new sections only with backed behavior. |
| Theme model | Light/dark/system top-level setting plus separate light/dark chrome themes and code theme IDs. | Settings persists `appearanceTheme`, per-variant chrome theme objects, and per-variant code theme IDs while preserving legacy preset compatibility. | Migration and resolver are in place; future work is only new product-backed theme fields. | `Complete` | Keep typecheck/settings smoke on the appearance resolver and schema before install builds. |
| Custom colors | Editable accent, surface/background, ink/foreground, contrast, translucent/opaque window behavior, semantic colors. | Appearance settings edit per-variant accent/background/foreground/contrast/opaque windows plus semantic diff/skill colors. | The core Codex-style color surface is covered. | `Complete` | Extend only when additional semantic tokens are visible in the app shell. |
| Theme import/export | Portable `codex-theme-v1:` JSON theme string with validation. | Appearance settings copy/import portable light and dark themes through the shared parser. | Round-trip and validation are covered; no known open gap. | `Complete` | Keep parser tests aligned with any future schema version. |
| Typography and motion | Raw UI/code font families, UI/code font sizes, font smoothing, pointer cursor, reduce motion. | Settings exposes UI/code font choices, numeric font sizes, font smoothing, pointer cursor, and reduced-motion toggles. | Core controls are covered. | `Complete` | Add new accessibility controls only with rendered UI verification. |
| Token layer | Broad `text-token-*`, `bg-token-*`, `border-token-*`, and `--color-token-*` bridge across app surfaces. | Theme application maps Orchestrator variables into Codex-like token aliases used by shared surfaces. | Current bridge is sufficient for the app shell; deeper token expansion can follow new surfaces. | `Complete` | Revisit as right-panel workbench components need more shared token names. |
| Right panel architecture | Reusable app-shell panel with durable tab controller, closeable/reorderable/context-menu tabs, overflow polish, width/full-width persistence, empty states. | `ContextSidebar` has per-session `rightPanel` state, closeable/reorderable tabs, width/full-width state, and smoke-covered shell behavior. | Core architecture is covered. | `Complete` | Keep future workbench tabs inside this shell. |
| Right panel existing content | Existing panels can live as durable tabs before deeper redesign. | Plan, Review, Agents, Extensions, Browser, Files, and Side Chat render through durable tab kinds. | Core panel port is covered. | `Complete` | Preserve tab contracts when redesigning individual panels. |
| Review/diff surface | Review tab has changed-file search, diff CSS, view controls, binary/no-content states, file actions, and bounded source previews. | `DiffPanel` now uses the shared bounded preview IPC for binary/image/PDF/source fallback states while preserving git diff rendering. | The core Review workbench behavior is covered; comments/action affordances remain future product work. | `Complete` | Keep inspector smoke covering search, textual diff, binary state, file actions, and no stale preview behavior. |
| Workspace/files browser | Side panel can browse workspace tree, search files, preview source/markdown/images/PDFs, copy paths, open with editor, add to chat. | Files tab browses/searches workspace files, previews text/images, gives PDF/binary fallbacks, copies/reveals/opens paths, and adds files to chat. | Current core workbench behavior is covered. | `Complete` | Keep inspector smoke covering text preview, binary fallback, empty search, and add-to-chat. |
| Browser tab | First-class Browser tab with URL bar, empty state, local server affordances, screenshot, external open, cache/cookie/device/find controls, browser-use manager state, and fork continuity. | Browser tab has URL/navigation, empty state, local affordances, screenshot/external open, in-page find, zoom, mobile preview, cache reload, renderer-local manager bridge state, a provider-agnostic `browser.manager_state` run-event reducer/application path, Codex app-server parsing for current browser-use state/viewport/capture/cursor notifications, host-scoped webview partitions, body-attached active/hidden webview hosts, fork-time Browser workbench transfer, cross-host DOM webview transfer with inherited source partition, and a focused browser-use no-mutation smoke that proves capture/cursor state does not change the visible tab, URL/address, partition, body-hosted webview object, viewport mode, hidden webview count, or composer text. | Cookie controls are intentionally deferred until there is a clear user-facing browser-data workflow; live provider-emitted Codex/browser-use session proof remains separate from the synthetic no-mutation smoke, and non-Codex providers need adapters only if they expose equivalent browser payloads. | `Complete` | Keep Browser smoke covering embedded load, screenshot, find, zoom, mobile preview, cache reload, manager state bridge, browser-use no-mutation, body-host containment, hidden webview containment, fork transfer, and DOM transfer. |
| Side chat | Side chats are real nested conversation tabs with independent IDs and cleanup on close. | `/btw` creates closeable side-chat tabs with independent history, persisted drafts, pending/error/unread tab badges, and close cleanup. | Core side-chat tab behavior is covered. | `Complete` | Add richer side-chat commands only when provider/runtime use cases appear. |
| Terminal panel | Bottom panel uses the same app-shell tab-controller ideas, multiple terminal tabs, persisted sizing, shared panel animation/progress metadata, shared edge-marked overlay resize/reset separators backed by the shared resize controller with keyboard/ARIA support, shared header chrome, service snapshots, Codex-matched xterm content spacing, Codex-bundle-aligned 280 px default total panel size, and theme-aware xterm colors. | Bottom/right terminal tabs have persisted height/tabs, theme-aware xterm colors, Codex line-height/content padding, live font/theme option sync from Appearance tokens, shared app-shell header chrome, shared animated size/progress state, top-edge overlay separator drag/double-click reset coverage through the shared zoom-normalized resize controller, keyboard resize/reset on the shared separator, hide/show restore, right/bottom move routing, focused terminal `Cmd+T`, link routing into Browser, pipe-backed fallback content, service snapshots, tab context menu reorder/close, and 230 px content plus 50 px chrome default/reset sizing. | Core terminal shell parity is covered; remaining parity work is exact live Codex animation timing comparison and broader root shell controller ownership for all tab kinds. | `Complete` | Keep terminal behavior and visual smokes separate; add provider-terminal handoff shortcuts only with explicit product flows. |
| Plan/tasks/usage/resources | Plan/tasks/rate-limit/resource surfaces exist as polished app pages or side surfaces where useful. | Plan and usage/resource concepts exist, usage/provider diagnostics live in Settings, and resources live mainly in Capabilities/settings. | Session-local task/resource workbench concepts can still broaden later. | `Partial` | Keep usage/provider diagnostics in Settings; put session-local plan/tasks/resources in right-panel tabs only when they help the active run. |
| Pets/personalization | Pet overlay has precise resize corner affordance and broader personalization settings. | Pet overlay has corner-only resize behavior, packaged atlas resources, packaged overlay smoke coverage, and a Pets settings section. | Current practical parity is covered; add personalization controls only when backed by stable product behavior. | `Complete` | Keep packaged pet-overlay smoke and resource listing in the release/install verification path. |
| Verification discipline | Codex-like polish is validated through rendered UI behavior, not screenshots alone. | Automated UI smokes cover sidebar hover/status, settings/theme, right-panel tabs, files, review, browser, side-chat, terminal, pets, transcript stress, and command palette. | Core parity surfaces now have targeted smokes. | `Complete` | Add focused smokes with each new surface instead of broad screenshot-only checks. |

### Codex Parity Backlog

This is the preferred implementation order. Each item should land as a small checkpoint with tests/smokes and a decision-log note.

| ID | Scope | Outcome | Status | Depends on | Verification |
| --- | --- | --- | --- | --- | --- |
| CP-001 | Sidebar row primitive | Reusable row with compact title/meta/status/action slots and stable hover/focus behavior. | `Complete` | Existing sidebar state. | `npx tsc -p tsconfig.node.json --noEmit`; `npx tsc -p tsconfig.web.json --noEmit`; `npm run smoke:ui:auto -- --sidebar`; `git diff --check`. |
| CP-002 | Sidebar hover metadata | Hover card shows project, root/cwd, branch/worktree, provider/model, status, and updated time. | `Complete` | CP-001; project/session metadata. | Sidebar smoke now asserts hover card and environment icon visibility. |
| CP-003 | Sidebar organization | Persisted organize/sort modes, project actions, show more/less. | `Complete` | CP-001 and CP-002. | Sidebar smoke covers organize menu options, Chronological list, By project restore, project action menu, project rename, project pin ordering, and show more/less for long project groups. |
| CP-004 | Right panel state model | Per-session durable `rightPanel` store replaces boolean inspector state. | `Complete` | Current `ContextSidebar` behavior mapped. | `npx tsc -p tsconfig.node.json --noEmit`; `npx tsc -p tsconfig.web.json --noEmit`; `npm run smoke:ui:auto -- --inspector`; `git diff --check`. |
| CP-005 | Right panel shell | Closeable tabs, active tab persistence, width/full-width persistence, overflow/empty states. | `Complete` | CP-004. | Inspector smoke covers active tab, tab list, persisted width path, Expand/Restore panel behavior, tab context menu, and tab reordering. |
| CP-006 | Port existing panels | Diff, Plan, Agents, Extensions, and Side Questions run inside the new shell unchanged where possible. | `Complete` | CP-005. | Covered by the right-panel shell checkpoint; existing panel rendering still flows through the durable tab shell and inspector smoke. |
| CP-007 | Review tab v2 | Changed-file search, view controls, open/reveal/copy actions, binary/no-content states, and bounded source previews. | `Complete` | CP-006. | Inspector smoke seeds a git repo with modified/deleted/untracked/binary files, verifies the Review search path, textual diff, and binary state; typechecks and `git diff --check` are the code gate. |
| CP-008 | Files tab | Workspace tree/search/preview/actions and add-to-chat. | `Complete` | CP-005; editor-open settings. | Inspector smoke opens the Files tab, searches a nested path with spaces, previews file content, attaches the selected file to the composer, verifies binary fallback copy, and verifies the no-results empty state. |
| CP-009 | Browser tab | URL bar, navigation, empty state, screenshot, external open, local server affordance. | `Complete` | CP-005. | Inspector smoke opens the Browser tab, loads a local HTTP fixture, verifies the page title, captures a screenshot preview, and exercises find, zoom, mobile preview, and cache reload controls. |
| CP-010 | Side-chat tabs | `/btw` becomes durable side-chat tabs with independent composer/history and close cleanup. | `Complete` | CP-005. | Inspector smoke opens multiple `/btw` side-chat tabs, verifies independent draft persistence across tab switches, and verifies close cleanup. |
| CP-011 | Terminal shell parity | Persisted bottom panel state, theme-aware terminal colors, optional multiple terminal tabs. | `Complete` | Theme tokens; existing terminal. | Terminal smoke verifies multi-tab state persists across hide/show and verifies terminal tab context menu/reorder. |
| CP-012 | Theme model v2 | Light/dark/system plus per-variant chrome/code theme settings and migration. | `Complete` | Current settings schema. | Typecheck and settings smoke cover the expanded schema, light/dark/system resolver, Codex-style token bridge, and code/UI font-size tokens. |
| CP-013 | Custom theme editor | Editable accent/surface/foreground/contrast/semantic colors, font sizes, motion/cursor toggles. | `Complete` | CP-012. | Settings smoke verifies the expanded settings surface still renders; controls edit per-variant chrome colors, semantic colors, font sizes, and motion/cursor/font-smoothing toggles. |
| CP-014 | Theme import/export | Validated `codex-theme-v1:` import/export. | `Complete` | CP-012 and CP-013. | Settings smoke imports a valid `codex-theme-v1:` string and verifies sharing controls are present; `themeSharing.test.ts` validates round-trip, defaults, variant, prefix, hex colors, contrast, and semantic colors. |
| CP-015 | Settings taxonomy | Settings sections match the level of Codex polish without crowding normal preferences. | `Complete` | CP-012; capabilities surfaces. | Settings smoke verifies Appearance is grouped into Mode, Presets, Theme editor, Sharing, Typography, and Layout and reading. |
| CP-016 | Pet/personalization polish | Packaged pets render cleanly and resize/state behavior matches Codex expectations where practical. | `Complete` | Current pet overlay. | Pet overlay smoke verifies compact corner-only resize affordance, hidden grip on normal mascot hover, visible grip on handle hover/focus, geometry bounds, and status mapping. |
| CP-017 | Header thread identity | Compact header provenance without the old noisy status chip. | `Complete` | Sidebar metadata, project/session state, provider/model state. | Inspector smoke asserts title-first header identity, metadata line, and backed copy actions for folder/project/session/provider session IDs. |
| CP-018 | Settings structure split | Provider defaults, diagnostics, data controls, browser controls, appearance, shortcuts, and personalization are separated without exposing capability matrices in normal chat. | `Complete` | CP-015 and provider diagnostics. | Settings smoke asserts Provider diagnostics, Browser, and Data controls sections in addition to the Appearance taxonomy. |

### Next Polish Queue

These are the next small, user-visible polish slices to consider after the current reliability checkpoint.

| ID | Polish Area | Target UX | Status | Next action |
| --- | --- | --- | --- | --- |
| POL-001 | File cards | Cards appear only for confident, useful file references; no review prose, numeric literals, or unresolved relative guesses. | `Complete` | Renderer smoke now proves unresolved relative prose cards are suppressed while explicit absolute missing files still show disabled actions. |
| POL-002 | Pet overlay | Built-in pets render without white-square glitches and state transitions feel close to Codex. | `Complete` | Packaged pet-overlay smoke and packaged resource listing passed after rebuilding `dist/mac-arm64/Orchestrator.app`. |
| POL-003 | Command palette | Keyboard-first actions feel native and do not duplicate composer slash commands. | `Complete` | Transcript-layout smoke verifies command palette open paths, grouping, recent commands, fuzzy search, shortcut labels, and the search-transcript action. |
| POL-004 | Sidebar polish | Pinned, unread, running, rename, and project grouping feel stable during live runs. | `Complete` | Sidebar smoke now covers running/completion transitions while a pinned session receives messages, including spinner, unread dot, and stable pinned order. |
| POL-005 | Transcript polish | Long threads feel complete, searchable, and scroll-stable without exposing implementation copy. | `Complete` | `transcript-stress`, `session-switch`, and `scroll` smokes are the pre-install gate after transcript changes. |
| POL-006 | Codex-style settings taxonomy | Settings follow Codex's sectioned model while keeping Orchestrator behavior provider-neutral. | `Complete` | CP-015 and CP-018 are complete; add future sections only when backed by product behavior. |
| POL-007 | Theme parity | Theming supports per-variant custom colors, typography, semantic tokens, and import/export. | `Complete` | CP-012 through CP-014 are complete; revisit only when adding new visible theme surfaces. |
| POL-008 | Right sidebar parity | The right inspector becomes a durable app-shell workbench with tabs, files, review, browser, side chat, and polished empty states. | `Complete` | CP-004 through CP-010 are complete; keep new right-panel surfaces inside this shell. |
| POL-009 | Sidebar Codex parity | Chat rows expose project/branch/worktree/status metadata on hover and support richer organization modes. | `Complete` | CP-001 through CP-003 are complete; revisit only when new connection/cloud project data exists. |

### P0: Re-establish Installed-App Verification

| ID | Task | Status | Verification Required | Notes |
| --- | --- | --- | --- | --- |
| P0-001 | Diagnose final installed-app Computer Use attach timeout. | `Complete` | After reinstall/relaunch, `Computer Use get_app_state` returned the installed Orchestrator accessibility tree in 0.5-2.3 seconds. | Previous timeout did not reproduce after restart/reinstall; treat as stale app/CUA session unless it recurs. |
| P0-002 | Add repeatable install/restart smoke checklist. | `Complete` | `Installed App Smoke Checklist` below documents commands and expected observable app state; checklist was run from a clean package on 2026-05-12. | Keep this checklist as the minimum installed-app gate after major changes. |
| P0-003 | Verify final installed app can start a plain Claude session. | `Complete` | Live installed app returned `FINAL_INSTALLED_APP_SMOKE_OK` with Claude Sonnet 4.6 High in Ask mode. | Verified before the terminal command-bar patch; no Claude runtime changes landed afterward. |
| P0-004 | Verify packaged resources load. | `Complete` | `npm run test:smoke-config` passed; `/Applications/Orchestrator.app/Contents/Resources/pets/*/{pet.json,spritesheet.webp}` contains ditto, orchestrator, pika, and psyduck. `/pet` applied without visible error. | Pet overlay animation fidelity remains separate P-polish work. |
| P0-005 | Add isolated UI verification window/profile. | `Complete` | `node scripts/launch-isolated-app.mjs --print --profile smoke-check --user-data-dir /private/tmp/orchestrator-profile-check --workspace-dir /private/tmp/orchestrator-workspace-check` printed the expected isolated profile config; `npm run smoke:app:packaged -- --reset --profile smokecua` launched a temp renamed bundle with separate `userData`; TypeScript/build/provider tests passed. | Future UI verification should start here instead of the user's active Orchestrator window. Packaged smoke remains useful for parity, but current Computer Use can list the renamed app while `get_app_state` does not attach to it by name. |
| P0-006 | Use dev Electron as the primary Computer Use target. | `Complete` | `npm run smoke:app -- --reset --profile devcua --workspace-dir /private/tmp/orchestrator-agent-ui-smoke` launched `Orchestrator - Devcua`; Computer Use `get_app_state("Electron")` attached to `localhost:5173`, showed the `Devcua profile` badge and clean empty project state, and a Settings click opened the dev Settings UI. | Isolated profiles skip legacy user-data migration so old projects/sessions do not leak into smoke runs. |
| P0-007 | Add automated detached UI driver. | `Complete` | `npm run smoke:ui:auto` launches an isolated Electron profile, bootstraps a disposable project/session, inspects profile badge/composer/sidebar rail, and exits with JSON evidence. | Extend assertions as new primary UI flows land. |

### P1: Claude Core Run Semantics

| ID | Task | Status | Verification Required | Notes |
| --- | --- | --- | --- | --- |
| P1-001 | Plain answer stream. | `Complete` | Live installed-app Claude answer streams and ends idle; fixture covers same event shape; P0 installed artifact was verified. | Keep covered by provider fixtures and installed-app smoke checklist. |
| P1-002 | Multi-turn continuity. | `Complete` | Installed-app two-turn smoke remembered `ORCHID-912` and returned `CONTINUITY_OK: ORCHID-912` without duplicate transcript. | Verified with Claude Sonnet 4.6 High in Ask mode. |
| P1-003 | Stop during assistant text. | `Complete` | Installed-app stop smoke interrupted a long streaming answer, removed the streaming cursor, restored idle composer state, and re-enabled Send after text entry. | Added regression coverage for interrupted streaming/queued text settlement. |
| P1-004 | Stop during tool execution. | `Complete` | Installed-app Bash sleep smoke was approved once, stopped while the tool was active, returned to idle, and composer Send re-enabled after text entry. | Permission card cleanup was hardened so stale approvals no longer look active. |
| P1-005 | Stop during permission pause. | `Complete` | Installed-app Write permission pause now shows Stop; clicking Stop closes the request, restores the composer, and both smoke target files remained absent. | Added `canStopSession` coverage for `waiting_for_permission` and inactive permission-card rendering. |
| P1-006 | Queue next message. | `Complete` | Installed-app active-run queue smoke showed a queued card, then ran the follow-up to `P1_QUEUE_FOLLOWUP_OK` after primary completion. | Queue card includes Steer action while pending. |
| P1-007 | Steer queued message. | `Complete` | Installed-app retry queued a follow-up, clicked Steer, interrupted the active stream around line 181, avoided provider error, and ran `P1_STEER_RETRY_OK`. | Fixed intentional interrupt failure filtering and streaming-message settlement before follow-up resume. |
| P1-008 | Decide Claude one-process vs bidirectional stream. | `Complete` | Decision: keep current structured one-prompt-per-process plus resume/interrupt path for P1; bidirectional stdin remains deferred until a same-process-only user-question or plan behavior proves necessary. | P1-002 through P1-007 now pass without a bidirectional transport replacement. |

### P2: Workspace Effects, Diff, And Files

| ID | Task | Status | Verification Required | Notes |
| --- | --- | --- | --- | --- |
| P2-001 | File create. | `Complete` | Installed-app smoke created `p2-created-by-claude.txt`; transcript showed `Wrote 1 file`, file card opened/revealed, Diff showed untracked file, and filesystem content was `P2_CREATE_OK`. | Verified in disposable git repo `/private/tmp/orchestrator-agent-ui-smoke`. |
| P2-002 | File edit. | `Complete` | Installed-app smoke edited `p2-edit-target.txt`; transcript showed `Edited 1 file`, file card existed, Diff showed `+1 -1`, and filesystem content matched the requested two-line result. | Verified in disposable git repo. |
| P2-003 | File delete. | `Complete` | Installed-app smoke deleted `p2-delete-target.txt`; missing card disabled Open/Reveal and Diff showed deleted-file mode with removed baseline line. | Verified in disposable git repo. |
| P2-004 | File read/search/list. | `Complete` | Installed-app smoke found `P2_SEARCH_NEEDLE`; transcript summary stayed compact as `Read 2 files · Listed 1 listing`. | Verified with Claude Sonnet 4.6 High. |
| P2-005 | Bash allow once. | `Complete` | Installed-app Bash `printf 'P2_BASH_ONCE_OK\n'` prompted, `Allow Once` resumed, card showed `Allowed once`, and final reply was `P2_BASH_ONCE_DONE`. | Harmless no-network command. |
| P2-006 | Bash allow session. | `Complete` | Installed-app two-command Bash smoke prompted on the first command, `Allow Session` resumed, second command ran without another prompt, and final reply was `P2_BASH_SESSION_DONE`. | Session grant behavior verified. |
| P2-007 | Bash deny. | `Complete` | Installed-app denied Bash redirection showed `Denied` plus `Error — Permission denied by user`; `p2-bash-deny.txt` remained absent and no bogus success text appeared. | Current behavior ends the run with explicit denied-tool error rather than an assistant success reply. |
| P2-008 | File reference resolution matrix. | `Complete` | Installed-app retry after fixes showed cards for cwd-relative/absolute file, quoted path with spaces, long path with spaces, generated file, missing file with disabled actions, and `~/Desktop/Orchestrator/docs/orchestrator-source-of-truth.md`. | Fixed parser support for quoted/space/tilde paths and raised visible reference cap to 8. |
| P2-009 | Git Diff edge cases. | `Complete` | Installed-app Diff showed six states: modified, deleted, large modified `+50 -1`, staged added, staged modified, and untracked; previews worked for deleted, large, and staged-added rows. | Verified in disposable git repo. |

### P3: Permissions, Questions, And Plan Mode

| ID | Task | Status | Verification Required | Notes |
| --- | --- | --- | --- | --- |
| P3-001 | Write allow once. | `Complete` | Live installed-app smoke created disposable file after `Allow Once`; provider tests pass. | Already verified before commit `97d30c39`. |
| P3-002 | Write allow session. | `Complete` | Live installed-app smoke created two files with only first Write prompt after `Allow Session`; tests pass. | Already verified before commit `97d30c39`. |
| P3-003 | Write deny. | `Complete` | Live installed-app smoke denied Write, run ended idle, file absent; tests pass. | Already verified before commit `97d30c39`. |
| P3-004 | AskUserQuestion choices. | `Complete` | Installed-app smoke showed `Answer Required`, `ALPHA_P3_CHOICE`/`BETA_P3_CHOICE` options, answer sent, user bubble, and final `P3_CHOICES_DONE:ALPHA_P3_CHOICE`. | Choice path verified separate from permission resume. |
| P3-005 | AskUserQuestion free-form answer. | `Complete` | Installed-app smoke typed `FREEFORM_P3_TOKEN_742`, showed answer sent/user bubble, and final `P3_FREEFORM_DONE:FREEFORM_P3_TOKEN_742`. | Claude also emitted placeholder option buttons in this run; free-form path still worked. |
| P3-006 | Plan approve. | `Complete` | Installed-app smoke showed plan artifact auto-allow, native `Plan Ready`, `Plan approved`, real Write permission, `Allowed once`, file content `P3_PLAN_APPROVE_OK`, and final `P3_PLAN_APPROVE_DONE`. | Fixture capture remains P7-002, not a P3 blocker. |
| P3-007 | Plan keep-planning. | `Complete` | Installed-app smoke clicked `Keep Planning`, showed `Kept planning`, answered follow-up with `Keep planning`, and verified `p3-plan-keep-planning.txt` remained missing. | No workspace edit occurred. |
| P3-008 | Permission scope details. | `Complete` | Installed-app Write permission card showed full path scope; parser tests cover Write/Edit/Bash/ExitPlanMode/WebFetch/MCP and long-path truncation. | Add screenshots later if permission-card CSS changes. |
| P3-009 | Permission mode picker. | `Complete` | Installed-app smoke previously verified Claude permission modes; follow-up CUA verified Settings default Auto, primary composer choices Auto/Plan/Ask first, and advanced disclosure for Auto-edit/Preapproved only/raw tool fields/Bypass unsafe. | Existing sessions can retain their saved mode. |

### P4: Agents And Subagents

| ID | Task | Status | Verification Required | Notes |
| --- | --- | --- | --- | --- |
| P4-001 | Task subagent happy path. | `Complete` | Live installed-app smoke used Task to read README; main chat showed compact delegation summary and Agents sidebar showed final child transcript. | Fixture-backed by `task-agent.jsonl`; live run verified against installed Sonnet app. |
| P4-002 | Active agent chips while running. | `Complete` | Long Task smoke paused on `Bash sleep 18`; running chip appeared above composer and opened the active agent tab. | Verified before allowing the pending Bash permission. |
| P4-003 | Completed agent sidebar tabs. | `Complete` | Live installed-app smoke showed completed agent selectable with cleaned transcript. | Covered by commit `97d30c39` and P4-001 retry. |
| P4-004 | Nested/sidechain transcript capture. | `Complete` | Real Claude Task sidechain transcript appeared in the sidebar without raw event spam. | Richer raw sidechain fixture capture remains P7-005, not a P4 blocker. |
| P4-005 | Agent failure/cancel states. | `Complete` | Denying a subagent Bash permission now finalizes the child as failed, removes the running chip, shows a red failed tab, and leaves the target file absent. | Added event-buffer and saved-transcript regression coverage. |
| P4-006 | Selected-agent launch option. | `Complete` | Installed-app composer listed configured Claude agents, selecting `Explore` changed the label to `Claude · Explore · Sonnet 4.6 · High`, and the run returned `P4_SELECTED_AGENT_OK`. | Parser bug found and fixed during CUA smoke: CLI headings like `Built-in agents:` no longer render as agent chips. |

### P5: Slash Commands, Skills, MCP, Plugins, Agents

| ID | Task | Status | Verification Required | Notes |
| --- | --- | --- | --- | --- |
| P5-001 | Slash palette app commands. | `Complete` | Installed-app CUA smoke showed grouped app/project/global/skill command entries and no duplicate `/agents`; `npm run test:providers` covers registry behavior. | Keep palette grouping stable as more provider commands land. |
| P5-002 | Project command run. | `Complete` | Installed-app CUA smoke ran `.claude/commands/ui-smoke.md`; Claude requested harmless Bash `ls`, `Allow Once` resumed, and final output included `P5_PROJECT_COMMAND_OK`. | `project-command.jsonl` backs automated parsing. |
| P5-003 | Global command discovery/run. | `Complete` | Created disposable `~/.claude/commands/orchestrator-global-smoke.md`, ran `/orchestrator-global-smoke` to `P5_GLOBAL_COMMAND_OK`, then removed it. | No smoke-only global command remains in user config. |
| P5-004 | Project skill discovery. | `Complete` | Installed-app CUA smoke showed `tiny-skill/SKILL.md` in Skills panel and slash palette. | Covered by project-skill fixture and provider tests. |
| P5-005 | Project skill run. | `Complete` | Installed-app CUA smoke ran `/skill:tiny-skill`; response was `tiny skill loaded`. | `project-skill.jsonl` backs automated parsing. |
| P5-006 | Global skill discovery/run. | `Complete` | Created disposable `~/.claude/skills/orchestrator-global-smoke/SKILL.md`, ran `/skill:orchestrator-global-smoke` to `P5_GLOBAL_SKILL_OK`, then removed it. | No smoke-only global skill remains in user config. |
| P5-007 | Settings MCP list/get. | `Complete` | Installed-app settings CUA smoke rendered `git` and `wiki-server` failure rows; `MCP details` expanded structured details for `git`. | Local MCP failures are readable and non-blocking. |
| P5-008 | Settings plugin list. | `Complete` | Installed-app settings CUA smoke rendered plugin output compactly as `None`. | Use no-mutation command only. |
| P5-009 | Settings agents list. | `Complete` | Installed-app settings CUA smoke rendered `4 active agents`: `Explore`, `general-purpose`, `Plan`, and `statusline-setup`. | Selected-agent launch remains P4-006. |
| P5-010 | Mutating provider-management gates. | `Complete` | Installed-app settings CUA smoke showed `Purge project state` as a disabled/manual terminal handoff; no destructive command was executed. | Keep destructive/provider-state commands gated. |

### P6: Layout, Design, And Accessibility QA

| ID | Task | Status | Verification Required | Notes |
| --- | --- | --- | --- | --- |
| P6-001 | No page-level horizontal scroll. | `Complete` | Installed-app CUA walkthrough covered long file cards, settings output, permission cards, tool rows, and sidebar panels with no visible page-level horizontal scroll at the tested desktop viewport. | Add viewport-resized screenshot automation if Browser/Playwright stability improves. |
| P6-002 | Bounded tool expansions. | `Complete` | Code caps expanded tool output panes and installed-app P5/P6 walkthrough showed compact tool rows/details rather than transcript-wide dumps. | Keep max-height behavior when adding new tool detail renderers. |
| P6-003 | Permission card visual polish. | `Complete` | Long paths wrap inside max-width cards; live permission smokes showed readable cards and no layout spill. | Re-verify if approval-card structure changes. |
| P6-004 | Answered user-question and permission-card polish. | `Complete` | Installed-app CUA smoke approved a Bash permission, navigated to Settings and back, and the card still showed `Allowed once`; the run finished `P6_PERMISSION_DECISION_DONE`. | Keep result-state labels stable for inactive permission and user-question cards. |
| P6-005 | Terminal command input. | `Complete` | In the installed app, Computer Use entered `echo TERMINAL_COMMAND_VISIBLE_OK`; the terminal pane showed the command and `TERMINAL_COMMAND_VISIBLE_OK`. | Added an accessible command bar backed by `terminal:runCommand`, fixed terminal live-event targeting to the main renderer, and made terminal colors consistently dark. |
| P6-006 | Sidebar control audit. | `Complete` | Installed-app CUA walkthrough showed the right rail reduced to Agents, Diff, and Skills; Terminal remains in the header/bottom pane rather than duplicated in the rail. | Keep secondary controls out of the main transcript. |

### P7: Fixtures And Automated Coverage

| ID | Task | Status | Verification Required | Notes |
| --- | --- | --- | --- | --- |
| P7-001 | Save live hook approval fixture. | `Complete` | Added `hook-approval.jsonl`; tests cover approval broker event shape. | Refresh if Claude hook payload changes. |
| P7-002 | Save plan approval fixture. | `Complete` | Added `plan-approval-live.jsonl`; tests cover native plan approval shape. | Refresh after future plan-mode UX changes. |
| P7-003 | Save project command fixture. | `Complete` | Added `project-command.jsonl`; tests cover discovered command execution shape. | Keep in sync with P5 project command smoke. |
| P7-004 | Save skill fixture. | `Complete` | Added `project-skill.jsonl`; tests cover discovered skill execution shape. | Keep in sync with P5 skill smoke. |
| P7-005 | Save sidechain/nested agent fixture from real run. | `Complete` | Added `sidechain-real.jsonl`; tests cover sidechain transcript capture without raw event spam. | Refresh if Claude changes `Task`/sidechain event naming. |
| P7-006 | Save MCP/web approval fixtures. | `Complete` | Added `mcp-web-approval.jsonl`; tests cover MCP and WebFetch/WebSearch approval event mapping. | Live network/web fetch remains gated by local provider/network state. |
| P7-007 | Save auth/rate/quota error fixtures. | `Complete` | Added `failure-categories.jsonl`; tests cover auth, quota, rate-limit, and generic model failure classification. | Keep synthetic failure text realistic and narrow. |
| P7-008 | Add renderer/Playwright smoke harness or documented CUA script. | `Complete` | `Installed App Smoke Checklist` now includes rebuild/install/launch and core CUA checks; Decision Log records the P5-P7 CUA script and observed states. | Promote to automated Playwright only if app automation becomes stable enough. |

### P8: Cross-Provider After Claude Closure

| ID | Task | Status | Verification Required | Notes |
| --- | --- | --- | --- | --- |
| P8-001 | Shared runtime event contract cleanup. | `Planned` | Common types cover session/message/tool/permission/user_input/plan/agent/diff/usage across providers. | Do not start broad refactor until Claude UX is stable. |
| P8-002 | Codex app-server approval runtime. | `Complete` | `codex --help`, `codex app-server --help`, generated schema, parser fixtures, fake transport test, and `npm run live:codex-appserver` verified. | Follow-up only for richer settings surfaces: app/plugin/skill/model/account browsers. |
| P8-003 | Cursor diagnostics and partial-output fixture. | `Research` | Keychain/auth/partial-output cases are captured and classified. | Defer until Claude completion gates pass. |
| P8-004 | Copilot CLI/SDK fixture capture. | `Research` | No-quota fixture or SDK event capture maps user input/permission/subagent events. | Defer until Claude completion gates pass. |
| P8-005 | Antigravity SDK research spike. | `Research` | Official docs identify the Antigravity managed agent, Interactions API, Python/JS/REST calls, streaming events, resumable environments, pricing shape, and preview limits; PyPI identifies `google-antigravity` 0.1.0 as alpha with macOS arm64/Linux wheels. | Next spike: temp venv import probe, no-mutation chat smoke if credentials allow, streaming fixture, policy `ask_user` fixture, MCP fixture, and usage/cost telemetry confirmation. |

## Required Fixtures

Add or refresh these fixtures as features are implemented:

Current Claude fixture files that back the implemented rows:

- `plain-answer.jsonl`
- `partial-message.jsonl`
- `repo-actions.jsonl`
- `permission-denied.jsonl`
- `ask-user-question.jsonl`
- `plan-todos.jsonl`
- `exit-plan-denial.jsonl`
- `task-agent.jsonl`
- `agent-tool.jsonl`
- `task-progress.jsonl`
- `agent-partial-message.jsonl`
- `sidechain-agent.jsonl`
- `task-permission-denied.jsonl`
- `hook-approval.jsonl`
- `plan-approval-live.jsonl`
- `project-command.jsonl`
- `project-skill.jsonl`
- `sidechain-real.jsonl`
- `mcp-web-approval.jsonl`
- `failure-categories.jsonl`
- `brief-usage.jsonl`

- [x] Claude plain answer.
- [x] Claude partial assistant message.
- [x] Claude file/tool action bundle.
- [x] Claude permission denial.
- [x] Claude AskUserQuestion.
- [x] Claude plan/todo events.
- [x] Claude ExitPlanMode denial.
- [x] Claude Task/Agent subagent events.
- [x] Claude Task/subagent permission-denied failure event.
- [x] Claude live hook approval event stream.
- [x] Claude MCP tool approval.
- [x] Claude web fetch/search approval.
- [x] Claude plan approval live transcript.
- [x] Claude sidechain/nested real transcript.
- [x] Claude slash command real transcript beyond `/help`.
- [x] Claude project/global command fixture.
- [x] Claude skill fixture.
- [x] Claude rate limit/quota/auth error.
- [x] Claude SendUserMessage/brief usage fixture.
- [x] Codex app-server approval fixture.
- [x] Codex MCP elicitation fixture.
- [ ] Cursor partial-output fixture.
- [ ] Copilot user-input/permission/subagent fixture.

## Completion Gates

Do not mark Claude support `Complete` until all of these pass or are explicitly marked `Gated`/`Won't Do` with a reason:

1. Structured Claude session starts with Sonnet and streams assistant text.
2. Multi-turn continuity works.
3. Stop works during text, tool, permission, and queued-message states.
4. Queue next works and visibly sends after completion.
5. Steer works at the next sensible boundary or is explicitly deferred with product rationale.
6. File create/edit/delete/read/search produce transcript summaries, file cards, and Diff state.
7. Bash permission flow supports allow once, allow session, deny, and resumed execution.
8. AskUserQuestion uses user-input UI and resumes without permission-path confusion.
9. Plan mode captures the plan, approves, and keeps planning.
10. Task/subagent run shows chips and opens transcript tabs with real content.
11. Slash palette supports app commands, provider commands, discovered project/global commands, and terminal handoffs.
12. Skills discovery works for project and global skills, with at least one safe live skill run.
13. MCP/plugin/agent list surfaces render compactly in settings.
14. Mutating provider-management commands are gated.
15. Main window and sidebar have no page-level horizontal scroll under long code, tables, long paths, and agent cards.
16. Large tool expansions are bounded in scrollable panes.
17. No capability matrix appears in normal user-facing UI.
18. Provider diagnostics remain available for development/support.
19. Automated tests pass.
20. Live Claude Sonnet verification passes when auth/quota/network allow.
21. Installed app or isolated packaged smoke profile is rebuilt and smoke-verified after major changes.

## Isolated UI Smoke Profile

Use this path when the user has active work in their normal Orchestrator window. It launches a separate app profile with its own Electron `userData` directory, a visible `Smoke profile` titlebar badge, and the pet overlay disabled by default.

For Computer Use, prefer the dev Electron lane:

```bash
npm run smoke:app -- --reset
```

Then target Computer Use at:

```text
Electron
```

Expected: the CUA tree shows `Window: "Orchestrator - <Profile>"`, `URL: localhost:5173/`, and the profile badge. Do not target `Orchestrator` while the user has active installed-app work.

For packaged parity against `dist/mac-arm64/Orchestrator.app` without replacing `/Applications/Orchestrator.app`:

```bash
npm run pack:mac
npm run smoke:app:packaged -- --reset
npm run smoke:ui:auto -- --packaged --transcript-layout
```

Useful options:

```bash
npm run smoke:app -- --profile p8-codex --user-data-dir /private/tmp/orchestrator-p8-codex --workspace-dir /private/tmp/orchestrator-agent-ui-smoke
npm run smoke:app -- --print --profile smoke-check
```

Expected:

- The titlebar shows `<Profile> profile`.
- `window.api.app.getProfile()` returns `isIsolated: true` and a `/private/tmp` or otherwise explicit `userDataDir`.
- Existing sessions/settings in the user's normal Orchestrator window do not appear in the smoke profile.
- Computer Use targeting is reliable for the dev lane via `Electron`. Packaged renamed-bundle targeting is not fully solved in the current plugin: it can list the renamed smoke app, but `get_app_state` did not attach by renamed app name in the 2026-05-13 smoke.
- Packaged smoke scripts prepare a renamed temporary app bundle with a profile-specific bundle id before launching, so they do not register as the normal installed app.
- Do not quit or replace the user's installed `/Applications/Orchestrator.app` unless they explicitly ask for that.

## Installed App Smoke Checklist

Use this only when the installed app itself must be replaced or verified. Prefer the isolated smoke profile above when the user is actively using their normal Orchestrator window. Record the result in the Decision Log.

1. Build the packaged app:

```bash
npm run pack:mac
```

2. Quit the currently installed app before replacing it only if the user has approved touching the active installed app:

```bash
killall Orchestrator
```

3. Copy the fresh package into `/Applications` through the guarded installer:

```bash
npm run install:mac
```

Expected: if `/Applications/Orchestrator.app` is still running, the installer refuses to copy. Only bypass with `ORCHESTRATOR_ALLOW_RUNNING_APP=1` when intentionally accepting the risk of replacing a live app bundle.

4. Launch the installed app:

```bash
open -a /Applications/Orchestrator.app
```

5. Use Computer Use on `Orchestrator` and confirm `get_app_state` returns the accessibility tree for:

```text
file:///Applications/Orchestrator.app/Contents/Resources/app.asar/out/renderer/index.html
```

6. Start or select a disposable smoke project session. Send:

```text
Reply exactly FINAL_INSTALLED_APP_SMOKE_OK. Do not use tools.
```

Expected: the transcript shows `FINAL_INSTALLED_APP_SMOKE_OK` and the run returns idle.

7. Verify packaged resources:

```bash
find /Applications/Orchestrator.app/Contents/Resources/pets -maxdepth 2 -type f | sort
```

Expected: each bundled pet has `pet.json` and `spritesheet.webp`. Running `/pet` should not show missing-resource errors.

8. Toggle Terminal and run:

```text
echo TERMINAL_COMMAND_VISIBLE_OK
```

Expected: the terminal pane shows the command and `TERMINAL_COMMAND_VISIBLE_OK`.

## P5-P7 Computer Use Smoke Script

Use this after command/skill/settings/fixture changes. The current disposable project is `/private/tmp/orchestrator-agent-ui-smoke`.

1. Project command: open slash palette, run `/ui-smoke`, approve only the harmless listed command if prompted, and verify `P5_PROJECT_COMMAND_OK`.
2. Project skill: open slash palette, run `/skill:tiny-skill`, and verify `tiny skill loaded`.
3. Global command: create a disposable global command only for the smoke, run it from the palette, verify `P5_GLOBAL_COMMAND_OK`, then delete the global command file.
4. Global skill: create a disposable global skill only for the smoke, run it from the palette, verify `P5_GLOBAL_SKILL_OK`, then delete the global skill directory.
5. Settings MCP: click `MCP servers` and `MCP details`; failure states should render as compact status rows and details should be structured, not raw JSON.
6. Settings plugins: click `Plugins`; empty local config should render compactly as `None`.
7. Settings agents: click `Configured agents`; the installed app should show the current configured Claude agents compactly.
8. Gated provider management: inspect `Purge project state`; it must remain a disabled/manual terminal handoff unless the user explicitly confirms a destructive provider-state action.
9. Layout/design pass: while completing the above, watch for page-level horizontal scroll, overwide cards, raw event spam, duplicated sidebar controls, and stale active-state labels on answered permission/question cards.

## Verification Commands

Use the strongest feasible set for the change:

```bash
npm run test:providers
npx tsc -p tsconfig.web.json --noEmit
npm run test:smoke-config
npm run smoke:providers
npm run build
npm run live:claude-capabilities
LIVE_PROVIDERS=claude npm run live:providers
```

Live Claude commands may need to run outside the sandbox because the local Claude API key helper can require network/keychain access. Use Sonnet for live Claude tests.

## Update Protocol For Future Agents

When implementing against this plan:

1. Read this file first.
2. Pick the first unchecked backlog item whose dependencies are done.
3. Confirm the current implementation from code before changing status.
4. Implement the smallest durable slice that moves a feature toward `Complete`.
5. Add or update tests and fixtures.
6. Run relevant verification commands.
7. Update the Feature Registry row, Execution Backlog checkbox, Required Fixtures, and Completion Gates.
8. Add a dated note below only for important live verification results or product decisions.
9. Commit at a stable checkpoint if the change is broad or user-facing.

## Decision Log

### 2026-05-24

- Workbench new-tab surface checkpoint: re-read the current Codex side-panel bundle chunk for `thread.sidePanel.newTab.*` actions, replaced the small Workbench plus dropdown path with a real `New tab` page, and verified Files/Side chat/Browser/Review/Terminal action cards through focused right-panel smoke. Verification passed: `npm run build`, `npm run test:providers` (250 tests), `git diff --check`, and `npm run smoke:ui:auto -- --right-panel` with `workbenchPanelNewTabPage=true`. Evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779619959129.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779619959129.png`.
- Workbench new-tab visual checkpoint: added a dedicated `--workbench-new-tab` capture and full-inventory entry so the New Tab action grid itself is screenshot evidence, not just a transient state inside the right-panel smoke. Latest evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1779621364872.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1779621364872.png`; inventory screenshot `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-current/workbench-new-tab.png`.
- Review/Environment preservation checkpoint: opening Review now seeds the sibling Environment Workbench tab through the store-level `setShowDiff` path, so the Codex-style Review entry card, Review tab, and Environment tab stay reachable after the New Tab replacement. Latest `npm run smoke:ui:auto -- --diff-entry` passed with `reviewTranscriptCard=true`, `reviewTranscriptCardUndo=true`, `reviewMetadataToolbar=true`, and `reviewEnvironmentPanel=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-entry-1779621196095.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-entry-1779621196096.png`.
- Review/Environment action-state checkpoint: Environment rows now expose staged/unstaged state, `Commit` routes to Review when local changes exist, and PR metadata turns `Create pull request` into an enabled `View pull request` external-open row while missing PR metadata stays explicitly disabled. Latest `npm run smoke:ui:auto -- --diff-entry` passed with actionable Environment row assertions and `reviewEnvironmentPanel=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-entry-1779623243361.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-entry-1779623243361.png`. Dedicated `npm run smoke:ui:auto -- --environment` passed with `environmentPanelVisual=true`, `environmentActionRows=true`, and `environmentSources=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1779623586809.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1779623586809.png`. Verification also passed: `npm run build`, `npm run test:providers` (250/250), and `git diff --check`.
- Full side-panel visual inventory checkpoint: refreshed the full screenshot matrix after fixing stale New Tab harness assumptions and adding the Environment capture. `npm run smoke:visual:side-panels -- --full --out tmp/side-panel-visual-inventory-current` passed with 23 captures and no failures; manifest `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-current/manifest.json`; Environment screenshot `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-current/environment.png`.
- Multi-window native-menu isolation checkpoint: native menu availability is now stored per `BrowserWindow` and applied through an app-owned active-window command target, so a background renderer can no longer clobber the focused window's panel command enabled state and the most recently created window no longer hijacks menu command routing. `npm run smoke:ui:auto -- --multi-window-focus` now passes for second-window creation, pending session navigation, first-window Browser command availability, second-window Browser command disablement, background-window no-clobber behavior, app-owned active-window restoration, first-window menu restoration, and menu-command routing back to the first window. Evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-multi-window-focus-1779624345721.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-multi-window-focus-1779624345721.png`. Raw Electron `BrowserWindow.getFocusedWindow()` still reports false after programmatic refocus in this automation path, so live/manual OS focus proof remains separate.
- Packaged right-panel shell checkpoint: rebuilt the temp packaged app with `npm run pack:mac`; `npm run smoke:ui:auto -- --packaged --right-panel` passed with command-state, Browser command routing, focused find routing, close/reorder/resize behavior, and Browser visual reset covered. Evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779618973456.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779618973456.png`.
- Side-panel visual inventory reliability checkpoint: the Settings smoke now reopens Settings, restores the Graphite Dark appearance preset, lands on General, and scrolls to the top before screenshot capture, so `settings.png` is real Settings evidence instead of a post-worktree chat surface.
- Verification passed for this checkpoint: `npm run smoke:ui:auto -- --settings` and core `npm run smoke:visual:side-panels -- --out tmp/side-panel-visual-inventory-current`; manifest `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-current/manifest.json`, Settings screenshot `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-current/settings.png`.
- Settings host-context checkpoint: re-read Codex's `settings-host-context`, `settings-host-selection`, `settings-host-dropdown`, and `settings-page` bundle chunks, added a provider-host selector backed by session host metadata, placed it as the Host navigation section action like Codex, persisted and normalized the selected Settings host id, and exposed selected host metadata on the Settings shell. Verification passed: `npm run build`, `npm run test:providers` (252/252), and `npm run smoke:ui:auto -- --settings` with `settingsHostContext=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779628185325.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779628185325.png`. Remaining Settings parity is route/page ownership and host-specific page filtering/behavior.
- Browser manager run-event checkpoint: added provider-agnostic `browser.manager_state` events, reducer coverage, and renderer session-event application so provider-originated Browser manager state can open Browser and update browser-use active/turn id, viewport, capture surface, and cursor state. Verification passed: `npm run build`, `npm run test:providers` (255/255), `git diff --check`, and `npm run smoke:ui:auto -- --browser` with `browserManagerStateBridge=true`, `browserWebviewPersistence=true`, and `browserHiddenWebviewContainment=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779628706045.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779628706045.png`. Remaining Browser parity is real provider adapter emission, shell-level webview host ownership, conversation/fork transfer, route lifecycle, capture geometry ownership, and deeper annotation behavior.
- Codex browser-use notification adapter checkpoint: re-extracted current Codex Browser/app-server chunks from `/Applications/Codex.app/Contents/Resources/app.asar` into `/private/tmp/orch-codex-browser-audit` and confirmed `browser-sidebar-manager-ivre5jEI.js` subscribes to `browser-sidebar-browser-use-state`, `browser-sidebar-browser-use-viewport`, `browser-sidebar-browser-use-capture-surface`, and `browser-sidebar-browser-use-cursor-state`. The Codex provider parser now converts those channels, plus direct `browser.manager_state` payloads, into provider-agnostic Browser manager run events. Verification passed: `npm run test:providers` (256/256), `npm run build`, `git diff --check`, and `npm run smoke:ui:auto -- --browser` with `browserManagerStateBridge=true`, `browserWebviewPersistence=true`, and `browserHiddenWebviewContainment=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779629083505.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779629083505.png`. Later 2026-05-24 Browser slices covered body-host ownership, local fork DOM transfer, and local browser-use no-mutation smoke. Fresh `npm run live:codex-browser-appserver` proof reaches the real Codex app-server and completes a turn, but the tested app-server client protocol emits no `browser.manager_state` events, sends no browser/tool server requests, and returns `CODEX_BROWSER_LIVE_NO_BROWSER`; artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-browser-appserver-live-proof/result.json`. Remaining Browser parity is finding an exposed live Codex browser client-tool/state boundary, route lifecycle, capture geometry ownership, and deeper annotation behavior.
- Browser fork-transfer checkpoint: Codex's Browser manager exposes `transferWebview(sourceConversationId, targetConversationId)`. Orchestrator now transfers Browser workbench state when a chat is forked from titlebar or sidebar actions, opens the Browser tab in the fork when it was open in the source, and clears transient browser-use overlay state from the source. Verification passed: `npm run build`, `git diff --check`, and `npm run smoke:ui:auto -- --browser` with `browserForkTransfer=true` plus the existing manager bridge and hidden-webview checks; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779629557406.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779629557406.png`. The later Browser cross-host DOM transfer slice covers the existing-node transfer gap, and the later no-mutation slice covers local browser-use overlay safety; remaining Browser parity is live provider-emitted Codex/browser-use proof, route lifecycle, capture geometry ownership, and deeper annotation behavior.
- Terminal visual-evidence checkpoint: `--terminal` remains the behavior smoke, while `--terminal-visual` now opens a stable bottom terminal panel for inventory capture. Core `npm run smoke:visual:side-panels -- --out tmp/side-panel-visual-inventory-current` passes and `terminal-bottom-panel.png` is no longer a black frame or failure-overlay capture. When `node-pty` fails with `posix_spawnp failed`, Terminal now falls back to a pipe-backed shell and `--terminal-visual` asserts `terminalVisualHealthyContent=true`. Remaining caveat: exact live Codex height/animation spacing and native PTY availability still need separate proof.
- Review copy-git-apply checkpoint: Review options now copies a `git apply` heredoc for the loaded Review patch set instead of only the selected file. Focused `npm run smoke:ui:auto -- --diff-core` passed with `reviewGitApplyCommandCoversAll=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1779610610409.json`.

### 2026-05-19

- Header provenance checkpoint: the titlebar now shows a compact environment icon plus project/folder, branch when available, and provider/model metadata under the active chat title. The old idle/status chip stays removed; deeper thread actions live in the chat actions menu, which now includes copy folder path and copy session ID.
- Verification passed for the header provenance checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, and `npm run smoke:ui:auto -- --inspector`; inspector smoke now asserts `headerIdentity` and `headerActionMenu`.
- Sidebar project-actions checkpoint: project headers now have a compact actions menu for rename, pin/unpin, open folder, archive project chats, and remove project. Pinned projects sort above unpinned projects without changing pinned-chat ordering.
- Verification passed for the sidebar project-actions checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, and `npm run smoke:ui:auto -- --sidebar`; sidebar smoke now asserts `projectActionMenuWorks`, `projectRenameWorks`, and `projectPinWorks`.
- Settings structure checkpoint: Settings now separates provider defaults from provider diagnostics/config/probes and adds Browser plus Data controls sections for the active local profile. Capabilities remains a separate left-nav surface instead of being buried in Settings.
- Verification passed for the settings structure checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, and `npm run smoke:ui:auto -- --settings`; settings smoke now asserts `settingsDiagnosticsSection`, `settingsBrowserPage`, `settingsBrowserSurface`, `settingsBrowserModule`, and `settingsDataControls`.
- Theme parity coverage checkpoint: portable theme import/export now lives in shared `src/types/themeSharing.ts` so Settings and node tests use the same schema validator.
- Verification passed for the theme parity coverage checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `npm run test:providers`, and `npm run smoke:ui:auto -- --settings`; the provider test suite now covers valid round-trip, default code themes, missing prefix, invalid variant, invalid hex colors, invalid contrast, and invalid semantic colors.
- Command palette verification checkpoint: the transcript-layout smoke now explicitly verifies command-palette open shortcuts, grouping, recent commands, fuzzy terminal lookup, shortcut labels, and the command-palette search-transcript action.
- File-card confidence smoke checkpoint: the transcript-layout fixture now includes both an unresolved relative prose reference and an explicit absolute missing path, and the smoke verifies the relative prose card is suppressed while the absolute missing card keeps disabled actions.
- Sidebar live-transition checkpoint: the sidebar smoke now drives a pinned row from running to completed/unread while another chat is pinned, and verifies the spinner, completion dot, and pinned order remain stable.
- Transcript polish gate checkpoint: `npm run smoke:ui:auto -- --transcript-stress`, `npm run smoke:ui:auto -- --session-switch`, and `npm run smoke:ui:auto -- --scroll` passed as the long-thread/session-switch/streaming-scroll verification set.
- Packaged pet-overlay checkpoint: `npm run pack:mac` rebuilt the packaged app, `npm run smoke:ui:auto -- --packaged --pet-overlay` passed in a renamed temp bundle, and packaged resources include ditto/orchestrator/pika/psyduck `pet.json` plus `spritesheet.webp`.
- Right-panel tab shell checkpoint: the right-panel tab strip now follows stored tab order, exposes a context menu with move-left/move-right/close actions, and inspector smoke verifies the browser/files active order changes after moving Browser left.
- Browser workbench checkpoint: Browser tab controls now include persisted in-page find, zoom in/out/reset, desktop/mobile preview, and reload without cache; webview popups are denied/externalized by the main process. Inspector smoke verifies real find matches, zoom state, mobile viewport width, cache reload completion, screenshot, and local embedded page load.
- Browser screenshot status checkpoint: Browser screenshot capture now announces `Screenshot saved` through a live status and `Add screenshot` updates it to `Screenshot attached`, with focused Browser smoke verification passed at `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780030113342.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780030113342.png`; the gate is `browserScreenshotStatusA11y=true`.
- Files workbench checkpoint: Files tab now renders image previews, PDF/system-viewer fallback copy, binary fallback copy, and bounded text previews through a preview IPC that sniffs extensionless binaries before decoding; inspector smoke verifies text preview, binary fallback, no-results state without a stale preview/action, and add-to-chat.
- Right-sidebar completion checkpoint: side-chat tabs now persist independent drafts across tab switches and expose pending/error/unread badges; terminal tabs now have a context menu with move-left/move-right/close behavior; header chat actions expose backed copy actions for folder/project/session/provider session IDs. The parity matrix was reconciled so completed sidebar/right-panel rows no longer read as stale `Partial` work.
- Transcript verification checkpoint: streaming assistant text, code block behavior, markdown table wrapping, and raw-event cleanliness are now marked complete. `npm run smoke:ui:auto -- --scroll` verifies visible streaming cursor, manual-scroll preservation, completion cursor cleanup, and final-text dedupe; `npm run smoke:ui:auto -- --transcript-layout` verifies desktop/narrow code and table bounds plus raw provider data staying out of the transcript.
- Provider diagnostics checkpoint: Settings diagnostics now shows captured local usage/cost/duration alongside provider quota and budget/fallback support status. The settings smoke seeds Claude usage metadata and verifies the Provider diagnostics usage card; Antigravity SDK is now tracked as a future research provider in P8.
- Resume/session checkpoint: reconciled the stale top-level runtime rows with detailed P1/P2/P3 evidence. Claude structured chat and resume/continue are now marked complete because installed-app smokes covered multi-turn continuity, permission continuation, AskUserQuestion answers, and plan continuation; provider tests now lock the permission and user-input resume prompt shapes.
- Antigravity launch research checkpoint: captured the current split between official Gemini API managed-agent docs, the new Antigravity CLI/desktop/SDK launch announcements, and the alpha `google-antigravity` package. Treat Antigravity as a future SDK-first provider track until local package import, streaming, policy, MCP, and usage fixtures exist.

### 2026-05-18

- Sidebar/shortcut checkpoint: pinned chats are global above projects, pin/unpin is available on hover, double-click rename works from the sidebar row, running rows use a spinner in the status-dot slot, idle gray status dots and the header idle/status chip were removed, and Settings now includes a Shortcuts section.
- Transcript search decision: search should not be permanent thin chrome. It opens on `Cmd+F`/`Ctrl+F`, focuses the input, and closes without leaving empty search UI behind.
- Command palette decision: app-wide actions should be discoverable with `Cmd+K`/`Ctrl+K`, while provider/project prompt commands remain in the composer slash palette.
- Codex parity shortcut decision: mirror the installed Codex app's broad shortcut shape where it fits Orchestrator: native Electron menu accelerators, `Cmd+Shift+P` as a command-palette alternate, `Cmd+J` for terminal, `Cmd+Alt+R` rename, `Cmd+Alt+P` pin/unpin, `Cmd+1-9` chat jumps, and a searchable Shortcuts settings table.
- App install safety decision: `/Applications/Orchestrator.app` replacement must go through `npm run install:mac`, which refuses to copy over a running installed app unless `ORCHESTRATOR_ALLOW_RUNNING_APP=1` is explicitly set. Packaged smokes launch renamed temp bundles instead of the raw `dist/mac-arm64/Orchestrator.app`.
- Command shortcut source-of-truth decision: native menus, command palette, and Settings shortcuts share `src/types/appCommands.ts`; command palette filtering uses scored fuzzy matching and records a small local Recent section.
- Crash note: `Orchestrator-2026-05-18-152656.ips` was an early native `_RegisterApplication`/`NSApplication init` abort from a packaged smoke-test app under the repo path, not the installed `/Applications/Orchestrator.app`. Avoid direct raw packaged launches while the installed app is running.
- Transcript paging copy decision: users should not see implementation language like "earlier messages hidden for faster chat switching." Older pages remain available, but the affordance should read as a quiet history action, not a performance warning.
- Transcript scroll decision: streaming output should not fight manual reading. Auto-follow is allowed only while the user is already near the bottom; upward scroll intent disables following until the user explicitly jumps back to latest.
- Transcript performance direction: bounded recent rendering is acceptable as an implementation detail, but the product promise is a complete searchable transcript. If long chats keep growing, prefer virtualized rendering and lazy hydration over user-visible "hidden messages" language.
- Lazy loading direction: the next transcript performance pass should make the full thread feel physically scrollable while only mounting the visible window plus overscan. Manual "Show earlier" can remain as a fallback, but it should not be the primary long-thread experience.
- Virtualization checkpoint: transcript rows now render through a measured virtual window with overscan, while older chunks hydrate automatically near the top and preserve the visible row anchor. This keeps long threads feeling complete without mounting every loaded row at once.
- Stress checkpoint: `npm run smoke:ui:auto -- --transcript-stress` seeds a 2,501-message transcript, verifies the latest page renders within budget, lazy-loads older chunks near the top, jumps to an early result through full-transcript search, and keeps mounted virtual rows bounded.
- Verification passed for the transcript-virtualization checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, `npm run smoke:ui:auto -- --transcript-stress`, `npm run smoke:ui:auto -- --session-switch`, `npm run smoke:ui:auto -- --transcript-layout`, and `npm run smoke:ui:auto -- --scroll`.
- Verification passed for the lazy-transcript checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, `npm run smoke:ui:auto -- --session-switch`, and `npm run smoke:ui:auto -- --scroll`.
- Verification passed for the streaming-scroll checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, `npm run smoke:ui:auto -- --scroll`, `npm run pack:mac`, copy to `/Applications/Orchestrator.app`, and packaged-vs-installed `app.asar` hash comparison.
- Verification passed for the sidebar/search/layout checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, `npm run smoke:ui:auto -- --sidebar`, `npm run smoke:ui:auto -- --transcript-layout`, `npm run smoke:ui:auto -- --session-switch`, `npm run pack:mac`, copy to `/Applications/Orchestrator.app`, and packaged-vs-installed `app.asar` hash comparison.
- Codex sidebar parity checkpoint: chat rows now use a compact title-first layout without a leading folder icon, reserving folder chrome for projects. Hover-revealed actions, keyboard focus behavior, and a Codex-style hover card still expose project, folder, branch/worktree, provider/model, status, and updated time. Branch lookup is lazy through the main-process git IPC so long sidebars do not eagerly probe every repo.
- Verification passed for the sidebar parity checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --sidebar`; the sidebar smoke now asserts hover-card visibility and the absence of chat environment icons.
- Focused smoke taxonomy checkpoint: targeted UI verification is split by surface so small changes do not need the broad inspector regression path. Use `npm run smoke:ui:auto -- --header` for titlebar/header actions, `--right-panel` for tab chrome and panel sizing, `--diff` for Review/Diff behavior, `--files` for Files previews/actions, `--side-chat` for side-chat tabs, and the existing focused flags such as `--sidebar`, `--browser`, `--settings-providers`, `--plan`, `--terminal`, `--extensions`, `--composer`, and transcript smokes for their surfaces. Keep `--inspector` as an explicit broad regression run.
- Sidebar organization checkpoint: the sidebar now has a persisted Organize menu with By project, Recent projects, Chronological list, Sort by updated, and Sort by created modes, plus show-more/less inside long project groups. This matches the Codex direction of making organization a sidebar-level control while keeping Orchestrator provider-neutral.
- Verification passed for the sidebar organization checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --sidebar`; the sidebar smoke now switches into Chronological list and restores By project.
- Files-tab checkpoint: the right-panel shell now has an explicit Files tab opener. The tab keeps its durable shell state, shows a bounded workspace tree with search, previews text files, can copy/reveal/open selected paths, and can attach a selected file to the active composer.
- Verification passed for the Files-tab checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --inspector`; inspector smoke seeds a nested folder with spaces, searches that path in the Files tab, verifies preview content, attaches it to the composer, then returns to the Changes tab so existing right-panel assertions still compare against the Review surface.
- Browser-tab checkpoint: the right-panel shell now has an explicit Browser tab opener with a Codex-shaped empty state, URL bar, back/forward/reload controls, screenshot capture preview, external-open IPC, and quick local URL affordances. Electron `webviewTag` is enabled for the main window so this tab is a real embedded browser surface instead of a static link preview.
- Verification passed for the Browser-tab checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --inspector`; inspector smoke starts a local HTTP fixture, loads it in the Browser tab, confirms the side-panel title path, captures a screenshot preview, and returns to the Changes tab.
- Side-chat checkpoint: `/btw` now creates durable `sidechat:<id>` right-panel tabs with independent message arrays, per-chat titles, active-chat tracking, and close cleanup. Existing side-question storage remains as a compatibility fallback, but new composer and slash-palette `/btw` flows use side-chat tabs.
- Verification passed for the Side-chat checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --inspector`; inspector smoke opens two blank `/btw` side-chat tabs, verifies both are present in the tab strip, closes the active side-chat tab, and verifies the tab is removed.
- Terminal shell checkpoint: bottom-panel terminal state now lives in the per-session store with persisted height, tab list, active tab, and next-tab id. The terminal shell exposes Codex-like bottom-panel focus/data hooks while preserving the existing xterm-backed shell behavior and explicit tab close/kill path.
- Verification passed for the Terminal shell checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --terminal`; terminal smoke opens a second terminal tab, hides and restores the panel, and verifies the tab list, active tab, and height are preserved.
- Theme model checkpoint: settings now include Codex-shaped light/dark/system theme fields, per-variant chrome themes, per-variant code theme IDs, UI/code font sizes, pointer-cursor/font-smoothing/reduced-motion toggles, and a token bridge from Orchestrator variables to `--color-token-*` style names. `applyAppearance` resolves the v2 model while retaining legacy named-theme compatibility.
- Verification passed for the Theme model checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --settings`.
- Custom theme editor checkpoint: Appearance settings now expose Codex-style light/dark/system selection, per-variant chrome editors for accent/background/foreground/contrast/opaque windows, semantic color controls, UI/code font-size sliders, and font-smoothing/pointer-cursor/reduce-motion toggles.
- Verification passed for the Custom theme editor checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --settings`.
- Theme import/export checkpoint: Appearance settings now copy light/dark portable `codex-theme-v1:` strings and import validated portable themes into the matching light or dark variant while preserving code theme IDs.
- Verification passed for the Theme import/export checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --settings`; settings smoke imports a valid portable light theme and verifies the sharing controls render.
- Settings taxonomy checkpoint: Appearance settings are grouped as Mode, Presets, Theme editor, Sharing, Typography, and Layout and reading, so the new Codex-level theme controls are categorized instead of being one long mixed list.
- Verification passed for the Settings taxonomy checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --settings`; settings smoke now asserts the Appearance taxonomy headings.
- Pet personalization checkpoint: the overlay resize affordance is now a compact bottom-right corner target instead of a broad pet-surface hit area, and the overlay root no longer advertises a resize cursor while the normal mascot body remains draggable.
- Verification passed for the Pet personalization checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --pet-overlay`; pet smoke now asserts compact handle geometry, non-resize root cursor, hidden grip on mascot hover, visible grip on handle hover/focus, and existing notification/status behavior.
- Right panel state checkpoint: the inspector now writes through per-session `rightPanel` state with durable tab ids, active tab, width, full-width flag, and open state while preserving existing Diff, Plan, Agents, Extensions, and Side Question behavior. This is the foundation for replacing the inspector with a Codex-style app-shell tab workbench.
- Verification passed for the right panel state checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --inspector`; the inspector smoke now asserts the right-panel active tab, tabs list, and persisted width data path.
- Right panel shell checkpoint: the inspector now exposes Codex-like app-shell hooks, closeable durable tabs, a persisted Expand/Restore panel width control, and a right-panel focus area/tab-controller structure. This is still a shell pass; tab reordering and context-menu polish are deferred to the deeper workbench pass.
- Verification passed for the right panel shell checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --inspector`; the inspector smoke now clicks Expand panel and verifies the full-width state before restoring.
- Existing panel port checkpoint: Diff, Plan, Agents, Extensions, and Side Question content now render through the durable right-panel shell without changing their internal behavior, so future CP-007 and CP-010 work can improve individual tabs without replacing the container again.
- Review tab checkpoint: the Changes tab now behaves more like a review surface, with git-backed smoke data, changed-file search, a line-wrap control, open-file action, clearer no-diff context, and a changed-file preview that stays inside the right-panel shell. This is not yet Codex's full file-source tab system, but it moves the existing Diff panel into the Review direction.
- Verification passed for the Review tab checkpoint: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `git diff --check`, and `npm run smoke:ui:auto -- --inspector`; the inspector smoke seeds modified/deleted/untracked files and verifies Review search against the changed-file list.
- Review preview hardening checkpoint: the Review tab now uses the shared bounded file-preview IPC for binary/image/PDF/source fallback states, disables open/reveal for deleted files, adds reveal/copy actions, clears stale search selection, and shows an intentional binary state instead of raw git binary output.
- Pin-order reliability checkpoint: pinned chat ordering now lives in shared `src/types/sessionOrdering.ts`, is covered by `sessionOrdering.test.ts`, and is used by both main-process migration and renderer sorting. New pins append after existing pinned chats, and message recency no longer moves ordered pins.
- File-card confidence checkpoint: unresolved relative references extracted from assistant prose are suppressed after workspace resolution fails, while explicit absolute missing references can still render as disabled missing cards. This prevents review comments like bare class names or snippets from becoming visible missing-file cards.
- Verification passed for the pin/file-card checkpoint: `npm run test:providers` passed 156/156, `npm run build` passed, `npm run smoke:ui:auto -- --sidebar` passed, and `git diff --check` passed.

### 2026-05-11

- Created this canonical source-of-truth file.
- Current code default for Claude sessions is structured/headless CLI mode with hook approval bridge. The old Claude-native chat parser/prompt bridge has been removed from the app runtime.
- Older docs now point here for active status. Some historical notes still describe native CLI experiments, but their active runtime-decision sections have been aligned to the structured Claude default.
- Removed superseded plan/checklist docs from the remote-bound tree so this file remains the only active product specification.

### 2026-05-12

- Added `ProviderRuntimeManager` as the process lifecycle owner for current CLI lanes, including Claude hook prep, stdout parsing, stop, and interrupt-for-steer behavior.
- Added project/global Claude command and skill discovery with frontmatter descriptions, source-scoped slash palette grouping, and `$ARGUMENTS` expansion.
- Settings Native surfaces now include compact MCP list/details, plugin JSON list, and agents list rendering while mutating/quota commands remain terminal/confirmation handoffs.
- Fixed live Claude capability capture to parse structured `-p stream-json` stdout and use the structured runtime for normal Claude capability scenarios.
- Last verified: `CLAUDE_CAPABILITY_STRICT_EMPTY_MCP=1 npm run live:claude-capabilities` passed with Sonnet (`claude-sonnet-4-6`, low effort), covering plain answer, file ops, plan mode, streaming, auth status, MCP list, plugin JSON list, auto-mode defaults, and agents list.
- Last verified: `LIVE_PROVIDERS=claude npm run live:providers` passed with Sonnet (`claude-sonnet-4-6`, low effort), capturing `session.started`, assistant streaming, and `run.completed`.
- `npm run pack:mac` rebuilt `dist/mac-arm64/Orchestrator.app`; packaged resources include the bundled pets, and the packaged app launch was confirmed by process list. It was not copied over `/Applications/Orchestrator.app`.
- Verified `npm run test:providers`, `npx tsc -p tsconfig.web.json --noEmit`, `npm run test:smoke-config`, `npm run smoke:providers`, `npm run build`, `npm run pack:mac`, and `git diff --check`.
- Checkpoint commit `97d30c39` (`Polish Claude agent UI flows`) landed the live-tested permission, plan, subagent, skills, settings, slash, and terminal polish pass.
- Live installed-app GUI smoke before the final label-only polish verified Write allow once, Write allow session, Write deny, AskUserQuestion choices, slash palette grouping, Diff for a real smoke git repo, project command discovery, project skill directory rendering, completed subagent sidebar transcript, and plan approval flow through Claude structured `Plan Ready`.
- The final installed-app Computer Use attach timeout no longer reproduces after restart/reinstall. `get_app_state` returned the `/Applications/Orchestrator.app/.../out/renderer/index.html` accessibility tree in 0.5-2.3 seconds.
- Final installed app plain Claude smoke passed: `Reply exactly FINAL_INSTALLED_APP_SMOKE_OK. Do not use tools.` returned `FINAL_INSTALLED_APP_SMOKE_OK` using Claude Sonnet 4.6 High in Ask mode.
- Packaged resource smoke passed: `npm run test:smoke-config` passed, installed pet resources include ditto/orchestrator/pika/psyduck `pet.json` and `spritesheet.webp`, and `/pet` applied without visible missing-resource errors.
- Terminal command input smoke passed in the installed app: Computer Use entered `echo TERMINAL_COMMAND_VISIBLE_OK`, clicked `Run`, and the terminal pane showed both the command and `TERMINAL_COMMAND_VISIBLE_OK`.
- Terminal design smell fixed during smoke: the terminal pane now uses a consistent dark palette instead of inheriting the light app background.
- Verification for this installed-app checkpoint: `npm run test:providers`, `npx tsc -p tsconfig.web.json --noEmit`, `npm run test:smoke-config`, `npm run pack:mac`, copy to `/Applications`, relaunch, and Computer Use GUI smoke.
- P1 continuity smoke passed in the installed app: turn 1 returned `TURN1_OK`; turn 2 remembered `ORCHID-912` and returned `CONTINUITY_OK: ORCHID-912`.
- P1 stop-during-assistant-text initially failed: Stop removed the running backend state, but the partial assistant message kept its streaming cursor and the composer stayed visually stuck. Fixed by finalizing interrupted streaming/queued text when a run stops or when old non-running sessions load.
- P1 stop-during-assistant-text retry passed in the installed app: a long `STREAM_STOP_RETRY_LINE` run was interrupted, the assistant message no longer showed the streaming cursor, Stop disappeared, and entering `Composer usable after stop check` re-enabled Send.
- Verification for the P1 stop/continuity checkpoint: `npm run test:providers` passed 123/123, `npx tsc -p tsconfig.web.json --noEmit` passed, `git diff --check` passed, `npm run pack:mac` had rebuilt the app, and the rebuilt app was copied to `/Applications` before the Computer Use smoke.
- Residual verification note: one `Computer Use get_app_state` attach after relaunch took 87 seconds before returning the installed app tree. The app was responsive afterward, so treat this as a CUA/session flake unless it recurs.
- P1 stop-during-tool-execution passed in the installed app: a harmless Bash sleep command was allowed once, then stopped while active; the session returned to idle and the composer re-enabled Send after text entry.
- P1 stop-during-permission-pause initially exposed a real gap: the composer did not show Stop while Claude was waiting on a Write approval. Fixed by making `canStopSession` include `waiting_for_permission` and by rendering closed permission cards as inactive. Retry passed: Stop closed the pending request, the composer was usable, and both smoke files remained absent.
- P1 queue-next passed in the installed app: a follow-up was queued while a long primary answer streamed, the queued card showed `Queued` and `Steer`, and the follow-up ran to `P1_QUEUE_FOLLOWUP_OK` after the primary completed.
- P1 steer-next initially failed with a false provider-error state because Claude's intentional interrupt emitted `run.failed`, which lifecycle handling treated as a crash and killed the runtime before the queued follow-up could start. Fixed by filtering expected interrupt failures while a steer follow-up is pending and by settling the interrupted assistant stream before resuming the follow-up.
- P1 steer-next retry passed in the freshly reinstalled app: the primary answer stopped around `P1_STEER_RETRY_PRIMARY 181`, no error card appeared, and the follow-up returned `P1_STEER_RETRY_OK`.
- P1 transport decision: do not replace the current structured resume/interrupt path with a bidirectional stdin transport for P1. P1-002 through P1-007 now pass; keep `--input-format stream-json` as a deferred option for future same-process-only user-question or plan semantics.
- P3 AskUserQuestion choices passed in the installed app: Claude showed `Answer Required` with `ALPHA_P3_CHOICE`/`BETA_P3_CHOICE`, `ALPHA_P3_CHOICE` resumed as user input, and the run ended with `P3_CHOICES_DONE:ALPHA_P3_CHOICE`.
- P3 AskUserQuestion free-form passed in the installed app: typed `FREEFORM_P3_TOKEN_742`, the answer resumed through user-input UI rather than permission UI, and Claude returned `P3_FREEFORM_DONE:FREEFORM_P3_TOKEN_742`.
- P3 plan approve passed in the installed app: native `Plan Ready` appeared, `Approve Plan` resumed, a real workspace Write prompt appeared for `/private/tmp/orchestrator-agent-ui-smoke/p3-plan-approve.txt`, `Allow Once` created content `P3_PLAN_APPROVE_OK`, and Claude returned `P3_PLAN_APPROVE_DONE`.
- P3 plan keep-planning passed in the installed app: `Keep Planning` changed the plan card to `Kept planning`, Claude stayed in plan mode, follow-up `Keep planning` kept execution blocked, and `/private/tmp/orchestrator-agent-ui-smoke/p3-plan-keep-planning.txt` remained missing.
- P3 permission scope coverage is complete for the current contract: live Write cards show readable path scopes, and `toolActions.test` now covers Write/Edit/Bash/ExitPlanMode/WebFetch/MCP plus long-path truncation.
- P3 permission mode picker passed in the installed app before the later simplification: the menu showed Ask, Auto-edit, Plan, Auto safe, Allowlist, and isolated-only Bypass unsafe; no-tool runs in Auto-edit, Auto safe, and Allowlist returned exact sentinel replies. Bypass unsafe was inspected but not run.
- Verification for the P1 semantics checkpoint: `npm run test:providers` passed 125/125, `npx tsc -p tsconfig.web.json --noEmit` passed, `npm run pack:mac` passed, the app was copied to `/Applications`, relaunched, and Computer Use verified the installed UI steer retry.
- P2 workspace-effects smoke passed in the installed app on disposable repo `/private/tmp/orchestrator-agent-ui-smoke`: Claude read/listed/searched `P2_SEARCH_NEEDLE`, created `p2-created-by-claude.txt`, edited `p2-edit-target.txt`, deleted `p2-delete-target.txt`, and the filesystem/Diff panel matched those changes.
- P2 Bash permission smoke passed in the installed app: allow-once resumed a harmless `printf`, allow-session let the second harmless Bash command run without another prompt, and deny produced `Error — Permission denied by user` while leaving `p2-bash-deny.txt` absent.
- P2 file-reference matrix initially exposed a real bug: paths with spaces truncated into a bogus missing `p2` card, quoted paths and `~/...` paths were missed, and the four-card cap hid later references. Fixed by adding quoted/whole-line/tilde extraction, home-path resolution, truncated-space guardrails, and an 8-card visible cap.
- P2 file-reference retry passed in the installed app: cards resolved `p2-read-search.txt`, `"p2 paths/quoted path file.txt"`, the long path with spaces, generated `p2-created-by-claude.txt`, missing `p2-missing-reference.txt` with disabled Open/Reveal, and `~/Desktop/Orchestrator/docs/orchestrator-source-of-truth.md`.
- P2 Diff edge-case smoke passed in the installed app: the Diff panel showed `3 modified · 1 added · 1 deleted · 1 untracked +53 -3` across deleted, edited, large modified, staged added, staged modified, and untracked files, with previews for deletion, large diff, and staged add.
- Verification for the P2 checkpoint: `npm run test:providers` passed 126/126, `npx tsc -p tsconfig.web.json --noEmit` passed, `npm run pack:mac` passed twice after UI fixes, the app was copied to `/Applications`, relaunched, and Computer Use verified P2-008 and P2-009 in the installed app.
- P4 Task happy path passed in the installed app: Claude delegated README reading through Task, the main transcript showed compact delegation/tool summaries, and the Agents sidebar showed the child transcript with cleaned content.
- P4 active agent chips passed in the installed app: a long Task run paused on `Bash sleep 18`, showed a running chip above the composer, and clicking it opened the active child transcript tab before the permission was allowed.
- P4 agent failure smoke initially exposed a real stale-chip bug: denying a subagent Bash permission failed the main run but left the child chip running. Fixed by finalizing active agents on run failure for both event-buffer and saved-transcript reconstruction.
- P4 failure retry passed in the freshly reinstalled app: denying subagent Bash removed the running chip, showed a red failed agent tab with useful approval context, and `/private/tmp/orchestrator-agent-ui-smoke/p4-denied-agent-after-fix-2.txt` remained absent.
- Verification for the P4 checkpoint: `npm run test:providers` passed 128/128, `npx tsc -p tsconfig.web.json --noEmit` passed, `npm run pack:mac` passed, the app was copied to `/Applications`, relaunched, and Computer Use verified Task happy path, running-chip focus, completed sidebar tabs, sidechain transcript display, and failure/denial state.
- P5 command and skill smoke passed in the installed app: `/ui-smoke` returned `P5_PROJECT_COMMAND_OK`, `/skill:tiny-skill` returned `tiny skill loaded`, disposable global command `/orchestrator-global-smoke` returned `P5_GLOBAL_COMMAND_OK`, and disposable global skill `/skill:orchestrator-global-smoke` returned `P5_GLOBAL_SKILL_OK`; the temporary global files were removed afterward.
- P5 settings smoke passed in the installed app: MCP servers/details rendered compact failure rows for local `git`/`wiki-server`, plugin list rendered `None`, configured agents rendered four compact rows, and `Purge project state` stayed a manual terminal handoff without executing a destructive command.
- P6 design walkthrough found one real smell: permission cards could degrade to `Request closed` after approval/navigation. Fixed by persisting explicit permission decisions on result messages and rendering `Allowed once`, `Allowed for session`, `Denied`, or `Kept planning` after the request is inactive.
- P6 permission-card polish retry passed in the rebuilt installed app: approved a harmless Bash `printf`, navigated to Settings and back, and the card still showed `Allowed once`; Claude returned `P6_PERMISSION_DECISION_DONE`.
- P7 fixture refresh landed `hook-approval.jsonl`, `plan-approval-live.jsonl`, `project-command.jsonl`, `project-skill.jsonl`, `sidechain-real.jsonl`, `mcp-web-approval.jsonl`, and `failure-categories.jsonl`; provider tests now cover hook approval, plan approval, project commands/skills, sidechain transcript capture, MCP/web approval mapping, and auth/quota/rate-limit/generic failure categories.
- P4 selected-agent launch option passed in the rebuilt installed app: the composer loaded configured Claude agents from `claude agents`, selecting `Explore` changed the run label to `Claude · Explore · Sonnet 4.6 · High`, and the run returned `P4_SELECTED_AGENT_OK`.
- Selected-agent smoke caught and fixed one parser/design smell: the heading `Built-in agents:` briefly rendered as a bogus agent chip; parser coverage now skips headings/count lines and deduplicates entries.
- Verification for the selected-agent checkpoint: `npm run test:providers` passed 137/137, `npx tsc -p tsconfig.node.json --noEmit` passed, `npx tsc -p tsconfig.web.json --noEmit` passed, `npm run pack:mac` passed, the app was copied to `/Applications`, relaunched, and Computer Use verified the installed UI run.
- Permission mode polish decision: Claude `auto` is now the product default because it best balances convenience with Claude's native safety classifier. Installed-app CUA verified the Settings default-mode control with Auto selected, plus the composer picker showing Auto/Plan/Ask first by default and moving Auto-edit, Preapproved only, raw allow/deny/tools/dirs controls, and Bypass unsafe behind Advanced.
- Isolated UI verification decision: future Computer Use smokes should launch the dev lane with `npm run smoke:app -- --reset --profile devcua --workspace-dir /private/tmp/orchestrator-agent-ui-smoke` and target `Electron`, not `Orchestrator`. The 2026-05-13 retry verified CUA attached to `Orchestrator - Devcua`, showed a clean isolated project state after skipping legacy migration, and opened Settings in the dev window. Packaged smoke still creates a temp renamed bundle and preserves Electron framework symlinks; Computer Use can list that bundle, but this CUA build returned `appNotFound` for `get_app_state("Orchestrator Smokecua")`.
- P8 Codex spike verified on 2026-05-13: local Codex 0.128.0 help/features/schema, live `codex exec --json`, and generated app-server v2 bindings show the real split between headless exec automation and structured app-server approval/question semantics. Dev CUA also caught a settings smell: raw provider config could expose secret-looking env values; the advanced config editor now redacts those values and disables saving redacted files.
- Claude structured Plan sidebar polish verified on 2026-05-13 in isolated dev CUA profile `plan-agent-polish`: a structured plan run rendered TodoWrite tasks in the new Plan rail/sidebar, then after hot reload the sidebar reconstructed the ExitPlanMode markdown plan body from saved chat messages. The smoke also caught and fixed a duplication smell in recent plan updates.
- Claude structured subagent polish verified on 2026-05-13 in the same dev CUA run: two Task subagents completed with readable tabs (`Read README.md first sentence`, `Count files in docs directory`), cleaned transcripts, and no raw `toolu_...` tab labels after lifecycle updates. The run also verified a subagent Bash permission card can allow once and resume.
- `/btw` decision verified on 2026-05-13: `claude -p --output-format json --max-budget-usd 0.02 "/btw ..."` returned `/btw isn't available in this environment.` with zero turns/cost. Treat `/btw` as native interactive side-chat behavior, not a structured `-p` feature; build an Orchestrator-owned side question/chat if we want that UX.
- Attachment/usage/brief polish landed on 2026-05-13: local files can be attached from the composer, Claude file resources map to native `--file` specs, result usage/cost rolls up into a Usage sidebar, and `SendUserMessage` is supported as an assistant status card if a future Claude `--brief` stream emits it.
- Automated detached UI smoke landed on 2026-05-13: `npm run smoke:ui:auto` launches an isolated Electron profile, bootstraps a disposable project/session before renderer load, verifies the profile badge/composer/sidebar rail, and exits with JSON evidence without touching the user's active Orchestrator window.

### 2026-05-14

- Capabilities robustness checkpoint: renderer typecheck was restored, custom capability create results now only claim providers with actual file-backed resources, and portable plugin update/delete keeps mirrored Claude/Codex skill folders in sync instead of leaving stale global skills behind.
- Plugin compatibility decision: Claude and Codex plugin packages can share a plugin root and common `skills/` content, but they are not one manifest/marketplace format. Orchestrator writes both `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json`, plus provider-specific local marketplace entries, then leaves native install/enable as a gated provider action.
- Verification passed: `npx tsc -p tsconfig.node.json --noEmit`, `npx tsc -p tsconfig.web.json --noEmit`, `npm run test:providers` 141/141, `npm run test:smoke-config`, `npm run smoke:providers`, `npm run build`, `git diff --check`, and `npm run smoke:ui:auto -- --capabilities` with screenshot `/var/folders/5n/nwtbs9wj6jl7whlscmg47_pc0000gn/T/orchestrator-automated-ui-smoke-capabilities-1778792716697.png`.

### 2026-05-28

- Composer enter-behavior checkpoint: General Settings now persists a `composerEnterBehavior` preference with `Enter sends` and `Enter inserts line` choices. The main composer listens for settings updates, keeps plain Enter as send by default, and switches newline-mode sending to `Command/Ctrl+Enter` while preserving `Shift-Enter` newline behavior in send mode.
- Verification passed for the composer enter-behavior checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node scripts/run-automated-ui-smoke.mjs --composer`, and `node scripts/run-automated-ui-smoke.mjs --settings`. The focused smokes gate `composerEnterBehaviorSetting=true` and `settingsGeneralComposerEnterBehavior=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780010616758.json` and `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780010650988.json`.
- Worktree lifecycle checkpoint: active chats for pending or failed app-managed worktree sessions now show a header-adjacent lifecycle notice with fork/source context, workdir path, pending status semantics, failed alert semantics, and failed-state retry. This closes the immediate "what is this isolated workspace doing?" gap for daily coding worktree forks while leaving richer provider-native worktree metadata/adapters tracked in PP-038.
- Verification passed for the worktree lifecycle checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --worktree-lifecycle`. The focused smoke gates `pendingWorktreeNotice=true`, `failedWorktreeNotice=true`, and `failedWorktreeRetry=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-worktree-lifecycle-1780013752364.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-worktree-lifecycle-1780013752364.png`.
- Settings search checkpoint: Settings now has a compact topbar search/jump field backed by the shared `WorkbenchSearchField` primitive. It searches visible Settings sections for the current host, shows the matched section target, and routes through the same owned `/settings/{section}` path as sidebar navigation.
- Verification passed for the Settings search checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --settings`. The focused smoke gates `settingsSearchNavigation=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780014386620.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780014386620.png`.
- Settings per-control search checkpoint: General Settings now exposes File handoff and Composer as focused search anchors, Browser Settings exposes Data, Permissions, and Domains anchors, Provider Settings exposes Provider picker/defaults anchors, and Shortcuts exposes a Shortcut bindings anchor so control keywords such as `composer`, `downloads`, `models`, and `keybinding` jump to the exact control group instead of only the page.
- Verification passed for the Settings per-control search checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --settings`. The focused smoke gates `settingsInPageSearchTarget=true`, `settingsBrowserInPageSearchTarget=true`, `settingsProviderInPageSearchTarget=true`, and `settingsShortcutsInPageSearchTarget=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029723853.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029723853.png`.
- Git Workbench tab checkpoint: the Workbench New tab launcher now includes a dedicated Git tab for local branch/change state and first-class Stage all, Unstage all, and Open Review actions. This establishes PP-054's separate Git surface while leaving Review focused on diff/review reading and Copy git apply behavior.
- Verification passed for the Git Workbench tab checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `workbenchNewTabGitAction=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780015277484.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780015277484.png`.
- Git Workbench commit checkpoint: the Git tab now supports the next day-to-day local Git step with a commit-message field and `Commit staged`, backed by main-process `git:commitStaged` IPC. It rejects empty messages/no-staged-change commits and refreshes the right-panel file state after a successful commit.
- Verification passed for the Git Workbench commit checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `workbenchNewTabGitCommit=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780015734717.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780015734717.png`.
- Git Workbench discard checkpoint: the Git tab now supports confirmed `Discard all` through main-process `git:discardPaths` IPC, moving destructive local Git cleanup toward the dedicated Workbench Git surface instead of Review-only bridge controls.
- Verification passed for the Git Workbench discard checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `workbenchNewTabGitDiscard=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780016316039.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780016316039.png`.
- Git Workbench branch checkpoint: the Git tab now supports local `Create branch` through main-process `git:createBranch` IPC, including branch-name validation, checkout to the new branch, refreshed branch state, and preserving dirty working-tree changes.
- Verification passed for the Git Workbench branch checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `workbenchNewTabGitBranch=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780016736246.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780016736246.png`.
- Git Workbench PR-command checkpoint: the Git tab now has a Pull Request card that opens existing PR metadata when present and otherwise generates/copies a `gh pr create --fill --base <base> --head <branch>` command for the current topic branch through the app clipboard bridge.
- Verification passed for the Git Workbench PR-command checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `workbenchNewTabGitPrCommand=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780017110971.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780017110971.png`.
- Agents runtime-event action chrome checkpoint: selected Agents inspector event details now use shared compact toolbar icon buttons for Copy payload and Add to chat, preserving the existing event-detail status region while aligning the inspector action chrome with the rest of the Workbench.
- Verification passed for the Agents runtime-event action chrome checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `agentRuntimeEventActionChrome=true` plus existing copy/add-to-chat/context/filter/transport checks with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780031671592.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780031671592.png`.
- Workbench New Tab keyboard checkpoint: the Workbench launcher action list now supports roving keyboard focus across enabled Files, Side chat, Browser, Git, Review, Agents, and Terminal actions, with Arrow/Home/End navigation and disabled-action skipping.
- Verification passed for the Workbench New Tab keyboard checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `workbenchNewTabKeyboardNavigation=true` while preserving New Tab visual/no-overflow checks plus Git and Agents action checks with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780032925540.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780032925540.png`.
- Workbench New Tab singleton-switch checkpoint: already-open singleton launcher actions such as Git, Files, Browser, Review, and Agents now stay enabled, show an `Open` state, and switch to the existing tab instead of becoming disabled cards.
- Verification passed for the Workbench New Tab singleton-switch checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `workbenchNewTabSingletonSwitch=true` by reopening Git from the launcher without duplicating the tab, with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780033185880.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780033185880.png`.
- Git Workbench checkout checkpoint: the Git tab Branch card now supports switching to an existing branch through main-process `git:checkoutBranch` IPC. It validates the branch name, refreshes branch/change state, and shows smoke-visible checked-out-branch status.
- Verification passed for the Git Workbench checkout checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node --test out-test/src/main/__tests__/gitChanges.test.js`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `workbenchNewTabGitCheckout=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780020466988.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780020466988.png`.
- Git Workbench per-file action checkpoint: Git tab file rows now expose compact Stage and Unstage actions backed by the existing safe `git.stagePaths` and `git.unstagePaths` IPC paths, so selective commits can start in the right Workbench panel without using terminal Git.
- Verification passed for the Git Workbench per-file action checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `workbenchNewTabGitFileActions=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780020692507.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780020692507.png`.
- Git Workbench per-file discard checkpoint: Git tab file rows now expose compact Discard actions that open the existing destructive confirmation dialog against one selected path. Confirming still goes through the safe `git.discardPaths` IPC path, while cancel/close clears the targeted discard state without changing the worktree.
- Verification passed for the Git Workbench per-file discard checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --workbench-new-tab`. The focused smoke gates `workbenchNewTabGitFileDiscard=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780021068460.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780021068460.png`.
- Review copy-status checkpoint: Review copy/action feedback is no longer coupled to the local Stage/Unstage/Revert pill. When a provider source such as Last turn has no local Git actions, Review now renders a status-only bottom pill so `Copy git apply command` still announces copied/failed state.
- Verification passed for the Review copy-status checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `node scripts/run-automated-ui-smoke.mjs --diff-source`, and `node scripts/run-automated-ui-smoke.mjs --diff-core`. The focused smokes gate `reviewLastTurnGitApplyCopyStatus=true`, `reviewGitApplyCopyStatus=true`, and `reviewFloatingGitActionStatus=true` with source evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1780021424055.json` plus core evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780021472816.json`.
- Review-to-Git handoff checkpoint: the local Review floating action pill now includes `Open Git`, giving users a direct path from diff review into the dedicated Git Workbench tab that owns branch, commit, and discard workflows.
- Verification passed for the Review-to-Git handoff checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --diff-core`. The focused smoke gates `reviewFloatingGitOpenTab=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780021759605.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780021759605.png`.
- Review directory metadata checkpoint: grouped Review directory rows now show aggregated file count plus additions/deletions and expose smoke-readable group metadata. Verification passed with `diffDirectoryMetadata=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780030681929.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780030681929.png`.
- Source annotation tab-pinning checkpoint: creating a source annotation in a preview file tab now pins the tab before persisting annotation state, preventing annotation work from being replaced by the next preview-file open. Verification passed with `fileSourceAnnotationPinsTab=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780030998435.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780030998435.png`.
- Composer permission context refresh checkpoint: the composer permission popover now shows a refresh action and announced refresh status for live, fallback, and static runtime config contexts. Verification stayed targeted to the touched surface: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --composer` passed with `composerPermissionContextRefresh=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780032566596.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780032566596.png`.
- Edit-as-draft source checkpoint: user-message `Edit message as draft` now passes source metadata into the composer, which renders an `Editing a copy` notice explaining that the original transcript message is preserved. The notice exposes the source message id, attachment count, and a clear action for abandoning the copied draft.
- Verification passed for the edit-as-draft source checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and `node scripts/run-automated-ui-smoke.mjs --transcript-layout`. The focused smoke gates `chatUserMessageEditDraftSourceStatus=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780022086471.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780022086471.png`.
- Provider Settings runtime activity checkpoint: the provider Runtime details card now exposes a compact `Copy` action that writes the latest provider runtime connections/events, or the empty runtime state, through the app clipboard bridge and announces the result. `pnpm run smoke:ui:suggest` now maps dirty files to the narrowest focused smoke target before running Electron validation, so small Settings/Provider slices do not default to broad smoke.
- Verification passed for the Provider Settings runtime activity checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and elevated `node scripts/run-automated-ui-smoke.mjs --settings-providers`. The focused smoke gates `settingsProviderRuntimeCopy=true` and `settingsProviderRuntimeCopyStatusA11y=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780042434745.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780042434745.png`.
- Workbench launcher coverage checkpoint: the New tab launcher now exposes Environment and Extensions alongside the existing daily coding actions. A new split `--workbench-launcher` smoke target validates launcher discovery, Environment/Extensions activation, singleton open-state chips, and no-overflow without rerunning the heavier Git/agent `--workbench-new-tab` workflow.
- Verification passed for the Workbench launcher coverage checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and elevated `node scripts/run-automated-ui-smoke.mjs --workbench-launcher`. The focused smoke gates `workbenchLauncherDiscovery=true`, `workbenchLauncherEnvironmentAction=true`, `workbenchLauncherExtensionsAction=true`, `workbenchLauncherOpenState=true`, and `workbenchLauncherNoHorizontalOverflow=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780042931745.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780042931745.png`.
- Bottom-panel language checkpoint: the header toggle, resize handle, tablist, and hide action now use bottom-panel wording while `New terminal` and `Clear terminal` remain scoped to terminal tabs. This is a naming/accessibility step toward the shared bottom-panel model, not a claim that non-terminal bottom tabs are complete.
- Verification passed for the bottom-panel language checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, elevated `node scripts/run-automated-ui-smoke.mjs --terminal`, and elevated `node scripts/run-automated-ui-smoke.mjs --header`. The Terminal smoke gates `terminalBottomPanelLabels=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780043369596.json`; the Header smoke passed with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1780043394760.json`.
- Changed-files smoke plan checkpoint: `scripts/suggest-ui-smoke-targets.mjs` now emits a concrete targeted validation plan, supports `--json` for machine-readable comparison, and supports `--run` so `pnpm run smoke:ui:changed` executes only the checks implied by the current dirty files. Broad-smoke review is reported explicitly and the no-flag UI smoke is not run automatically.
- Verification passed for the changed-files smoke plan checkpoint: `node -c scripts/suggest-ui-smoke-targets.mjs`, `node scripts/suggest-ui-smoke-targets.mjs --json scripts/suggest-ui-smoke-targets.mjs package.json`, `pnpm run smoke:ui:suggest`, and `pnpm run smoke:ui:changed`. For the helper/package-only dirty state, the generated plan ran `git diff --check`, `pnpm exec tsc --noEmit`, and `node -c scripts/suggest-ui-smoke-targets.mjs` with no focused UI smoke, while still flagging `package.json` as a broad-smoke review file.
- Plan context handoff checkpoint: the Plan Workbench tab now exposes `Add to chat`, inserting a bounded goal/review/task summary into the main composer with visible/announced status.
- Verification passed for the Plan context handoff checkpoint: `pnpm run smoke:ui:suggest` selected `--plan`; `pnpm run smoke:ui:changed` completed `git diff --check`, `pnpm exec tsc --noEmit`, and `node -c scripts/run-automated-ui-smoke.mjs` before the known sandbox localhost bind failure; elevated `node scripts/run-automated-ui-smoke.mjs --plan` passed with `planAddToChat=true`. Evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-plan-1780044146805.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-plan-1780044146805.png`.
- Review source-root identity checkpoint: the Review toolbar source summary now shows a compact workspace-root chip, exposes smoke-readable source root/name metadata, and includes the root name in the accessible source label so multi-root/worktree sessions are easier to distinguish from the right Workbench panel.
- Verification passed for the Review source-root identity checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `pnpm run smoke:ui:suggest`, elevated `node scripts/run-automated-ui-smoke.mjs --diff-core`, and elevated `node scripts/run-automated-ui-smoke.mjs --design-system`. The focused Review smoke gates `reviewSourceRootMetadata=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780044632172.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780044632172.png`; design-system evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-design-system-1780044652042.json`.
- Review selected-line handoff checkpoint: selected current-file diff lines now expose compact Copy reference and Add to chat actions directly in Review. Copy goes through the app clipboard bridge; Add to chat inserts a bounded Review line block into the main composer and announces inline status.
- Verification passed for the Review selected-line handoff checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, elevated `node scripts/run-automated-ui-smoke.mjs --diff-core`, and elevated `node scripts/run-automated-ui-smoke.mjs --design-system`. The focused Review smoke gates `reviewDiffLineComposerHandoff=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780045509123.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780045509123.png`; design-system evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-design-system-1780045530278.json`.
- Browser screenshot artifact-actions checkpoint: the Browser console screenshot row now exposes compact Add screenshot to chat, Copy screenshot path, and Reveal screenshot file actions. Copy uses the app clipboard bridge, reveal uses `fs.showInFolder`, and the artifact path exposes smoke-readable metadata.
- Verification passed for the Browser screenshot artifact-actions checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, `pnpm run smoke:ui:suggest`, and elevated `node scripts/run-automated-ui-smoke.mjs --browser`. The focused Browser smoke gates `browserScreenshotPathActions=true` and `browserScreenshotAttachment=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780046018097.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780046018097.png`.
- Plan bottom-panel transfer checkpoint: the bottom panel now supports a non-terminal `plan` tab kind. Plan can move from the right Workbench context menu into the bottom panel, render embedded Plan content there, suppress the duplicate right-panel Plan tab while bottom-owned, and move back to the right panel from the shared bottom tab menu.
- Verification passed for the Plan bottom-panel transfer checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, focused elevated `node scripts/run-automated-ui-smoke.mjs --terminal`, focused elevated `node scripts/run-automated-ui-smoke.mjs --header`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --workbench-launcher`. The terminal smoke gates `bottomPanelPlanTransfer=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780047823386.json`; header evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1780047657321.json`; Workbench launcher evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780047655099.json`.
- Bottom-hosted Plan handoff checkpoint: the focused Terminal smoke now proves the embedded Plan `Add to chat` action works while Plan is hosted in the bottom panel and inserts the active goal context into the main composer.
- Verification passed for the bottom-hosted Plan handoff checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `git diff --check`, and focused elevated `node scripts/run-automated-ui-smoke.mjs --terminal`. The terminal smoke gates `bottomPanelPlanAddToChat=true` alongside `bottomPanelPlanTransfer=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780048294721.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780048294721.png`.
- Bottom-panel Plan-only close-policy checkpoint: Plan remains closable when it is the final bottom tab, while the single-terminal tab close guard stays in place and final bottom-tab close returns focus to the titlebar bottom-panel toggle.
- Verification passed for the bottom-panel Plan-only close-policy checkpoint: `pnpm exec tsc --noEmit`, `pnpm exec tsc -p tsconfig.node.json --outDir out-test --module commonjs`, `node --test out-test/src/main/__tests__/panelTabs.test.js`, elevated `node scripts/run-automated-ui-smoke.mjs --terminal`, and elevated `node scripts/run-automated-ui-smoke.mjs --cross-panel-keyboard`. Terminal evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780049649508.json`; cross-panel keyboard evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-cross-panel-keyboard-1780049675999.json`.
- Panel-tab validation targeting checkpoint: `smoke:ui:changed` now adds the compiled node `panelTabs` unit test whenever `src/types/panelTabs.ts` or `src/main/__tests__/panelTabs.test.ts` changes, while preserving the existing `src/types/panelTabs.ts` mapping to the focused `--cross-panel-keyboard` UI smoke.
- Verification passed for the panel-tab validation targeting checkpoint: `node -c scripts/suggest-ui-smoke-targets.mjs`, `node scripts/suggest-ui-smoke-targets.mjs --json src/types/panelTabs.ts src/main/__tests__/panelTabs.test.ts`, `pnpm exec tsc -p tsconfig.node.json --outDir out-test --module commonjs`, `node --test out-test/src/main/__tests__/panelTabs.test.js`, `pnpm run smoke:ui:suggest`, and `git diff --check`.
- Bottom-panel Plan toolbar checkpoint: the bottom-panel toolbar now exposes a compact `Open Plan in bottom panel` action beside `New terminal`, using the shared transfer path to create/activate a bottom Plan tab and report visible status.
- Verification passed for the bottom-panel Plan toolbar checkpoint: `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `pnpm exec tsc --noEmit`, `git diff --check`, and elevated `node scripts/run-automated-ui-smoke.mjs --terminal`. The Terminal smoke gates `bottomPanelPlanToolbarAction=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780050293140.json`.
- Bottom-panel allowed-tab boundary checkpoint: Terminal and Plan are now the explicit shared allowed kinds for bottom-panel transfer, and unsupported Workbench tabs show a visible `Bottom panel supports Terminal and Plan tabs.` menu boundary instead of only smoke-readable metadata.
- Verification passed for the bottom-panel allowed-tab boundary checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `node -c scripts/suggest-ui-smoke-targets.mjs`, `pnpm run smoke:ui:suggest`, `pnpm exec tsc -p tsconfig.node.json --outDir out-test --module commonjs`, `node --test out-test/src/main/__tests__/panelTabs.test.js`, `git diff --check`, and elevated `node scripts/run-automated-ui-smoke.mjs --right-panel`. The Right Workbench smoke gates `rightPanelTransferUnsupportedBoundary=true` with visible boundary-message coverage; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1780050734087.json`.
- Workbench launcher Plan checkpoint: when goal/plan state exists and Plan is not bottom-hosted, the New Tab launcher now exposes Plan as a first-class singleton action and switches to the right Workbench Plan tab.
- Verification passed for the Workbench launcher Plan checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and elevated `node scripts/run-automated-ui-smoke.mjs --workbench-launcher`. The focused smoke gates `workbenchLauncherPlanAction=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780051042787.json`.
- Workbench launcher Plan close/reopen checkpoint: closing the right Workbench Plan tab now dismisses it instead of re-deriving it from active goal state, while the New Tab launcher keeps Plan available as a `New` singleton action so it can be reopened intentionally.
- Verification passed for the Workbench launcher Plan close/reopen checkpoint: `pnpm exec tsc --noEmit`, `node -c scripts/run-automated-ui-smoke.mjs`, `pnpm run smoke:ui:suggest`, `git diff --check`, and elevated `node scripts/run-automated-ui-smoke.mjs --workbench-launcher`. The focused smoke gates `workbenchLauncherPlanCloseReopen=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780051493371.json`.
