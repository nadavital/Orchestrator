# Codex Side Panel Live Comparison

Last updated: 2026-05-28

This file is the current comparison ledger for the side-panel parity goal. It separates Codex-bundle evidence from Orchestrator fixture-smoke evidence so passing local smokes do not accidentally count as full Codex parity.

## Runnable Comparison

- Reuse the latest side-panel smoke manifest and compare it with the current Codex app bundle:
  `npm run compare:codex-side-panels`
- Regenerate the full side-panel visual inventory first, then compare:
  `npm run compare:codex-side-panels -- --run-smoke --full`
- Regenerate from the temp packaged app without replacing `/Applications/Orchestrator.app`:
  `npm run compare:codex-side-panels -- --run-smoke --full --packaged`
- Regenerate from `/Applications/Orchestrator.app` with an isolated profile:
  `npm run compare:codex-side-panels -- --run-smoke --full --installed`
- Generate the same report without failing the shell on known mismatches:
  `npm run compare:codex-side-panels -- --no-fail`
- The generated report is written to `tmp/codex-side-panel-comparison/comparison-report.md` and `tmp/codex-side-panel-comparison/comparison-report.json`.
- The same run also writes `tmp/codex-side-panel-comparison/header-panel-contact-sheet.html`, which groups the live Codex screenshot with Orchestrator's sidebar, focused header, transcript/header, Workbench right-panel, Files panel, Browser panel, Review panel, Terminal bottom-panel, and multi-window shell captures for direct header/panel relationship review.
- If the smoke runner cannot start the Electron/Vite server, for example `listen EPERM 127.0.0.1`, the comparison classifies affected rows as `needs-smoke` instead of `mismatch`. Without `--no-fail`, `needs-smoke` exits nonzero so automation can tell the report is incomplete. Treat that as harness evidence to rerun with the approved localhost-capable command path, not as product parity evidence.

Scope note: "side panel" in the comparison matrix means the right-side Workbench panel unless a row explicitly says Bottom panel, Left sidebar, or Settings.

## Product Priority Phases

The current product priority is daily coding usability first, Codex-derived artifact depth second. Codex remains a useful reference for app-shell maturity, but Orchestrator should not treat Codex as a permanent product roadmap or block original Orchestrator workflows on pixel-for-pixel parity.

Phase 1 work should focus on the surfaces that make Orchestrator usable as a high-quality coding app every day:

- App shell, header, left sidebar, right Workbench side panel, bottom panel, and their shared focus/route/window behavior.
- Main chat thread readability, streaming/tool-call/error states, long-thread behavior, and continuation/retry ergonomics.
- Composer attachment/context/model/permission controls, keyboard behavior, submit/stop states, and side-chat composer consistency.
- Inspector/context panels that help coding workflows directly, including useful empty/loading states and clear relationships to the selected thread, browser, review, files, or runtime event.
- Settings as a durable app surface for provider/account/runtime/browser/shell preferences, with clear unavailable states only where real adapters are missing.
- Review, Browser, Terminal, Files source tabs, and Side Chat as practical coding workflow surfaces, not only Codex comparison rows.

Phase 2 keeps the deeper document/artifact renderer backlog visible without letting it lead the current work:

- Full DOCX layout fidelity, style inheritance, pagination, revision/comment threading, and editing hooks.
- Full XLSX/PPTX canvas fidelity, advanced workbook/slide editing, chart/object selection, persisted edits, and deeper Popcorn-equivalent behavior.
- Full PDF canvas rendering, provider-backed annotations, and artifact metadata beyond the current lightweight controls.
- Provider-backed artifact comments/blame/metadata once providers expose useful data.

## Evidence Used

- Codex app bundle: `/Applications/Codex.app/Contents/Resources/app.asar`, `CFBundleShortVersionString=26.519.41501`.
- Extracted Codex side-panel chunks under `tmp/codex-app-assets`.
- Current Orchestrator dev visual inventory: `tmp/side-panel-visual-inventory-current/manifest.json`, 27 captures, no failures, refreshed at `2026-05-28T04:35:53.601Z`. This full run completed with the visual-inventory progress output, including `workbenchPanelActiveTabVisibleAfterResize=true`, `workbenchPanelOverflowFadeMasksClippedTabs=true`, `workbenchPanelTabCodexMetrics=true`, `terminalPanelTabCodexMetrics=true`, `headerPanelSharedBand=true`, `filesDocumentTextStyles=true`, `filesSpreadsheetBorders=true`, `filesSpreadsheetComments=true`, `filesSpreadsheetConditionalFormatting=true`, and `terminal-behavior` at `ok=true`.
- Current Orchestrator installed full visual inventory: `tmp/side-panel-visual-inventory-installed-full/manifest.json`, app mode `installed`, 27 captures, no failures, created at `2026-05-27T05:58:55.047Z`.
- Current runnable comparison: `tmp/codex-side-panel-comparison/comparison-report.json`, refreshed at `2026-05-28T04:36:11.577Z` with the current full dev visual manifest, smoke app mode `dev`, status `fixture-covered=2`, `blocked=8`, `mismatch=0`, `needsSmoke=0`, `needsProof=0`. The Header/panel row now gates `workbenchPanelTabCodexMetrics=true`, `workbenchPanelTabReadableSeparation=true`, `workbenchPanelActiveTabVisibleAfterResize=true`, `workbenchPanelOverflowFadeMasksClippedTabs=true`, `terminalPanelTabCodexMetrics=true`, and `mainContentFrameContinuous=true` in addition to the shared sidebar/main/right-panel header-band checks. The Workbench row gates `workbenchPanelTabCodexWidthCap=true`, `workbenchPanelTabCodexMetrics=true`, `workbenchPanelActiveTabVisibleAfterResize=true`, and `workbenchPanelOverflowFadeMasksClippedTabs=true` in addition to the shared overflow/start-edge-close/new-tab checks. The Browser row gates exact device preset catalog order/labels with `browserDevicePresetCatalog=true`, plus `browserClientToolActions=true`, `browserClientToolScreenshot=true`, `browserClientToolScreenshotImage=true`, and `browserClientToolAdvancedActions=true`, and the comparison artifact checks require live Codex app-server calls for `browser_screenshot` with `includeImage=true` plus `browser_fill`, `browser_key`, `browser_select`, `browser_check`, and `browser_scroll`. The Files row gates `filesSlidesShapeLayout=true`, `filesSlidesColorFills=true`, `filesSlidesImageShapes=true`, `filesDocumentTableRendering=true`, `filesDocumentImageRendering=true`, `filesDocumentSectionMetadata=true`, `filesDocumentColumnLayout=true`, `filesDocumentShapeRendering=true`, `filesDocumentFootnotes=true`, `filesDocumentComments=true`, `filesDocumentReviewMarks=true`, `filesDocumentHyperlinks=true`, `filesDocumentTextStyles=true`, `filesDocumentListRendering=true`, `filesSpreadsheetFormulaEvaluation=true`, `filesSpreadsheetCellStyles=true`, `filesSpreadsheetMergedCells=true`, `filesSpreadsheetSizing=true`, `filesSpreadsheetFreezePanes=true`, `filesSpreadsheetFreezeHandles=true`, `filesSpreadsheetAlignment=true`, `filesSpreadsheetTables=true`, `filesSpreadsheetConditionalFormatting=true`, `filesSpreadsheetDataValidation=true`, `filesSpreadsheetDataValidationMessages=true`, `filesSpreadsheetDataValidationOverlay=true`, `filesSpreadsheetDataValidationRange=true`, `filesSpreadsheetComments=true`, `filesSpreadsheetBorders=true`, `filesSpreadsheetCharts=true`, `filesSpreadsheetChartPlot=true`, `filesSpreadsheetDrawings=true`, `filesSpreadsheetSparklines=true`, `filesSpreadsheetFormulaEditing=true`, `filesSlidesThumbnailRail=true`, `filesDocumentPdfZoomMenu=true`, `filesOfficeZoomMenu=true`, `filesSlidesSpeakerNotes=true`, and `filesPdfAnnotations=true` against Codex Popcorn/shared/PDF artifact bundle evidence. The top-level summary reports `optionalFileEvidenceFailures=2` for the black live Codex screenshot evidence used by the Header/panel and Workbench rows, so "zero mismatches" no longer hides missing pixel-proof evidence. It also reports `remainingParityGaps=19`, grouped as `live-codex-ui=7`, `provider-adapter=8`, `provider-proof=2`, `runtime-signal=1`, and `renderer-fidelity=1`, so fixture-covered rows are not confused with final pixel/provider/runtime parity. The latest installed proof passed at `2026-05-27T06:17:17.758Z`.
- Latest Phase 1 Workbench New tab layout refresh: focused Workbench New Tab smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1779976699031.json` passed with `workbenchNewTabListLauncher=true`, `workbenchNewTabCompactLauncher=true`, `workbenchNewTabFinalCapture=true`, and `workbenchNewTabSingleAddAffordance=true`, proving the right Workbench New tab now renders a top-aligned compact command list instead of a large card grid while keeping the Agents inspector, runtime event copy/add-to-chat, filtering, failure grouping, issue triage, selected-agent timeline, and transport-log gates green.
- Latest Phase 1 Git Workbench tab refresh: focused Workbench New Tab smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780021068460.json` passed with `workbenchNewTabGitAction=true`, `workbenchNewTabGitFileActions=true`, `workbenchNewTabGitFileDiscard=true`, `workbenchNewTabGitBranch=true`, `workbenchNewTabGitCheckout=true`, `workbenchNewTabGitPrCommand=true`, `workbenchNewTabGitCommit=true`, and `workbenchNewTabGitDiscard=true`, proving the right Workbench New tab can open a dedicated Git tab, stage/unstage one file, open and cancel a path-specific per-file discard confirmation, create/check out a local branch, switch back to an existing branch, copy a generated PR command through the app clipboard bridge, verify the confirmed Discard all cancel path, exercise Stage all / Unstage all, stage again, enter a commit message, and commit staged fixture changes cleanly while keeping the New tab launcher, Agents inspector, runtime event copy/add-to-chat, filtering, failure grouping, issue triage, selected-agent timeline, and transport-log gates green.
- Latest Phase 1 Environment/Git target-focus refresh: focused Environment smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1780075001480.json` passed with `environmentBranchOpensGit=true`, `environmentCommitOpensGit=true`, and `environmentCreatePrOpensGit=true`, proving Environment branch, commit, and missing-PR rows open the Git Workbench tab focused on the matching Branch, Commit, or Pull Request card while preserving Environment visual/action/source/settings checks. The changed-files smoke selector now keeps this cross-file Environment-to-Git focus path on `--environment` only, avoiding the full Workbench New Tab/Git smoke for this narrow handoff.
- Latest Phase 1 Browser actions refresh: focused Browser smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780075276988.json` passed with `browserActionsPageContext=true`, proving the Browser toolbar actions menu can send the current page URL/title/visible structure to the main composer with visible status while preserving Browser local-targets, webview, screenshot, find, zoom, device, client-tool, history, menu, clear-data, context-menu, comment, inspector, target-action, policy, hidden-state, fork-transfer, status-row, and no-overflow gates. The changed-files smoke selector keeps this Browser-owned composer insertion on `--browser` only.
- Latest Phase 1 Files/source-tab workspace identity refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780026275802.json` passed with `workbenchFileTab=true`, proving the right Workbench file tab now shows workspace-root identity and exposes `data-file-tab-workdir` plus `data-file-tab-absolute-path` while preserving source mode, line selection, blame, inline line utilities, open-target diagnostics, add-to-chat, annotations, source search, virtualization, and file-tab reset/pin gates.
- Latest Phase 1 source annotation tab-pinning refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780030998435.json` passed with `fileSourceAnnotationPinsTab=true`, proving adding a source annotation to a preview file tab now pins the tab before persisting annotation state, while keeping file search, source-line utilities, blame, add-to-chat, file-tab reset/pin, artifact previews, and renderer gates green.
- Latest Phase 1 main-chat Continue recovery refresh: focused Transcript Layout smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1779978669229.json` passed with `chatContinueFailedStartCleansSyntheticPrompt=true`, proving a failed assistant-continuation start removes the synthetic user Continue prompt while preserving the visible provider-start error. The same run kept the transcript layout, retry, copy, partial-response, user-input, permission, file-card, tool-summary, and narrow-layout gates green.
- Latest Phase 1 main-chat retry preparation failure refresh: focused Transcript Layout smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1779979248403.json` passed with `errorRecoveryRetryPrepareFailure=true`, proving retrying the last user message returns the thread to `error` with a visible system failure when provider request preparation fails before a run starts. The same run kept `errorRecoveryRetry=true`, `errorRecoveryRetryA11y=true`, `chatContinueFailedStartCleansSyntheticPrompt=true`, transcript layout, copy, partial-response, user-input, permission, file-card, tool-summary, and narrow-layout gates green.
- Latest Phase 1 transcript file-reference open outcome refresh: focused Transcript Layout smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780026624546.json` passed with `fileReferenceOpenOutcome=true`, proving assistant transcript file-reference cards now preserve structured open-target results with target/method/opened-with diagnostics and announced status while keeping missing-file suppression, disabled missing-card actions, transcript bounds, copy, retry, continue/regenerate, permission, partial-response, and narrow-layout gates green.
- Latest Phase 1 transcript tool-summary collapse refresh: focused Transcript Layout smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780039986621.json` passed with `toolSummaryCollapsedByDefault=true`, proving grouped tool activity starts closed with row-count/error/default-collapsed diagnostics and a compact action count while still expanding to a bounded scrollable body. The implementation also updates the virtualizer estimate to the collapsed disclosure height so long threads do not reserve expanded tool-row space before measurement.
- Latest Phase 1 Terminal scoped URL handoff refresh: focused Terminal smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780041168899.json` passed with `terminalLinkScopedRouting=true` and `terminalLinkRouting=true`, proving terminal URL opens are routed through the terminal-id-specific opener and into the right-panel Browser handoff. Right-panel terminals now use the same Browser route path as bottom terminals; broader live Codex terminal timing and mixed-surface stress remain separate.
- Latest Phase 1 Provider Settings permission-config refresh: focused Provider Settings smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780041717089.json` passed with `settingsProviderPermissionRefresh=true`, proving Provider Settings can explicitly refresh live/fallback permission config with visible status while preserving provider defaults, model-list, config-editor, diagnostics, sidebar-refresh, and settings chrome gates.
- Latest Phase 1 transcript file-reference Workbench handoff refresh: focused split smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780027530147.json` passed with `fileReferenceOpenOutcome=true`, `fileReferenceOpenWorkbench=true`, and `fileReferenceMissingActionsDisabled=true`, proving assistant transcript file-reference cards can open a workspace-contained `:line` reference into the right Workbench source tab with selected/revealed line state while preserving structured external-editor open diagnostics and disabled actions for missing files.
- Latest Phase 1 additional-root transcript file-reference handoff refresh: focused split smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780027838839.json` passed with `fileReferenceOpenOutcome=true`, `fileReferenceOpenWorkbench=true`, `fileReferenceAdditionalRootWorkbench=true`, and `fileReferenceMissingActionsDisabled=true`, proving assistant transcript file-reference cards can open a configured additional-root `:line` reference into the right Workbench source tab with the additional root preserved as the tab host/workdir and selected/revealed line state.
- Latest Phase 1 transcript file-reference Attach-to-chat refresh: focused split smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028239061.json` passed with `fileReferenceAttachToChat=true` while preserving the open outcome, Workbench, additional-root Workbench, and missing-action gates, proving assistant transcript file-reference cards can attach a resolved referenced file directly into the main composer for a follow-up prompt.
- Latest Phase 1 additional-root source-line composer handoff refresh: focused split smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028491456.json` passed with `fileReferenceAdditionalRootLineAddToChat=true` while preserving the transcript file-reference gates, proving source tabs opened from configured additional roots insert absolute `:line` references into the composer instead of ambiguous relative paths.
- Latest Phase 1 additional-root source-tab Copy path refresh: focused split smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028669298.json` passed with `fileReferenceAdditionalRootCopyPath=true` while preserving the transcript file-reference gates, proving source tabs opened from configured additional roots copy absolute file paths instead of ambiguous relative paths.
- Latest Phase 1 additional-root source-tab blame refresh: focused split smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-file-reference-1780028914261.json` passed with `fileReferenceAdditionalRootBlame=true` while preserving the transcript file-reference gates, proving source tabs opened from configured additional roots query git blame against the encoded tab root instead of the primary session workdir.
- Latest Phase 1 edit-as-draft refresh: focused Transcript Layout smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780022483489.json` passed with `chatUserMessageEditDraftSourceStatus=true` and `chatUserMessageEditDraftRestore=true`, proving `Edit message as draft` now shows a composer-side `Editing a copy` status with original-transcript preservation copy, source message id metadata, attachment count metadata, a clear action, and a restore action when it replaces an existing unsent composer draft while preserving edit-to-draft, attachment, retry, continue, regenerate, transcript bounds, permission, and narrow-layout gates.
- Latest Phase 1 approval/input resume preparation failure refresh: focused User Input smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-user-input-1779979561742.json` passed with `userInputResumePrepareFailure=true`, and focused Permission smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-permission-1779979577095.json` passed with `permissionResumePrepareFailure=true`, proving both resume cards surface request-preparation failures as visible errors and leave the backing session in `error` instead of `running`.
- Latest Phase 1 queued follow-up start failure refresh: focused Streaming Typing smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-streaming-typing-1779979936725.json` passed with `queuedFollowUpStartFailureVisible=true` and `queuedFollowUpStartFailureState=true`, proving queued follow-ups stay visibly queued and the session moves to `error` when the follow-up cannot start after the previous run ends. The same run kept streaming responsiveness, queue summary, steering cancel, stop-run, and will-queue composer gates green.
- Latest Phase 1 composer send-start cleanup refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779979016978.json` passed with `composerSendProviderFalseCleansTranscript=true`, proving a provider start failure after `sendMessage` appends the user draft removes the phantom transcript prompt while restoring the composer draft and preserving the provider-start error. The same run kept `composerSendFailureRestoresDraft=true`, `composerSendFalseRestoresDraft=true`, `composerSlashEscapePreservesDraft=true`, `composerActiveThreadModelSwitchPersisted=true`, `composerActiveThreadProviderSwitchPolicyPersisted=true`, attachment, side-chat, blocked-send, and toolbar gates green.
- Latest app-shell tab/header metric refresh: focused right-panel smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779942546384.json` passed with `workbenchPanelTabReadableSeparation=true`, `workbenchPanelActiveTabVisibleAfterResize=true`, `workbenchPanelOverflowFadeMasksClippedTabs=true`, `workbenchPanelTabCodexMetrics=true`, `rightPanelHeaderSeam=true`, and `headerPanelSharedBand=true`; focused terminal visual smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-visual-1779942606419.json` and terminal behavior smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779914792949.json` passed with `terminalPanelTabCodexMetrics=true`. This aligns the shared app-shell tab/header metrics with Codex bundle evidence for `h-7`, `rounded-lg`, `px-2 py-1`, `text-sm`, and the 160/156 px tab caps, while also gating that crowded Workbench tab labels remain visually separated, clipped overflow is masked, and the active tab stays fully visible after constrained-width resize paths.
- Latest Phase 1 Agents inspector context handoff refresh: focused Workbench New Tab smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1779981114150.json` passed with `agentRuntimeEventAddToChatContext=true` and `agentRuntimeEventAddToChat=true`, proving selected runtime-event details inserted into the main composer now include thread, runtime, status, and workspace context while keeping `agentRuntimeEventCopy=true`, event filtering, failure grouping, issue triage, selected-agent timeline, and transport-log gates green.
- Latest Phase 1 Agents inspector action-chrome refresh: focused Workbench New Tab smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780031671592.json` passed with `agentRuntimeEventActionChrome=true`, proving selected runtime-event Copy payload/Add to chat actions now use the shared compact toolbar icon-button primitive with tooltip labels and no native-title leakage while preserving copy, add-to-chat, context handoff, filtering, failure grouping, issue triage, selected-agent timeline, and transport-log gates.
- Latest Phase 1 Workbench launcher keyboard refresh: focused Workbench New Tab smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780032925540.json` passed with `workbenchNewTabKeyboardNavigation=true`, proving the Workbench launcher action list uses one enabled tab stop, arrow-key navigation, Home/End jumps, and disabled-action skipping while preserving New Tab visual/no-overflow checks plus Git and Agents launcher gates.
- Latest Phase 1 Workbench launcher singleton-switch refresh: focused Workbench New Tab smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780033185880.json` passed with `workbenchNewTabSingletonSwitch=true`, proving already-open singleton launcher actions remain enabled, show an `Open` state, and switch to the existing tab without duplicating it while preserving keyboard navigation, Git, and Agents launcher gates.
- Latest Phase 1 Environment settings action refresh: focused Environment smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1779989046068.json` passed with `environmentSettingsOpensProviders=true`, proving the Environment header settings control opens Providers settings instead of remaining an enabled icon with no action.
- Latest Phase 1 Environment source-boundary refresh: focused Environment smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1779989228491.json` passed with `environmentSourceBoundary=true`, proving the provider Web search source row is visibly unavailable with a concrete reason instead of appearing like an inert action.
- Latest Phase 1 Environment disabled-row semantics refresh: focused Environment smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1779998274477.json` passed with `environmentDisabledRowsA11y=true`, proving unavailable Environment rows expose `aria-disabled` and a reason-bearing row label while preserving the visual source-boundary, action-row, and settings-open gates.
- Latest Phase 1 Browser Security origin-policy controls refresh: focused Browser smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779999461705.json` passed with `browserSecurityPolicyOriginControls=true` and `browserSecurityPolicyMutualExclusion=true`, proving per-session Browser Security rows expose explicit Allow/Block/Clear controls for download/upload origin policy state and that Allow/Block interactions do not leave the current origin in contradictory lists while preserving the Browser local-server, browser-use, target-action, comment, load-error, visibility, lifecycle, and transfer gates.
- Latest Phase 1 Side Chat follow-up context refresh: focused Side Chat smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-side-chat-1779989645139.json` passed with `sideChatFollowupContext=true`, proving questions sent from an existing side-chat panel carry the prior side-chat user/assistant exchange into the provider prompt instead of only sending the latest question plus the main transcript. The same run kept side-chat tab, compact composer, draft persistence, multiline draft, metadata, status, retry, personalization, and close gates green.
- Latest Phase 1 General Settings editor-preference refresh: focused Settings smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779990009149.json` passed with `settingsGeneralActionStatus=true` and `settingsGeneralPreferredEditorPersistence=true`, proving General Settings now confirms preferred-editor saves through an announced status region and persists the selected editor used by file handoff/open actions.
- Latest Phase 1 composer live permission-context refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779982239036.json` passed with `composerContextDisabledPermissionBlocksSend=true`, proving live app-server disabled permission policies now block send and surface the app-server sandbox reason while keeping existing composer draft, attachment, blocked-send, provider/model, slash-menu, and toolbar gates green.
- Latest Phase 1 composer permission-config refresh affordance: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780032566596.json` passed with `composerPermissionContextRefresh=true`, proving the permission popover exposes a refresh action and announced status for live, fallback, and static config contexts while preserving `composerPermissionContextSignal=true`, blocked-send recovery, provider/model switching, draft/attachment isolation, prompt history, and toolbar gates.
- Latest Phase 1 composer settings-send lifecycle refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779982975316.json` passed with `composerSendFlushesPendingSettings=true`, proving a send immediately after an active-thread model change waits for the pending settings IPC before checking the session state used for the run. The same run kept the active-thread provider/model persistence, live-disabled permission, draft/attachment isolation, send-failure recovery, slash-menu, and toolbar gates green.
- Latest Phase 1 live composer app-server proof: elevated `npm run live:codex-composer-appserver` passed with artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-appserver-live-proof/result.json`. The proof starts the real Codex app-server in a disposable git workspace with `model=gpt-5.4-mini`, `effort=low`, `approvalPolicy=on-request`, `approvalsReviewer=user`, and `sandbox=workspace-write`; Codex returns a managed permission profile, emits a completed `commandExecution` item for the proof command, leaves a git-visible edit, and replies `CODEX_COMPOSER_LIVE_OK`. The run observed `permissionRequestCount=0`, which means an allowed workspace-write command did not produce an explicit approval card; approval-producing provider-native permission proof remains separate.
- Latest Phase 1 live command-approval proof: elevated `npm run live:codex-composer-approval` passed with artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-approval-live-proof/result.json`. The proof intentionally asks Codex to write just outside the disposable workspace, observes a real `item/commandExecution/requestApproval` request with numeric JSON-RPC id `0`, answers it, then verifies `serverRequest/resolved`, a completed command item, the parent-directory proof file, and `CODEX_COMPOSER_LIVE_OK`.
- Latest Phase 1 live composer resume proof: elevated `npm run live:codex-composer-resume` passed with artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-resume-live-proof/result.json`. The proof starts a non-ephemeral Codex app-server thread, completes a first turn that asks Codex to remember `ORCH_RESUME_MEMORY_741`, terminates that app-server process, starts a fresh app-server process, calls `thread/resume` for the same thread id, and validates that the resumed turn recalls the prior-turn proof key before archiving the disposable thread. This closes backend Codex app-server resume/context-continuity proof; UI-level retry/continue timing, partial-continue semantics, and non-Codex adapters remain separate.
- Latest Phase 1 live composer runtime lifecycle proof: elevated `npm run live:codex-composer-runtime-lifecycle` passed with artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-runtime-lifecycle-live-proof/result.json`. The proof runs through Orchestrator's `CodexAppServerRuntimeManager`, not a hand-rolled JSON-RPC loop: it starts a live Codex app-server thread, observes Orchestrator runtime events and `thread/start`, resumes the same provider session id through runtime `mode="resume"` with `thread/resume`, validates prior-turn context in the resumed assistant text, and archives the disposable thread. This closes the runtime-manager layer beneath retry/continue; renderer click flow and full `sessionManager` retry/continue proof remain separate because `sessionManager` is Electron Store/app-profile coupled.
- Latest Phase 1 live composer session lifecycle proof: elevated `npm run live:codex-composer-session-lifecycle` passed with artifact `/Users/nadav/Desktop/Orchestrator/tmp/codex-composer-session-lifecycle-live-proof/result.json`. The proof runs the compiled main-process `sessionManager` under isolated Electron app/user-data mocks, creates a Codex app-server session, sends the first prompt through `sendMessage`, continues it through `continueLastTurn`, forces a retry-recovery user prompt, retries it through `retryLastUserMessage`, and validates all three completed against the same provider session id with one live `thread/start`, two live resume runs, recalled prior-turn context, and archived disposable Codex thread. This closes the main-process session-manager lifecycle proof; later renderer proofs close partial-response and model-switching variants, leaving non-Codex adapters and live Codex UI pixel/timing proof as separate work.
- Latest Phase 1 live composer renderer lifecycle proof: elevated `npm run live:codex-composer-renderer-lifecycle` passed with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-lifecycle-1779986634530.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-lifecycle-1779986634530.png`. The proof launches the real renderer, creates a live Codex app-server transcript, clicks the transcript `Continue` button and the error-card `Retry last message` button, then validates the first, continued, and retried assistant messages all came back from the same provider session id (`019e6f78-aa75-7460-8413-3ef30e98ada6`) with `liveRendererStartMethodCount=2` and `liveRendererResumeMethodCount=4`. This closes renderer click-to-live retry/continue proof for Codex; later focused proofs close interrupted-assistant continuation and model switching, leaving non-command permission variants, non-Codex adapters, and live Codex pixel/timing proof as separate work.
- Latest Phase 1 live partial-response continuation proof: elevated `npm run live:codex-composer-partial-continue` passed with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-partial-continue-1779987197970.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-partial-continue-1779987197970.png`. The proof launches the real renderer, creates a live Codex app-server transcript, injects a visible interrupted assistant message with the `Partial response stopped` chip, clicks the actual transcript `Continue` button, and validates the resumed assistant message returns `CODEX_RENDERER_PARTIAL_CONTINUE_OK:ORCH_RENDERER_PARTIAL_MEMORY_529` on the same provider session id (`019e6f81-45ca-7a41-9c42-8f4d81aaf959`) with `livePartialResumeMethodCount=2`. This closes live partial-response continuation through the visible Codex renderer path; the following model-switch proof closes the resumed-thread model variant, leaving non-command permission variants, non-Codex adapters, and live Codex pixel/timing proof as separate work.
- Latest Phase 1 live model-switch continuation proof: elevated `npm run live:codex-composer-model-switch` passed with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-model-switch-1779987672302.json` and screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-model-switch-1779987672302.png`. The proof launches the real renderer, starts a live Codex app-server transcript with `gpt-5.4-mini`, updates the existing session to `gpt-5.4`, clicks the actual transcript `Continue` button, and validates the resumed turn completes on the same provider session id (`019e6f88-7fe8-7042-8714-78c205e98a9f`). Runtime diagnostics record both `thread/start` and `turn/start` with `model=gpt-5.4-mini`, then `thread/resume` and `turn/start` with `model=gpt-5.4`. This closes live Codex model-switching across a resumed provider thread; remaining lifecycle work is non-command permission variants, non-Codex adapters, and live Codex pixel/timing proof.
- Latest Phase 1 non-command permission card proof: focused Permission smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-permission-1779988391389.json` passed with `permissionVariantCards=true`, proving transcript approval cards render Codex app-server file-change approvals as `File Approval` with the grant root and permission-profile approvals as `Permission Profile` with filesystem/network details. `npm run test:providers` also covers inline app-server `item/fileChange/requestApproval` and `item/permissions/requestApproval` normalization. This closes local/parser/UI coverage for non-command permission variants; live provider forcing of those exact app-server request methods remains separate because current live approval proof deterministically exercises command approval only.
- Latest Phase 1 permission decision-label refresh: focused Permission smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-permission-1779988770839.json` passed with `permissionProfileDecisionLabels=true`, proving a single Codex `permissions` approval card labels actions as `Allow once for permission profile`, `Allow for session for permission profile`, and `Deny permission profile` instead of the awkward provider-token phrase `permissions permission`.
- Latest Phase 1 Settings Shortcuts status refresh: focused Settings smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779961645668.json` passed with `settingsShortcutActionStatusA11y=true`, proving shortcut save, conflict, and per-binding clear results are visible and announced while keeping existing Settings Shortcuts edit, conflict, punctuation capture, per-binding clear, compactness, and shared-action gates green.
- Latest Phase 1 Side Chat personalization refresh: focused Side Chat smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-side-chat-1779980367218.json` passed with `sideChatPersonalizationContext=true`, proving `/btw` side-chat answers now receive the same enabled personalization custom-instruction and coding-preference wrapper as main-thread sends. The same run kept `sideChatActionStatusA11y=true`, `sideChatRetryStatusA11y=true`, draft persistence, multiline input, context metadata, retry, and close gates green.
- Latest Phase 1 Files/source-tab overflow refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779944224395.json` passed with `fileTabStripOverflow=true`, and focused visual inventory `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-files-tab-overflow/manifest.json` captures the dense source-tab row without tab-label collision. The shared tab-strip wrapper change was regression-checked with focused right-panel smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779944285288.json` and terminal visual smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-visual-1779944283694.json`.
- Latest Phase 1 Files row copy refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779960351909.json` passed with `filesRowCopyPathClipboard=true`, proving Files row `Copy path` uses the app clipboard bridge, announces copied status, and copies `Nested Folder/nested note.md` while keeping row context menu and preview gates green.
- Latest Phase 1 Files/source selected-line context refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780022815383.json` passed with `fileSourceLineAddToChat=true`, `fileSourceActionStatus=true`, and `fileSourceLineUtilities=true`, proving source-tab selected-line actions can now insert `review-base.txt:2` plus its source text into the main composer, announce the result, and still copy the selected line reference through the app clipboard bridge while keeping existing file-source, search, artifact, and preview gates green.
- Latest Phase 1 Files content-search open-line refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779955742571.json` passed with `filesContentSearchOpenLine=true`, proving content-search hits open the matched file in source mode with the matched line selected and revealed.
- Latest Phase 1 Files/source tab reset refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780004708217.json` passed with `workbenchFileTabReset=true`, proving Workbench file tabs expose the shared `Reset tab` menu path and clear source/search/selection/reveal/blame view state while preserving the file tab identity and preview/pin lifecycle.
- Latest Phase 1 Review copy-action refresh: focused Diff source smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1780021424055.json` passed with `reviewLastTurnGitApplyCopyStatus=true`, proving provider Last turn `Copy git apply command` now shows visible copied feedback through a standalone Review status pill even when no local Git action bridge is present. Focused Diff core smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780021472816.json` also passed with `reviewGitApplyCopyStatus=true` and `reviewFloatingGitActionStatus=true`, proving the existing local Review git-action status path stayed intact.
- Latest Phase 1 Review/Git handoff refresh: focused Diff core smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780021759605.json` passed with `reviewFloatingGitOpenTab=true`, proving the local Review floating action pill can open the dedicated Git tab, verify the Git panel is idle, then return to Review with hydrated diff rows before completing the existing Review core gates.
- Latest Phase 1 Review Git bridge retirement refresh: focused Diff core smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780024835038.json` passed with `reviewFloatingGitBridgeRetired=true`, `reviewFloatingGitOpenTab=true`, and `reviewFloatingGitActionStatus=true`, proving Review no longer exposes Stage all, Unstage all, Revert all, or a Review-specific revert confirmation while preserving the `Open Git` handoff and copy/apply status feedback.
- Latest Phase 1 Review directory metadata refresh: focused Diff core smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780030681929.json` passed with `diffDirectoryMetadata=true`, proving grouped Review directory rows now expose aggregated file count, additions, and deletions while keeping Review toolbar, file-section, diff rendering, tree navigation, Git handoff, and bridge-retirement gates green.
- Latest Phase 1 Settings in-page search refresh: focused Settings smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780025552163.json` passed with `settingsInPageSearchTarget=true`, proving Settings topbar search can target the General Composer controls directly, scroll them into view, move focus to the matched group, and expose the active search anchor while keeping section-route search, Settings history navigation, shared header band, provider/data/browser/worktree/shortcut/personalization gates green.
- Latest Phase 1 Settings Browser search refresh: focused Settings smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029250662.json` passed with `settingsBrowserInPageSearchTarget=true`, proving Settings topbar search can target Browser permissions from a control keyword such as `downloads`, route to Browser, focus the permissions group, and expose the active search anchor while preserving the broader Settings gates.
- Latest Phase 1 Settings Provider search refresh: focused Settings smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029502642.json` passed with `settingsProviderInPageSearchTarget=true`, proving Settings topbar search can target Provider defaults from a control keyword such as `models`, route to Providers, focus the defaults group, and expose the active search anchor while preserving the broader Settings gates.
- Latest Phase 1 Settings Shortcuts search refresh: focused Settings smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780029723853.json` passed with `settingsShortcutsInPageSearchTarget=true`, proving Settings topbar search can target Shortcut bindings from a control keyword such as `keybinding`, route to Shortcuts, focus the bindings/search/edit table, and expose the active search anchor while preserving the broader Settings gates.
- Latest Phase 1 Settings Worktrees search refresh: focused Settings smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1780031328081.json` passed with `settingsWorktreesInPageSearchTarget=true`, proving Settings topbar search can target Worktree create controls from `base ref`, route to Worktrees, focus the Create group, and expose the active search anchor while preserving the broader Settings gates.
- Latest Phase 1 Browser hidden lifecycle refresh: focused Browser smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780025917536.json` passed with `browserHiddenIdentity=true`, proving the hidden Browser state shows the currently loaded page title/URL while preserving `browserHiddenWebviewPersistence=true`, `browserLifecycleResync=true`, `browserHiddenWebviewContainment=true`, and the broader Browser toolbar, history, security, browser-use, target-action, comment, load-error, tab-reset, and fork-transfer gates.
- Latest Phase 1 Browser screenshot status refresh: focused Browser smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780030113342.json` passed with `browserScreenshotStatusA11y=true`, proving Browser screenshot capture now announces `Screenshot saved` through a live status while keeping screenshot preview, attachment, toolbar, history, lifecycle, security, browser-use, target-action, comment, load-error, tab-reset, and fork-transfer gates green.
- Latest Phase 1 Browser screenshot artifact-action refresh: focused Browser smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1780046018097.json` passed with `browserScreenshotPathActions=true` and `browserScreenshotAttachment=true`, proving saved Browser screenshots now expose Add to chat, Copy path, and Reveal file actions while preserving Browser screenshot status, attachment, toolbar, history, lifecycle, security, browser-use, target-action, comment, load-error, tab-reset, and fork-transfer gates.
- Latest dev comparison refresh: `tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T17:54:59.780Z`, still reports `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, `needsProof=0`, and `remainingParityGaps=18`. The Files row now also gates `filesSpreadsheetDataValidationOverlay=true` for the local XLSX list option overlay/select flow.
- Latest Files renderer refresh: `tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T18:43:24.999Z`, keeps `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, `needsProof=0`, and adds `filesSpreadsheetComments=true` for read-only XLSX cell comments alongside `filesSpreadsheetDataValidationRange=true` and `filesSpreadsheetChartPlot=true`.
- Latest XLSX validation metadata refresh: `tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T19:16:31.224Z`, keeps `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, `needsProof=0`, and includes `filesSpreadsheetDataValidationMessages=true` for read-only XLSX data-validation prompt/error metadata against a clean full 27-capture manifest. The focused Files smoke evidence remains `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779908033816.json`; the full visual-inventory runner now emits per-capture progress and supports `--only <capture-id>` for targeted repros.
- Latest XLSX drawing-object refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779910386517.json` passed with `filesSpreadsheetDrawings=true`, proving local read-only worksheet shape/image drawing cards against Codex workbook bundle evidence for `sparklines.add`, `shapes.add`, `images.add`, geometry, and anchors.
- Latest XLSX sparkline refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779917933648.json` passed with `filesSpreadsheetSparklines=true`, proving local read-only `x14:sparklineGroups` extraction and compact sparkline rendering against Codex workbook bundle evidence for `sparklines.add`. A clean full 27-capture dev visual manifest was refreshed at `2026-05-27T21:57:50.732Z` with no failures and `filesSpreadsheetSparklines=true` in the Files capture; comparison report `/Users/nadav/Desktop/Orchestrator/tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T21:57:58.052Z`.
- Latest XLSX conditional-rule refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779920365153.json` passed with `filesSpreadsheetConditionalFormatting=true`, proving local read-only XLSX `cellIs` and basic custom expression conditional rules with differential fill/font styling alongside the existing color-scale rendering. A clean full 27-capture dev visual manifest was refreshed at `2026-05-27T22:25:31.009Z` with no failures and `filesSpreadsheetConditionalFormatting=true` in the Files capture; comparison report `/Users/nadav/Desktop/Orchestrator/tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T22:25:37.513Z`.
- Latest XLSX threaded-comment refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779921352999.json` passed with `filesSpreadsheetComments=true`, proving local read-only legacy comments plus a resolved threaded range comment with reply-count metadata against Codex workbook bundle evidence for `comments.addThread`, `addReply`, and `resolve`. A clean full 27-capture dev visual manifest was refreshed at `2026-05-27T22:44:41.602Z` with no failures and `filesSpreadsheetComments=true` in the Files capture; comparison report `/Users/nadav/Desktop/Orchestrator/tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T22:44:47.946Z`.
- Latest XLSX per-side border refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779922472669.json` passed with `filesSpreadsheetBorders=true`, proving local read-only per-side border color/style extraction and rendering against Codex workbook bundle evidence for `applyRangeBorders`. A clean full 27-capture dev visual manifest was refreshed at `2026-05-27T23:01:09.861Z` with no failures and `filesSpreadsheetBorders=true` in the Files capture; comparison report `/Users/nadav/Desktop/Orchestrator/tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T23:01:13.902Z`.
- Latest DOCX text-style refresh: focused Files smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1779923502120.json` passed with `filesDocumentTextStyles=true`, proving local read-only DOCX run color, font size, and font family rendering in addition to paragraph style, bold, italic, underline, and highlight metadata against Codex document bundle evidence for `textStyle` / `textStyles`. A clean full 27-capture dev visual manifest was refreshed at `2026-05-27T23:27:09.654Z` with no failures and `filesDocumentTextStyles=true` in the Files capture; comparison report `/Users/nadav/Desktop/Orchestrator/tmp/codex-side-panel-comparison/comparison-report.json`, created at `2026-05-27T23:27:13.651Z`.
- Latest Phase 1 composer usability refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779925833750.json` passed with `composerEmptySuggestionFillsDraft=true`, proving empty-chat starter suggestions now populate the active composer through the shared composer insertion path. This is daily coding usability work, not Office/PDF renderer parity.
- Latest Phase 1 active-thread composer settings refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779936345825.json` passed with `composerActiveThreadSettings=true`, proving the existing-thread provider/model composer label opens a compact model/agent/effort settings popover instead of being read-only text.
- Latest Phase 1 active-thread provider/model switch refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779976107972.json` passed with `composerActiveThreadModelSwitchPersisted=true`, `composerActiveThreadProviderChoices=true`, `composerActiveThreadProviderSwitch=true`, `composerActiveThreadProviderSwitchPersisted=true`, and `composerActiveThreadProviderSwitchPolicyPersisted=true`, proving active threads can persist a model change from the `Thread settings` popover, expose the same provider choice row as new-thread composer setup, switch from Claude to Codex through the popover, and persist the session as Codex app-server with a GPT model, Codex `Ask` permission policy, and low effort while keeping provider-neutral header identity, agent, thinking, permission, slash-menu focus, blocked-send recovery, attachment, side-chat guard, and toolbar gates green.
- Latest Phase 1 composer slash-model refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779963656800.json` passed with `composerSlashModelOpensSettings=true`, proving `/model` from the slash palette opens the existing thread model/settings popover instead of inserting a dead literal command.
- Latest Phase 1 composer slash-menu focus refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779964006792.json` passed with `composerSlashModelFocusesMenu=true`, `composerSlashPermissionsOpensMenu=true`, and `composerSlashPermissionsFocusesMenu=true`, proving slash-opened model and permission menus move keyboard focus into the opened menu.
- Latest Phase 1 composer accessibility refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779945983816.json` passed with `composerPermissionTriggerAriaExpanded=true`, `composerPermissionTriggerAriaCollapsed=true`, `composerAgentTriggerAriaExpanded=true`, and `composerAgentTriggerAriaCollapsed=true`, proving Agent/Permission composer popover triggers expose menu popup state while preserving Escape/outside-click dismissal and focus return.
- Latest Phase 1 composer keyboard refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779946332468.json` passed with `composerPermissionRovingKeyboard=true`, proving ArrowDown can enter an open composer permission popover from its trigger and continue moving focus across enabled popover actions.
- Latest Phase 1 composer blocked-send accessibility refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779951707875.json` passed with `composerSendStatusExplainsBlocked=true`, `composerSendStatusA11yLive=true`, `composerSendStatusActionOpensPermissions=true`, and `composerSendStatusRecoveryClearsBlock=true`, proving unsupported permission policies show a visible live status explanation, the direct permission-change action has an explicit accessible name, opens matching permission controls, and choosing a supported mode clears the block instead of relying on a disabled send button alone.
- Latest Phase 1 composer permission-source refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779954161525.json` passed with `composerPermissionContextSignal=true`, proving the closed composer permission trigger exposes whether the current policy came from static, live, or fallback provider config before opening the menu.
- Latest Phase 1 composer attachment-status refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779956869196.json` passed with `composerAttachmentStatus=true`, proving attachment add/remove feedback is visible, announced, and mirrored on the composer shell instead of only mutating attachment chips.
- Latest Phase 1 composer attachment switch refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780000690946.json` passed with `composerAsyncAttachmentSwitchIsolation=true` and `composerManualAttachmentSwitchIsolation=true`, proving pasted/dropped saves and manual file-picker selections still persist to the original chat after a mid-save chat switch without leaking completion status, pending UI, or focus into the newly active composer.
- Latest Phase 1 composer side-chat attachment guard refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779963265449.json` passed with `composerSideChatAttachmentGuard=true`, proving `/btw` side-chat sends no longer silently drop attached files and instead show a recoverable status guard before send.
- Latest Phase 1 composer permission-rule refresh: focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779957287664.json` passed with `composerPermissionRuleStatus=true`, proving advanced Allow/Deny/Tools/Dirs permission-rule edits have labelled controls and visible polite saved feedback.
- Latest Phase 1 main-chat message-copy refresh: focused Transcript layout smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1779958260403.json` passed with `chatMessageCopy=true` and `chatMessageCopyA11y=true`, proving assistant message copy uses the app clipboard bridge, updates its copied label, and announces success through a polite live status while keeping transcript layout/search/retry/continue gates green.
- Latest Phase 1 transcript thinking refresh: focused Scroll smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-scroll-1779962918884.json` passed with `thinkingIndicatorDuringUpdate=true` and `thinkingIndicatorHiddenAfterComplete=true`, proving streamed assistant updates expose a polite atomic thinking/status indicator while preserving scroll lock, streaming cursor, dedupe, and jump-to-latest behavior.
- Latest Phase 1 active-run queue refresh: focused Streaming typing smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-streaming-typing-1779937027194.json` passed with `composerQueuedSummary=true` and `composerWillQueueStatus=true`, proving the composer surfaces queued/steering follow-up counts during an active streaming run and shows a pre-send queued-follow-up status while typing.
- Latest Phase 1 side-chat recovery/composer/context refresh: focused Side chat smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-side-chat-1779944877204.json` passed with `sideChatErrorRetry=true`, `sideChatMultilineDraft=true`, and `sideChatContextMetadata=true` alongside side-chat tab, draft, compact composer, calm-label, and close gates, proving failed side-chat answers expose a direct retry path, multiline side-chat drafts persist across right-panel tabs, and `/btw` side chats show their attached main-thread context.
- Latest Phase 1 Browser local-server action-status refresh: focused Browser smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779974468590.json` passed with `browserLocalTargetActionStatus=true` and `browserLocalRouteActionStatus=true`, proving local dev-server refresh/hide/restore and provider-route removal publish compact live feedback while keeping Browser local-target, route-normalization, client-tool, screenshot, load-error, and fork-transfer gates green.
- Latest Phase 1 thread route/link refresh: focused Sidebar smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1780019865173.json` passed with `actionCopyThreadLinkWorks=true` alongside `actionCopyDeeplinkWorks=true` and `actionOpenInNewWindowWorks=true`, proving session actions can copy both in-app `/threads/{id}` / `#/threads/{id}` routes and app-protocol deeplinks. Focused Session Switch smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1780019938197.json` passed after the same change, preserving direct startup route, opening-state, archived restore, missing recovery, unread-visible, transcript search, and virtualized switch gates.
- Latest Phase 1 Browser inspector target-action refresh: focused Browser smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779990437515.json` passed with `browserTargetActionStatus=true`, proving manual Browser target actions now publish a visible/announced result while preserving existing target key/fill/type/read/select/check, client-tool, screenshot, local-server, load-error, and fork-transfer gates.
- Latest Phase 1 Codex app-server non-command permission bridge refresh: targeted runtime test `node --test out-test/src/main/__tests__/codexAppServerRuntime.test.js` passed after adding file-change and permission-profile approval request/response assertions, proving Orchestrator's app-server bridge sends the expected accept/session, turn-scoped permissions, and denial payloads for the non-command permission variants already covered by local transcript UI/parser fixtures.
- Latest Phase 1 Browser copy-status refresh: focused Browser smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779959006682.json` passed with `browserCopyUrlStatus=true`, proving Browser `Copy URL` uses the app clipboard bridge and announces copied status in the load-error recovery panel while keeping Browser load-error, client-tool, inspector, and no-overflow gates green.
- Latest Phase 1 Browser load-error accessibility refresh: focused Browser smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-browser-1779952133626.json` passed with `browserLoadErrorA11yLive=true`, proving failed-page recovery is exposed as an assertive atomic alert with a named recovery action group while keeping the existing shared notice, retry, hard-reload, copy-URL, and external-open controls green.
- Latest Phase 1 Terminal tab-action status refresh: focused Terminal smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1779980713140.json` passed with `terminalTabActionStatus=true`, proving bottom-panel Terminal tab creation now publishes the same visible/announced panel action status as clear/copy/paste recovery. The same run kept `terminalClipboardStatus=true`, `terminalClearActionStatus=true`, `terminalFailureStateA11y=true`, terminal tab, resize, transfer, service-snapshot, link-routing, theme, and telemetry gates green.
- Latest Phase 1 Browser Settings accessibility refresh: focused Settings smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779953313884.json` passed with `settingsBrowserStatusA11y=true` and `settingsBrowserDomainControlsA11y=true`, proving Browser clear/save feedback is announced as polite atomic status, domain-rule inputs are labelled by their rows, domain lists expose list semantics, and compact icon-only remove controls keep accessible `Remove ...` names.
- Latest Phase 1 long-thread refresh: focused Transcript stress smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-stress-1779935886533.json` passed with `longThreadLoadControl=true`, `longThreadVisibleCountIncreased=true`, `lazyLoadedOlderChunk=true`, and `searchJumpFound=true`, proving the main chat exposes explicit long-history counts, lazy-loads older history, and shared-find jumps to early long-thread results without mounting thousands of rows. Focused Session switch smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1779936029719.json` also passes against the same long-history/shared-find contract.
- Latest Phase 1 chat waiting-card accessibility refresh: focused Transcript user-input smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-user-input-1779951732062.json` passed with `userInputMultiQuestionCard=true`, `userInputOptionSelection=true`, `userInputMultiSelect=true`, `userInputStructuredSubmit=true`, `userInputStructuredStatusLive=true`, `userInputResumeError=true`, and `userInputResumeErrorA11yLive=true`, proving multi-question provider prompts collect structured single-select/multi-select answers before resuming, sent state is announced as status, and failed resume paths stay recoverable as alerts. Focused Transcript permission smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-permission-1779951744852.json` passed with `permissionResumeError=true`, `permissionResumeErrorA11yLive=true`, and `permissionActionsRecoverable=true`, proving approval cards do not optimistically mark missing-resume approvals as handled and their recovery actions stay grouped/named. Adjacent focused Transcript layout smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1779940019059.json` passed with `userInputCard=true`, `permissionCard=true`, `permissionActionsWrap=true`, and `narrowPermissionActionsWrap=true`, proving main-transcript waiting cards and narrow permission actions still hold.
- Latest Phase 1 chat continuation/recovery refresh: focused Transcript layout smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1779954533585.json` passed with `errorRecoveryRetry=true`, `errorRecoveryRetryA11y=true`, `chatContinueLastTurn=true`, and `chatContinueLastTurnA11y=true`, proving failed provider result messages render an actionable recovery card with announced retry status and the latest assistant response exposes a visible resume-backed `Continue` action with announced sent state, without launching a live provider during smoke.
- Latest Phase 1 tool-failure recovery refresh: focused Transcript tool-failure smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-tool-failure-1779954531317.json` passed with `transcriptToolFailureVisible=true`, `transcriptToolFailureSummary=true`, `transcriptToolFailureRecovery=true`, `transcriptToolFailureRetry=true`, and `transcriptToolFailureRetryA11y=true`, proving failed tool calls inside the compact tool summary expose a bounded recovery row and announced retry status when retrying the last user request.
- Latest Phase 1 provider Settings refresh: focused Settings providers smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1779948600942.json` passed with `settingsProviderDropdown=true`, `settingsProviderModelListShared=true`, `settingsProviderConfigEditorShared=true`, `settingsProviderCommandOutputShared=true`, `settingsProviderBoundaries=true`, and `settingsProviderContentAnchored=true`, proving the Providers page exposes existing runtime capability gaps as compact boundaries, keeps the Providers title/subtitle visible below the shared topbar, renders provider command/capability output through a compact shared card without running provider commands, keeps the provider picker/model-list/config-editor controls on shared Settings chrome, and keeps those controls contained with bounded edit states and no horizontal overflow. Focused visual-inventory artifact `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-settings-providers-topbar/manifest.json` captures the same top-anchored state.
- Latest Phase 1 Appearance Settings refresh: focused Settings smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-1779949155951.json` passed with `settingsAppearanceColorSwatchesShared=true` and `settingsAppearanceImportControlsShared=true` alongside `themeImport=true`, `themeSharingControls=true`, `themePresetPreview=true`, and the shared Settings surface/module gates, proving Appearance chrome editors, all color inputs, and portable theme import controls use shared Settings chrome without inline control styles.
- Latest Phase 1 Extensions panel refresh: focused Extensions smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-extensions-1779949749916.json` passed with `extensionsPanelSharedPrimitives=true`, `extensionsPanelCalm=true`, `extensionsEmbeddedCopyCompact=true`, and the existing right-panel shell gates, proving `/extensions` keeps the embedded instructions surface compact while summary, disclosure, file-row, command-section, command-row, and item-row chrome expose shared contracts instead of another local panel styling pass.
- Latest Phase 1 Capabilities smoke refresh: focused Capabilities smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-capabilities-1779950085573.json` passed with `capabilitySeededFixture=true`, `capabilityEditSheet=true`, and `capabilitySyncSheet=true`, proving edit/sync sheet coverage now comes from an isolated seeded skill fixture instead of whichever provider resources happen to exist on the local machine.
- Latest Phase 1 reduced-motion screenshot refresh: focused reduced-motion smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-motion-reduced-1779950464033.json` passed with `mainRightPanelScreenshotRegion=true`, `mainBottomPanelScreenshotRegion=true`, `mainPopoverScreenshotRegion=true`, `mainSheetScreenshotRegion=true`, and `mainReducedMotionScreenshotCaptured=true`. The main-app screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-motion-reduced-1779950464033-main.png` captures the right panel, bottom terminal panel, shared popover, and shared sheet in the forced reduced-motion renderer while the existing overlay screenshot remains `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-motion-reduced-1779950464033.png`.
- Latest Phase 1 exit-retention refresh: focused Capabilities smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-capabilities-1779951242586.json` passed with `capabilityMenuExitRetained=true`, `capabilitySheetExitRetained=true`, and `capabilityDialogExitRetained=true`, proving shared menu, sheet, and dialog surfaces remain mounted with `data-motion-exit="true"` on close long enough for exit animation instead of immediate unmount. Focused Composer smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779951266182.json` passed after the shared popover close timing change, proving permission and agent popover dismissal/focus contracts still hold.
- Latest Phase 1 Agents diagnostics refresh: focused Workbench New tab smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1779929811010.json` passed with `agentRuntimeIssueTriage=true`, `agentRuntimeEventFilter=true`, `agentRuntimeEventFacetFilters=true`, and `agentRuntimeEventDetail=true`, proving runtime issue rows summarize failed/waiting events, search can narrow to a matching payload, severity/source facets can isolate failures versus approval waits, and matching events open the same bounded event detail path.
- Latest Phase 1 Agents event-copy refresh: focused Workbench New tab smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1779957820717.json` passed with `agentRuntimeEventCopy=true`, proving selected runtime-event payloads can be copied through the app-owned clipboard bridge with visible status feedback.
- Latest Sidebar provider pin refresh: focused Sidebar smoke `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779913512316.json` passed with `sidebarProviderPinBoundary=true` and `sidebarProviderPinnedOrderPreserved=true`, proving provider-pinned rows remain read-only and keep provider metadata during pinned drag reorder while local pin rows stay scoped to local state. Codex bundle evidence under `tmp/codex-app-assets` includes `list-pinned-threads`, `set-thread-pinned`, and `set-pinned-threads-order`, but the refreshed live proof artifact `tmp/codex-pinned-threads-live-proof/result.json` now records `failedMethod="list-pinned-threads"`, `requestFailureMethods=["list-pinned-threads"]`, and the exact unknown-variant JSON-RPC error from the current stdio app-server.
- Current packaged core side-panel inventory: `tmp/side-panel-visual-inventory-packaged-core/manifest.json`, app mode `packaged`, 8 captures, no failures, created at `2026-05-27T02:21:23.400Z`. Focused packaged sidebar evidence passes `hoverCardMaterial=true` after making the session hover-card material explicit on the portaled element; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779848313353.json`.
- Installed app side-panel replacement proof now exists: after `npm run pack:mac` and `npm run install:mac`, elevated `npm run compare:codex-side-panels -- --run-smoke --full --installed --no-fail --smoke-out tmp/side-panel-visual-inventory-installed-full` passed against `/Applications/Orchestrator.app` with 27 captures and no comparison failures. The latest installed Browser capture proves `browserClientToolScreenshot=true` and `browserClientToolAdvancedActions=true` for the Browser screenshot and advanced dynamic-tool slices; screenshot `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-installed-full/browser.png`. Focused installed Sidebar smoke evidence passed with `hoverCardMaterial=true`; JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779849160385.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779849160385.png`. The installed Workbench right-panel capture proves `rightPanelPanelOpenCloseTelemetry=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779852816510.json`; screenshot `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-installed-full/workbench-right-panel.png`. The installed multi-window capture proves `loadedDeepLinkDoesNotLeavePendingNavigation=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-multi-window-focus-1779852980419.json`; screenshot `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-installed-full/multi-window-focus.png`.
- Current dynamic client-tool transport proof: `tmp/codex-dynamic-tools-live-proof/result.json`, status `ok=true`; the live Codex app-server accepted advertised `dynamicTools`, sent `item/tool/call` for `orchestrator.browser_bridge_status`, received Orchestrator's response, and completed with `CODEX_BROWSER_LIVE_OK`.
- Current real Browser dynamic-tool proof: `tmp/codex-browser-tools-live-proof/result.json`, status `ok=true`, refreshed at `2026-05-27T06:45:10.225Z`; the live Codex app-server accepted the production Browser tool specs, sent `item/tool/call` for `orchestrator.browser_open`, `orchestrator.browser_read`, `orchestrator.browser_click`, `orchestrator.browser_type`, `orchestrator.browser_screenshot` with `includeImage=true`, `orchestrator.browser_fill`, `orchestrator.browser_key`, `orchestrator.browser_select`, `orchestrator.browser_check`, `orchestrator.browser_scroll`, then a final `orchestrator.browser_read`, and completed with `CODEX_BROWSER_LIVE_OK`.
- Current comparison artifact gating: the Browser row now parses those live proof JSON files directly, including the `serverRequests[].paramsPreview` tool calls, so missing or stale live proof artifacts show up in the comparison report.
- Current Browser renderer bridge state: Orchestrator advertises `orchestrator.browser_open`, `orchestrator.browser_read`, `orchestrator.browser_click`, `orchestrator.browser_type`, `orchestrator.browser_screenshot`, `orchestrator.browser_fill`, `orchestrator.browser_key`, `orchestrator.browser_select`, `orchestrator.browser_check`, and `orchestrator.browser_scroll` from the Codex app-server runtime. It routes supported `item/tool/call` requests through main-to-renderer IPC, opens the Browser panel, returns URL/title/visible-structure plus target metadata from the actual Browser webview, executes click/type/fill/key/select/check/scroll actions against visible page targets or the page viewport, and captures the current webview as a saved PNG artifact with screenshot metadata plus an opt-in inline `data:image/png;base64` payload for `browser_screenshot` callers that set `includeImage=true`. Focused Browser smoke now gates the loopback with `browserClientToolBridge=true`, `browserClientToolActions=true`, `browserClientToolScreenshot=true`, `browserClientToolScreenshotImage=true`, and `browserClientToolAdvancedActions=true`, plus a visible/announced browser-use runtime status under `browserUseStatusVisible=true`; native browser-use event streaming is still pending.
- Current pushed branch: `codex-side-panel-parity-stabilization`.

## 2026-05-26 Visual Pass

Direct live Codex UI inspection through Computer Use is currently blocked by the app safety boundary: `com.openai.codex` is not inspectable from this environment. Chronicle is running but has no fresh screen-recording frame files in the current `$TMPDIR/chronicle/screen_recording` folder, so it cannot supply a current Codex screenshot either. A macOS `screencapture` route has worked in earlier passes, but the current attempts produce an all-black PNG. The comparison now has `--capture-live-codex`, which discovers visible Codex windows through CoreGraphics, tries `screencapture` against the window id, tries the discovered window bounds as a region, then falls back to full-screen capture before validating PNG nonblank signal. Latest elevated capture diagnostics found one visible Codex window (`id=39726`, bounds `0,30,1384,1320`), but window capture failed with `could not create image from window`, region capture failed with `could not create image from rect`, and the full-screen fallback exited `0` while still reporting `nonBlank=false`, `nonBlack=0`, `lumaStdDev=0`, and `colors=1`. The comparison JSON reports the optional live screenshot file aggregate as `passed=false` on the affected rows and now surfaces those failures in `summary.optionalFileEvidenceFailures` while keeping smoke-backed rows green. This pass still does not claim full pixel-level side-by-side parity with the running Codex app. It combines the live installed Codex bundle/version, live screenshot evidence only when the PNG is nonblank, the runnable comparison script, and the current Orchestrator 27-capture screenshot/behavior inventory.

Earlier live screenshot evidence from 2026-05-26 showed Codex's left sidebar top utility rows as `New chat`, `Search`, `Plugins`, and `Automations`, followed by Pinned, Projects, Chats, and a Settings footer. It also showed the Review right panel using compact Review tabs, a `Last turn` source row, file path header, and compact diff rows. Do not reuse the current `/private/tmp/codex-current-screen.png` as visual proof unless the comparison reports nonblank image metrics for it.

Reviewed Orchestrator screenshots:

- `tmp/side-panel-visual-inventory-current/header.png`
- `tmp/side-panel-visual-inventory-current/workbench-right-panel.png`
- `tmp/side-panel-visual-inventory-current/chat-sidebar.png`
- `tmp/side-panel-visual-inventory-current/review-entry.png`
- `tmp/side-panel-visual-inventory-current/review-last-turn.png`
- `tmp/side-panel-visual-inventory-current/review-source.png`
- `tmp/side-panel-visual-inventory-current/browser.png`
- `tmp/side-panel-visual-inventory-current/terminal-bottom-panel.png`
- `tmp/side-panel-visual-inventory-current/terminal-behavior.png`
- `tmp/side-panel-visual-inventory-current/settings.png`
- `tmp/side-panel-visual-inventory-current/multi-window-focus.png`

Result: the local 27-capture inventory looks coherent against the current shared shell primitives, and the current comparison reports `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`. The latest full visual-inventory wrapper still writes separate `terminal-behavior` and `multi-window-focus` captures so transfer semantics, window-scoped pending navigation, loaded-window deep-link handoff, and app-owned route/window/menu-command routing can be compared without overloading the stable visual panel screenshots. The latest full refresh completed cleanly with progress output, so `terminal-behavior` is no longer an infrastructure caveat; use `--only terminal-behavior` when debugging that capture. The Settings capture now proves the host-scoped Browser page with `settingsBrowserPage=true`, `settingsBrowserSurface=true`, `settingsBrowserModule=true`, `settingsBrowserPolicyPersistence=true`, and local Orchestrator Personalization with `settingsPersonalizationLocal=true`. The Browser capture now proves persisted Browser Use defaults reach the right-side Browser Security pane with `browserPersistedPolicyDefaults=true`, and the Browser dynamic-client-tool path now gates `browserClientToolAdvancedActions=true` for fill/key/select/check/scroll in addition to open/read/click/type/screenshot. The Files capture now proves Codex-style artifact Open/options controls, Workbench file-source action labels, compact options/open action cluster, shared `content-search-input` source find, workbook-style XLSX sheet tabs, active-cell/formula-bar chrome with scoped formula editing/recalculation, simple cell fill/font style rendering, merged cells, row/column sizing, freeze panes with visible freeze-line handles, wrapped/aligned cells, table metadata/style/filter rendering, color-scale, `cellIs`, and basic custom expression conditional-format rendering, list data-validation values/range-backed dropdowns plus read-only validation prompt/error metadata, legacy and threaded range comment indicators/list cards, lightweight chart metadata cards, read-only anchored shape/image drawing cards, and compact read-only sparkline cards for XLSX, Codex-named DOCX page surfaces with table/block rendering plus footnotes, comments, review marks, hyperlinks, and list/numbering rendering, PPTX positioned-shape slide-stage rendering, shared PDF/DOCX and XLSX/PPTX preset/fit zoom menus, local PDF annotation-mode comments, and PPTX speaker notes. The real remaining parity risk is not obvious one-off padding or color drift in the current Orchestrator screenshots; it is the set of areas that still need either live Codex screenshot/timing evidence, provider-backed data that fixtures cannot create, or header/panel relationship checks where individually passing panels could still feel unlike Codex.

Highest-value next UI/parity work:

1. Header and panel interaction: keep sidebar, main header/titlebar, Workbench right panel, Terminal bottom panel, and app-owned window/menu command routing attached to the same shell contract. Treat this as an active user-observed parity risk: Orchestrator can look unlike Codex when each panel passes independently but the header/panel relationship is inconsistent. Header actions should read as compact toolbar chrome in the same band as right-panel chrome, not as a heavier independent button cluster. Any future panel layout or route/window change should update or keep green the dedicated comparison row before it is treated as parity-safe, and any visible header/panel mismatch in the contact sheet should be promoted to the active parity list before more one-panel work continues.
2. Browser agent-driven parity: keep the real Browser dynamic-tool proof plus renderer loopback smoke green for `browser_open`, `browser_read`, `browser_click`, `browser_type`, `browser_screenshot(includeImage)`, `browser_fill`, `browser_key`, `browser_select`, `browser_check`, and `browser_scroll`. Manual Browser UI is strong, generic dynamic-tool transport works, those real tool requests are live-proven through the Codex app-server path and installed-smoke-proven through `/Applications/Orchestrator.app`, and server-initiated unimplemented tool calls still surface an explicit unavailable status. Continue only from native browser-use events, provider design-change application, or live pixel/timing evidence.
3. Review provider data parity: add one real provider-backed source at a time, especially provider blame or live commented-PR proof. Do not treat local fixture comments as final hosted/cloud parity.
4. Terminal visual parity: capture live Codex bottom-panel height/animation at multiple window sizes if a manual screenshot path becomes available; local Orchestrator terminal layout is smoke-covered but exact Codex timing is not.
5. Settings adapter parity: local Orchestrator Personalization is now implemented and smoke-covered, but provider-native Host Personalization and remote-host pages must stay explicitly unavailable until real provider adapters exist. Deepen Browser Use settings only when a provider or browser-use runtime exposes more policy state than the current persisted approval/history/download/upload/domain defaults.
6. Review visual spacing: defer further Review padding/color work unless a live Codex screenshot or a new visual fixture demonstrates a specific mismatch.

Panel/header interaction remains a first-class contract, not an incidental visual note: any future sidebar, main titlebar, Review, Workbench right-panel, Files panel, Browser panel, Terminal bottom-panel, or route/window command-routing change must keep the dedicated `Header and panel interaction` comparison row green and inspect `tmp/codex-side-panel-comparison/header-panel-contact-sheet.html`. Treat a visible mismatch in how the panels meet or share the header band as an active parity gap even when each individual panel smoke passes. The comparison report also records the live Codex screenshot file backing this row, including presence, size, age, and nonblank PNG metrics, so stale, missing, or black live evidence is visible during future parity runs.

The generated comparison report now includes a `Remaining Parity Gaps` table and matching `summary.remainingParityGaps` JSON. Use that list to pick the next implementation slice: live screenshot/timing gaps should wait for usable Codex UI evidence, provider-adapter gaps should wait for real provider events/APIs, native Browser runtime gaps should wait for browser-use events or route/capture signals, and renderer-fidelity gaps should be scoped around concrete artifact renderer behavior rather than broad styling.

## Recently Aligned

| Surface | Codex evidence | Orchestrator state | Follow-up |
| --- | --- | --- | --- |
| Header and panel interaction contract | Live screenshot `/private/tmp/codex-current-screen.png` shows Codex treats sidebar, conversation header, right panel, and bottom panel as coordinated shell surfaces rather than independent stacked panes, with a title-first compact session header and compact top-right controls instead of a visible provenance/profile strip. Bundle evidence in `app-shell-state-HP0T5lEX.js`, `app-shell-BJK30dyj.css`, `app-shell-Bh-lgoQk.js`, and `thread-page-bottom-panel-state-D1Lz0U4Y.js` confirms shared right/bottom shell state, a continuous app-shell main content frame, and terminal bottom-panel ownership. | The runnable comparison now has a dedicated `Header and panel interaction` row covering `sidebarTopInsetCodexLike=true`, `profileBadgeCompact=true`, `headerActionChromeCompact=true`, `sessionHeaderInPrimaryColumn=true`, `rightPanelHeaderSeam=true`, `headerPanelSharedBand=true`, `mainContentFrameContinuous=true`, `filesHeaderPanelSeam=true`, `browserHeaderPanelSeam=true`, `headerMetadataTooltipOnly=true`, `rightPanelMaterialSolid=true`, `terminalPanelMaterialSolid=true`, `terminalBottomPanelSizeDecomposition=true`, `terminalVisualHealthyContent=true`, `secondWindowCreated=true`, `secondWindowNavigated=true`, `pendingNavigationConsumedOnce=true`, `pendingNavigationWindowScoped=true`, `loadedDeepLinkDoesNotLeavePendingNavigation=true`, `firstWindowBrowserFocusArea=true`, `firstWindowBrowserMenuEnabled=true`, `secondWindowBrowserMenuDisabled=true`, `backgroundWindowMenuDoesNotClobberFocusedWindow=true`, `activeWindowAfterRefocus=true`, `focusSwitchRestoresFirstWindowMenu=true`, and `menuCommandRoutedToFocusedWindow=true` across `chat-sidebar`, `header`, `transcript-narrow`, `workbench-right-panel`, `files`, `browser`, `review-last-turn`, `review-core`, `terminal-bottom-panel`, and `multi-window-focus` captures. | Keep all future sidebar, main header, Files, Browser, Review, Workbench, Terminal shell, and route/window command-routing moves under this contract. Exact live Codex pixel spacing, OS focus behavior, and animation timing still need new live screenshot evidence. |
| App-shell panel material | Codex app-shell bundle uses shared app-shell panel state and main-surface panel backgrounds; live screenshot evidence shows the right panel as a solid shell surface rather than a translucent overlay over the transcript/header. | Orchestrator now marks shared right/bottom `AppShellPanel` surfaces as `data-app-shell-panel-material="solid"`, gives Workbench and Terminal shell/chrome surfaces solid `var(--surface-bg)` backgrounds, and smoke-gates computed background opacity with `rightPanelMaterialSolid=true` and `terminalPanelMaterialSolid=true`. | Keep panel material in the header/panel row so future overlay or animation changes do not reintroduce see-through side/bottom panels. |
| App-shell panel open/close telemetry | Codex `app-shell-tab-controller-B2eCi4Le.js` records `codex_thread_side_panel_opened`, `codex_thread_side_panel_closed`, and `codex_thread_side_panel_tab_viewed` with active-tab and route-kind metadata. | Orchestrator shared `AppShellPanel` now records `panel.opened` and `panel.closed` for right/bottom surfaces with `panelSurface`, `activeTab`, and `routeKind`; the Workbench comparison gates this with `rightPanelPanelOpenCloseTelemetry=true`. | This narrows the product-level panel lifecycle gap. Broader route/window lifecycle, exact live timing, and Codex pixel spacing remain open. |
| Header and panel interaction | Live screenshot `/private/tmp/codex-current-screen.png` shows the Codex right-panel/review shell chrome starts in the same compact top band as the conversation header/titlebar rather than below it. Codex app-shell CSS also frames main content as a continuous app-shell surface rather than a rounded card island. | Orchestrator now renders the session titlebar inside the primary chat column while the Workbench right panel remains a sibling in the same session row. The main content shell no longer has the previous rounded left edge or shadow, and focused right-panel smoke gates `rightPanelHeaderSeam=true`, `headerPanelSharedBand=true`, and `mainContentFrameContinuous=true`. Terminal visual smoke confirms the bottom panel still attaches across the content area after the layout change. | Keep future work to live Codex pixel/timing comparison or a new screenshot-proven mismatch; the structural header/right-panel/main-frame relationship is now smoke-covered. |
| Workbench tab overflow and close chrome | Codex bundle evidence in `app-shell-tab-controller-B2eCi4Le.js` shows right-panel tabs are owned by the shared app-shell tab controller, with active-tab scrolling instead of collapsing tab affordances into the header band. The root app-shell tab renderer in `app-shell-Bh-lgoQk.js` caps tab wrappers at `max-w-40`/`max-w-39`, uses `overflow-x-auto`, and renders closable tab controls as hidden start-edge close affordances (`start-0`) with `codex.tabs.closeNamed` labels. | Orchestrator now caps shared panel tabs at 160 px with a 156 px content cap, keeps the active tab wider/readable, gates the strip with `workbenchPanelTabOverflowController=true`, `workbenchPanelTabCodexWidthCap=true`, `workbenchPanelTabReadableSeparation=true`, keeps the active tab fully inside the scroll row after constrained-width resizes with `workbenchPanelActiveTabVisibleAfterResize=true`, masks clipped left-edge overflow with `workbenchPanelOverflowFadeMasksClippedTabs=true`, prevents tooltip-wrapped tab buttons from overlapping in dense Files/source-tab rows with `fileTabStripOverflow=true`, and places the shared tab close affordance on the start edge with `workbenchPanelTabCloseStartEdge=true`. | Keep exact active/overflow spacing open for live Codex screenshots, but do not allow the local tab row to collapse labels, visually run labels together, spill into the header action band, leave hard-clipped overflow text, hide the active tab after resize, or drift back to right-edge close buttons. |
| Workbench New tab add affordance and launcher density | Live Codex screenshot evidence shows the right-panel tab strip exposes a single add affordance adjacent to the tabs, not a duplicated add control while the add/new-tab surface itself is active. Codex bundle evidence keeps the panel chrome on compact shared tab/header primitives; the live Codex app window is blocked from Computer Use in this environment, so the remaining pixel proof still needs a separate capture path. | Orchestrator hides the trailing Workbench add button when `New tab` is the active tab, leaving the active `+ New tab` tab as the single add affordance. The New Tab launcher is now top-aligned with compact action cards instead of centered as a large landing page. Focused `--workbench-new-tab` smoke gates this with `workbenchNewTabSingleAddAffordance=true`, `workbenchNewTabCompactLauncher=true`, and `workbenchNewTabFinalCapture=true`, and the comparison includes the add-affordance check in the Right-side Workbench shell row. | Keep the normal trailing add button visible for non-new-tab Workbench states, avoid duplicated add affordances in the active New tab chrome, and keep the launcher compact/top-aligned unless fresh live Codex evidence shows a different right-panel pattern. |
| Agents inspector access | Product-use evidence from the Phase 1 backlog: runtime/context panels need to be useful in normal coding sessions, not only after a provider has already emitted a subagent. | Orchestrator now exposes `Agents` from the Workbench New tab and honors `showEvents` by mounting the Agents tab even when no agent is live. The empty Agents inspector shows provider/model/status, workspace, message/event/agent counts, and recent activity when available. Recent runtime activity rows are searchable/filterable by type, summary, bounded payload, severity, and source family, selectable, and render an `Event detail` section with event type, timestamp, summary, bounded JSON payload, shared compact icon actions for Copy payload/Add to chat, and app-owned clipboard/status feedback. Runtime issue triage also groups failure causes, grouped failures can be inserted into the main composer with bounded thread/runtime/status/workspace context, selected agent tabs show a compact lifecycle timeline, and raw provider transport output is summarized with basic redaction. Focused `--workbench-new-tab` smoke gates the path with `workbenchNewTabAgentsAction=true`, `agentRuntimeEventFilter=true`, `agentRuntimeEventFacetFilters=true`, `agentRuntimeEventDetail=true`, `agentRuntimeEventCopy=true`, `agentRuntimeEventActionChrome=true`, `agentRuntimeFailureGroups=true`, `agentRuntimeFailureGroupAddToChat=true`, `agentSelectedTimeline=true`, and `agentTransportLog=true`. | Continue with deeper runtime diagnostics only from real coding workflow gaps: richer provider-backed observability and live coding-session proof. |
| Review transcript-card open state | Live screenshot `/private/tmp/codex-current-screen.png` shows a transcript `Edited ... files` Review card opening the side-panel Review state with `Last turn` selected, source-level `+/-` stats, path-first file headers, compact diff rows, and no persistent changed-files rail visible. Codex bundle evidence still shows a changed-files side pane exists behind a show/hide files control. | Orchestrator now treats the transcript card source as authoritative: Last turn cards persist/open Review with the `last-turn` source selected, source-level stats in the Review toolbar, a path-first selected-file header with the decorative change glyph hidden, and the changed-files pane hidden, while normal local Review keeps the existing files rail behavior. Focused `--diff-last-turn` smoke gates `reviewTranscriptCardLastTurn=true`, `reviewFileHeaderPathFirst=true`, and `reviewLastTurnVisualState=true`, and the visual inventory now writes `review-last-turn.png` for direct Codex comparison. | Keep the changed-files rail for explicit file navigation/search; card-opened Last turn Review should stay single-column unless the user explicitly shows files. |
| Chat sidebar top inset | Live screenshot `/private/tmp/codex-current-screen.png` shows Codex's first sidebar utility row closer to the window/header band than Orchestrator's previous 64 px spacer allowed. | Orchestrator reduced the left sidebar drag spacer to 48 px, exposes it as `sidebar-window-drag-spacer`, and focused sidebar smoke now gates `sidebarTopInsetCodexLike=true` while preserving 240 px sidebar width, compact rows, and section-order checks. | Keep exact row spacing/pixel comparison open only if a new live screenshot shows measurable drift. |
| Empty project New chat row | Live screenshot evidence and Codex sidebar copy use sentence-case `New chat` for the primary new-chat action; Codex-style sidebar rows should not drift to title-case variants. | Orchestrator now uses `New chat` for the empty-project compact row as well as the primary sidebar action, and focused Sidebar smoke gates that row copy through `emptyProjectNewChatCompact=true`. | Keep sidebar row copy sentence-case with Codex unless a provider-specific label is intentionally different. |
| Terminal bottom-panel default size and transfer | Codex root app-shell bundle `tmp/codex-app-assets/app-shell-Bh-lgoQk.js` stores `app-shell:bottom-panel-height` with a 280 px fallback, clamps through `Tn`, and passes `defaultSize: 280` to the top-edge resize handle. The Terminal chunk keeps Terminal content inside that shared bottom-panel shell, and `app-shell-tab-controller-B2eCi4Le.js` exposes shared `moveTabTo` / `receiveMovedTab` controller semantics. | Orchestrator now opens/resets Terminal to 230 px content plus 50 px chrome for a 280 px total bottom panel, migrates legacy 260/350 px stored defaults to the current target, and uses generic `Move tab to ... panel` transfer copy in both directions. The focused Terminal smoke and visual comparison capture both gate that total through `terminalBottomPanelSizeDecomposition=true`; the visual smoke captures healthy pipe-backed terminal content at the new size with `terminalPanelMaterialSolid=true`; and the comparison now includes `terminalSharedTransferModel=true`, `terminalMoveToRightPanel=true`, and `terminalMoveBackToBottom=true` from the separate `terminal-behavior` capture. | Live Codex animation timing remains unverified; keep future work to timing/route-shell ownership unless a new screenshot shows a concrete visual mismatch. |
| Chat sidebar top-level section structure | Live screenshot `/private/tmp/codex-current-screen.png` shows Pinned, Projects, then Chats as text-first top-level sidebar sections after the utility rows. | Orchestrator now renders projectless Chats as a top-level sibling section next to Projects instead of nesting it inside the project group list, and the Chats section header no longer carries a decorative identity dot. The existing "Chats before projects" preference still works, while default screenshot capture is reset to the Codex-like Projects then Chats order for directly comparable visual inventory evidence. Focused Sidebar smoke gates this with `chatsHeaderTextFirst=true`. | Keep custom-section and user preference behavior covered by focused sidebar smoke, but keep visual inventory captures in default Codex-like order. |
| Chat sidebar provider pin boundary | Codex bundle evidence exposes provider/sidebar pinned thread-key signals plus `list-pinned-threads`, `set-thread-pinned`, and `set-pinned-threads-order` bridge methods, but the current live stdio app-server rejects `list-pinned-threads` as an unknown variant. | Orchestrator keeps provider-projected pinned rows read-only instead of exposing a broken Codex mutation path. Local pinned rows still mutate locally, and provider-pinned rows preserve `providerPinned`, `providerPinOrder`, and `providerPinnedThreadKey` during drag reorder. The focused Sidebar smoke gates `sidebarProviderPinBoundary=true` and `sidebarProviderPinnedOrderPreserved=true`. | Re-enable provider pin/list/order mutation only when a live app-server or provider bridge exposes a safe boundary; keep non-Codex provider pin adapters tracked separately. |
| Notebook artifact read-only controls | Codex `notebook-preview-panel-CAO-aRhM.js` renders notebook previews with `Read only`, disabled `Run all`, and disabled `Restart kernel` controls. | Orchestrator notebook file-tab previews now expose the same read-only execution boundary in the artifact header, while preserving Copy path, View source, Copy raw preview, Open file, and Reveal file actions. Focused Files smoke gates `filesNotebookReadOnlyControls=true`. | Continue with deeper notebook output/cell rendering only if artifact parity is the chosen Files slice; this does not prove provider-backed artifact metadata. |
| Notebook artifact output rendering | Codex `notebook-preview-panel-CAO-aRhM.js` parses and renders notebook output types including streams/text, errors, JSON, HTML/Markdown, and embedded images, hides summarized raw payloads behind `Raw output`, and prefers `text/plain` before JSON when both MIME payloads exist. | Orchestrator now preserves notebook cell outputs, renders stream/text, error, JSON, HTML/Markdown, and embedded image outputs under each cell, uses raw-output disclosures for summarized raw payloads, and follows the same text/plain-before-JSON precedence. Focused Files smoke gates `filesNotebookOutputRendering=true`, `filesNotebookOutputSummaries=true`, and `filesNotebookRawOutputDisclosure=true`. | Continue with exact output sizing/polish and provider-backed artifact metadata in a future Files slice. |
| Notebook artifact cell disclosure | Codex `notebook-preview-panel-CAO-aRhM.js` renders notebook cells as open `details`/`summary` disclosure sections inside the read-only preview. | Orchestrator now renders each notebook cell as an open `details` element with a summary header, chevron disclosure marker, body wrapper, source block, and preserved output rendering. Focused Files smoke gates `filesNotebookCellDisclosure=true`. | Continue with richer output styling and provider-backed artifact metadata. |
| Notebook artifact execution count | Codex `notebook-preview-panel-CAO-aRhM.js` preserves code-cell `execution_count`, renders `Cell {cellNumber} of {totalCellCount}`, shows `Run {executionCount}` for executed code cells, and exposes a disabled per-cell run affordance in read-only previews. | Orchestrator now preserves notebook `execution_count`, renders cell position labels, shows code-cell `Run N` labels, and adds a disabled per-cell run control while keeping the notebook preview read-only. Focused Files smoke gates `filesNotebookExecutionCount=true` against a code cell with `execution_count: 7`. | Continue with output polish and provider-backed artifact metadata; do not add runnable notebook execution until a real kernel/runtime path exists. |
| Notebook artifact cell metadata | Codex `notebook-preview-panel-CAO-aRhM.js` reads cell metadata from direct and nested Codex metadata keys, including `title`/`cellTitle` and `descriptionMarkdown` variants, uses metadata titles in cell headers, and renders described code cells with description markdown plus a nested Code disclosure containing a titled `Python` code snippet. | Orchestrator now reads direct and nested Codex notebook cell metadata, uses metadata titles in notebook cell headers, renders code-cell description markdown, and keeps the code source behind a nested read-only Code disclosure with a titled Python code shell when a description exists. Focused Files smoke gates `filesNotebookCellMetadata=true` and `filesNotebookCodeSnippet=true`. | Continue with output polish and provider-backed artifact metadata. |
| Notebook artifact cell spacing and width | Codex `notebook-preview-panel-CAO-aRhM.js` wraps notebook cells in `px-4 py-4 sm:px-6 sm:py-5`, centers the cell column with `mx-auto`, caps it at `max-w-3xl`, uses `gap-4`, and renders `rounded-lg` cells with `px-4 py-2` headers. | Orchestrator notebook previews now use a centered `notebook-preview-list-inner` column with 768 px max width, 16 px cell gap, 16 px mobile padding, 20 px/24 px wider padding, 8 px cell radius, and 8 px/16 px summary headers. Focused Files smoke gates `filesNotebookCellSpacing=true`. | Continue with deeper output renderer polish and provider-backed artifact metadata. |
| Notebook artifact output/source chrome | Codex `notebook-preview-panel-CAO-aRhM.js` renders cell source bodies with `px-4 py-3` and renders code-cell outputs in a distinct `border-t` secondary-tinted band with `px-4 py-3` and `gap-3`. | Orchestrator notebook previews now wrap source content in a Codex-like padded source body and render outputs in a separate bordered, lightly tinted output band with 16 px horizontal padding, 12 px vertical padding, 12 px output gap, and an inset top highlight. Focused Files smoke gates `filesNotebookOutputChrome=true`. | Continue with deeper per-output item styling and provider-backed artifact metadata. |
| Notebook artifact per-output item chrome | Codex `notebook-preview-panel-CAO-aRhM.js` renders stream/text raw output containers as rounded `bg-token-main-surface-primary/40 p-3` blocks, error output containers as `border-token-charts-red/30 bg-token-charts-red/5 p-3` blocks, and image/markdown outputs as contained item surfaces. | Orchestrator notebook stream/text output items now use Codex-like rounded, lightly tinted item containers with 12 px padding and raw-output pre blocks with 12 px padding. A separate `notebook-output-types-smoke.ipynb` fixture now proves markdown, error, and image item chrome through `filesNotebookRichOutputItemChrome=true`, while the original stream/text fixture remains stable through `filesNotebookOutputItemChrome=true`. | Provider-backed artifact metadata and full artifact renderer fidelity remain open. |
| Notebook artifact output summaries | Codex `notebook-preview-panel-CAO-aRhM.js` reads `outputSummaries[index].summaryMarkdown` from direct and nested Codex cell metadata, attaches it to stream/error/text/JSON outputs, and renders the summary as markdown before a raw-output disclosure. | Orchestrator now reads notebook output summary metadata, preserves it on rendered outputs, shows summary markdown above stream/text/JSON/error output content, and hides summarized raw payloads behind `Raw output`. Focused Files smoke gates `filesNotebookOutputSummaries=true` and `filesNotebookRawOutputDisclosure=true`. | Continue with exact Codex output sizing/polish and provider-backed artifact metadata. |
| Artifact preview title/type headers | Codex `docx-preview-panel-BjyRJuYA.js` renders DOCX artifacts through the shared artifact preview header with `artifactType: DOC` and a title stripped of `.docx`; `pdf-preview-panel-CkrOHSbs.js` does the same for `PDF` and `.pdf`; `notebook-preview-panel-CAO-aRhM.js` renders `IPYNB` plus cell count in the artifact type text. | Orchestrator now uses a shared artifact-style header for PDF, DOCX, and notebook previews. The visible title is extensionless and the type label is separate (`PDF`, `DOC`, `IPYNB · 3 cells` in the smoke fixture). Focused Files smoke gates `filesArtifactHeaderTitleType=true`, and the runnable comparison includes the Codex DOC/PDF/notebook bundle evidence. | Continue with true full-fidelity artifact renderer/provider controls and exact output spacing; this slice only aligns visible artifact header identity. |
| Artifact preview Open/options controls | Codex `artifact-preview-status-D5kFRFQE.js` renders artifact header actions around a primary Open affordance with an adjacent options menu that includes reveal/open-in-folder behavior, instead of exposing Open and Reveal as equal loose toolbar buttons. | Orchestrator now groups artifact preview open behavior behind a primary `Open` toolbar button plus adjacent options chevron/menu across JSON, CSV, PDF, DOCX, XLSX, PPTX, and IPYNB previews. Focused Files smoke gates `filesArtifactOpenOptions=true` and proves the menu exposes both `Open` and `Open in folder`; the full visual inventory includes the same check in the Files capture. | Full remote download/open-provider options and provider-backed artifact metadata remain open. Keep future artifact action work tied to real provider capabilities instead of adding unsupported menu items. |
| Workbench file-source action labels and cluster | Codex `review-file-source-tab-CjS7Xe_W.js` exposes `File viewer options`, `Open in editor`, `Copy path`, `Enable/Disable word wrap`, `Enable/Disable rich view`, and `Show/Hide git blame`; its breadcrumb/action cluster renders the options menu plus the `Open in editor` affordance. | Orchestrator file tabs now use `File viewer options` on the ellipsis button, `Open in editor` for file-open actions, `Open selected line in editor` for line actions, and `Enable/Disable word wrap` in the shared options menu. The visible file-tab action cluster is now the Codex-style `File viewer options` plus `Open in editor` pair, while source/rich, wrap, blame, selection, copy, reveal, and add-to-chat actions stay available through the menu or selected-line affordances. Focused Files smoke gates `workbenchFileTabCodexActionLabels=true` and `workbenchFileTabCodexActionCluster=true`; the full visual inventory includes both checks in the Files capture. | Remaining source-tab gaps are source-search/global-find structure, provider-backed comments/blame, and broader indexed workspace search. |
| Workbench file-source shared find | Codex `review-file-source-tab-CjS7Xe_W.js` registers file-source search through `sourceSearchSource`, while `review-runtime-bridge-CZUIqW4U.js` owns the shared `content-search-input` with `Search chat` and `Search diffs` scope controls. | Orchestrator file-source tabs no longer expose an inline `workbench-file-tab-source-search` field. Focused file tabs route `Cmd+F` into the shared `content-search-input` in diff scope, and shared query/next/close events update source line highlighting and active match metadata. Focused and full Files smoke now gate this with `fileSourceSearch=true`. | Keep exact focus timing and global/provider-indexed workspace search open; the visible inline-search mismatch is closed. |
| PDF page/zoom/invert/presentation/annotation controls | Codex `pdf-preview-panel-CkrOHSbs.js` and `artifact-preview-status-D5kFRFQE.js` render page navigation, shared zoom percent/fit controls, invert-colors controls, `artifactPdfPreview.annotate`, `artifactPdfPreview.commentInput`, a `commentLayer`, local PDF context, and `artifact-pdf-presentation` mode with an `Exit` control. | Orchestrator now parses local PDF page count, renders previous/next controls plus a `current/total` indicator in the artifact header center slot, renders a Codex-style preset/fit zoom menu, exposes an invert-colors control that applies a real iframe filter, updates the embedded PDF URL fragment including `page-fit`, opens a Codex-named PDF presentation surface with keyboard/click page navigation plus Exit, and exposes a local annotation-mode side layer with page-scoped comment input/list/counts. Focused Files smoke gates `filesPdfPreviewControls=true`, `filesDocumentPdfZoomMenu=true`, `filesPdfPresentationMode=true`, and `filesPdfAnnotations=true` against a two-page fixture, and the full comparison row includes the Codex PDF annotation and presentation bundle terms. | PDF canvas rendering, download handling, provider-backed annotation metadata, persisted/provider comments, and provider-backed PDF metadata remain open. |
| DOCX page/zoom controls | Codex `docx-preview-panel-BjyRJuYA.js` uses a dedicated `docx-preview-panel` / `codex-docx-preview` surface, exposes `docx-preview-zoom-trigger`, and shares artifact page/zoom primitives including zoom percent/fit state. `PopcornElectronDocumentPanel-BnYPHweX.js` also exercises document tables, images, shapes, footnotes, comments, comment references, review marks, hyperlinks, link rects, text style metadata, numbering definitions, paragraph numbering, bullet paragraphs, sections, page setup, columns, header text, and footer text. | Orchestrator DOCX previews now expose previous/next controls plus a `current/total` indicator in the artifact header center slot, render a Codex-style preset/fit zoom menu, show Codex-named document page shells, and parse/render local DOCX table blocks, embedded raster image blocks, basic text-box shapes, basic section/header/footer metadata, read-only footnote references/list rendering, read-only comment references/list rendering, read-only review mark rendering, read-only hyperlink metadata/rendering, read-only paragraph/run text style rendering including run color, font size, and font family, read-only list/numbering rendering, and read-only multi-column content flow inside the preview. Focused Files smoke gates `filesDocumentPageControls=true`, `filesDocumentTableRendering=true`, `filesDocumentImageRendering=true`, `filesDocumentSectionMetadata=true`, `filesDocumentColumnLayout=true`, `filesDocumentShapeRendering=true`, `filesDocumentFootnotes=true`, `filesDocumentComments=true`, `filesDocumentReviewMarks=true`, `filesDocumentHyperlinks=true`, `filesDocumentTextStyles=true`, `filesDocumentListRendering=true`, and `filesDocumentPdfZoomMenu=true` through a two-page DOCX fixture, and the comparison row includes the Codex DOCX preview/zoom plus Popcorn document bundle terms. | Full DOCX renderer fidelity, true layout pagination, section-specific page geometry, exact shape geometry, footnote placement, comment placement/threading/replies, revision editing/history, link hit-rect geometry, style inheritance, list continuation, download/provider metadata, and provider-backed artifact annotations remain open. |
| Spreadsheet/slides artifact controls | Codex `artifact-tab-file-kind-fsu6JKhI.js` maps `.xlsx`/`.xlsm` to spreadsheet artifacts and `.pptx` to slides artifacts; `artifact-tab-content.electron-DayvYBGS.js` routes them through `ExtractXlsxProto` / `PopcornElectronWorkbookPanel` and `ExtractSlidesProto` / `PopcornElectronPresentationPanel` with `renderHeaderZoomControl`. The shared artifact status bundle also exposes `artifactTab.preview.zoomPercent` and `artifactTab.preview.zoomToFit`. The Popcorn workbook bundle exposes `popcorn-pages-bar`, `popcorn-sheet-tab-*`, `popcorn-add-sheet`, `popcorn-zoom-select`, `popcorn-formula-bar`, `popcorn-selection`, range `format`, fill/font styling, merged-range `merge()` usage, `columnWidthPx`, `rowHeightPx`, `freezePanes`, `popcorn-freeze-column-line`, `popcorn-freeze-row-handle`, `wrapText`, `horizontalAlignment`, `verticalAlignment`, `tables.add`, `TableStyleMedium2`, `showFilterButton`, `conditionalFormats.addColorScale`, `conditionalFormats.add('cellIs')`, `conditionalFormats.addCustom`, `dataValidation`, `popcorn-data-validation-overlay`, `applyRangeBorders`, `charts.add`, `comments.addThread`, `addReply`, `resolve`, `sparklines.add`, `shapes.add`, `images.add`, drawing geometry/anchors, and recalculation/formula behavior; the Popcorn presentation bundle exposes `popcorn-presentation-page-navigation`, `popcorn-presentation-zoom-select`, `popcorn-presentation-thumbnails`, `popcorn-presentation-codex-thumbnail-rail`, `popcorn-presentation-thumbnail-stack`, `popcorn-presentation-add-slide`, `popcorn-presentation-notes-panel`, `popcorn-presentation-notes`, positioned shapes, background/fill/color styling, and image hydration/thumbnail behavior in its default Popcorn slide document. | Orchestrator now classifies XLSX/XLSM and PPTX as artifact previews instead of generic binary fallback, with extensionless titles, `XLSX` / `PPTX` type labels, compact Copy path/Open/Reveal actions, no unsupported source toggle, and lightweight OOXML rendering for local files. XLSX previews now render one active sheet at a time with header sheet navigation, Codex-like preset/fit zoom controls, Codex-like sheet tabs plus a disabled add-sheet affordance, row/column headers, selectable cells, scoped formula extraction/evaluation, simple cell fill/font style rendering, merged-cell col/row-span rendering, local row/column sizing, frozen rows/columns with visible freeze-line handles, wrapped text plus horizontal/vertical alignment, table range/style metadata with header filter affordances and banded-row styling, two/three-color scale, `cellIs`, and basic custom expression differential-style conditional-format rendering, list data-validation metadata/dropdown affordances, local option overlay/select flow, same-sheet range-backed list extraction, read-only legacy and threaded/resolved range comment indicators/list cards, per-side border color/style rendering, lightweight chart metadata cards with a local plot, read-only anchored shape/image drawing cards, read-only x14 sparkline cards, and a formula bar that displays formula text while cells display computed values; formula-bar edits update local preview state and recalculate same-sheet formulas. PPTX previews now render one active slide surface with slide navigation, Codex-like preset/fit zoom controls, a Codex-style left thumbnail rail, positioned text shapes on the active slide stage when OOXML geometry is available, simple slide background fills, shape fills, text colors, embedded raster image shapes, disabled read-only add-slide affordance, responsive thumbnail-stack styling, outline cards, and a read-only speaker-notes panel when slide notes exist. Focused Files smoke gates `filesSpreadsheetSlidesArtifactBoundary=true`, `filesSpreadsheetRenderer=true`, `filesSlidesRenderer=true`, `filesSlidesShapeLayout=true`, `filesSlidesColorFills=true`, `filesSlidesImageShapes=true`, `filesSpreadsheetControls=true`, `filesSpreadsheetSheetTabs=true`, `filesSpreadsheetActiveCell=true`, `filesSpreadsheetFormulaEvaluation=true`, `filesSpreadsheetCellStyles=true`, `filesSpreadsheetMergedCells=true`, `filesSpreadsheetSizing=true`, `filesSpreadsheetFreezePanes=true`, `filesSpreadsheetFreezeHandles=true`, `filesSpreadsheetAlignment=true`, `filesSpreadsheetTables=true`, `filesSpreadsheetConditionalFormatting=true`, `filesSpreadsheetDataValidation=true`, `filesSpreadsheetDataValidationOverlay=true`, `filesSpreadsheetDataValidationRange=true`, `filesSpreadsheetComments=true`, `filesSpreadsheetBorders=true`, `filesSpreadsheetCharts=true`, `filesSpreadsheetChartPlot=true`, `filesSpreadsheetDrawings=true`, `filesSpreadsheetSparklines=true`, `filesSpreadsheetFormulaEditing=true`, `filesOfficeZoomMenu=true`, `filesSlidesControls=true`, `filesSlidesThumbnailRail=true`, and `filesSlidesSpeakerNotes=true`. | This is a lightweight local renderer/control layer, not full Codex Popcorn parity. Persisted workbook edits, advanced conditional formatting beyond color-scale/cellIs/basic expression rules, border editing/persisted border changes, editable comments, custom/cross-sheet/named-range validations, true drawing canvas positioning/selection/resizing, gradient/theme fill fidelity, interactive chart canvas editing, full slide canvas fidelity, editing hooks, true Popcorn zoom/canvas fitting, and provider-backed artifact metadata remain open. |
| Global find / Review search | `review-runtime-bridge-CZUIqW4U.js` registers `find-in-thread`, renders `content-search-input`, and provides `Search chat` / `Search diffs` scope buttons. If Browser is the active right-panel tab and the browser webview/right-panel is focused, the command dispatches `browser-sidebar-command` `{ type: "open-find" }`. | Orchestrator now opens a shared floating `content-search-input` for transcript/Review find, exposes `Search chat` and `Search diffs` scopes, routes the diff scope into Review content search/match stepping, keeps Files on workspace file search, and preserves Browser-focused `open-find` routing. The focused right-panel smoke treats `rightPanelFindShortcutRouting=true` as this Codex-style contract, not the older per-panel Review search focus. | Keep live Codex side-by-side comparison for spacing, animation, and focus timing. Keep the smoke contract focused on behavior. |

Update: XLSX list data-validation now includes a Codex-evidence-backed local option overlay/select flow plus same-sheet range-backed option extraction, gated by `filesSpreadsheetDataValidationOverlay=true` and `filesSpreadsheetDataValidationRange=true` alongside the existing `filesSpreadsheetDataValidation=true` metadata/affordance gate. XLSX legacy and threaded/resolved range comments now render as read-only indicators/list cards gated by `filesSpreadsheetComments=true`. XLSX per-side border colors/styles now render under `filesSpreadsheetBorders=true` instead of collapsing to a single representative border. XLSX chart cards now render a local plot gated by `filesSpreadsheetChartPlot=true` instead of metadata-only cards. XLSX worksheet drawing relationships now render read-only anchored shape/image cards gated by `filesSpreadsheetDrawings=true`. XLSX sparkline groups now render compact read-only sparkline cards gated by `filesSpreadsheetSparklines=true`. XLSX `cellIs` and basic custom expression conditional rules now apply differential fill/font styling in the local preview under `filesSpreadsheetConditionalFormatting=true`. Custom/cross-sheet/named-range validations, persisted workbook edits, persisted border edits, true drawing canvas positioning/selection/resizing, interactive chart canvas editing, conditional rules beyond color-scale/cellIs/basic expressions, editable comments, and full Popcorn workbook canvas fidelity remain open.

## Verified Mismatches

| Surface | Codex evidence | Orchestrator state | Next action |
| --- | --- | --- | --- |
| Chat sidebar top actions, width, inset, and section order | Live screenshot `/private/tmp/codex-current-screen.png` shows the Codex sidebar starts with `New chat`, `Search`, `Plugins`, and `Automations` above top-level Pinned/Projects/Chats, measures roughly 240 px wide, and places those utility rows close to the window/header band. | Orchestrator now renders those four top utility rows above the sidebar sections, uses the shared sidebar row primitive, fixes the left sidebar width at 240 px instead of the old 300 px wide desktop layout, reduces the top drag spacer from 64 px to 48 px, renders Chats as a sibling section after Projects by default, and keeps the Chats header text-first without a decorative leading dot. Smoke gates order, placement, behavior, tightened width, top inset, projectless metadata, text-first Chats header, and the optional Chats-before-Projects preference. | Keep future sidebar visual work anchored in live screenshots; next sidebar gap is provider-backed pin/list mutation semantics or exact row spacing if a new live screenshot shows drift. |
| Chat sidebar row density and text-first rows | Live screenshot `/private/tmp/codex-current-screen.png` plus Codex bundle CSS show a compact sidebar rail using small row text, with session rows reading text-first rather than carrying an always-visible provider/color dot before every title. | Orchestrator now tags the sidebar as `data-sidebar-density="codex-compact"`, uses 12 px sidebar row text, 26 px nav/thread rows, 22 px compact rows, keeps provider/thread identity as row metadata, and removes always-on leading color dots from normal session rows. Focused Sidebar smoke and the runnable comparison gate `sidebarRowDensityCodexLike=true`, `sessionRowsTextFirst=true`, and `sidebarPinnedRowsTextFirst=true`. | Keep row density and text-first row changes behind this smoke contract; do not reintroduce always-visible leading identity dots without fresh Codex screenshot evidence. |
| Review source metadata | Codex review chunks publish host snapshot metrics through `set-review-pane-snapshot-metrics-for-host` and support PR/check/reviewer/comment/blame toolbar/flyout surfaces. Live screenshot evidence shows the selected-file header as path-first, without a prominent status glyph before the file path. | Orchestrator has local and GitHub-backed PR/check/reviewer/general comment plus inline review-thread summary metadata paths. GitHub provider comments can render as read-only per-line cards in Review with fixture coverage, including review-comment commit/blame metadata from real GraphQL `commit` / `originalCommit` fields. The comparison now includes a direct `review-last-turn` capture that proves the transcript-card Last turn Review panel opens with the changed-files rail hidden, source-level stats visible, and `reviewFileHeaderPathFirst=true`. The Review source menu keeps unsupported provider-native rows visible but now explains each unavailable boundary inline and through attributes: Last turn has no provider turn diff until a provider event exists, Cloud has no review adapter, and Worktree has no provider worktree source outside an app-managed worktree session. Live Codex app-server proof captures real `turn/diff/updated` events with provider session/turn ids and no checkpoint ids; `thread/rollback` works with `numTurns: 1` for persisted thread history but does not revert the workspace file/git diff. Provider-native hosted/cloud review sources, real working-tree checkpoint restore, and live commented-PR proof remain unavailable. | Keep provider Last turn Undo disabled unless a future provider path restores workspace changes, not just thread history. Add real provider-backed review sources only from provider events/adapters; otherwise keep unsupported rows explicit. |
| Browser webview lifecycle | Codex `browser-sidebar-manager-ivre5jEI.js` maintains body-attached hidden/visible webviews keyed by `data-browser-sidebar-conversation-id`, transfers webviews between conversations, and subscribes to browser-use state, viewport, capture surface, cursor, and local-server notifications. `app-server-manager-signals-Csopz8aM.js` also contains `dynamic-tools-for-thread-start-requested`, `dynamic-tool-call-requested`, `item/tool/call`, `capture-browser-use-turn-route`, `browser-use-turn-route-capture`, `browser-use-turn-route-release`, and `codex/browserUse` evidence. | Orchestrator has manager state parsing, visible browser-use runtime status, body-host webview containment, fork transfer, DOM transfer, browser-use no-mutation smokes, and persisted Browser Use approval/history/download/upload/domain defaults that apply to the right-side Browser Security pane. Fresh live proof through `npm run live:codex-browser-appserver` reaches the real Codex app-server and completes a turn with `session.started`, assistant deltas, and `run.completed`, but it emits zero `browser.manager_state` events, sends no browser/tool server requests, and replies `CODEX_BROWSER_LIVE_NO_BROWSER`; artifact: `tmp/codex-browser-appserver-live-proof/result.json`. A separate live proof through `npm run live:codex-dynamic-tools` advertises `orchestrator.browser_bridge_status` in `thread/start.dynamicTools`; Codex sends `item/tool/call` with JSON-RPC id `0`, Orchestrator answers it, and the turn completes with `CODEX_BROWSER_LIVE_OK`; artifact: `tmp/codex-dynamic-tools-live-proof/result.json`. The real Browser tools proof advertises the production `orchestrator.browser_open`, `orchestrator.browser_read`, `orchestrator.browser_click`, `orchestrator.browser_type`, `orchestrator.browser_screenshot`, `orchestrator.browser_fill`, `orchestrator.browser_key`, `orchestrator.browser_select`, `orchestrator.browser_check`, and `orchestrator.browser_scroll` specs; Codex sends `item/tool/call` for those tools, including `browser_screenshot` with `includeImage=true`, then a final read, and completes with `CODEX_BROWSER_LIVE_OK`; artifact: `tmp/codex-browser-tools-live-proof/result.json`. Orchestrator routes those supported calls through `browser:clientToolCall` IPC to the Browser renderer, opens the Browser panel via `browser.manager_state`, returns URL/title/visible-structure/target metadata from the real webview, executes click/type/fill/key/select/check/scroll against visible page targets or the viewport, and captures the current webview to a saved PNG artifact with width/height/size metadata plus opt-in inline image data. Focused Browser smoke proves the main IPC to renderer webview loopback with `browserClientToolBridge=true`, action execution with `browserClientToolActions=true`, screenshot capture with `browserClientToolScreenshot=true`, inline screenshot image return with `browserClientToolScreenshotImage=true`, advanced action execution with `browserClientToolAdvancedActions=true`, browser-use runtime status visibility with `browserUseStatusVisible=true`, local dev-server hide/restore and route-removal feedback with `browserLocalTargetActionStatus=true` plus `browserLocalRouteActionStatus=true`, load-error recovery alert semantics with `browserLoadErrorA11yLive=true`, and the Settings-to-Browser policy path with `browserPersistedPolicyDefaults=true`. If an unimplemented server-initiated `item/tool/call` arrives, Orchestrator still emits a visible `Client tool unavailable` assistant status and returns a structured JSON-RPC unsupported-tool error. | Generic dynamic client-tool transport, visible runtime status for parsed browser-use state, live real Browser open/read/click/type/screenshot(includeImage)/fill/key/select/check/scroll requests, persisted default policy, installed-smoke Browser bridge proof, renderer-backed Browser bridge, local dev-server action feedback, and accessible load-error recovery are implemented and smoke/live-proof-covered. Keep synthetic no-mutation smoke labeled fixture-only and keep unsupported runtime boundaries user-visible. Continue Browser expansion only from native browser-use events, provider design-change application, or live pixel/timing evidence. |
| Browser device presets | Codex `browser-sidebar-manager-ivre5jEI.js` defines the preset catalog in this order: responsive 390x844, 4K, laptop-l, laptop, Surface Pro 7, iPad Air, iPad Mini, Surface Duo, iPhone 15 Pro Max, Pixel 8, iPhone 15 Pro, Samsung Galaxy S24 Ultra, and iPhone SE, with 240-4096 width and 160-4096 height clamps. `thread-side-panel-tabs-CVr2AbYP.js` provides the visible labels, including `Laptop L` and `Samsung Galaxy S24 Ultra`. | Orchestrator's Browser preset select now renders the same flat visible order and labels, keeps the matching dimensions/clamps, and the Browser smoke gates the exact catalog with `browserDevicePresetCatalog=true` plus `browserVisibleGeometry=true`. | Live UI evidence is still useful for menu styling and timing, but label/order drift is now bundle-backed and smoke-covered. |
| Terminal bottom panel | Codex bottom-panel chunk is terminal-specific, uses xterm, terminal service snapshots, session-conversation mapping, `Terminal {index}` fallback titles, font/theme sync, bottom/right move helpers, and the root app-shell bottom panel defaults to a 280 px total shell. | Orchestrator has bottom/right terminal movement, shared tab strips, snapshots, theme/font sync, a pipe fallback when `node-pty` fails, a 280 px total default/reset bottom-panel target, focused keyboard-close restoration for both Hide terminal and final close-active-panel-tab flows under `terminalHideFocusRestored=true` and `terminalFinalCloseFocusRestored=true`, and composer handoffs for whole-output/latest-command output from both bottom and right-panel Terminal tabs under `terminalOutputAddToChat=true`, `terminalCommandOutputAddToChat=true`, `terminalRightPanelOutputAddToChat=true`, and `terminalRightPanelCommandOutputAddToChat=true`. Latest focused Terminal evidence: `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780068514160.json`. Exact Codex animation timing remains unverified. | Compare live Codex and Orchestrator terminal open/close animation timing when live terminal evidence is available. Keep `--terminal-visual` as screenshot evidence, not timing proof. |
| Review local git controls | Codex Review exposes local review actions, guarded destructive operations, and actionable provider review context. Local action results should be visible without requiring users to infer state changes from a refreshed diff. | Orchestrator has a Codex-style floating local-git action pill for Revert all, Stage all, and Unstage all in detailed Review. Revert all is gated behind a confirmation dialog, Copy git apply covers the loaded patch set, the floating pill exposes pending/success/failure action feedback, and provider inline comments now expose Copy/Apply suggestion actions for current unresolved GitHub suggestion fences. Latest focused `--diff-source` evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1780018233948.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-source-1780018233948.png`. | Keep provider checkpoint Undo and hosted/cloud rollback separate until a provider adapter can restore workspace state, not only thread history. Live PR creation and provider-hosted Git metadata still need real auth/provider proof. |
| Files / file source tabs | Codex file-source tabs expose Open file empty state, Open in editor, Copy path, word wrap, rich view, artifact preview, git blame controls, resettable tab state, and shared source find through the review runtime's `content-search-input`. | Orchestrator has Files and file tabs with shared `content-search-input` source find, previews, fallback notices, actions, selected lines, comments/blame placeholders, Codex-style artifact header title/type splitting for PDF/DOCX/IPYNB previews, Codex-style artifact Open/options controls, Codex-style Workbench file-source action labels, a compact Codex-style file-source options/open action cluster, dense source-tab overflow protection with `fileTabStripOverflow=true`, live file-source action feedback under `fileSourceActionStatus=true`, source-tab composer attachment proof under `fileSourceAddToChat=true`, selected-line composer context proof under `fileSourceLineAddToChat=true`, content-search hit open-to-line proof under `filesContentSearchOpenLine=true`, file/source tab reset proof under `workbenchFileTabReset=true`, Codex-like workbook sheet tabs, active-cell/formula-bar chrome with scoped local formula editing/recalculation for XLSX previews, and local XLSX style coverage for fills/fonts, merges, sizing, freeze panes, wrapping, alignment, and table ranges/styles/filter affordances. Full artifact renderer parity and provider-backed comments/blame remain incomplete. | Compare deeper artifact renderer controls and provider metadata paths; prioritize exact shared-find focus timing, provider/global indexed workspace search, provider-backed comments/blame, or concrete coding-use file-source gaps over more local file-tree styling. Keep deep Office/PDF renderer fidelity in Phase 2 unless it blocks coding workflows. |
| Settings host scope | Codex has many settings chunks for appearance, general, shortcuts, Browser Use, remote connections, personalization, plugins, skills, usage, worktrees, MCP, hooks, git, and local environments. | Orchestrator has shared settings primitives, route-owned settings, grouped app/host nav, section-change scroll reset for the shared Settings content host, a host-scoped Browser settings page with real in-app Browser data clearing, persisted Browser Use approval/history/download/upload/domain defaults, local Orchestrator Personalization preferences that prepend enabled custom instructions/coding preferences to new runs, explicit unavailable boundaries for remote host-scoped Personalization adapters, Worktrees Settings create/delete/open coverage with action/status semantics, and a provider `Boundaries` row that exposes existing runtime capability gaps from the provider registry. Focused Settings providers smoke gates provider boundaries with `settingsProviderBoundaries=true` and `settingsProviderContentAnchored=true`; focused Settings smoke gates Worktrees action/status semantics with `settingsWorktreesActionA11y=true`. Real remote-host adapters, Codex provider-native memory/personality/custom-instruction sync, provider-native worktree adapters, and provider-backed Browser Use adapters remain incomplete. | Add real host-scoped adapters only where provider data exists; keep explicit unavailable states for unsupported provider-native host pages and deepen Browser Use or Worktrees settings only when a provider or browser-use/worktree runtime exposes more policy/state. |
| Chat sidebar provider state | Codex sidebar chunks expose project groups, signals, thread keys, thread list signals, pinned metadata, provider/worktree metadata paths, and live screenshot evidence shows a footer collapse affordance beside Settings. | Orchestrator has provider thread-list projection, read-only provider pin boundaries, local pin order, provider-pinned metadata preservation during drag reorder, grouping, Codex-compact row density, text-first rows, and a smoke-gated footer collapse/expand affordance with a 48 px collapsed rail. Live provider pin set/list mutation is still externally blocked through the current app-server bridge. | Find a safe state boundary for provider pin set/list/order, or keep local/provider-projected pin behavior clearly scoped as Orchestrator-local display behavior. |
| Composer active-thread model settings | Phase 1 coding-use evidence identified an active-thread composer gap: provider/model state was visible but not inspectable or adjustable from the main composer once a thread existed. | Orchestrator now turns the existing-thread provider/model label into a compact `Thread settings` popover with current provider identity plus provider, model, Claude agent, and effort/thinking controls where supported. Focused `--composer` smoke gates `composerActiveThreadSettings=true`, `composerActiveThreadModelSwitchPersisted=true`, `composerActiveThreadProviderChoices=true`, `composerActiveThreadProviderSwitch=true`, `composerActiveThreadProviderSwitchPersisted=true`, and `composerActiveThreadProviderSwitchPolicyPersisted=true`; elevated `npm run live:codex-composer-model-switch` proves a persisted Codex model change is used for `thread/resume` and `turn/start` on the same provider session. Latest local evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779976107972.json`; live evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-model-switch-1779987672302.json`. | Continue composer work around context/permission workflow polish and non-command permission proof; do not broaden into speculative provider adapters. |
| Composer slash-menu dismissal | Phase 1 coding-use evidence identified slash commands as a daily composer workflow. Escape should dismiss the slash menu without destroying the typed draft, because dismissing a transient command palette is not the same action as clearing the message. | Orchestrator now tracks dismissed slash queries separately from composer draft text. Pressing Escape hides the current slash palette and preserves the textarea contents until the text changes, while selecting app-action slash commands can still intentionally clear the command text. Focused `--composer` smoke gates `composerSlashEscapePreservesDraft=true` with latest evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779977049179.json`. | Continue composer work around deeper context/permission workflow polish and live provider-backed send/model/permission behavior. |
| Composer send failure recovery | Phase 1 coding-use evidence identified the composer as the highest-risk daily workflow because drafts and attachments are user-authored state. A failed send transport should not erase that state before the app knows a provider run was accepted. | Orchestrator now snapshots the current draft and attachments before sending. If the send IPC throws or returns `false`, the composer restores both the draft and attachments, resizes the textarea, and shows an assertive `composer-run-action-status` error. Focused `--composer` smoke injects both failed paths and gates `composerSendFailureRestoresDraft=true` plus `composerSendFalseRestoresDraft=true` with latest evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779977976195.json`. | Continue with live provider-backed send/model/permission behavior and deeper context workflow proof. |
| Composer blocked send status | Phase 1 coding-use evidence identified a disabled-send clarity gap: unsupported runtime permission policies, temporary attachment-saving waits, and queued follow-ups should be visible in the composer, not only hidden behind a disabled send arrow/title or after-the-fact transcript row. | Orchestrator now renders a compact `composer-send-status` strip for unsupported permission policies, attachment-save waits, and queued follow-ups. Unsupported permission status includes a `Change` action that opens the matching permission menu; choosing a supported mode closes the menu and clears the block. The status strip is now a polite atomic live region, and the recovery action has an explicit `Change permission mode` accessible name. The closed composer permission trigger also shows a compact Static/Live/Fallback config-source badge and exposes the same source in its accessible name. Focused `--composer` smoke gates `composerSendStatusExplainsBlocked=true`, `composerSendStatusA11yLive=true`, `composerSendStatusActionOpensPermissions=true`, `composerSendStatusRecoveryClearsBlock=true`, and `composerPermissionContextSignal=true` with latest evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779954161525.json`; focused `--streaming-typing` smoke gates `composerWillQueueStatus=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-streaming-typing-1779937027194.json`. | Continue with deeper context/permission workflow polish and provider-backed permission switching proof when a real provider path is available. |
| Composer permission rules | Phase 1 coding-use evidence identified advanced permission/context rules as an edit path that should not silently save on blur. Allow/Deny/Tools/Dirs edits need explicit field labels and a result state because they directly shape tool and directory access for the next run. | Orchestrator now labels the advanced Claude permission-rule fields and shows a compact polite saved status after edits. Focused `--composer` smoke gates `composerPermissionRuleStatus=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779957287664.json`. | Continue with real provider-backed permission switching proof and broader context/permission workflow redesign only when a real provider path is available. |
| Composer attachment feedback | Phase 1 coding-use evidence identified composer attachments as an active workflow, not passive metadata: add/remove/save/cancel/failure transitions should be visible and announced, especially when files are inserted from Files/source tabs, pasted/dropped into the composer, or selected through the file picker. | Orchestrator now renders a compact `composer-attachment-status` live region and mirrors the latest text/tone on the composer shell. Focused `--composer` smoke gates `composerAttachmentStatus=true`, async pasted/dropped saves are isolated across chat switches under `composerAsyncAttachmentSwitchIsolation=true`, and delayed manual file-picker selections are isolated under `composerManualAttachmentSwitchIsolation=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1780000690946.json`. | Continue composer work around deeper context/permission workflow polish and provider-backed model/permission/retry proof. |
| Main-chat user message reuse | Phase 1 coding-use evidence identified a PP-052 gap: prior user prompts could be copied only manually, and Orchestrator had no message-level affordance to reuse/edit a prompt from transcript history. | Completed user messages now expose an `Edit message as draft` icon action. It replaces the active composer draft and attachments, focuses the composer, and announces the result without mutating existing transcript history. Focused transcript-layout smoke gates `chatUserMessageEditToDraft=true` and `chatUserMessageEditAttachments=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1780001499310.json`. | Remaining PP-052 parity is provider-backed edit/resend/fork behavior, original-history preservation UI, and provider fork/resume mapping where a real adapter supports it. |
| Main-chat message-level forks | PP-053 identified a missing branch-history surface: users need a way to fork from a specific transcript turn, not only fork an entire chat from the sidebar/header. | Completed user messages and the latest assistant response now expose `Fork from here`, and the provider-neutral fork path accepts `throughMessageId` so the new fork includes history only through the selected message, clears provider session id and provider-owned sidebar metadata, appends a lineage marker, persists fork-source metadata, surfaces the source in the sidebar row/hover detail, exposes local/same-worktree/new-worktree message-fork choices when available, preserves local pin placement for forks from locally pinned sources without leaking provider-readonly pin state, and makes titlebar/sidebar session-menu forks explicit latest-turn forks that exclude later control/status rows. Focused `--transcript-fork` smoke gates `chatSessionForkLatestTurnUi=true`, `chatSessionForkLatestTurnTruncatesControls=true`, `chatSessionForkLatestTurnMenuLabel=true`, `chatMessageForkButtonVisible=true`, `chatMessageForkMenuChoices=true`, `chatMessageForkSameWorktreeUi=true`, `chatMessageForkFromHere=true`, `chatMessageForkTruncatesLaterTurns=true`, `chatMessageForkClearsProviderSession=true`, `chatMessageForkClearsProviderSidebarMetadata=true`, `chatMessageForkPreservesLocalPinPlacement=true`, `chatMessageForkPersistsLineage=true`, `chatMessageForkSameWorktreeFromHere=true`, `chatMessageForkNewWorktreeFromHere=true`, `chatMessageForkSidebarLineage=true`, and `chatMessageForkHoverLineage=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-fork-1780004191107.json`. | Remaining fork parity is provider-native fork/resume mapping where available. |
| Main-chat route opening lifecycle | PP-033/PP-037 identified a page-open reliability gap: unknown thread routes should not leave the previous transcript visible while the app checks whether the target is archived or missing, and copied links should distinguish in-app thread routes from app-protocol deeplinks. | Unknown `/threads/{id}` and `#/threads/{id}` routes now render an `Opening chat` lifecycle notice immediately, then settle to archived restore or missing-chat recovery. Session action menus now include `Copy thread link` beside `Copy deeplink`; the copied thread link uses the same route helper as renderer navigation and is verified against the app clipboard. Focused `--session-switch` smoke gates `sessionRouteResolvingVisible=true`, `archivedRouteRecoveryVisible=true`, `archivedRouteRestoreWorks=true`, `missingRouteRecoveryVisible=true`, and `missingRouteReturnWorks=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-session-switch-1780019938197.json`; focused `--sidebar` smoke gates `actionCopyThreadLinkWorks=true`, `actionCopyDeeplinkWorks=true`, and `actionOpenInNewWindowWorks=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1780019865173.json`. | Provider-native remote/hotkey route semantics and exact live Codex route timing remain separate. |
| Composer queued follow-up cancellation | Phase 1 coding-use evidence identified active-run queue controls as a daily usability gap: follow-up messages queued during an active run need an obvious escape hatch, and stopped runs should not leave queued text behind as normal sent messages. | Orchestrator now exposes a compact `Cancel` action beside `Steer` on queued user messages, exposes `Cancel` on `Steering next` follow-ups as well, removes queued/steering follow-ups through a dedicated session command, broadcasts `messageRemoved` to renderer state, drops pending queued messages when an active run is stopped, and shows a compact composer queue summary during active streaming runs. Focused `--composer` smoke gates `composerQueuedCancel=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-composer-1779947552486.json`; focused `--streaming-typing` smoke gates `composerQueuedSummary=true` and `composerSteeringCancel=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-streaming-typing-1779947532377.json`. | Continue composer work around context/model/permission flow polish and real provider-backed retry/continue proof. |
| Side chat answer recovery, composer consistency, and context metadata | Phase 1 side-panel/composer consistency evidence identified side-chat answer failures as a dead-end state in the right Workbench panel, the side-chat composer was still a single-line input unlike the main coding composer, separate side-chat tabs did not show what thread/provider/model context they were attached to, and follow-up questions typed inside a side-chat panel did not carry the prior side-chat exchange into the provider prompt. | Orchestrator now renders a compact `Retry` action on failed side-chat and legacy side-question assistant answers. Retry reuses the preceding user question, keeps the same answer card, and reruns the side-answer request through the existing side-question provider path with messages before the failed answer. The side-chat composer is now a compact textarea with Enter-to-send and Shift+Enter/newline draft support. New side chats store a context snapshot when opened from composer `/btw`, slash `/btw`, or the Workbench New Tab, the panel header shows the source, main thread, provider/model, message count, and question preview, and follow-up sends include bounded prior side-chat messages before the main transcript. Focused `--side-chat` smoke now gates `sideChatErrorRetry=true`, `sideChatMultilineDraft=true`, `sideChatContextMetadata=true`, and `sideChatFollowupContext=true` with latest evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-side-chat-1779989645139.json`. | Continue side-chat work around provider-backed streaming proof, deeper fork-turn/provider metadata, and exact live Codex timing when live evidence is available. |
| Chat long-thread ergonomics | Phase 1 coding-use evidence identified long transcripts as a main-chat gap: users need clear history counts, bounded rendering, reliable older-history loading, and find jumps that land on the matching row. | Orchestrator now shows explicit visible/total long-history counts, distinguishes unloaded history loading from revealing already loaded history, keeps an all-loaded status for large fully present transcripts, forwards stable row `data-*` attributes for regression checks, and seeds the virtual scroller before shared-find result jumps. Focused `--transcript-stress` smoke gates `longThreadLoadControl=true`, `longThreadVisibleCountIncreased=true`, `lazyLoadedOlderChunk=true`, and `searchJumpFound=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-stress-1779935886533.json`. | Continue with exact live Codex long-thread focus timing only when live evidence is available; otherwise prioritize real provider-backed retry/continue/partial-continue proof and active-run workflow polish. |
| Chat user-input multi-question flow | Phase 1 main-chat evidence identified approval and user-input card ergonomics as a daily coding-use surface; the previous option UI submitted immediately, which made multi-question provider prompts hard to answer coherently. | Orchestrator now keeps single-question option prompts fast, but multi-question prompts collect per-question selections and optional details before sending one structured answer back through the existing user-input resume path. Questions marked `multiSelect` can toggle multiple selected options and include them in the structured answer. The main-process answer path now returns explicit success/failure, so missing provider resume state renders an inline recoverable error instead of a false sent state. The sent state is now a polite atomic status region, and resume failure is an assertive alert. Focused `--transcript-user-input` smoke gates `userInputMultiQuestionCard=true`, `userInputOptionSelection=true`, `userInputMultiSelect=true`, `userInputStructuredSubmit=true`, `userInputStructuredStatusLive=true`, `userInputResumeError=true`, and `userInputResumeErrorA11yLive=true` with latest evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-user-input-1779951732062.json`; adjacent `--transcript-layout` evidence `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1779939415007.json` keeps the broader transcript waiting-card gates green. | Continue with real provider-backed user-input proof and richer answer side-effect recovery when provider events expose those paths. |
| Chat partial response recovery | Phase 1 coding-use evidence identified partial streaming recovery as a main-chat gap: stopped or abandoned assistant streams should not look like final answers. | Orchestrator now preserves interrupted assistant stream state, renders a compact `Partial response stopped` transcript status, sends a partial-aware Continue prompt when the latest assistant response was interrupted, and has live Codex renderer proof that clicking Continue from that visible partial state resumes the same provider thread. Focused `--transcript-layout` smoke gates `partialResponseStatus=true` and `chatContinueLastTurn=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-layout-1779933783981.json`; elevated `npm run live:codex-composer-partial-continue` passed with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-transcript-live-partial-continue-1779987197970.json`. | Continue with richer continuation around tool boundaries and long-thread continuation ergonomics. |
| Agents selected timeline | Phase 1 inspector evidence identified selected-agent timelines as a remaining context-panel gap: global runtime events are useful, but a selected side task needs its own compact lifecycle trail. | Orchestrator now renders a `Timeline` section inside the selected agent tab, derived from matching agent lifecycle, agent text, and agent-tool events. Focused `--workbench-new-tab` smoke gates `agentSelectedTimeline=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1779934099616.json`. | Continue inspector work around richer provider-backed observability and live coding-session proof. |
| Agents failure cause groups | Phase 1 inspector evidence identified grouped failure causes as a runtime diagnostics gap: failure counts alone do not distinguish provider-run failures from failed tools. | Orchestrator now groups runtime failures by cause, shows per-cause counts and latest details, treats failed `tool.completed` events as danger-tone failures for counts, filters, and grouping, and exposes an `Add to chat` handoff for each group. Focused `--workbench-new-tab` smoke gates `agentRuntimeFailureGroups=true` and `agentRuntimeFailureGroupAddToChat=true` with latest evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780039280329.json`. | Continue inspector work around richer provider-backed observability and live coding-session proof. |
| Agents transport log summary | Phase 1 inspector evidence identified provider transport logs as a runtime diagnostics gap: raw provider output was stored but not inspectable from the session context panel. | Orchestrator now renders a compact `Transport log` section from the session raw buffer, labels JSON transport lines, extracts useful previews, redacts obvious secrets, and caps renderer raw transport state to the latest 20 KB. Focused `--workbench-new-tab` smoke gates `agentTransportLog=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1779934995630.json`. | Continue inspector work around richer provider-backed observability and live coding-session proof. |

## Smoke Harness Assessment

The current smoke setup is valuable but not sufficient for the parity goal.

What it does correctly:

- It launches isolated profiles, seeds deterministic fixtures, and gives repeatable screenshots without touching the user's active app state.
- The 27-capture visual/behavior inventory is a useful regression net for broad UI drift and shell transfer regressions.
- Focused flags such as `--diff-core`, `--browser`, `--terminal`, `--settings`, and `--sidebar` keep most failures localized.
- Recent timeout stabilization made the full inventory reliable enough to run after parity slices.
- Smoke infrastructure failures are now separated from product mismatches: startup failures without screenshots/check payloads become `needs-smoke`, while completed smoke assertions can still produce `mismatch`.

Where it has been holding progress back:

- Some checks can validate Orchestrator's approximation instead of Codex's behavior. `rightPanelFindShortcutRouting` was the clearest example; it has been rewritten to prove the shared thread find bar with chat/diff scope plus special Browser routing.
- The transcript-layout smoke now also asserts the shared `content-search-input`/scope controls instead of the old local `transcript-search` field, so the full inventory and focused right-panel check validate the same find contract.
- Broad smoke failures can look like product parity gaps when the real issue is harness timing or fixture setup.
- Screenshot inventory proves visual regression coverage, not provider-backed semantics.
- Synthetic browser-use and review metadata events prove reducer/UI behavior, not that live Codex/provider sessions emit the same data in the app. The `live:codex-browser-appserver` proof makes this explicit for Browser: the live app-server path works, the Codex bundle contains browser-use client route evidence, but no browser-use surface is exposed to this stdio client in the tested protocol.

Correct trigger model going forward:

1. Use focused smokes while developing a slice.
2. Run the full 26-capture visual inventory only as the final regression net for a stable slice.
3. Add small Codex contract checks only after verifying the behavior in the Codex bundle or live Codex UI.
4. Keep provider/live smokes separate from fixture smokes, and label fixture-only proof as fixture-only.
5. If a smoke assertion conflicts with Codex evidence, change or downgrade the assertion before building more UI around it.
6. For agent-driven Browser, do not treat manual Browser parity or synthetic manager-state smokes as provider parity. A runtime either needs a real dynamic client-tool bridge or an explicit unavailable status when a server requests a client tool.

## Immediate Plan

1. Re-run right-panel and Review focused smokes after each find/search change.
2. Use the full visual inventory only after the focused contract checks pass.
3. For Browser, separate manual Browser panel parity from agent-driven browser-use parity: the Browser panel should stay provider-neutral, while agent-driven browser-use needs a real dynamic tool bridge. Runtimes without that bridge should keep the current explicit unavailable status path.
4. Move to the next provider-backed Review gap only after live proof identifies an actual provider event/API. For checkpoint Undo, `thread/rollback` alone is insufficient because it rolls back history but not the working tree.
5. Keep newly aligned global/thread find under the comparison script so regressions fail directly.

Latest Phase 1 header/right-panel refresh: Orchestrator now labels the header control as `Toggle side panel`, marks the header action group as owning the `right-workbench` side panel, and opens the Workbench New tab launcher when the side panel has no tabs. Focused header smoke passed with `headerPanelEmptyFallback=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1779962518986.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1779962518986.png`.

Latest focused-validation refresh: `pnpm run smoke:ui:suggest` now maps changed files to focused UI smoke targets before running Electron. The Provider Settings runtime-copy slice used that helper to stay on `--settings-providers` only, and the focused smoke gates `settingsProviderRuntimeCopy=true` plus `settingsProviderRuntimeCopyStatusA11y=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780042434745.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780042434745.png`. Launcher-only Workbench edits now have an even narrower `--workbench-launcher` target, which gates Environment/Extensions launcher discovery and activation without rerunning the heavy Git/agent New tab suite; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780042931745.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780042931745.png`.

Latest bottom-panel alignment: the bottom shell now exposes panel-level labels for the header toggle, resize handle, tablist, and hide action while keeping terminal wording for terminal-tab actions. It also has the first non-terminal bottom tab: Plan can move from the right Workbench into the bottom panel, render there, suppress the duplicate right-panel Plan tab while bottom-owned, move back to the right panel from the shared bottom tab menu, and use `Add to chat` while bottom-hosted. Plan-only bottom tabs now stay closable when all terminal tabs are gone while the single-terminal close guard remains intact. Focused terminal smoke gates `terminalBottomPanelLabels=true`, `bottomPanelPlanTransfer=true`, and `bottomPanelPlanAddToChat=true` with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780049649508.json`; focused cross-panel keyboard smoke passed with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-cross-panel-keyboard-1780049675999.json`; focused header smoke passed with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-header-1780047657321.json`; focused Workbench launcher smoke passed with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-launcher-1780047655099.json`.

Latest smoke-cadence refresh: `pnpm run smoke:ui:changed` now executes the changed-files plan generated by `scripts/suggest-ui-smoke-targets.mjs --run`, and `--json` exposes the same plan in machine-readable form. For helper/package-only edits it ran static checks only (`git diff --check`, `pnpm exec tsc --noEmit`, and changed-script syntax), reported the package-level broad-smoke review boundary, and did not launch the no-flag UI smoke automatically.

Latest Plan/context refresh: the Plan Workbench tab now has a compact `Add to chat` handoff that inserts current goal, Review mode, and task context into the main composer with a polite status row. Focused Plan smoke gates this under `planAddToChat=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-plan-1780044146805.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-plan-1780044146805.png`.

Latest Review/root refresh: the Review source summary now includes compact workspace-root identity beside the active source/count/stats, with accessible and smoke-readable root metadata. Focused Review smoke gates this under `reviewSourceRootMetadata=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780044632172.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780044632172.png`. Focused design-system smoke also passed for the CSS touch with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-design-system-1780044652042.json`.

Latest Review line-handoff refresh: the selected-line action strip in Review now supports copying the current-line reference and adding the selected Review line directly to the main composer. Focused Review smoke gates this under `reviewDiffLineComposerHandoff=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780045509123.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780045509123.png`. Focused design-system smoke passed for the status-row CSS with evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-design-system-1780045530278.json`.

Latest Review-to-Git selected-file refresh: Review's `Open Git` handoff now carries the selected diff path into the dedicated Git Workbench tab, highlights the matching Git file row, and exposes the focused path in the Git Files header so users do not have to re-find the file after switching surfaces. The changed-files smoke selector now keeps this narrow Review/Git handoff to static checks plus `--diff-core` rather than launching unrelated Workbench launcher/New Tab/design-system smokes. Focused Review smoke passed with `reviewGitHandoffSelectedFile=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780070844527.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780070844527.png`.

Latest Git-to-Review selected-file refresh: Git file rows now expose `Open in Review`, carrying the row path into Review so the same file is selected and revealed after switching surfaces. The focused Review smoke now gates the bidirectional local Review/Git handoff with `reviewGitHandoffSelectedFile=true` and `gitReviewHandoffSelectedFile=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780071284684.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-diff-core-1780071284684.png`.

Latest Terminal selected-output refresh: bottom and right-panel Terminal tabs now track the current xterm selection and expose `Add selected terminal output to chat`, inserting a bounded fenced excerpt with cwd and terminal context into the main composer. The smoke planner keeps this selected-output handoff on the focused Terminal path instead of adding generic Composer/right-panel launcher runs. Focused Terminal smoke passed with `terminalSelectedOutputAddToChat=true` and `terminalRightPanelSelectedOutputAddToChat=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780071781353.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-terminal-1780071781353.png`.

Latest Agent Activity approval-focus refresh: permission and user-input event details now include a compact `Open approval in chat` / `Open question in chat` action that focuses the matching transcript approval/question card from the right Workbench inspector. The changed-files smoke planner keeps this Agent Activity handoff on `--workbench-new-tab` and suppresses redundant transcript/header runs. Focused Workbench smoke passed with `agentRuntimeEventOpenInChat=true` while preserving existing Agent Activity, Git, and New Tab gates; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780073019107.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780073019107.png`.

Latest Environment PR-creation refresh: Environment no longer leaves `Create pull request` disabled when provider PR metadata is missing. It now routes the row to the Git Workbench tab, where local PR-command generation already lives, while preserving the existing `View pull request` state when metadata exists. Focused Environment smoke passed with `environmentCreatePrOpensGit=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1780073891697.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1780073891697.png`.

Latest Environment commit-handoff refresh: Environment now routes `Commit` to the Git Workbench tab, where staging and commit controls live, while keeping `Changes` as the Review entry point. Focused Environment smoke passed with `environmentCommitOpensGit=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1780074239615.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-environment-1780074239615.png`.

Latest Files Add-to-chat status refresh: Files action-menu `Add to chat` now reports a visible/polite status from the Files panel after attaching the selected file to the composer. This keeps Files handoffs consistent with the copy/open status pattern and lets the focused smoke prove the originating surface, not just the resulting composer pill. Focused Files smoke passed with `filesAddToChatStatus=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780075650478.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-files-1780075650478.png`.

Latest Git file-row copy-path refresh: Git changed-file rows now include `Copy path for {path}` beside stage/unstage/discard/open-Review actions. The action uses the app clipboard bridge, announces status from the Git panel, and keeps path-copy parity with Files and Review row workflows. Focused Workbench New Tab smoke passed with `workbenchNewTabGitFileCopyPath=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780075978785.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780075978785.png`.

Latest Git file-row source handoff refresh: non-deleted Git changed-file rows now include `Open {path} in Workbench`, opening the full source file in the right-panel file tab from the Git surface. This gives Git rows the same direct source-view handoff already available from Files and Review rows. Focused Workbench New Tab smoke passed with `workbenchNewTabGitFileOpenWorkbench=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780076528446.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780076528446.png`.

Latest Git status handoff refresh: the Git summary header now includes `Add Git status to chat`, sending workspace, branch, change totals, staged/unstaged counts, optional PR URL, and a bounded changed-file summary to the main composer with Git-panel status feedback. Focused Workbench New Tab smoke passed with `workbenchNewTabGitStatusHandoff=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780076810592.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-workbench-new-tab-1780076810592.png`.

Latest Provider Settings runtime handoff refresh: Provider Settings runtime activity now has `Add to chat` next to `Copy`, inserting a bounded provider runtime activity block into the active chat draft with visible/polite Settings-card status. The changed-files smoke planner now keeps provider-only Settings source/harness diffs on `--settings-providers` instead of also running generic Settings smoke. Focused Provider Settings smoke passed with `settingsProviderRuntimeAddToChat=true` and `settingsProviderRuntimeAddToChatStatusA11y=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780077329853.json`, screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-settings-providers-1780077329853.png`.
