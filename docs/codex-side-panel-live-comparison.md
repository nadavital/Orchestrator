# Codex Side Panel Live Comparison

Last updated: 2026-05-27

This file is the current comparison ledger for the side-panel parity goal. It separates Codex-bundle evidence from Orchestrator fixture-smoke evidence so passing local smokes do not accidentally count as full Codex parity.

## Runnable Comparison

- Reuse the latest side-panel smoke manifest and compare it with the current Codex app bundle:
  `npm run compare:codex-side-panels`
- Regenerate the full side-panel visual inventory first, then compare:
  `npm run compare:codex-side-panels -- --run-smoke --full`
- Regenerate from the temp packaged app without replacing `/Applications/Orchestrator.app`:
  `npm run compare:codex-side-panels -- --run-smoke --full --packaged`
- Regenerate from `/Applications/Orchestrator.app` with an isolated profile:
  `npm run compare:codex-side-panels -- --run-smoke --full --installed`
- Generate the same report without failing the shell on known mismatches:
  `npm run compare:codex-side-panels -- --no-fail`
- The generated report is written to `tmp/codex-side-panel-comparison/comparison-report.md` and `tmp/codex-side-panel-comparison/comparison-report.json`.
- The same run also writes `tmp/codex-side-panel-comparison/header-panel-contact-sheet.html`, which groups the live Codex screenshot with Orchestrator's sidebar, focused header, transcript/header, Workbench right-panel, Files panel, Browser panel, Review panel, Terminal bottom-panel, and multi-window shell captures for direct header/panel relationship review.
- If the smoke runner cannot start the Electron/Vite server, for example `listen EPERM 127.0.0.1`, the comparison classifies affected rows as `needs-smoke` instead of `mismatch`. Without `--no-fail`, `needs-smoke` exits nonzero so automation can tell the report is incomplete. Treat that as harness evidence to rerun with the approved localhost-capable command path, not as product parity evidence.

Scope note: "side panel" in the comparison matrix means the right-side Workbench panel unless a row explicitly says Bottom panel, Left sidebar, or Settings.

## Evidence Used

- Codex app bundle: `/Applications/Codex.app/Contents/Resources/app.asar`, `CFBundleShortVersionString=26.519.41501`.
- Extracted Codex side-panel chunks under `tmp/codex-app-assets`.
- Current Orchestrator dev visual inventory: `tmp/side-panel-visual-inventory-current/manifest.json`, 27 captures, no failures, refreshed at `2026-05-27T08:29:09.678Z` with the Files capture updated from the focused Files smoke after the full wrapper hung in the unrelated `workbench-new-tab` capture.
- Current Orchestrator installed full visual inventory: `tmp/side-panel-visual-inventory-installed-full/manifest.json`, app mode `installed`, 27 captures, no failures, created at `2026-05-27T05:58:55.047Z`.
- Current runnable comparison: `tmp/codex-side-panel-comparison/comparison-report.json`, refreshed at `2026-05-27T08:30:40.550Z` with the current dev visual manifest, smoke app mode `dev`, status `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, `needsProof=0`. The Browser row now gates `browserClientToolActions=true`, `browserClientToolScreenshot=true`, `browserClientToolScreenshotImage=true`, and `browserClientToolAdvancedActions=true`, and the comparison artifact checks require live Codex app-server calls for `browser_screenshot` with `includeImage=true` plus `browser_fill`, `browser_key`, `browser_select`, `browser_check`, and `browser_scroll`. The Files row now gates `filesSpreadsheetFormulaEvaluation=true`, `filesSpreadsheetFormulaEditing=true`, `filesSlidesThumbnailRail=true`, `filesDocumentPdfZoomMenu=true`, `filesOfficeZoomMenu=true`, `filesSlidesSpeakerNotes=true`, and `filesPdfAnnotations=true` against Codex Popcorn/shared/PDF artifact bundle evidence. The top-level summary reports `optionalFileEvidenceFailures=2` for the black live Codex screenshot evidence used by the Header/panel and Workbench rows, so "zero mismatches" no longer hides missing pixel-proof evidence. It also reports `remainingParityGaps=18`, grouped as `live-codex-ui=7`, `provider-adapter=8`, `provider-proof=1`, `runtime-signal=1`, and `renderer-fidelity=1`, so fixture-covered rows are not confused with final pixel/provider/runtime parity. The latest installed proof passed at `2026-05-27T06:17:17.758Z`.
- Current packaged core side-panel inventory: `tmp/side-panel-visual-inventory-packaged-core/manifest.json`, app mode `packaged`, 8 captures, no failures, created at `2026-05-27T02:21:23.400Z`. Focused packaged sidebar evidence passes `hoverCardMaterial=true` after making the session hover-card material explicit on the portaled element; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779848313353.json`.
- Installed app side-panel replacement proof now exists: after `npm run pack:mac` and `npm run install:mac`, elevated `npm run compare:codex-side-panels -- --run-smoke --full --installed --no-fail --smoke-out tmp/side-panel-visual-inventory-installed-full` passed against `/Applications/Orchestrator.app` with 27 captures and no comparison failures. The latest installed Browser capture proves `browserClientToolScreenshot=true` and `browserClientToolAdvancedActions=true` for the Browser screenshot and advanced dynamic-tool slices; screenshot `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-installed-full/browser.png`. Focused installed Sidebar smoke evidence passed with `hoverCardMaterial=true`; JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779849160385.json`; screenshot `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-sidebar-1779849160385.png`. The installed Workbench right-panel capture proves `rightPanelPanelOpenCloseTelemetry=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-right-panel-1779852816510.json`; screenshot `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-installed-full/workbench-right-panel.png`. The installed multi-window capture proves `loadedDeepLinkDoesNotLeavePendingNavigation=true`; evidence JSON `/var/folders/bj/cxpn19xd78q4k1h9w4c_99700000gn/T/orchestrator-automated-ui-smoke-multi-window-focus-1779852980419.json`; screenshot `/Users/nadav/Desktop/Orchestrator/tmp/side-panel-visual-inventory-installed-full/multi-window-focus.png`.
- Current dynamic client-tool transport proof: `tmp/codex-dynamic-tools-live-proof/result.json`, status `ok=true`; the live Codex app-server accepted advertised `dynamicTools`, sent `item/tool/call` for `orchestrator.browser_bridge_status`, received Orchestrator's response, and completed with `CODEX_BROWSER_LIVE_OK`.
- Current real Browser dynamic-tool proof: `tmp/codex-browser-tools-live-proof/result.json`, status `ok=true`, refreshed at `2026-05-27T06:45:10.225Z`; the live Codex app-server accepted the production Browser tool specs, sent `item/tool/call` for `orchestrator.browser_open`, `orchestrator.browser_read`, `orchestrator.browser_click`, `orchestrator.browser_type`, `orchestrator.browser_screenshot` with `includeImage=true`, `orchestrator.browser_fill`, `orchestrator.browser_key`, `orchestrator.browser_select`, `orchestrator.browser_check`, `orchestrator.browser_scroll`, then a final `orchestrator.browser_read`, and completed with `CODEX_BROWSER_LIVE_OK`.
- Current comparison artifact gating: the Browser row now parses those live proof JSON files directly, including the `serverRequests[].paramsPreview` tool calls, so missing or stale live proof artifacts show up in the comparison report.
- Current Browser renderer bridge state: Orchestrator advertises `orchestrator.browser_open`, `orchestrator.browser_read`, `orchestrator.browser_click`, `orchestrator.browser_type`, `orchestrator.browser_screenshot`, `orchestrator.browser_fill`, `orchestrator.browser_key`, `orchestrator.browser_select`, `orchestrator.browser_check`, and `orchestrator.browser_scroll` from the Codex app-server runtime. It routes supported `item/tool/call` requests through main-to-renderer IPC, opens the Browser panel, returns URL/title/visible-structure plus target metadata from the actual Browser webview, executes click/type/fill/key/select/check/scroll actions against visible page targets or the page viewport, and captures the current webview as a saved PNG artifact with screenshot metadata plus an opt-in inline `data:image/png;base64` payload for `browser_screenshot` callers that set `includeImage=true`. Focused Browser smoke gates this loopback with `browserClientToolBridge=true`, `browserClientToolActions=true`, `browserClientToolScreenshot=true`, `browserClientToolScreenshotImage=true`, and `browserClientToolAdvancedActions=true`; native browser-use event streaming is still pending.
- Current pushed branch: `codex-side-panel-parity-stabilization`.

## 2026-05-26 Visual Pass

Direct live Codex UI inspection through Computer Use is currently blocked by the app safety boundary: `com.openai.codex` is not inspectable from this environment. Chronicle is running but has no fresh screen-recording frame files in the current `$TMPDIR/chronicle/screen_recording` folder, so it cannot supply a current Codex screenshot either. A macOS `screencapture` route has worked in earlier passes, but the current attempts produce an all-black PNG. The comparison now has `--capture-live-codex`, which discovers visible Codex windows through CoreGraphics, tries `screencapture` against the window id, tries the discovered window bounds as a region, then falls back to full-screen capture before validating PNG nonblank signal. Latest elevated capture diagnostics found one visible Codex window (`id=39726`, bounds `0,30,1384,1320`), but window capture failed with `could not create image from window`, region capture failed with `could not create image from rect`, and the full-screen fallback exited `0` while still reporting `nonBlank=false`, `nonBlack=0`, `lumaStdDev=0`, and `colors=1`. The comparison JSON reports the optional live screenshot file aggregate as `passed=false` on the affected rows and now surfaces those failures in `summary.optionalFileEvidenceFailures` while keeping smoke-backed rows green. This pass still does not claim full pixel-level side-by-side parity with the running Codex app. It combines the live installed Codex bundle/version, live screenshot evidence only when the PNG is nonblank, the runnable comparison script, and the current Orchestrator 27-capture screenshot/behavior inventory.

Earlier live screenshot evidence from 2026-05-26 showed Codex's left sidebar top utility rows as `New chat`, `Search`, `Plugins`, and `Automations`, followed by Pinned, Projects, Chats, and a Settings footer. It also showed the Review right panel using compact Review tabs, a `Last turn` source row, file path header, and compact diff rows. Do not reuse the current `/private/tmp/codex-current-screen.png` as visual proof unless the comparison reports nonblank image metrics for it.

Reviewed Orchestrator screenshots:

- `tmp/side-panel-visual-inventory-current/header.png`
- `tmp/side-panel-visual-inventory-current/workbench-right-panel.png`
- `tmp/side-panel-visual-inventory-current/chat-sidebar.png`
- `tmp/side-panel-visual-inventory-current/review-entry.png`
- `tmp/side-panel-visual-inventory-current/review-last-turn.png`
- `tmp/side-panel-visual-inventory-current/review-source.png`
- `tmp/side-panel-visual-inventory-current/browser.png`
- `tmp/side-panel-visual-inventory-current/terminal-bottom-panel.png`
- `tmp/side-panel-visual-inventory-current/terminal-behavior.png`
- `tmp/side-panel-visual-inventory-current/settings.png`
- `tmp/side-panel-visual-inventory-current/multi-window-focus.png`

Result: the local 27-capture inventory looks coherent against the current shared shell primitives, and the current comparison reports `fixture-covered=8`, `aligned=2`, `mismatch=0`, `blocked=0`, `needsSmoke=0`, and `needsProof=0`. The latest full visual-inventory wrapper still writes separate `terminal-behavior` and `multi-window-focus` captures so transfer semantics, window-scoped pending navigation, loaded-window deep-link handoff, and app-owned route/window/menu-command routing can be compared without overloading the stable visual panel screenshots, but the most recent wrapper attempt hung in `workbench-new-tab`; the Files capture was therefore refreshed from the focused Files smoke. The Settings capture now proves the host-scoped Browser page with `settingsBrowserPage=true`, `settingsBrowserSurface=true`, `settingsBrowserModule=true`, and `settingsBrowserPolicyPersistence=true`. The Browser capture now proves persisted Browser Use defaults reach the right-side Browser Security pane with `browserPersistedPolicyDefaults=true`, and the Browser dynamic-client-tool path now gates `browserClientToolAdvancedActions=true` for fill/key/select/check/scroll in addition to open/read/click/type/screenshot. The Files capture now proves Codex-style artifact Open/options controls, Workbench file-source action labels, compact options/open action cluster, shared `content-search-input` source find, workbook-style XLSX sheet tabs, active-cell/formula-bar chrome with scoped formula editing/recalculation, shared PDF/DOCX and XLSX/PPTX preset/fit zoom menus, local PDF annotation-mode comments, and PPTX speaker notes. The real remaining parity risk is not obvious one-off padding or color drift in the current Orchestrator screenshots; it is the set of areas that still need either live Codex screenshot/timing evidence, provider-backed data that fixtures cannot create, or header/panel relationship checks where individually passing panels could still feel unlike Codex.

Highest-value next UI/parity work:

1. Header and panel interaction: keep sidebar, main header/titlebar, Workbench right panel, Terminal bottom panel, and app-owned window/menu command routing attached to the same shell contract. Treat this as an active user-observed parity risk: Orchestrator can look unlike Codex when each panel passes independently but the header/panel relationship is inconsistent. Header actions should read as compact toolbar chrome in the same band as right-panel chrome, not as a heavier independent button cluster. Any future panel layout or route/window change should update or keep green the dedicated comparison row before it is treated as parity-safe, and any visible header/panel mismatch in the contact sheet should be promoted to the active parity list before more one-panel work continues.
2. Browser agent-driven parity: keep the real Browser dynamic-tool proof plus renderer loopback smoke green for `browser_open`, `browser_read`, `browser_click`, `browser_type`, `browser_screenshot(includeImage)`, `browser_fill`, `browser_key`, `browser_select`, `browser_check`, and `browser_scroll`. Manual Browser UI is strong, generic dynamic-tool transport works, those real tool requests are live-proven through the Codex app-server path and installed-smoke-proven through `/Applications/Orchestrator.app`, and server-initiated unimplemented tool calls still surface an explicit unavailable status. Continue only from native browser-use events, provider design-change application, or live pixel/timing evidence.
3. Review provider data parity: add one real provider-backed source at a time, especially provider blame or live commented-PR proof. Do not treat local fixture comments as final hosted/cloud parity.
4. Terminal visual parity: capture live Codex bottom-panel height/animation at multiple window sizes if a manual screenshot path becomes available; local Orchestrator terminal layout is smoke-covered but exact Codex timing is not.
5. Settings adapter parity: keep unsupported Host Personalization and remote-host pages explicitly unavailable until real provider adapters exist; deepen Browser Use settings only when a provider or browser-use runtime exposes more policy state than the current persisted approval/history/download/upload/domain defaults.
6. Review visual spacing: defer further Review padding/color work unless a live Codex screenshot or a new visual fixture demonstrates a specific mismatch.

Panel/header interaction remains a first-class contract, not an incidental visual note: any future sidebar, main titlebar, Review, Workbench right-panel, Files panel, Browser panel, Terminal bottom-panel, or route/window command-routing change must keep the dedicated `Header and panel interaction` comparison row green and inspect `tmp/codex-side-panel-comparison/header-panel-contact-sheet.html`. Treat a visible mismatch in how the panels meet or share the header band as an active parity gap even when each individual panel smoke passes. The comparison report also records the live Codex screenshot file backing this row, including presence, size, age, and nonblank PNG metrics, so stale, missing, or black live evidence is visible during future parity runs.

The generated comparison report now includes a `Remaining Parity Gaps` table and matching `summary.remainingParityGaps` JSON. Use that list to pick the next implementation slice: live screenshot/timing gaps should wait for usable Codex UI evidence, provider-adapter gaps should wait for real provider events/APIs, native Browser runtime gaps should wait for browser-use events or route/capture signals, and renderer-fidelity gaps should be scoped around concrete artifact renderer behavior rather than broad styling.

## Recently Aligned

| Surface | Codex evidence | Orchestrator state | Follow-up |
| --- | --- | --- | --- |
| Header and panel interaction contract | Live screenshot `/private/tmp/codex-current-screen.png` shows Codex treats sidebar, conversation header, right panel, and bottom panel as coordinated shell surfaces rather than independent stacked panes, with a title-first compact session header and compact top-right controls instead of a visible provenance/profile strip. Bundle evidence in `app-shell-state-HP0T5lEX.js` and `thread-page-bottom-panel-state-D1Lz0U4Y.js` confirms shared right/bottom shell state and terminal bottom-panel ownership. | The runnable comparison now has a dedicated `Header and panel interaction` row covering `sidebarTopInsetCodexLike=true`, `profileBadgeCompact=true`, `headerActionChromeCompact=true`, `sessionHeaderInPrimaryColumn=true`, `rightPanelHeaderSeam=true`, `headerPanelSharedBand=true`, `filesHeaderPanelSeam=true`, `browserHeaderPanelSeam=true`, `headerMetadataTooltipOnly=true`, `rightPanelMaterialSolid=true`, `terminalPanelMaterialSolid=true`, `terminalBottomPanelSizeDecomposition=true`, `terminalVisualHealthyContent=true`, `secondWindowCreated=true`, `secondWindowNavigated=true`, `pendingNavigationConsumedOnce=true`, `pendingNavigationWindowScoped=true`, `loadedDeepLinkDoesNotLeavePendingNavigation=true`, `firstWindowBrowserFocusArea=true`, `firstWindowBrowserMenuEnabled=true`, `secondWindowBrowserMenuDisabled=true`, `backgroundWindowMenuDoesNotClobberFocusedWindow=true`, `activeWindowAfterRefocus=true`, `focusSwitchRestoresFirstWindowMenu=true`, and `menuCommandRoutedToFocusedWindow=true` across `chat-sidebar`, `header`, `transcript-narrow`, `workbench-right-panel`, `files`, `browser`, `review-last-turn`, `review-core`, `terminal-bottom-panel`, and `multi-window-focus` captures. | Keep all future sidebar, main header, Files, Browser, Review, Workbench, Terminal shell, and route/window command-routing moves under this contract. Exact live Codex pixel spacing, OS focus behavior, and animation timing still need new live screenshot evidence. |
| App-shell panel material | Codex app-shell bundle uses shared app-shell panel state and main-surface panel backgrounds; live screenshot evidence shows the right panel as a solid shell surface rather than a translucent overlay over the transcript/header. | Orchestrator now marks shared right/bottom `AppShellPanel` surfaces as `data-app-shell-panel-material="solid"`, gives Workbench and Terminal shell/chrome surfaces solid `var(--surface-bg)` backgrounds, and smoke-gates computed background opacity with `rightPanelMaterialSolid=true` and `terminalPanelMaterialSolid=true`. | Keep panel material in the header/panel row so future overlay or animation changes do not reintroduce see-through side/bottom panels. |
| App-shell panel open/close telemetry | Codex `app-shell-tab-controller-B2eCi4Le.js` records `codex_thread_side_panel_opened`, `codex_thread_side_panel_closed`, and `codex_thread_side_panel_tab_viewed` with active-tab and route-kind metadata. | Orchestrator shared `AppShellPanel` now records `panel.opened` and `panel.closed` for right/bottom surfaces with `panelSurface`, `activeTab`, and `routeKind`; the Workbench comparison gates this with `rightPanelPanelOpenCloseTelemetry=true`. | This narrows the product-level panel lifecycle gap. Broader route/window lifecycle, exact live timing, and Codex pixel spacing remain open. |
| Header and panel interaction | Live screenshot `/private/tmp/codex-current-screen.png` shows the Codex right-panel/review shell chrome starts in the same compact top band as the conversation header/titlebar rather than below it. | Orchestrator now renders the session titlebar inside the primary chat column while the Workbench right panel remains a sibling in the same session row. Focused right-panel smoke gates `rightPanelHeaderSeam=true`, proving the titlebar, primary content, right panel shell, right panel surface, and right-panel chrome share the same top seam and that the titlebar right edge meets the right-panel left edge. Terminal visual smoke confirms the bottom panel still attaches across the content area after the layout change. | Keep future work to live Codex pixel/timing comparison or a new screenshot-proven mismatch; the structural header/right-panel seam is now smoke-covered. |
| Workbench tab overflow and close chrome | Codex bundle evidence in `app-shell-tab-controller-B2eCi4Le.js` shows right-panel tabs are owned by the shared app-shell tab controller, with active-tab scrolling instead of collapsing tab affordances into the header band. The root app-shell tab renderer in `app-shell-Bh-lgoQk.js` renders closable tab controls as hidden start-edge close affordances (`start-0`) with `codex.tabs.closeNamed` labels. | Orchestrator now gives Workbench tabs stable compact minimum widths, keeps the active tab wider/readable, gates the strip with `workbenchPanelTabOverflowController=true`, and places the shared tab close affordance on the start edge with `workbenchPanelTabCloseStartEdge=true`. | Keep exact active/overflow spacing open for live Codex screenshots, but do not allow the local tab row to collapse labels or drift back to right-edge close buttons. |
| Workbench New tab add affordance | Live Codex screenshot evidence shows the right-panel tab strip exposes a single add affordance adjacent to the tabs, not a duplicated add control while the add/new-tab surface itself is active. | Orchestrator now hides the trailing Workbench add button when `New tab` is the active tab, leaving the active `+ New tab` tab as the single add affordance. Focused `--workbench-new-tab` smoke gates this with `workbenchNewTabSingleAddAffordance=true`, and the comparison includes that check in the Right-side Workbench shell row. | Keep the normal trailing add button visible for non-new-tab Workbench states, but avoid duplicated add affordances in the active New tab chrome. |
| Review transcript-card open state | Live screenshot `/private/tmp/codex-current-screen.png` shows a transcript `Edited ... files` Review card opening the side-panel Review state with `Last turn` selected, source-level `+/-` stats, path-first file headers, compact diff rows, and no persistent changed-files rail visible. Codex bundle evidence still shows a changed-files side pane exists behind a show/hide files control. | Orchestrator now treats the transcript card source as authoritative: Last turn cards persist/open Review with the `last-turn` source selected, source-level stats in the Review toolbar, a path-first selected-file header with the decorative change glyph hidden, and the changed-files pane hidden, while normal local Review keeps the existing files rail behavior. Focused `--diff-last-turn` smoke gates `reviewTranscriptCardLastTurn=true`, `reviewFileHeaderPathFirst=true`, and `reviewLastTurnVisualState=true`, and the visual inventory now writes `review-last-turn.png` for direct Codex comparison. | Keep the changed-files rail for explicit file navigation/search; card-opened Last turn Review should stay single-column unless the user explicitly shows files. |
| Chat sidebar top inset | Live screenshot `/private/tmp/codex-current-screen.png` shows Codex's first sidebar utility row closer to the window/header band than Orchestrator's previous 64 px spacer allowed. | Orchestrator reduced the left sidebar drag spacer to 48 px, exposes it as `sidebar-window-drag-spacer`, and focused sidebar smoke now gates `sidebarTopInsetCodexLike=true` while preserving 240 px sidebar width, compact rows, and section-order checks. | Keep exact row spacing/pixel comparison open only if a new live screenshot shows measurable drift. |
| Empty project New chat row | Live screenshot evidence and Codex sidebar copy use sentence-case `New chat` for the primary new-chat action; Codex-style sidebar rows should not drift to title-case variants. | Orchestrator now uses `New chat` for the empty-project compact row as well as the primary sidebar action, and focused Sidebar smoke gates that row copy through `emptyProjectNewChatCompact=true`. | Keep sidebar row copy sentence-case with Codex unless a provider-specific label is intentionally different. |
| Terminal bottom-panel default size and transfer | Codex root app-shell bundle `tmp/codex-app-assets/app-shell-Bh-lgoQk.js` stores `app-shell:bottom-panel-height` with a 280 px fallback, clamps through `Tn`, and passes `defaultSize: 280` to the top-edge resize handle. The Terminal chunk keeps Terminal content inside that shared bottom-panel shell, and `app-shell-tab-controller-B2eCi4Le.js` exposes shared `moveTabTo` / `receiveMovedTab` controller semantics. | Orchestrator now opens/resets Terminal to 230 px content plus 50 px chrome for a 280 px total bottom panel, migrates legacy 260/350 px stored defaults to the current target, and uses generic `Move tab to ... panel` transfer copy in both directions. The focused Terminal smoke and visual comparison capture both gate that total through `terminalBottomPanelSizeDecomposition=true`; the visual smoke captures healthy pipe-backed terminal content at the new size with `terminalPanelMaterialSolid=true`; and the comparison now includes `terminalSharedTransferModel=true`, `terminalMoveToRightPanel=true`, and `terminalMoveBackToBottom=true` from the separate `terminal-behavior` capture. | Live Codex animation timing remains unverified; keep future work to timing/route-shell ownership unless a new screenshot shows a concrete visual mismatch. |
| Chat sidebar top-level section structure | Live screenshot `/private/tmp/codex-current-screen.png` shows Pinned, Projects, then Chats as top-level sidebar sections after the utility rows. | Orchestrator now renders projectless Chats as a top-level sibling section next to Projects instead of nesting it inside the project group list. The existing "Chats before projects" preference still works, while default screenshot capture is reset to the Codex-like Projects then Chats order for directly comparable visual inventory evidence. | Keep custom-section and user preference behavior covered by focused sidebar smoke, but keep visual inventory captures in default Codex-like order. |
| Chat sidebar provider pin boundary | Codex bundle evidence exposes provider/sidebar pinned thread-key signals, but current live app-server probing does not expose safe provider pin set/list mutation. | Orchestrator keeps provider-projected pinned rows in the Pinned section with provider metadata, but the row pin button is explicitly `provider-readonly` and disabled unless a local pin exists. The focused Sidebar smoke now gates `sidebarProviderPinBoundary=true`, separating local pin-order behavior from provider-backed mutation parity. | Add real provider pin set/list adapters only when a provider exposes a safe mutation API; keep local pin order clearly scoped until then. |
| Notebook artifact read-only controls | Codex `notebook-preview-panel-CAO-aRhM.js` renders notebook previews with `Read only`, disabled `Run all`, and disabled `Restart kernel` controls. | Orchestrator notebook file-tab previews now expose the same read-only execution boundary in the artifact header, while preserving Copy path, View source, Copy raw preview, Open file, and Reveal file actions. Focused Files smoke gates `filesNotebookReadOnlyControls=true`. | Continue with deeper notebook output/cell rendering only if artifact parity is the chosen Files slice; this does not prove provider-backed artifact metadata. |
| Notebook artifact output rendering | Codex `notebook-preview-panel-CAO-aRhM.js` parses and renders notebook output types including streams/text, errors, JSON, HTML/Markdown, and embedded images, hides summarized raw payloads behind `Raw output`, and prefers `text/plain` before JSON when both MIME payloads exist. | Orchestrator now preserves notebook cell outputs, renders stream/text, error, JSON, HTML/Markdown, and embedded image outputs under each cell, uses raw-output disclosures for summarized raw payloads, and follows the same text/plain-before-JSON precedence. Focused Files smoke gates `filesNotebookOutputRendering=true`, `filesNotebookOutputSummaries=true`, and `filesNotebookRawOutputDisclosure=true`. | Continue with exact output sizing/polish and provider-backed artifact metadata in a future Files slice. |
| Notebook artifact cell disclosure | Codex `notebook-preview-panel-CAO-aRhM.js` renders notebook cells as open `details`/`summary` disclosure sections inside the read-only preview. | Orchestrator now renders each notebook cell as an open `details` element with a summary header, chevron disclosure marker, body wrapper, source block, and preserved output rendering. Focused Files smoke gates `filesNotebookCellDisclosure=true`. | Continue with richer output styling and provider-backed artifact metadata. |
| Notebook artifact execution count | Codex `notebook-preview-panel-CAO-aRhM.js` preserves code-cell `execution_count`, renders `Cell {cellNumber} of {totalCellCount}`, shows `Run {executionCount}` for executed code cells, and exposes a disabled per-cell run affordance in read-only previews. | Orchestrator now preserves notebook `execution_count`, renders cell position labels, shows code-cell `Run N` labels, and adds a disabled per-cell run control while keeping the notebook preview read-only. Focused Files smoke gates `filesNotebookExecutionCount=true` against a code cell with `execution_count: 7`. | Continue with output polish and provider-backed artifact metadata; do not add runnable notebook execution until a real kernel/runtime path exists. |
| Notebook artifact cell metadata | Codex `notebook-preview-panel-CAO-aRhM.js` reads cell metadata from direct and nested Codex metadata keys, including `title`/`cellTitle` and `descriptionMarkdown` variants, uses metadata titles in cell headers, and renders described code cells with description markdown plus a nested Code disclosure containing a titled `Python` code snippet. | Orchestrator now reads direct and nested Codex notebook cell metadata, uses metadata titles in notebook cell headers, renders code-cell description markdown, and keeps the code source behind a nested read-only Code disclosure with a titled Python code shell when a description exists. Focused Files smoke gates `filesNotebookCellMetadata=true` and `filesNotebookCodeSnippet=true`. | Continue with output polish and provider-backed artifact metadata. |
| Notebook artifact cell spacing and width | Codex `notebook-preview-panel-CAO-aRhM.js` wraps notebook cells in `px-4 py-4 sm:px-6 sm:py-5`, centers the cell column with `mx-auto`, caps it at `max-w-3xl`, uses `gap-4`, and renders `rounded-lg` cells with `px-4 py-2` headers. | Orchestrator notebook previews now use a centered `notebook-preview-list-inner` column with 768 px max width, 16 px cell gap, 16 px mobile padding, 20 px/24 px wider padding, 8 px cell radius, and 8 px/16 px summary headers. Focused Files smoke gates `filesNotebookCellSpacing=true`. | Continue with deeper output renderer polish and provider-backed artifact metadata. |
| Notebook artifact output/source chrome | Codex `notebook-preview-panel-CAO-aRhM.js` renders cell source bodies with `px-4 py-3` and renders code-cell outputs in a distinct `border-t` secondary-tinted band with `px-4 py-3` and `gap-3`. | Orchestrator notebook previews now wrap source content in a Codex-like padded source body and render outputs in a separate bordered, lightly tinted output band with 16 px horizontal padding, 12 px vertical padding, 12 px output gap, and an inset top highlight. Focused Files smoke gates `filesNotebookOutputChrome=true`. | Continue with deeper per-output item styling and provider-backed artifact metadata. |
| Notebook artifact per-output item chrome | Codex `notebook-preview-panel-CAO-aRhM.js` renders stream/text raw output containers as rounded `bg-token-main-surface-primary/40 p-3` blocks, error output containers as `border-token-charts-red/30 bg-token-charts-red/5 p-3` blocks, and image/markdown outputs as contained item surfaces. | Orchestrator notebook stream/text output items now use Codex-like rounded, lightly tinted item containers with 12 px padding and raw-output pre blocks with 12 px padding. A separate `notebook-output-types-smoke.ipynb` fixture now proves markdown, error, and image item chrome through `filesNotebookRichOutputItemChrome=true`, while the original stream/text fixture remains stable through `filesNotebookOutputItemChrome=true`. | Provider-backed artifact metadata and full artifact renderer fidelity remain open. |
| Notebook artifact output summaries | Codex `notebook-preview-panel-CAO-aRhM.js` reads `outputSummaries[index].summaryMarkdown` from direct and nested Codex cell metadata, attaches it to stream/error/text/JSON outputs, and renders the summary as markdown before a raw-output disclosure. | Orchestrator now reads notebook output summary metadata, preserves it on rendered outputs, shows summary markdown above stream/text/JSON/error output content, and hides summarized raw payloads behind `Raw output`. Focused Files smoke gates `filesNotebookOutputSummaries=true` and `filesNotebookRawOutputDisclosure=true`. | Continue with exact Codex output sizing/polish and provider-backed artifact metadata. |
| Artifact preview title/type headers | Codex `docx-preview-panel-BjyRJuYA.js` renders DOCX artifacts through the shared artifact preview header with `artifactType: DOC` and a title stripped of `.docx`; `pdf-preview-panel-CkrOHSbs.js` does the same for `PDF` and `.pdf`; `notebook-preview-panel-CAO-aRhM.js` renders `IPYNB` plus cell count in the artifact type text. | Orchestrator now uses a shared artifact-style header for PDF, DOCX, and notebook previews. The visible title is extensionless and the type label is separate (`PDF`, `DOC`, `IPYNB · 3 cells` in the smoke fixture). Focused Files smoke gates `filesArtifactHeaderTitleType=true`, and the runnable comparison includes the Codex DOC/PDF/notebook bundle evidence. | Continue with true full-fidelity artifact renderer/provider controls and exact output spacing; this slice only aligns visible artifact header identity. |
| Artifact preview Open/options controls | Codex `artifact-preview-status-D5kFRFQE.js` renders artifact header actions around a primary Open affordance with an adjacent options menu that includes reveal/open-in-folder behavior, instead of exposing Open and Reveal as equal loose toolbar buttons. | Orchestrator now groups artifact preview open behavior behind a primary `Open` toolbar button plus adjacent options chevron/menu across JSON, CSV, PDF, DOCX, XLSX, PPTX, and IPYNB previews. Focused Files smoke gates `filesArtifactOpenOptions=true` and proves the menu exposes both `Open` and `Open in folder`; the full visual inventory includes the same check in the Files capture. | Full remote download/open-provider options and provider-backed artifact metadata remain open. Keep future artifact action work tied to real provider capabilities instead of adding unsupported menu items. |
| Workbench file-source action labels and cluster | Codex `review-file-source-tab-CjS7Xe_W.js` exposes `File viewer options`, `Open in editor`, `Copy path`, `Enable/Disable word wrap`, `Enable/Disable rich view`, and `Show/Hide git blame`; its breadcrumb/action cluster renders the options menu plus the `Open in editor` affordance. | Orchestrator file tabs now use `File viewer options` on the ellipsis button, `Open in editor` for file-open actions, `Open selected line in editor` for line actions, and `Enable/Disable word wrap` in the shared options menu. The visible file-tab action cluster is now the Codex-style `File viewer options` plus `Open in editor` pair, while source/rich, wrap, blame, selection, copy, reveal, and add-to-chat actions stay available through the menu or selected-line affordances. Focused Files smoke gates `workbenchFileTabCodexActionLabels=true` and `workbenchFileTabCodexActionCluster=true`; the full visual inventory includes both checks in the Files capture. | Remaining source-tab gaps are source-search/global-find structure, provider-backed comments/blame, and broader indexed workspace search. |
| Workbench file-source shared find | Codex `review-file-source-tab-CjS7Xe_W.js` registers file-source search through `sourceSearchSource`, while `review-runtime-bridge-CZUIqW4U.js` owns the shared `content-search-input` with `Search chat` and `Search diffs` scope controls. | Orchestrator file-source tabs no longer expose an inline `workbench-file-tab-source-search` field. Focused file tabs route `Cmd+F` into the shared `content-search-input` in diff scope, and shared query/next/close events update source line highlighting and active match metadata. Focused and full Files smoke now gate this with `fileSourceSearch=true`. | Keep exact focus timing and global/provider-indexed workspace search open; the visible inline-search mismatch is closed. |
| PDF page/zoom/invert/presentation/annotation controls | Codex `pdf-preview-panel-CkrOHSbs.js` and `artifact-preview-status-D5kFRFQE.js` render page navigation, shared zoom percent/fit controls, invert-colors controls, `artifactPdfPreview.annotate`, `artifactPdfPreview.commentInput`, a `commentLayer`, local PDF context, and `artifact-pdf-presentation` mode with an `Exit` control. | Orchestrator now parses local PDF page count, renders previous/next controls plus a `current/total` indicator in the artifact header center slot, renders a Codex-style preset/fit zoom menu, exposes an invert-colors control that applies a real iframe filter, updates the embedded PDF URL fragment including `page-fit`, opens a Codex-named PDF presentation surface with keyboard/click page navigation plus Exit, and exposes a local annotation-mode side layer with page-scoped comment input/list/counts. Focused Files smoke gates `filesPdfPreviewControls=true`, `filesDocumentPdfZoomMenu=true`, `filesPdfPresentationMode=true`, and `filesPdfAnnotations=true` against a two-page fixture, and the full comparison row includes the Codex PDF annotation and presentation bundle terms. | PDF canvas rendering, download handling, provider-backed annotation metadata, persisted/provider comments, and provider-backed PDF metadata remain open. |
| DOCX page/zoom controls | Codex `docx-preview-panel-BjyRJuYA.js` uses a dedicated `docx-preview-panel` / `codex-docx-preview` surface, exposes `docx-preview-zoom-trigger`, and shares artifact page/zoom primitives including zoom percent/fit state. | Orchestrator DOCX previews now expose previous/next controls plus a `current/total` indicator in the artifact header center slot, render a Codex-style preset/fit zoom menu, and show a document page shell. Focused Files smoke gates `filesDocumentPageControls=true` and `filesDocumentPdfZoomMenu=true` through a two-page DOCX fixture, and the comparison row includes the Codex DOCX preview/zoom bundle terms. | Full DOCX renderer fidelity, true layout pagination, comments, download/provider metadata, and provider-backed artifact annotations remain open. |
| Spreadsheet/slides artifact controls | Codex `artifact-tab-file-kind-fsu6JKhI.js` maps `.xlsx`/`.xlsm` to spreadsheet artifacts and `.pptx` to slides artifacts; `artifact-tab-content.electron-DayvYBGS.js` routes them through `ExtractXlsxProto` / `PopcornElectronWorkbookPanel` and `ExtractSlidesProto` / `PopcornElectronPresentationPanel` with `renderHeaderZoomControl`. The shared artifact status bundle also exposes `artifactTab.preview.zoomPercent` and `artifactTab.preview.zoomToFit`. The Popcorn workbook bundle exposes `popcorn-pages-bar`, `popcorn-sheet-tab-*`, `popcorn-add-sheet`, `popcorn-zoom-select`, `popcorn-formula-bar`, `popcorn-selection`, and recalculation/formula behavior; the Popcorn presentation bundle exposes `popcorn-presentation-page-navigation`, `popcorn-presentation-zoom-select`, `popcorn-presentation-thumbnails`, `popcorn-presentation-codex-thumbnail-rail`, `popcorn-presentation-codex-thumbnail-stack`, `popcorn-presentation-add-slide`, `popcorn-presentation-notes-panel`, and `popcorn-presentation-notes`. | Orchestrator now classifies XLSX/XLSM and PPTX as artifact previews instead of generic binary fallback, with extensionless titles, `XLSX` / `PPTX` type labels, compact Copy path/Open/Reveal actions, no unsupported source toggle, and lightweight OOXML rendering for local files. XLSX previews now render one active sheet at a time with header sheet navigation, Codex-like preset/fit zoom controls, Codex-like sheet tabs plus a disabled add-sheet affordance, row/column headers, selectable cells, scoped formula extraction/evaluation, and a formula bar that displays formula text while cells display computed values; formula-bar edits update local preview state and recalculate same-sheet formulas. PPTX previews now render one active slide surface with slide navigation, Codex-like preset/fit zoom controls, a Codex-style left thumbnail rail, disabled read-only add-slide affordance, responsive thumbnail-stack styling, outline cards, and a read-only speaker-notes panel when slide notes exist. Focused Files smoke gates `filesSpreadsheetSlidesArtifactBoundary=true`, `filesSpreadsheetRenderer=true`, `filesSlidesRenderer=true`, `filesSpreadsheetControls=true`, `filesSpreadsheetSheetTabs=true`, `filesSpreadsheetActiveCell=true`, `filesSpreadsheetFormulaEvaluation=true`, `filesSpreadsheetFormulaEditing=true`, `filesOfficeZoomMenu=true`, `filesSlidesControls=true`, `filesSlidesThumbnailRail=true`, and `filesSlidesSpeakerNotes=true`. | This is a lightweight local renderer/control layer, not full Codex Popcorn parity. Persisted workbook edits, comments, annotations, slide canvas fidelity, editing hooks, true Popcorn zoom/canvas fitting, and provider-backed artifact metadata remain open. |
| Global find / Review search | `review-runtime-bridge-CZUIqW4U.js` registers `find-in-thread`, renders `content-search-input`, and provides `Search chat` / `Search diffs` scope buttons. If Browser is the active right-panel tab and the browser webview/right-panel is focused, the command dispatches `browser-sidebar-command` `{ type: "open-find" }`. | Orchestrator now opens a shared floating `content-search-input` for transcript/Review find, exposes `Search chat` and `Search diffs` scopes, routes the diff scope into Review content search/match stepping, keeps Files on workspace file search, and preserves Browser-focused `open-find` routing. The focused right-panel smoke treats `rightPanelFindShortcutRouting=true` as this Codex-style contract, not the older per-panel Review search focus. | Keep live Codex side-by-side comparison for spacing, animation, and focus timing. Keep the smoke contract focused on behavior. |

## Verified Mismatches

| Surface | Codex evidence | Orchestrator state | Next action |
| --- | --- | --- | --- |
| Chat sidebar top actions, width, inset, and section order | Live screenshot `/private/tmp/codex-current-screen.png` shows the Codex sidebar starts with `New chat`, `Search`, `Plugins`, and `Automations` above top-level Pinned/Projects/Chats, measures roughly 240 px wide, and places those utility rows close to the window/header band. | Orchestrator now renders those four top utility rows above the sidebar sections, uses the shared sidebar row primitive, fixes the left sidebar width at 240 px instead of the old 300 px wide desktop layout, reduces the top drag spacer from 64 px to 48 px, and renders Chats as a sibling section after Projects by default. Smoke gates order, placement, behavior, tightened width, top inset, projectless metadata, and the optional Chats-before-Projects preference. | Keep future sidebar visual work anchored in live screenshots; next sidebar gap is provider-backed pin/list mutation semantics or exact row spacing if a new live screenshot shows drift. |
| Chat sidebar row density and text-first rows | Live screenshot `/private/tmp/codex-current-screen.png` plus Codex bundle CSS show a compact sidebar rail using small row text, with session rows reading text-first rather than carrying an always-visible provider/color dot before every title. | Orchestrator now tags the sidebar as `data-sidebar-density="codex-compact"`, uses 12 px sidebar row text, 26 px nav/thread rows, 22 px compact rows, keeps provider/thread identity as row metadata, and removes always-on leading color dots from normal session rows. Focused Sidebar smoke and the runnable comparison gate `sidebarRowDensityCodexLike=true`, `sessionRowsTextFirst=true`, and `sidebarPinnedRowsTextFirst=true`. | Keep row density and text-first row changes behind this smoke contract; do not reintroduce always-visible leading identity dots without fresh Codex screenshot evidence. |
| Review source metadata | Codex review chunks publish host snapshot metrics through `set-review-pane-snapshot-metrics-for-host` and support PR/check/reviewer/comment/blame toolbar/flyout surfaces. Live screenshot evidence shows the selected-file header as path-first, without a prominent status glyph before the file path. | Orchestrator has local and GitHub-backed PR/check/reviewer/general comment plus inline review-thread summary metadata paths. GitHub provider comments can render as read-only per-line cards in Review with fixture coverage, including review-comment commit/blame metadata from real GraphQL `commit` / `originalCommit` fields. The comparison now includes a direct `review-last-turn` capture that proves the transcript-card Last turn Review panel opens with the changed-files rail hidden, source-level stats visible, and `reviewFileHeaderPathFirst=true`. The Review source menu keeps unsupported provider-native rows visible but now explains each unavailable boundary inline and through attributes: Last turn has no provider turn diff until a provider event exists, Cloud has no review adapter, and Worktree has no provider worktree source outside an app-managed worktree session. Live Codex app-server proof captures real `turn/diff/updated` events with provider session/turn ids and no checkpoint ids; `thread/rollback` works with `numTurns: 1` for persisted thread history but does not revert the workspace file/git diff. Provider-native hosted/cloud review sources, real working-tree checkpoint restore, and live commented-PR proof remain unavailable. | Keep provider Last turn Undo disabled unless a future provider path restores workspace changes, not just thread history. Add real provider-backed review sources only from provider events/adapters; otherwise keep unsupported rows explicit. |
| Browser webview lifecycle | Codex `browser-sidebar-manager-ivre5jEI.js` maintains body-attached hidden/visible webviews keyed by `data-browser-sidebar-conversation-id`, transfers webviews between conversations, and subscribes to browser-use state, viewport, capture surface, cursor, and local-server notifications. `app-server-manager-signals-Csopz8aM.js` also contains `dynamic-tools-for-thread-start-requested`, `dynamic-tool-call-requested`, `item/tool/call`, `capture-browser-use-turn-route`, `browser-use-turn-route-capture`, `browser-use-turn-route-release`, and `codex/browserUse` evidence. | Orchestrator has manager state parsing, body-host webview containment, fork transfer, DOM transfer, browser-use no-mutation smokes, and persisted Browser Use approval/history/download/upload/domain defaults that apply to the right-side Browser Security pane. Fresh live proof through `npm run live:codex-browser-appserver` reaches the real Codex app-server and completes a turn with `session.started`, assistant deltas, and `run.completed`, but it emits zero `browser.manager_state` events, sends no browser/tool server requests, and replies `CODEX_BROWSER_LIVE_NO_BROWSER`; artifact: `tmp/codex-browser-appserver-live-proof/result.json`. A separate live proof through `npm run live:codex-dynamic-tools` advertises `orchestrator.browser_bridge_status` in `thread/start.dynamicTools`; Codex sends `item/tool/call` with JSON-RPC id `0`, Orchestrator answers it, and the turn completes with `CODEX_BROWSER_LIVE_OK`; artifact: `tmp/codex-dynamic-tools-live-proof/result.json`. The real Browser tools proof advertises the production `orchestrator.browser_open`, `orchestrator.browser_read`, `orchestrator.browser_click`, `orchestrator.browser_type`, `orchestrator.browser_screenshot`, `orchestrator.browser_fill`, `orchestrator.browser_key`, `orchestrator.browser_select`, `orchestrator.browser_check`, and `orchestrator.browser_scroll` specs; Codex sends `item/tool/call` for those tools, including `browser_screenshot` with `includeImage=true`, then a final read, and completes with `CODEX_BROWSER_LIVE_OK`; artifact: `tmp/codex-browser-tools-live-proof/result.json`. Orchestrator routes those supported calls through `browser:clientToolCall` IPC to the Browser renderer, opens the Browser panel via `browser.manager_state`, returns URL/title/visible-structure/target metadata from the real webview, executes click/type/fill/key/select/check/scroll against visible page targets or the viewport, and captures the current webview to a saved PNG artifact with width/height/size metadata plus opt-in inline image data. Focused Browser smoke proves the main IPC to renderer webview loopback with `browserClientToolBridge=true`, action execution with `browserClientToolActions=true`, screenshot capture with `browserClientToolScreenshot=true`, inline screenshot image return with `browserClientToolScreenshotImage=true`, advanced action execution with `browserClientToolAdvancedActions=true`, and the Settings-to-Browser policy path with `browserPersistedPolicyDefaults=true`. If an unimplemented server-initiated `item/tool/call` arrives, Orchestrator still emits a visible `Client tool unavailable` assistant status and returns a structured JSON-RPC unsupported-tool error. | Generic dynamic client-tool transport, live real Browser open/read/click/type/screenshot(includeImage)/fill/key/select/check/scroll requests, persisted default policy, installed-smoke Browser bridge proof, and a renderer-backed Browser bridge are implemented and smoke/live-proof-covered. Keep synthetic no-mutation smoke labeled fixture-only and keep unsupported runtime boundaries user-visible. Continue Browser expansion only from native browser-use events, provider design-change application, or live pixel/timing evidence. |
| Browser device presets | Codex presets include responsive 390x844, 4k, laptop-l, laptop, Surface Pro 7, iPad Air, iPad Mini, Surface Duo, iPhone 15 Pro Max, Pixel 8, iPhone 15 Pro, Samsung Galaxy S24 Ultra, and iPhone SE, with 240-4096 width and 160-4096 height clamps. | Orchestrator's `BrowserPanel.tsx` preset dimensions match the Codex set and clamp ranges. The visible label for `laptopLarge` is `Laptop L`; Codex's bundle id is `laptop-l`. | Keep this as lower priority unless a live UI screenshot shows label/order differences. No implementation change needed from the bundle/code comparison alone. |
| Terminal bottom panel | Codex bottom-panel chunk is terminal-specific, uses xterm, terminal service snapshots, session-conversation mapping, `Terminal {index}` fallback titles, font/theme sync, bottom/right move helpers, and the root app-shell bottom panel defaults to a 280 px total shell. | Orchestrator has bottom/right terminal movement, shared tab strips, snapshots, theme/font sync, a pipe fallback when `node-pty` fails, and a 280 px total default/reset bottom-panel target. Exact Codex animation timing remains unverified. | Compare live Codex and Orchestrator terminal open/close animation timing when live terminal evidence is available. Keep `--terminal-visual` as screenshot evidence, not timing proof. |
| Files / file source tabs | Codex file-source tabs expose Open file empty state, Open in editor, Copy path, word wrap, rich view, artifact preview, git blame controls, and shared source find through the review runtime's `content-search-input`. | Orchestrator has Files and file tabs with shared `content-search-input` source find, previews, fallback notices, actions, selected lines, comments/blame placeholders, Codex-style artifact header title/type splitting for PDF/DOCX/IPYNB previews, Codex-style artifact Open/options controls, Codex-style Workbench file-source action labels, a compact Codex-style file-source options/open action cluster, Codex-like workbook sheet tabs, and active-cell/formula-bar chrome with scoped local formula editing/recalculation for XLSX previews. Full artifact renderer parity and provider-backed comments/blame remain incomplete. | Compare deeper artifact renderer controls and provider metadata paths; prioritize document/spreadsheet/slides/PDF fidelity, exact shared-find focus timing, provider/global indexed search, or provider-backed comments/blame over more local file-tree styling. |
| Settings host scope | Codex has many settings chunks for appearance, general, shortcuts, Browser Use, remote connections, personalization, plugins, skills, usage, worktrees, MCP, hooks, git, and local environments. | Orchestrator has shared settings primitives, route-owned settings, grouped app/host nav, a host-scoped Browser settings page with real in-app Browser data clearing, persisted Browser Use approval/history/download/upload/domain defaults, and explicit unavailable boundaries for host-scoped Personalization without adapters. Real remote-host adapters, Codex Personalization data, and provider-backed Browser Use adapters remain incomplete. | Add real host-scoped adapters only where provider data exists; keep explicit unavailable states for unsupported host pages and deepen Browser Use settings only when a provider or browser-use runtime exposes more policy state. |
| Chat sidebar provider state | Codex sidebar chunks expose project groups, signals, thread keys, thread list signals, pinned metadata, provider/worktree metadata paths, and live screenshot evidence shows a footer collapse affordance beside Settings. | Orchestrator has provider thread-list projection, local pin order, grouping, Codex-compact row density, text-first rows, and a smoke-gated footer collapse/expand affordance with a 48 px collapsed rail. Live provider pin set/list mutation is still externally blocked through the current app-server bridge. | Find a safe state boundary for provider pin set/list, or keep local pin order clearly scoped as Orchestrator-local behavior. |

## Smoke Harness Assessment

The current smoke setup is valuable but not sufficient for the parity goal.

What it does correctly:

- It launches isolated profiles, seeds deterministic fixtures, and gives repeatable screenshots without touching the user's active app state.
- The 27-capture visual/behavior inventory is a useful regression net for broad UI drift and shell transfer regressions.
- Focused flags such as `--diff-core`, `--browser`, `--terminal`, `--settings`, and `--sidebar` keep most failures localized.
- Recent timeout stabilization made the full inventory reliable enough to run after parity slices.
- Smoke infrastructure failures are now separated from product mismatches: startup failures without screenshots/check payloads become `needs-smoke`, while completed smoke assertions can still produce `mismatch`.

Where it has been holding progress back:

- Some checks can validate Orchestrator's approximation instead of Codex's behavior. `rightPanelFindShortcutRouting` was the clearest example; it has been rewritten to prove the shared thread find bar with chat/diff scope plus special Browser routing.
- The transcript-layout smoke now also asserts the shared `content-search-input`/scope controls instead of the old local `transcript-search` field, so the full inventory and focused right-panel check validate the same find contract.
- Broad smoke failures can look like product parity gaps when the real issue is harness timing or fixture setup.
- Screenshot inventory proves visual regression coverage, not provider-backed semantics.
- Synthetic browser-use and review metadata events prove reducer/UI behavior, not that live Codex/provider sessions emit the same data in the app. The `live:codex-browser-appserver` proof makes this explicit for Browser: the live app-server path works, the Codex bundle contains browser-use client route evidence, but no browser-use surface is exposed to this stdio client in the tested protocol.

Correct trigger model going forward:

1. Use focused smokes while developing a slice.
2. Run the full 26-capture visual inventory only as the final regression net for a stable slice.
3. Add small Codex contract checks only after verifying the behavior in the Codex bundle or live Codex UI.
4. Keep provider/live smokes separate from fixture smokes, and label fixture-only proof as fixture-only.
5. If a smoke assertion conflicts with Codex evidence, change or downgrade the assertion before building more UI around it.
6. For agent-driven Browser, do not treat manual Browser parity or synthetic manager-state smokes as provider parity. A runtime either needs a real dynamic client-tool bridge or an explicit unavailable status when a server requests a client tool.

## Immediate Plan

1. Re-run right-panel and Review focused smokes after each find/search change.
2. Use the full visual inventory only after the focused contract checks pass.
3. For Browser, separate manual Browser panel parity from agent-driven browser-use parity: the Browser panel should stay provider-neutral, while agent-driven browser-use needs a real dynamic tool bridge. Runtimes without that bridge should keep the current explicit unavailable status path.
4. Move to the next provider-backed Review gap only after live proof identifies an actual provider event/API. For checkpoint Undo, `thread/rollback` alone is insufficient because it rolls back history but not the working tree.
5. Keep newly aligned global/thread find under the comparison script so regressions fail directly.
